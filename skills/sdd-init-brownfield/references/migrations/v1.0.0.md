# Migración a v1.0.0 — capabilities/, marcador, script de estimación, entorno

1. **`funcional.md` → `capabilities/legacy.md`.** Si existe `.docs/sdd/funcional.md`: **gate** — crear primero la carpeta destino (`New-Item -ItemType Directory -Force .docs/sdd/capabilities`), porque `git mv` falla con `fatal: renaming ... failed: No such file or directory` si no existe; luego `git mv .docs/sdd/funcional.md .docs/sdd/capabilities/legacy.md` y añadir al principio del fichero:

   > Documento funcional heredado (anterior a `capabilities/` por capacidad). **Excepción temporal**: `legacy` no es una capacidad; cada task que toque una capacidad saca de aquí lo suyo a `capabilities/<capability>.md` y lo retira de este fichero. No trocear de golpe.

   Actualizar todo enlace o mención a `funcional.md` en `CLAUDE.md` y `.docs/sdd/*.md` para que apunte a `capabilities/legacy.md`. No crear ningún otro fichero en `capabilities/`.
2. **Script de estimación local.** Si existe `.tools/sdd/Build-EstimationLog.ps1` o `tools/sdd/Build-EstimationLog.ps1`: **gate** — `git rm` de ese fichero (solo ese: otros scripts de la carpeta son del proyecto); sustituir sus menciones en `estimation.md`, `README.md` y `CLAUDE.md` por el comando del kit `pwsh -NoProfile -File "<Base directory de sdd-init-brownfield>/../sdd-templates/scripts/Build-EstimationLog.ps1" -Root .`; ejecutarlo y presentar el diff de `.docs/sdd/estimation-log.md` al dev-lead antes de commitear (cambian columnas, separador decimal y aparece el factor por Tipo).
3. **Entorno por worktree.** Si el proyecto tiene scripts `env:setup` / `env:clean` (en `package.json`, `.tools/` o equivalente) y no existe `.docs/sdd/environments.md`: **gate** — proponer calcarlo de `environments-template.md` del skill `sdd-templates` con los datos reales de esos scripts. Si no hay scripts de entorno, nada.
4. **Artefactos en curso.** Las specs y planes existentes no se reescriben; las tasks nuevas usan las plantillas nuevas (spec ligera con delta por capacidad, plan con Restricciones globales).
5. **Marcador.** Escribir `.docs/sdd/sdd-kit.json`: `{ "version": "1.0.0", "channel": "plugin" | "cli", "updated": "<hoy>" }`.

**Escribe**: `sdd-kit.json` → `version`, `channel`, `updated`.

## Verificación

- `Test-Path .docs/sdd/funcional.md` → `False`; `Get-ChildItem .docs/sdd/capabilities` → solo `legacy.md` (salvo capacidades creadas después por tasks).
- `grep -rn "funcional\.md" CLAUDE.md .docs/sdd/*.md` → sin resultados (todo apunta a `capabilities/legacy.md`).
- `Test-Path .tools/sdd/Build-EstimationLog.ps1` → `False`; los demás ficheros de `.tools/sdd/` siguen.
- Primera línea de `.docs/sdd/estimation-log.md` empieza por `<!-- AUTO-GENERADO por Build-EstimationLog.ps1 (sdd-kit)`.
- `.docs/sdd/sdd-kit.json` existe con `"version": "1.0.0"`.
