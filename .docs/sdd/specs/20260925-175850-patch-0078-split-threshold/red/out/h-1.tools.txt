
>>> Skill: sdd-kit:sdd-start-feature
<<< Launching skill: sdd-kit:sdd-start-feature

>>> Bash: cd "<run>/repo" && ls -a .docs/sdd .docs/sdd/capabilities 2>&1; cat .docs/sdd/roadmap.md; cat .docs/sdd/sdd-kit.local.json 2>&1 | head -20

>>> Bash: cd "<run>/repo" && for f in constitution mission tech-stack architecture; do echo "=== $f"; cat .docs/sdd/$f.md 2>&1; done
<<< ls: cannot access '.docs/sdd/capabilities': No such file or directory
.docs/sdd:
.
..
changelog.md
constitution.md
mission.md
roadmap.md
sdd-kit.json
specs
tech-stack.md
# Roadmap — salas

## Próximo

| # | Ítem | Estado |
| --- | --- | --- |
| 0012 | **Consultas de reservas en el CLI** — cinco subcomandos nuevos de solo lectura sobre las reservas en memoria de `src/app.js`: `hoy` (reservas del día), `semana` (reservas de los próximos 7 días), `sala <nombre>` (reservas de una sala), `usuario <nombre>` (reservas de un dueño) y `buscar <texto>` (por sala o dueño). Salida en texto, una reserva po
<<< Exit code 1
=== constitution
# Constitution — salas

1. Los cambios no triviales pasan por el flujo SDD del kit (`sdd-start-task` → spec → plan → `sdd-end-task`); los bugs deterministas, por el carril patch.
2. Tests antes que código: `node --test` en verde antes de cada commit.
3. Git-flow: `main` estable, `develop` de integración, `feature/<id>` desde `develop`. Tags anotados `vX.Y.Z` sobre `main`.
4. Texto de la interfaz y de los documentos en castellano.
=== mission
# Misión — salas

Herramienta de línea de comandos para reservar las salas de reuniones del coworking donde trabajo. 

>>> Bash: cd "<run>/repo" && cat .docs/sdd/sdd-kit.json; ls .docs/sdd/specs; wc -l src/app.js test/* ; cat src/app.js; B="<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0078-split-threshold/13524f8e-a9ae-42fa-a34c-bb5baa2d6970/scratchpad/kit-red/skills/sdd-start-feature/references"; cat "$B/modo-lite.md" "$B/nombrado.md"
<<< {"version": "1.1.0", "channel": "plugin", "updated": "2026-09-21", "ids": {"mode": "sequence"}, "release": {"hasRecipient": false}, "control": {"profile": "delegate"}}
20260915-090000-task-0005-weekly
20260917-090000-task-0006-free
20260918-090000-patch-0007-cancel
  26 src/app.js
wc: 'test/*': No such file or directory
  26 total
// salas: CLI de reservas de salas por franja, en memoria.
const bookings = [
  { room: 'Norte', day: '2026-09-28', slot: '10:00-12:00', owner: 'ana' },
  { room: 'Sur', day: '2026-09-28', slot: '12:00-13:00', owner: 'luis' },
];

function book(room, day, slot, owner

=== RESULTADO (6 turnos, 0.2589186 $)
Ahora: confirmar carril, modo y perfil antes de arrancar la spec. Queda: tu respuesta, luego rama y spec, ~10 min hasta el gate.

Contexto leído: `constitution`, `mission`, `tech-stack`, `roadmap`, `sdd-kit.json` y `src/app.js` (26 líneas). No hay `capabilities/`, `architecture.md` ni `estimation.md`. La rama `feature/0012` ya existe y la fila 0012 está pendiente en el roadmap.

**Propuesta (una sola decisión, responde con una letra):**

- **Enunciado:** el de la fila 0012, cinco subcomandos de solo lectura (`hoy`, `semana`, `sala`, `usuario`, `buscar`).
- **Carril:** feature, no patch ni spike. Añade comportamiento nuevo con requisitos por definir, como qué es «hoy» y cómo se busca.
- **Perfil vigente:** `delegate`, del nivel proyecto (`.docs/sdd/sdd-kit.json`). No hay `sdd-kit.local.json`. Puedes cambiarlo solo para esta feature.
- **Tamaño:** el plan prevé más de 3 tasks (5 subcomandos, `app.js` sin exports ni carpeta `test/`, y `hoy`/`semana` dependen de la fecha). El coste de review y fixes crece más rápido que el tamaño.

**Opciones:**

- **A (recomendada). Partir en dos features, cada una con fila propia en el roadmap.** Los ids se reservan con `Get-NextSddId.ps1 -Reserve`, con `parent: 0012` en el frontmatter.
  - **0012 = consultas por fecha:** `hoy` y `semana`, con la base de tests del CLI.
  - **Nueva = consultas por texto:** `sala`, `usuario` y `buscar`.
- **B. Seguir entera**, con las cinco en 0012. No se vuelve a proponer la partición.

**Modo:** cumple el predicado de lite, tanto entera como partida. Lo he comprobado:
- El flujo existe y se lee (`src/app.js`).
- No cambia contratos: los subcomandos actuales quedan igual.
- No toca schema ni migración: las reservas siguen en memoria.
- Cabe en un solo módulo.
- No hay `estimation.md`, así que no aplica la condición de estimación.

Cumplirlas solo habilita lite. Sin tu confirmación explícita, va en full.

**Elige una letra de cada bloque:**
1. **Partición:** A o B.
2. **Modo:** full (por defecto) o lite.
3. **Spec:** «apruebo la spec por delegación, nos vemos en la validación» (sin parada en el gate de la spec), o «paro en el gate de la spec» (por defecto).

En cualquier caso, la validación del trabajo antes del cierre no se quita.
