--- [16]
Ahora: confirmar carril, modo y perfil de la 0012. Queda: tu respuesta, y después la spec (~10 min hasta la parada de aprobación).

Contexto leído: `constitution`, `mission`, `tech-stack`, `roadmap`, `sdd-kit.json`. No existen `architecture.md`, `capabilities/` ni `sdd-kit.local.json`, así que no hay claves que ignorar. Rama `feature/0012` con fila pendiente, así que el enunciado sale de ahí.

- **Carril:** feature. Cambia comportamiento y no es un bug determinista ni una pregunta de viabilidad.
- **Modo:** full. Lite no aplica, porque fallan tres de sus condiciones:
  - Toca el schema de datos y exige migración (`data/reservas.json`).
  - Crea un contrato público nuevo (`GET /reservas`).
  - No cabe en un solo módulo: hay persistencia, servidor HTTP y frontend.
- **Perfil:** `delegate`, tomado del proyecto (`sdd-kit.json`). Para en la spec, los desvíos y la validación. Puedes cambiarlo solo para esta feature.
- **Tamaño:** el código actual es un CLI con reservas en memoria y sin servidor HTTP. Prevé unas 4 tasks internas: persistencia, migración, servidor con `GET /reservas` y pantalla web. Son más de 3, y el coste de review y de fixes crece más rápido que el tamaño.

**Opciones (elige una):**

1. **Partir (recomendada).** Ids reservados con `Get-NextSddId.ps1 -Reserve` y fila propia en el roadmap para cada parte:
   - **0012:** reservas persistentes en `data/reservas.json`, con la migración que vuelca las actuales.
   - **Nueva, `parent: 0012`:** servidor HTTP con `GET /reservas`.
   - **Nueva, `parent: 0012`:** pantalla `public/index.html` que lista por sala y día.
2. **Seguir entera** como una sola feature en modo full, con `delegate`. No se vuelve a proponer partirla.
3. **Cualquiera de las anteriores con la spec aprobada por delegación** («apruebo la spec por delegación, nos vemos en la validación»). Te ahorra la parada de la spec, pero la validación final sigue siendo tuya.

Si prefieres otro perfil (`pair` o `unattended`) para esta feature, dilo con tu respuesta.

