--- [3]
Ahora: leo el contexto SDD (docs, no código). Queda: pregunta de carril/modo/perfil, ~2 min.

--- [15]
Ahora: confirmo carril, modo y perfil. Queda: spec con `superpowers:brainstorming` y parada en el gate, ~10 min.

Contexto leído: constitution, mission, tech-stack y roadmap. No hay `architecture.md` ni `capabilities/`. La rama `feature/0012` coincide con la fila pendiente del roadmap, así que uso esa fila como enunciado:

> **0012 · Consultas de reservas en el CLI**: cinco subcomandos de solo lectura sobre `bookings` de `src/app.js`: `hoy`, `semana`, `sala <nombre>`, `usuario <nombre>` y `buscar <texto>`. Salida en texto, una reserva por línea.

Es una feature, no un patch ni un spike. Propongo **modo lite** (`spec.md` corta, sin `plan.md` ni `tasks.md`). Estas son las condiciones del predicado, comprobadas una por una:

1. **El flujo ya existe y se puede leer:** sí. `src/app.js` tiene 27 líneas con `book`, `cancel` y `free`.
2. **No cambia contratos públicos:** sí. Solo añade subcomandos nuevos y no toca `reservar`, `cancelar` ni `libres`. Ningún módulo consume el CLI.
3. **No toca schema ni migración:** sí. Solo lee `bookings` en memoria.
4. **Cabe en un solo módulo:** sí, en `src/app.js` y su test.
5. **Estimación ≤ media jornada:** no aplica, porque no existe `.docs/sdd/estimation.md`.

- **Tamaño:** prevé unas 3 tasks internas, así que no propongo partirla.
- **Perfil vigente:** `delegate`, que sale del nivel proyecto (`.docs/sdd/sdd-kit.json`). No hay `sdd-kit.local.json` ni claves que ignorar.

¿Cómo la arranco? Responde con una letra:

- **A (recomendada):** modo lite, perfil `delegate`, y aprobación de la spec en el gate.
- **B:** modo lite, perfil `delegate`, y apruebo la spec por delegación, nos vemos en la validación.
- **C:** modo full en lugar de lite.
- **D:** cambiar el perfil para esta feature (`pair` o `unattended`).

