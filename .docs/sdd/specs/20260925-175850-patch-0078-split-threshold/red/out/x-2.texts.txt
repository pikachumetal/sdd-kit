--- [3]
Ahora: leer el contexto SDD (`.docs/sdd/`). Queda: la pregunta de enrutado (carril, modo, perfil), ~2 min.

--- [17]
Ahora: confirmar carril, modo y perfil. Queda: tu respuesta, y después la spec (invoco `brainstorming`), ~10 min hasta el gate de la spec.

Contexto leído: `constitution`, `mission`, `tech-stack`, `roadmap` y `sdd-kit.json`. No hay `architecture.md`, `capabilities/` ni `sdd-kit.local.json`, así que no hay claves locales que ignorar. Rama `feature/0012`, con la fila 0012 pendiente en el roadmap.

**Enunciado (de la fila 0012):** reservas persistentes en `data/reservas.json` con migración de las actuales, endpoint `GET /reservas` y pantalla `public/index.html` que las lista por sala y día.

- **Carril:** feature. No es patch, porque cambia comportamiento. No es spike ni planificación, porque pides hacerla.
- **Modo:** full. No cumple las condiciones de lite: toca schema de datos y exige migración, añade un contrato público nuevo (`GET /reservas`) y abarca más de un módulo.
- **Perfil:** `delegate`, que sale del nivel proyecto (`sdd-kit.json`). Puedes cambiarlo solo para esta feature.
- **Tamaño:** el plan preveo con más de 3 tasks. Hoy `src/app.js` es un CLI sin servidor HTTP, así que hacen falta cuatro: persistencia en fichero, migración, servidor con `GET /reservas` y pantalla web. El coste de review y fixes crece más rápido que el tamaño, por eso recomiendo partir.

Opciones:

1. **Partir en dos (recomendada).** La 0012 queda como persistencia más migración, con 2 tasks. Una feature nueva con `parent: 0012` y fila propia en el roadmap recibe el endpoint y la pantalla, también con 2 tasks. El id lo reservo con `Get-NextSddId.ps1 -Reserve`.
2. **Seguir entera.** No vuelvo a proponer partirla.
3. **Partir en dos y aprobar la 0012 por delegación.** Con esto apruebo yo la spec y sigo sin parar hasta la validación, que no se quita nunca. Registro tu frase literal y decido yo la review de spec.
4. **Seguir entera y aprobar la spec por delegación.** Es lo mismo que la opción 3 pero sin partir.

¿Qué opción eliges y con qué perfil? Si el perfil te vale, contesta solo con el número.

