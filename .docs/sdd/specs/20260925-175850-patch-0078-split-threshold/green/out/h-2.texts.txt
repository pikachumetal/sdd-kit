--- [3]
Ahora: cargo el contexto SDD (docs, no código). Queda: enrutado con una pregunta, ~2 min.

--- [12]
Ahora: enrutado, una única pregunta. Queda: tu respuesta; después spec con `superpowers:brainstorming` y la presento en el gate, ~10 min.

Contexto leído: constitution, mission, tech-stack y roadmap (no hay `architecture.md` ni `capabilities/`). La rama `feature/0012` ya existe y el roadmap tiene la fila 0012 pendiente, así que la propongo como enunciado: **Consultas de reservas en el CLI** (`hoy`, `semana`, `sala <nombre>`, `usuario <nombre>`, `buscar <texto>`, todas de solo lectura).

**Carril:** feature, no patch ni spike. Añade comportamiento nuevo y no es un bug ni una duda de viabilidad.

**Modo:** puede ir en lite. Comprobé las condiciones una por una:
- El flujo ya existe y se puede leer: el array `bookings` y los comandos `reservar`, `cancelar` y `libres` en `src/app.js`.
- No cambia contratos públicos: solo añade subcomandos, y los tres existentes quedan igual.
- No toca schema ni exige migración: `bookings` es un array en memoria y solo se lee.
- Cabe en un solo módulo: `src/app.js`, 26 líneas.
- No existe `estimation.md`, así que esa condición no aplica.

Cumplirlas solo lo habilita. Si eliges lite, la spec es corta y no hay `plan.md` ni `tasks.md`. Si al implementar cae alguna condición, subo a full.

**Perfil:** `delegate`, de `.docs/sdd/sdd-kit.json` (nivel proyecto). No hay `sdd-kit.local.json` ni perfil de release o feature que lo pise. En `delegate` hay gate de spec y validación final, y no paro en cada task.

**Partir:** prevé unas 5 tasks (una por subcomando). Todas tocan la misma superficie (CLI), sin migración. Por eso **no propongo partirla**.

**Delegación de la spec:** como te puedes ausentar, hay una opción para aprobar la spec por delegación y verte en la validación. La validación final no se quita.

¿Cómo seguimos?
1. **Lite, `delegate`, spec con gate normal** (recomendada: cumple el predicado y es un fichero de 26 líneas).
2. Full, `delegate`, spec con gate normal.
3. Lite, `delegate`, y apruebo la spec por delegación, nos vemos en la validación.
4. Full, `delegate`, y apruebo la spec por delegación, nos vemos en la validación.
5. Otro perfil para esta feature (`pair` o `unattended`).

