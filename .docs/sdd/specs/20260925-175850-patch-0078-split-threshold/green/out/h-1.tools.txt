
>>> Skill: sdd-kit:sdd-start-feature
<<< Launching skill: sdd-kit:sdd-start-feature

>>> Bash: ls -a .docs/sdd .docs/sdd/capabilities 2>&1; cat .docs/sdd/sdd-kit.local.json 2>&1 | head -30

>>> Read: <run>\repo\.docs\sdd\roadmap.md
<<< 1	# Roadmap — salas
2	
3	## Próximo
4	
5	| # | Ítem | Estado |
6	| --- | --- | --- |
7	| 0012 | **Consultas de reservas en el CLI** — cinco subcomandos nuevos de solo lectura sobre las reservas en memoria de `src/app.js`: `hoy` (reservas del día), `semana` (reservas de los próximos 7 días), `sala <nombre>` (reservas de una sala), `usuario <nombre>` (reservas de un dueño) y `buscar <texto>` (por sala o dueño). Salida en texto, una reserva por línea | Pendiente |
8	
9	## Backlog
10	
11	| # | Ítem | Origen |
12	| --- | --- | --- |
13	| B1 | Exportar reservas a calendario (.ics) | idea propia |
14
<<< ls: cannot access '.docs/sdd/capabilities': No such file or directory
.docs/sdd:
.
..
changelog.md
constitution.md
mission.md
roadmap.md
sdd-kit.json
specs
tech-stack.md
cat: .docs/sdd/sdd-kit.local.json: No such file or directory

>>> Bash: cd .docs/sdd; cat constitution.md mission.md tech-stack.md sdd-kit.json; ls specs; git diff --stat HEAD~0 >/dev/null; ls ../../src ../..
<<< # Constitution — salas

1. Los cambios no triviales pasan por el flujo SDD del kit (`sdd-start-task` → spec → plan → `sdd-end-task`); los bugs deterministas, por el carril patch.
2. Tests antes que código: `node --test` en verde antes de cada commit.
3. Git-flow: `main` estable, `develop` de integración, `feature/<id>` desde `develop`. Tags anotados `vX.Y.Z` sobre `main`.
4. Texto de la interfaz y de los documentos en castellano.
# Misión — salas

Herramienta de línea de comandos para reservar las salas de reuniones del coworking donde trabajo. La uso yo: la desarrollo, la pruebo y la 

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0078-split-threshold\13524f8e-a9ae-42fa-a34c-bb5baa2d6970\scratchpad\kit-green\skills\sdd-start-feature\references\modo-lite.md
<<< 1	## Modo lite — predicado observable, activación del usuario
2	
3	Una feature **puede** ir en modo lite si cumple **todas** estas condiciones:
4	
5	- El flujo a modificar ya existe en el repo y se puede leer.
6	- No cambia contratos públicos (API, interfaces que consume otro módulo).
7	- No toca schema de datos ni exige migración.
8	- Cabe en un solo módulo o área.
9	- Si existe `.docs/sdd/estimation.md`: la estimación es ≤ media jornada.
10	
11	Cumplirlas **habilita** el modo; NO lo activa. Propón el modo lite **citando una por una las condiciones que has comprobado** y espera la confirmació

>>> Read: <run>\repo\src\app.js
<<< 1	// salas: CLI de reservas de salas por franja, en memoria.
2	const bookings = [
3	  { room: 'Norte', day: '2026-09-28', slot: '10:00-12:00', owner: 'ana' },
4	  { room: 'Sur', day: '2026-09-28', slot: '12:00-13:00', owner: 'luis' },
5	];
6	
7	function book(room, day, slot, owner) {
8	  if (bookings.some(b => b.room === room && b.day === day && b.slot === slot)) throw new Error('sala ocupada');
9	  bookings.push({ room, day, slot, owner });
10	}
11	
12	function cancel(room, day, slot, owner) {
13	  const i = bookings.findIndex(b => b.room === room && b.day === day && b.slot === slot && b.owne

=== RESULTADO (8 turnos, 0.2582952 $)
Contexto SDD leído (constitution, mission, tech-stack, roadmap, `sdd-kit.json`). No hay `capabilities/` ni `sdd-kit.local.json`. La rama `feature/0012` tiene fila pendiente en el roadmap, así que propongo esa fila como enunciado.

**Confirma estas cuatro cosas:**

1. **Enunciado:** fila 0012, «Consultas de reservas en el CLI». Añade `hoy`, `semana`, `sala <nombre>`, `usuario <nombre>` y `buscar <texto>`. Los cinco son de solo lectura sobre `bookings` en `src/app.js`, con salida en texto y una reserva por línea.
2. **Carril y modo:** feature, no patch ni spike. Puede ir en lite, porque cumple las cinco condiciones:
   - El flujo ya existe: `src/app.js` tiene `book`, `cancel` y `free`.
   - No cambia contratos públicos: solo añade subcomandos, y ningún módulo consume el CLI.
   - No toca schema ni migración: `bookings` sigue en memoria y sin cambios.
   - Cabe en un solo módulo: todo vive en `src/app.js`.
   - No hay `estimation.md`, así que esa condición no aplica.

   Recomiendo **full**. Prevés 5 tasks, y `semana` y `buscar` dejan decisiones abiertas (ventana de 7 días, coincidencia parcial o exacta, mayúsculas). Con lite no habría plan, pero la spec, el smoke y el walkthrough se mantienen igual.
3. **Perfil:** `delegate`, que sale del proyecto (`.docs/sdd/sdd-kit.json`). No hay perfil a nivel de feature, de release ni local. Puedes cambiarlo solo para esta feature.
4. **Partición:** prevés 5 tasks, una por subcomando. Todas tocan la misma superficie (CLI en un fichero), son de solo lectura y no llevan migración. **No propongo partirla.**

**Opciones (elige una):**

- **A.** Full, `delegate`, sin partir. *(Recomendada.)*
- **B.** Como A, y además «apruebo la spec por delegación, nos vemos en la validación». Sirve si te vas a ausentar: apruebo yo la spec, registro tu frase y no paro en el gate. La validación final sigue siendo tuya.
- **C.** Lite, `delegate`, sin partir.
- **D.** Otro perfil o cambio de enunciado (dime cuál).

¿Qué opción eliges?
