--- [13]
Perfil `unattended`: la validación queda diferida al smoke de la release. Actualizo `patch.md` (Capacidades y validación) y junto el fix en un commit.

--- [15]
Ahora el commit de cierre: hash, changelog, roadmap.

--- [18]
El único fallo del validador está en `bookings.md` (sección «Historial», anterior al patch y que el delta no toca): no bloquea, lo dejo anotado. Commit de cierre y merge.

--- [22]
**Decisiones tomadas sin el dev-lead**
- Perfil `unattended` (`sdd-kit.json`): no hubo validación humana. `patch.md` §4 lleva `Validación diferida: 2026-09-25 · «perfil unattended: sin validación humana en el cierre» · disparador: smoke de la release`. La fila del patch en el roadmap empieza por `🧪 validación diferida a smoke de la release — `. Esa frase no es de una persona, es el perfil. Que pasen los tests no es validación.
- Bloque «Capacidades» de `patch.md`: `Ninguna, porque el fix devuelve salas reservar a lo que ya dice bookings`. `bookings.md` ya exigía el máximo de 2 h y el mensaje `Máximo 2 h por reserva`, así que no hay delta.
- Quité la fila 0013 de «Próximo» en el roadmap, porque el patch la salda. Solo añadí la fila en «Patches».
- Junté el fix y su `patch.md` en `0ed1e94`. Quedan dos commits: `0ed1e94` (fix) y `8438c99` (cierre).
- No hay `.docs/sdd/estimation.md`, así que no regeneré el estimation-log. §5 de `patch.md` mantiene el tiempo real de 0,3 h.

**Instrucciones del dev-lead**
- «Invoca sdd-end-patch y cierra el patch 0013»: hecho.
- «Si tienes una pregunta, escríbela en tu último mensaje y para»: no tengo ninguna.

**Pendiente**
- `Test-Capabilities.ps1` falla en `bookings.md`: `sección «Historial», resto del kit 1.x: lo quita la migración a 2.0.0`. Ya estaba antes del patch y el delta no la toca. No la edité; queda para el dev-lead.
- Validación humana del fix, diferida al smoke de la release.
- Push: no hecho.

¿Quieres que genere el ticket de mejora del kit con `sdd-feedback` en esta sesión?

**Terminado.** feature/0013 fusionada en develop (beade2c) · push: no hecho: `merge.push` no lo autoriza y no hay remoto.

