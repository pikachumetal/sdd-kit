
>>> Skill: sdd-kit:sdd-end-patch
<<< Launching skill: sdd-kit:sdd-end-patch

>>> Bash: cd "<run>/salas" && cat .docs/sdd/sdd-kit.json; ls .docs/sdd .docs/sdd/specs/*; cat .docs/sdd/specs/*0013*/patch.md; sed -n 1,80p ../../../kit-green/skills/sdd-start-task/references/control-profiles.md
<<< {"version": "2.0.0", "channel": "plugin", "ids": {"mode": "sequence"}, "release": {"hasRecipient": false}, "control": {"profile": "delegate"}, "merge": {"into": "develop", "noFf": true, "removeWorktree": false, "push": false}}
.docs/sdd:
capabilities
changelog.md
constitution.md
roadmap.md
sdd-kit.json
specs

.docs/sdd/specs/20260925-080000-patch-0013-reserva-maxima:
patch.md
---
id: 20260925-080000-patch-0013-reserva-maxima
task: 0013
title: Patch — `reservar` acepta reservas de más de 2 h
type: patch
status: done
created: 2026-09-25
branch: feature/0013
commit: <hash>
---

# Patch 0013 — `re

=== RESULTADO (4 turnos, 0.1765604 $)
Validación pendiente (paso 0). No he tocado nada del cierre. El perfil es `delegate` (`sdd-kit.json`, sin `sdd-kit.local.json`), así que hace falta tu validación antes de cerrar y antes de cualquier merge. «Cierra el patch» y «`node --test` pasa» no cuentan como validación.

**Smoke de `patch.md` §4:** `salas reservar Norte 10-13` debe responder `Máximo 2 h por reserva` y no guardar nada. Hasta ahora lo único comprobado es `node --test` (2/2 verde), y lo comprobó el agente.

**Guion de pruebas** (desde la raíz de `salas`, en `feature/0013`):

1. Ejecuta `salas reservar Norte 10-13` (3 h). Debe mostrar `Máximo 2 h por reserva` y terminar sin guardar la reserva.
2. Comprueba que no se guardó: la lista de reservas de Norte no incluye 10-13.
3. Ejecuta `salas reservar Norte 10-12` (exactamente 2 h). Debe guardarse sin error.
4. Ejecuta `salas reservar Norte 14-15` (1 h). Debe guardarse sin error.

**Pregunta:** ¿qué has probado y funciona? Si prefieres diferir la validación, dime el disparador, por ejemplo el smoke de la release. Si algo falla, dime qué, y vuelvo al fix.

Cuando respondas, registraré `Validado: <fecha> · «<tu frase>»` (o la validación diferida) en `patch.md` §4 y seguiré con los pasos 1 a 8.
