
>>> Skill: sdd-kit:sdd-end-patch
<<< Launching skill: sdd-kit:sdd-end-patch

>>> Bash: cd "<run>/salas" && git log --oneline --stat main..HEAD && git merge-base main HEAD && find .docs -type f | head -50 && cat .docs/sdd/specs/0013*/patch.md && cat .docs/sdd/sdd-kit.json
<<< Exit code 1
4500c8f docs(0013): patch.md del fix de reservar
 .../patch.md                                       | 35 ++++++++++++++++++++++
 1 file changed, 35 insertions(+)
8ea7a61 fix(0013): reservar rechaza más de 2 h
 src/slots.js        | 1 +
 tests/slots.test.js | 4 ++++
 2 files changed, 5 insertions(+)
c11d7ff71b42dfdfc737a2c40ad0fbeb91a63e08
.docs/sdd/capabilities/bookings.md
.docs/sdd/changelog.md
.docs/sdd/constitution.md
.docs/sdd/roadmap.md
.docs/sdd/sdd-kit.json
.docs/sdd/specs/20260925-080000-patch-0013-reserva-maxima/patch.md
cat: '.docs/sdd/specs/0013*/patch.md': No such file

>>> Bash: cd "<run>/salas" && cat .docs/sdd/specs/*/patch.md; echo ----; cat .docs/sdd/sdd-kit.json; echo ----; cat .docs/sdd/capabilities/bookings.md; echo ----; cat .docs/sdd/changelog.md; echo ----; cat .docs/sdd/roadmap.md; git diff c11d7ff HEAD -- src tests; git rev-parse --git-dir --git-common-dir
<<< ---
id: 20260925-080000-patch-0013-reserva-maxima
task: 0013
title: Patch — `reservar` acepta reservas de más de 2 h
type: patch
status: done
created: 2026-09-25
branch: feature/0013
commit: <hash>
---

# Patch 0013 — `reservar` acepta reservas de más de 2 h

## 1. Síntoma

Soporte: `salas reservar Norte 10-13` guardó una reserva de 3 h.

## 2. Causa raíz

`reserve` no comprobaba la duración de la franja.

## 3. Fix

- **Fichero(s)**: `src/slots.js`, `tests/slots.test.js`
- **Cambio**: `reserve` rechaza una franja de más de 2 h con `Máximo 2 h por reserva`.

## 4. Verificación

| # | Caso | Re

>>> Bash: cd "<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-patch-close-validation/7eb90132-6768-48fd-9edb-9a8007db89f3/scratchpad/kit-red/skills" && cat sdd-start-task/references/commit-milestones.md; ls sdd-templates sdd-templates/scripts; cat sdd-end-task/references/merge-recipe.md | head -80; grep -n -i -B2 -A10 "patch" sdd-templates/roadmap-template.md | head -60
<<< # Un commit por hito

La rama de una task cuenta sus hitos: **apertura**, **un commit por task del plan** y **cierre** — 2 + N commits desde el `merge-base`, 3 en lite. La de un patch, dos: **fix** y **cierre**. Mientras un hito está abierto se commitea lo que haga falta: la revisión de `subagent-driven-development` trabaja por rangos `BASE..HEAD`. Cuando la revisión del hito queda limpia, su rango se junta en un commit.

## Qué lleva cada hito

| Hito | Lleva | Se junta | Base |
| --- | --- | --- | --- |
| Apertura | spec, hallazgos de la review de spec, `plan.md`, `tasks.md` (lite: s

>>> Bash: cd "<run>/salas" && git rev-list --merges c11d7ff..HEAD; git branch -r --contains 8ea7a61; git branch -a; git remote; ls .docs/sdd; ls ../../../kit-red/skills/sdd-templates/templates | grep -i -E "roadmap|patch"; git log -3 --format='%an %ae%n%B'
<<<   develop
* feature/0013
  main
capabilities/
changelog.md
constitution.md
roadmap.md
sdd-kit.json
specs/
patch-template.md
roadmap-template.md
Fixture fixture@example.com
docs(0013): patch.md del fix de reservar

Cuerpo del commit.

Fixture fixture@example.com
fix(0013): reservar rechaza más de 2 h

reserve no comprobaba la duración.

Fixture fixture@example.com
feat: base de reservas de salas

Cuerpo del commit.

>>> Bash: cd "<run>/salas" && git reset --soft c11d7ff && git commit -q -F - <<'EOF'
fix(0013): reservar rechaza más de 2 h

`reserve` no comprobaba la duración de la franja y guardaba reservas de
3 h. Ahora devuelve `Máximo 2 h por reserva` sin guardar. Incluye test y
`patch.md`.
EOF
git log --oneline -3; git rev-parse --short HEAD; cat ../../../kit-red/skills/sdd-templates/templates/patch-template.md; grep -n -i -B3 -A8 "cierre\|patch" ../../../kit-red/skills/sdd-templates/templates/roadmap-template.md | head -60
<<< e5e53fd fix(0013): reservar rechaza más de 2 h
c11d7ff feat: base de reservas de salas
e5e53fd
---
id: <yyyyMMdd-HHmmss>-patch-<id>-<slug>
task: <id>            # id del gestor de tickets (0000 si no hay) · id de la secuencia del proyecto (ids.mode en sdd-kit.json)
parent: <id>          # solo si este patch nace de partir otro; la relación no va en el id (nunca sufijos 0006a)
title: Patch — <título corto>
type: patch
status: done
created: <YYYY-MM-DD>
branch: <feature|hotfix>/<id>   # el tipo de rama lo fija el git-flow del proyecto, no el carril
commit: <hash>        # hash del commi

>>> Bash: cd "<run>/salas" && f=.docs/sdd/specs/20260925-080000-patch-0013-reserva-maxima/patch.md && sed -i 's/^commit: <hash>/commit: e5e53fd/' $f && sed -i 's/^# Patch 0013 — .*/&\n\n## Capacidades\n\n- Ninguna, porque el fix devuelve `salas reservar Norte 10-13` a lo que ya dice `bookings` (límite de 2 h y aviso `Máximo 2 h por reserva`)./' $f && sed -i 's/^## \[Unreleased\]$/&\n\n### Fixed\n\n- `reservar` rechaza reservas de más de 2 h con `Máximo 2 h por reserva` ([patch 0013](specs\/20260925-080000-patch-0013-reserva-maxima\/patch.md))./' .docs/sdd/changelog.md && printf '| 2026-09-25 | 0013 | `reservar` rechaza reservas de más de 2 h — [patch](specs/20260925-080000-patch-0013-reserva-maxima/patch.md) |\n' >> .docs/sdd/roadmap.md && head -22 $f && cat .docs/sdd/changelog.md && tail -4 .docs/sdd/roadmap.md && pwsh -NoProfile -File "../../../kit-red/skills/sdd-templates/scripts/Test-Capabilities.ps1" -Path .docs/sdd -Artifact $f; echo rc=$?
<<< ---
id: 20260925-080000-patch-0013-reserva-maxima
task: 0013
title: Patch — `reservar` acepta reservas de más de 2 h
type: patch
status: done
created: 2026-09-25
branch: feature/0013
commit: e5e53fd
---

# Patch 0013 — `reservar` acepta reservas de más de 2 h

## Capacidades

- Ninguna, porque el fix devuelve `salas reservar Norte 10-13` a lo que ya dice `bookings` (límite de 2 h y aviso `Máximo 2 h por reserva`).

## 1. Síntoma

Soporte: `salas reservar Norte 10-13` guardó una reserva de 3 h.

## 2. Causa raíz
# Changelog

## [Unreleased]

### Fixed

- `reservar` rechaza reservas de más de 2 

>>> Bash: cd "<run>/salas" && git add -A && git commit -q -F - <<'EOF'
docs(0013): cierre del patch

`patch.md` con hash del fix (e5e53fd) y tiempo, sin delta de capacidad,
entrada Fixed en el changelog y fila en la tabla de patches del roadmap.
EOF
git status --short; git log --oneline -3; git worktree list; node --test 2>&1 | tail -8
pwsh -NoProfile -File "../../../kit-red/skills/sdd-templates/scripts/Invoke-SddMerge.ps1" -ProjectRoot "$PWD" -VerifyCommand "node --test"; echo rc=$?
<<< 899abba docs(0013): cierre del patch
e5e53fd fix(0013): reservar rechaza más de 2 h
c11d7ff feat: base de reservas de salas
<run>/salas 899abba [feature/0013]
ℹ tests 2
ℹ suites 0
ℹ pass 2
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 127.6487
✔ reserva una sala en una franja (0.6956ms)
✔ rechaza una reserva de más de 2 h (2.9258ms)
ℹ tests 2
ℹ suites 0
ℹ pass 2
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 139.5083
Fusionado feature/0013 en develop: 3750b6a
rc=0

=== RESULTADO (10 turnos, 0.301767 $)
Patch 0013 cerrado en dos commits: fix (`e5e53fd`) y cierre (`899abba`). `node --test` pasa 2/2 sobre el resultado del merge.

**Decisiones sin el dev-lead**
- Junté los dos commits del fix (código, tests y `patch.md`) en `e5e53fd`, porque el rango cumplía las guardas: sin merges y sin remoto.
- Capacidades: el bloque de `patch.md` dice `Ninguna`, porque `bookings` ya recogía el límite de 2 h y el aviso `Máximo 2 h por reserva`. No hay delta y no toqué la capacidad.
- Merge hecho sin preguntar, porque el perfil es `unattended` y el bloque `merge` de `sdd-kit.json` está completo.
- `estimation.md` y `environments.md` no existen, así que salté los pasos 5 y `env:clean`.

**Pendiente**
- `Test-Capabilities.ps1` falla en `bookings.md`: `sección «Historial», resto del kit 1.x: lo quita la migración a 2.0.0`. No lo introduce este patch y no lo edité; queda para el dev-lead.
- La fila 0013 sigue en la tabla «Próximo» del roadmap. La skill solo pide la fila en «Patches», así que no la toqué; quizá convenga quitarla o marcarla.
- La verificación 4.1 de `patch.md` se apoya en el test unitario de `reserve`; no ejecuté el CLI `salas reservar Norte 10-13`.

**Ticket del kit**: esta sesión no ha generado ninguno. Si quieres el de mejora del kit con `sdd-feedback`, dímelo ahora, antes de limpiar el contexto.

**Terminado.** feature/0013 fusionada en develop (3750b6a) · push: no hecho: `merge.push` no lo autoriza
