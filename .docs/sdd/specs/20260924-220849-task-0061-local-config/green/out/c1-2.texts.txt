--- [1]
Skill sdd-config matches: personal preference, kit config.

--- [8]
Marta, ya está configurado, solo para ti.

Estado previo, antes del cambio:

| Clave | Valor | Fichero |
| --- | --- | --- |
| `ids.mode` | `sequence` | `sdd-kit.json` |
| `control.profile` | `delegate` | `sdd-kit.json` |
| `merge` | `develop`, `noFf`, sin borrar worktree | `sdd-kit.json` |
| `execution` | falta, rige `auto` | — |
| `validation.startEnvironment` | falta, rige `false` | — |

Cambios:
- **`.docs/sdd/sdd-kit.local.json`** (nuevo): `control.profile: "pair"` y `validation.startEnvironment: true`.
- **`.gitignore`**: añadida la línea `.docs/sdd/sdd-kit.local.json`. Es lo único que aparece modificado en git. El fichero local no se commitea, y `.docs/sdd/sdd-kit.json` no se ha tocado, así que el equipo sigue en `delegate`.

Efecto:
- **Pair**: el agente para en todos los gates, incluido el plan y tras cada task.
- **Entorno**: al validar una task tuya, arranca el entorno antes de darte el guion de pruebas.
- **Precedencia**: tu `pair` local pisa el `delegate` del proyecto. Un `profile:` en el frontmatter de una spec o un `Perfil de control:` en el roadmap sigue mandando sobre ti solo en el caso de la task (el de la task gana; el de la release, no).

El `.gitignore` queda sin commitear. Como cualquiera que arranque sin esa línea vería el fichero como no rastreado, conviene commitearla. ¿Lo hago?

