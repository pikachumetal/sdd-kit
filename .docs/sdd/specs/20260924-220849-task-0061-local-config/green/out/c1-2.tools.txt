
>>> Skill: sdd-kit:sdd-config
<<< Launching skill: sdd-kit:sdd-config

>>> Bash: cd "<run>/salas"; ls -a .docs/sdd; cat .docs/sdd/sdd-kit.json; echo ---; cat .docs/sdd/sdd-kit.local.json; echo ---; cat .gitignore; echo ---; git config user.name

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0061\ff83d6ab-5aea-4102-ad31-a3e458822b2e\scratchpad\kit-green\skills\sdd-start-task\references\control-profiles.md
<<< ./
../
changelog.md
constitution.md
roadmap.md
sdd-kit.json
{"version": "2.0.0", "channel": "plugin", "ids": {"mode": "sequence"}, "release": {"hasRecipient": false}, "control": {"profile": "delegate"}, "merge": {"into": "develop", "noFf": true, "removeWorktree": false}}
---
cat: .docs/sdd/sdd-kit.local.json: No such file or directory
---
.superpowers/
---
Fixture
<<< 1	# Perfiles de control y gates
2	
3	Cuánto para el agente lo elige el usuario con un perfil. Cada gate de la tabla dice qué hace en cada uno. Contrato de las tasks 0005, 0006, 0012, 0021 y 0022: no lo dupliques, enlázalo.
4	
5	## Perfiles
6	
7	- **`pair`** — para en todos los gates de la tabla.
8	- **`delegate`** *(default)* — para en la spec, los desvíos y la validación; sin gate en el plan.
9	- **`unattended`** — no para en ningún punto hasta terminar la release, salvo el merge a `main`, el tag y las acciones hacia fuera (push, PR, publicar), que siempre decide una persona; el push de la ra

>>> Bash: cd "<run>/salas"; grep -qxF '.docs/sdd/sdd-kit.local.json' .gitignore || printf '.docs/sdd/sdd-kit.local.json\n' >> .gitignore; printf '{"control": {"profile": "pair"}, "validation": {"startEnvironment": true}}\n' > .docs/sdd/sdd-kit.local.json; cat .gitignore; git status --short
<<< .superpowers/
.docs/sdd/sdd-kit.local.json
 M .gitignore

=== RESULTADO (6 turnos, 0.20400340000000003 $)
Marta, ya está configurado, solo para ti.

Estado previo, antes del cambio:

| Clave | Valor | Fichero |
| --- | --- | --- |
| `ids.mode` | `sequence` | `sdd-kit.json` |
| `control.profile` | `delegate` | `sdd-kit.json` |
| `merge` | `develop`, `noFf`, sin borrar worktree | `sdd-kit.json` |
| `execution` | falta, rige `auto` | — |
| `validation.startEnvironment` | falta, rige `false` | — |

Cambios:
- **`.docs/sdd/sdd-kit.local.json`** (nuevo): `control.profile: "pair"` y `validation.startEnvironment: true`.
- **`.gitignore`**: añadida la línea `.docs/sdd/sdd-kit.local.json`. Es lo único que aparece modificado en git. El fichero local no se commitea, y `.docs/sdd/sdd-kit.json` no se ha tocado, así que el equipo sigue en `delegate`.

Efecto:
- **Pair**: el agente para en todos los gates, incluido el plan y tras cada task.
- **Entorno**: al validar una task tuya, arranca el entorno antes de darte el guion de pruebas.
- **Precedencia**: tu `pair` local pisa el `delegate` del proyecto. Un `profile:` en el frontmatter de una spec o un `Perfil de control:` en el roadmap sigue mandando sobre ti solo en el caso de la task (el de la task gana; el de la release, no).

El `.gitignore` queda sin commitear. Como cualquiera que arranque sin esa línea vería el fichero como no rastreado, conviene commitearla. ¿Lo hago?
