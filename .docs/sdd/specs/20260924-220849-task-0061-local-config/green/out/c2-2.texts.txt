--- [13]
Ahora: he leído el contexto SDD y la rama ya dice la task. Queda: confirmar carril, modo y perfil, luego branch, spec y su aprobación, ~15 min hasta la parada de la spec.

No hay `mission.md`, `tech-stack.md`, `architecture.md` ni `capabilities/` en `.docs/sdd/`. Solo existen constitution, roadmap, changelog y las dos configuraciones.

**Pregunta 1: carril, modo y perfil**

1. **Enunciado.** La rama es `feature/0012` y el roadmap tiene la fila pendiente 0012: «Validar el formato de la franja al reservar (`10-12`, no `1012`)», con `src/slots.js` y tamaño S. ¿Es esta la task?

2. **Carril.** Es una feature con comportamiento, no un bug determinista ni un spike. Va como task, no como patch.

3. **Modo.** Propongo **lite**. Estas son las condiciones del predicado y lo que he comprobado de cada una:
   - **Flujo existente y legible:** `src/slots.js` existe (3 líneas). No he leído su contenido, porque el gate me lo prohíbe antes de que confirmes la task.
   - **Sin cambio de contratos públicos:** según la fila, solo se añade validación de formato. Falta confirmar que la firma no cambia.
   - **Sin schema ni migración:** no hay schema en el repo.
   - **Un solo módulo:** la fila declara solo `src/slots.js`.
   - **Estimación:** no existe `.docs/sdd/estimation.md`, así que esa condición no aplica.

   ¿Confirmas lite (spec corta, sin `plan.md` ni `tasks.md`) o prefieres full? Sin confirmación explícita va en full. No propongo partir la task: es una sola fila, S, en un fichero.

4. **Perfil vigente: `pair`.** Sale de `.docs/sdd/sdd-kit.local.json` (nivel persona) y pisa el `delegate` de `sdd-kit.json` (proyecto). Con `pair` paro en la spec, en los desvíos y en la validación, y el plan también tiene gate (solo en full). ¿Lo mantienes o lo cambias para esta task (`delegate` o `unattended`)?

5. **Aprobación por delegación.** Como el perfil es `pair`, hay una opción extra: «apruebo la spec por delegación, nos vemos en la validación», para si te vas a ausentar. Con ella apruebo yo la spec y registro tu frase literal. La validación del final no se quita.

**Avisos sobre `sdd-kit.local.json`.** Solo admite `control.profile`, `execution` y `validation.startEnvironment`, así que ignoro estas dos claves:

- Aviso: se ignora `merge.noFf` de sdd-kit.local.json: solo admite control.profile, execution y validation.startEnvironment; lo demás es del proyecto y va en sdd-kit.json.
- Aviso: se ignora `ids.mode` de sdd-kit.local.json: solo admite control.profile, execution y validation.startEnvironment; lo demás es del proyecto y va en sdd-kit.json.

Siguen rigiendo `ids.mode: sequence` y `merge.noFf: true` de `sdd-kit.json`. Aquí el id 0012 sale de la fila del roadmap y la rama ya existe.

Contesta a los puntos 1, 3, 4 y 5 en un solo mensaje.

