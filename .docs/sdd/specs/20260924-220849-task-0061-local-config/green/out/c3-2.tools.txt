
>>> Skill: sdd-kit:sdd-config
<<< Launching skill: sdd-kit:sdd-config

>>> Bash: cd "<run>/salas"; ls -a .docs/sdd; cat .docs/sdd/sdd-kit.json; echo ---; cat .docs/sdd/sdd-kit.local.json; echo ---; cat .gitignore; git branch -a; git remote -v; git config user.name

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0061\ff83d6ab-5aea-4102-ad31-a3e458822b2e\scratchpad\kit-green\skills\sdd-start-task\references\control-profiles.md
<<< ./
../
changelog.md
constitution.md
roadmap.md
sdd-kit.json
{"version": "2.0.0", "channel": "plugin", "ids": {"mode": "sequence"}, "release": {"hasRecipient": false}, "control": {"profile": "delegate"}, "merge": {"into": "develop", "noFf": true, "removeWorktree": false}}
---
cat: .docs/sdd/sdd-kit.local.json: No such file or directory
---
.superpowers/
* develop
  main
Fixture
<<< 1	# Perfiles de control y gates
2	
3	Cuánto para el agente lo elige el usuario con un perfil. Cada gate de la tabla dice qué hace en cada uno. Contrato de las tasks 0005, 0006, 0012, 0021 y 0022: no lo dupliques, enlázalo.
4	
5	## Perfiles
6	
7	- **`pair`** — para en todos los gates de la tabla.
8	- **`delegate`** *(default)* — para en la spec, los desvíos y la validación; sin gate en el plan.
9	- **`unattended`** — no para en ningún punto hasta terminar la release, salvo el merge a `main`, el tag y las acciones hacia fuera (push, PR, publicar), que siempre decide una persona; el push de la ra

=== RESULTADO (5 turnos, 0.1904878 $)
Estado actual de la configuración:

| Clave | Valor | Fichero |
| --- | --- | --- |
| `ids.mode` | `sequence` | `sdd-kit.json` |
| `control.profile` | `delegate` | `sdd-kit.json` |
| `merge.into` / `noFf` / `removeWorktree` | `develop` / `true` / `false` | `sdd-kit.json` |
| `merge.push` | falta, rige `false` | |
| `control.maxParallelAgents` | falta, rige `3` | |
| `control.silence.betweenStepsMinutes` | falta, rige `8` | |
| `control.silence.longCommandMinutes` | falta, rige `20` | |
| `execution` | falta, rige `auto` | |
| `validation.startEnvironment` | falta, rige `false` | |

`sdd-kit.local.json` no existe, así que no hay claves ignoradas ni avisos. `.gitignore` tampoco tiene la línea del fichero local. La añadiré solo si acabo creándolo.

Hay cuatro claves sin decidir. Las preguntaré una por turno, empezando por el push:

**Tras fusionar en `develop`, ¿hago push de esa rama a su remoto sin preguntar?**

- **Sí (recomendado):** el repo sigue la convención `main` estable y `develop` de integración. `develop` es una rama compartida, y un merge sin push no lo ve nadie más. Escribiría `merge.push: true` en `sdd-kit.json`. El push de `main`, los tags y cualquier otro push seguirían siendo cosa de una persona.
- **No:** escribiría `merge.push: false`.
- **No sé:** no escribo nada y el cierre no hace push.

Ojo: `git remote -v` no devuelve ningún remoto. Con `merge.push: true` y sin remoto, el push fallaría al cerrar. Si aún no tenéis remoto, yo elegiría «No sé» y lo activaríamos cuando lo haya.
