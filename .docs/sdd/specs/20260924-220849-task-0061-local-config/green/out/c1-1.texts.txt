--- [1]
Skill `sdd-kit:sdd-config` aplica: preferencia personal de pair, sin cambiar la del equipo.

--- [7]
Marta, ya tienes tu configuración personal. El equipo sigue igual.

He creado `.docs/sdd/sdd-kit.local.json` con dos claves:

- `control.profile: "pair"`: paras en todos los gates, incluido el del plan.
- `validation.startEnvironment: true`: cuando te toque validar una task, te arranco el entorno antes de darte el guion de pruebas.

`sdd-kit.json` no lo he tocado. El proyecto sigue en `delegate` y el resto de la configuración (`ids`, `merge`, etc.) queda como estaba.

Este fichero es solo tuyo. Añadí `.docs/sdd/sdd-kit.local.json` a `.gitignore`, así que no se commitea. Ese cambio en `.gitignore` sí es un cambio del repo y está sin commitear (`M .gitignore`). Alguien tendrá que commitearlo para que a las demás personas del equipo también les ignore el fichero local. Yo no he hecho commit.

Como el fichero está ignorado, la configuración solo vale en esta copia del proyecto. Si trabajas desde otro clon o worktree, hay que repetirla.

En una task, el `profile:` del frontmatter de la spec manda sobre tu configuración personal. Si una task lo fija, prevalece esa.

