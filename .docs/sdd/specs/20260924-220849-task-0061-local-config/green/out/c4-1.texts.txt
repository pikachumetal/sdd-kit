--- [4]
Migración de kit: la gestiona sdd-init-brownfield.

--- [14]
Migrando el proyecto de v1.1.0 a v1.2.0: aplico primero los pasos sin gate (settings y .gitignore).

--- [17]
Migración a v1.2.0 a medias. Los pasos sin gate están aplicados y sin commit:

- **`.claude/settings.json`**: creado con `"autoMemoryEnabled": false`.
- **`.gitignore`**: añadidos `.playwright-mcp/` y `.docs/sdd/sdd-kit.local.json`. `.superpowers/` ya estaba.
- **Memoria automática**: la carpeta no tiene entradas, así que no hay nada que volcar.

Faltan las claves del kit (`ids`, `control.*`, `merge`, `execution`). Se preguntan de una en una vía `sdd-config`. No escribo el marcador ni hago commit hasta resolverlas.

**Pregunta 1 de 5: ¿cómo se numeran las tasks?**

- **`tracker`** (recomendada si usáis gestor de tickets): el id enlaza la task con su ticket.
- **`sequence`**: secuencia propia del proyecto; recomendada si no hay gestor de tickets.
- **«no sé»**: no se escribe nada y rige `tracker`.

