--- [5]
## Estado de la configuración

`.docs/sdd/sdd-kit.local.json` no existe. Los avisos de claves ignoradas no aplican.

| Clave | Valor | Origen |
| --- | --- | --- |
| `ids.mode` | `sequence` | `sdd-kit.json` |
| `control.profile` | `delegate` | `sdd-kit.json` |
| `merge.into` / `noFf` / `removeWorktree` | `develop` / `true` / `false` | `sdd-kit.json` |
| `merge.push` | falta; rige `false` | default |
| `control.maxParallelAgents` | falta; rige `3` | default |
| `control.silence.betweenStepsMinutes` | falta; rige `8` | default |
| `control.silence.longCommandMinutes` | falta; rige `20` | default |
| `execution` | falta; rige `auto` | default |
| `validation.startEnvironment` | falta; rige `false` | default |

`release.hasRecipient` está en `sdd-kit.json` pero no forma parte del catálogo de esta skill, así que no lo toco.

Faltan cuatro claves. Las pregunto de una en una. Esta es la primera:

**Tras fusionar en `develop`, ¿hago push de esa rama a su remoto sin preguntar (`merge.push: true`)?**

- **Sí (recomendado).** El repo tiene `main` y `develop`, es decir, git-flow. `develop` es la rama compartida y un merge sin push no lo ve nadie más. La rama estable, los tags y cualquier otro push siguen siendo decisión tuya.
- **No.** Escribe `merge.push: false` y el cierre nunca hace push.
- **No sé.** No escribo nada y el cierre no hace push.

Esta clave es del proyecto y va a `sdd-kit.json`, que está en git.

