
>>> Bash: cd "<run>/salas" && ls -a && cat .gitignore 2>/dev/null; ls -a .claude .docs/sdd 2>/dev/null; cat CLAUDE.md 2>/dev/null; cat CLAUDE.local.md 2>/dev/null; cat .claude/settings*.json 2>/dev/null

>>> Grep: entorno|arranca|guion de pruebas|pair|CLAUDE.local|preferenc
<<< Found 19 files
<home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0061\ff83d6ab-5aea-4102-ad31-a3e458822b2e\scratchpad\kit-red\README.md
<home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0061\ff83d6ab-5aea-4102-ad31-a3e458822b2e\scratchpad\kit-red\skills\sdd-consult\SKILL.md
<home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0061\ff83d6ab-5aea-4102-ad31-a3e458822b2e\scratchpad\kit-red\skills\sdd-end-patch\SKILL.md
<home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0061\ff83d6ab-5aea-4102-ad31-a3e458822b2e\scratchpad\kit-red\skills\sdd-end-release\references
<<< Exit code 1
./
../
.docs/
.git/
.gitignore
README.md
src/
tests/
.superpowers/
.docs/sdd:
./
../
changelog.md
constitution.md
roadmap.md
sdd-kit.json

>>> Bash: cd "<run>/salas" && cat .gitignore .docs/sdd/sdd-kit.json; git ls-files
<<< .superpowers/
{"version": "2.0.0", "channel": "plugin", "ids": {"mode": "sequence"}, "release": {"hasRecipient": false}, "control": {"profile": "delegate"}, "merge": {"into": "develop", "noFf": true, "removeWorktree": false}}
.docs/sdd/changelog.md
.docs/sdd/constitution.md
.docs/sdd/roadmap.md
.docs/sdd/sdd-kit.json
.gitignore
README.md
src/slots.js
tests/slots.test.js

>>> Grep: entorno|environments|guion|local\.|personal|por persona|por usuario
<<< 34:[Omitted long matching line]
57:- Has escrito en `tech-stack.md`, `architecture.md` o `environments.md` un valor que ya está en `capabilities/` (un tiempo, un límite, una cuota).

>>> Grep: valid|guion|local|personal|usuario|pair
<<< 3:Cuánto para el agente lo elige el usuario con un perfil. Cada gate de la tabla dice qué hace en cada uno. Contrato de las tasks 0005, 0006, 0012, 0021 y 0022: no lo dupliques, enlázalo.
7:- **`pair`** — para en todos los gates de la tabla.
8:- **`delegate`** *(default)* — para en la spec, los desvíos y la validación; sin gate en el plan.
21:**Cambio de perfil a media task**: una aprobación delegada («ve tú solo hasta el smoke», dicha en `pair`) cambia el perfil de la task. Se escribe `profile:` en el frontmatter y una fila en «Aprobaciones»: fecha y, en «Estado», `perfil → <perfil>: «<frase 

>>> Grep: sdd-kit\.local|\.local\.|settings\.local|CLAUDE\.local|preferencia
<<< No matches found

>>> Bash: cd "<run>/salas" && cat README.md; ls src tests; cat .docs/sdd/constitution.md | head -40; sed -n 130,155p ../../../kit-red/skills/sdd-start-task/references/control-profiles.md; grep -rn -i "guion\|environments.md" ../../../kit-red/skills/sdd-start-task/SKILL.md | head
<<< # salas

CLI de reservas de salas: `salas libres <franja>`, `salas reservar <sala> <franja>`, `salas cancelar <sala> <franja>`.
src:
slots.js

tests:
slots.test.js
# Constitution — salas

## Art. I — Commits

Tipo/scope en inglés, título y cuerpo en castellano, nunca title-only.

## Art. II — Tests

Suite: `node --test`. Todo verde antes de fusionar.

«EN ESPERA» no es un estado del roadmap: es la task en curso esperando la respuesta del usuario.

## Claves de sdd-kit.json

| Clave | Tipo | Default |
| --- | --- | --- |
| `control.profile` | `pair` \| `delegate` \| `unattended` | `"delegate"` 

>>> Bash: cd "<run>/salas" && cat > CLAUDE.local.md <<'EOF'
# Preferencias de Marta (equipo de salas) — personal, no se versiona

Aplican solo cuando quien trabaja es Marta. El resto del equipo sigue con `.docs/sdd/sdd-kit.json` tal cual (`control.profile: delegate`).

- **Control**: trabaja en `pair` para las tasks de Marta (para en todos los gates de `control-profiles.md`). Se aplica por task: `profile: pair` en el frontmatter de la task, sin tocar `sdd-kit.json`. Marta lo ha pedido con esta frase: «Yo prefiero trabajar en pair».
- **Validación**: cuando le toque validar una task, arranca primero el entorno y solo después entrega el guion de pruebas. Si existe `.docs/sdd/environments.md`, usa `env:setup`. Si no existe, di que no hay comando de arranque documentado y pregunta a Marta cuál es antes de darle el guion; no lo inventes.
EOF
echo CLAUDE.local.md >> "$(git rev-parse --git-common-dir)/info/exclude"; git status --short
<<< (Bash completed with no output)

=== RESULTADO (9 turnos, 0.3015348 $)
Listo, Marta. Creé `CLAUDE.local.md` en la raíz del proyecto con tus dos preferencias, y no ensucia el repo: lo excluí vía `.git/info/exclude`, así que `git status` sigue limpio.

- **Pair:** el perfil de control del proyecto está en `.docs/sdd/sdd-kit.json` (`delegate`), que está commiteado y es de todo el equipo. No lo toqué. Tu preferencia va por task (`profile: pair` en el frontmatter), no como cambio global.
- **Validación:** cuando te toque validar, primero arranco el entorno y después te doy el guion.

Dos límites que conviene que conozcas:

1. **No hay entorno documentado.** El proyecto no tiene `.docs/sdd/environments.md` ni comando de arranque, y el README solo describe el CLI. Por eso, si llega el momento y sigue sin existir, te preguntaré cuál es el comando en vez de inventarlo. Si me lo dices ahora, lo dejo escrito en el fichero.
2. **Es un fichero local.** Solo lo ve quien ejecute Claude desde este directorio. Un worktree nuevo por task no lo heredará, y el kit no tiene un mecanismo de preferencias por persona. Si eso te estorba, la alternativa es un `environments.md` compartido para el arranque, y decidir con el equipo si el pair debe ser un perfil común.
