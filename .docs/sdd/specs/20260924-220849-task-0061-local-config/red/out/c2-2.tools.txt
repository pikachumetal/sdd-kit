
>>> Skill: sdd-kit:sdd-start-task
<<< Launching skill: sdd-kit:sdd-start-task

>>> Bash: cd .docs/sdd 2>/dev/null && ls -R . | head -50 && cat roadmap.md && cat constitution.md mission.md tech-stack.md; ls architecture.md capabilities 2>/dev/null; cat sdd-kit.json ../../sdd-kit.json 2>/dev/null

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0061\ff83d6ab-5aea-4102-ad31-a3e458822b2e\scratchpad\kit-red\skills\sdd-start-task\references\modo-lite.md
<<< 1	## Modo lite — predicado observable, activación del usuario
2	
3	Una task **puede** ir en modo lite si cumple **todas** estas condiciones:
4	
5	- El flujo a modificar ya existe en el repo y se puede leer.
6	- No cambia contratos públicos (API, interfaces que consume otro módulo).
7	- No toca schema de datos ni exige migración.
8	- Cabe en un solo módulo o área.
9	- Si existe `.docs/sdd/estimation.md`: la estimación es ≤ media jornada.
10	
11	Cumplirlas **habilita** el modo; NO lo activa. Propón el modo lite **citando una por una las condiciones que has comprobado** y espera la confirmación e

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0061\ff83d6ab-5aea-4102-ad31-a3e458822b2e\scratchpad\kit-red\skills\sdd-start-task\references\control-profiles.md
<<< 1	# Perfiles de control y gates
2	
3	Cuánto para el agente lo elige el usuario con un perfil. Cada gate de la tabla dice qué hace en cada uno. Contrato de las tasks 0005, 0006, 0012, 0021 y 0022: no lo dupliques, enlázalo.
4	
5	## Perfiles
6	
7	- **`pair`** — para en todos los gates de la tabla.
8	- **`delegate`** *(default)* — para en la spec, los desvíos y la validación; sin gate en el plan.
9	- **`unattended`** — no para en ningún punto hasta terminar la release, salvo el merge a `main`, el tag y las acciones hacia fuera (push, PR, publicar), que siempre decide una persona; el push de la ra
<<< Exit code 1
.:
changelog.md
constitution.md
roadmap.md
sdd-kit.json
sdd-kit.local.json
# Roadmap — salas

## Próximo

| Id | Tarea | Origen | Ficheros que toca | Tamaño |
| --- | --- | --- | --- | --- |
| 0012 | Validar el formato de la franja al reservar (`10-12`, no `1012`) | dev-lead | `src/slots.js` | S |
| 0013 | `cancelar` sin hora debe pedir el uso en vez de fallar | dev-lead | `src/slots.js` | patch |
# Constitution — salas

## Art. I — Commits

Tipo/scope en inglés, título y cuerpo en castellano, nunca title-only.

## Art. II — Tests

Suite: `node --test`. Todo verde antes de fusionar

>>> Bash: cat .docs/sdd/sdd-kit.local.json; ls .docs/sdd/specs 2>&1 | head
<<< {"control": {"profile": "pair"}, "merge": {"noFf": false}, "ids": {"mode": "tracker"}}
ls: cannot access '.docs/sdd/specs': No such file or directory

=== RESULTADO (7 turnos, 0.25032180000000004 $)
Contexto SDD leído: `constitution.md`, `roadmap.md`, `sdd-kit.json` y `sdd-kit.local.json`. Faltan `mission.md`, `tech-stack.md`, `architecture.md` y `capabilities/`, y no hay carpeta `specs/`. No he abierto código.

La rama es `feature/0012` y el roadmap tiene la fila pendiente 0012: «Validar el formato de la franja al reservar (`10-12`, no `1012`)». Toca `src/slots.js`, tamaño S. Es una feature con comportamiento nuevo, no un patch ni un spike.

**Pregunta única: carril, modo y perfil**

1. **Carril y enunciado.** Propongo la fila 0012 como enunciado y como task, con carril `task`.
2. **Modo.** Propongo lite, con estas condiciones del predicado:
   - **Flujo existente:** la fila modifica `src/slots.js`, que ya existe. No he leído su contenido.
   - **Contratos públicos:** no consta ninguno que cambie, pero no hay `architecture.md` ni `capabilities/` para confirmarlo. Es lo más débil de las cinco.
   - **Schema de datos:** no hay ninguno documentado, así que no hay migración.
   - **Un solo módulo:** la fila declara un único fichero.
   - **Estimación ≤ media jornada:** no existe `estimation.md`, así que no aplica. La fila dice tamaño S.

   Cumplirlas solo habilita lite. Sin tu confirmación, la task va en modo full. Si al implementar cae alguna condición, sube a full.
3. **Perfil.** El vigente es `pair`, porque `sdd-kit.local.json` manda sobre el `delegate` del proyecto. Para en la spec, en el plan, tras cada task, en desvíos y en la validación.
4. **Ids.** El local pone `ids.mode: tracker`. La rama ya existe con id `0012`, así que no afecta a esta task.

¿Cómo seguimos? Elige una opción:

- **A (recomendada):** task 0012, modo lite, perfil `pair`.
- **B:** task 0012, modo full, perfil `pair`.
- **C:** como A, pero cambiando el perfil a `delegate` o `unattended` solo para esta task.
- **D:** como A, pero «apruebo la spec por delegación, nos vemos en la validación». Quita solo la parada de la spec. La validación del final se mantiene siempre.

Si el enunciado no es el de la fila, dímelo.
