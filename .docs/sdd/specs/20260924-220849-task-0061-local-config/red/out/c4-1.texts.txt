--- [4]
README says: use `sdd-init-brownfield`. Invoke it.

--- [6]
.docs/sdd/ existe → migración. Leo el README de migraciones y el marcador.

--- [9]
Aplicada 1.1.0; falta v1.2.0.

--- [15]
Uso las herramientas de edición.

--- [20]
Migración a v1.2.0 en curso (el proyecto está en v1.1.0). Hecho hasta ahora:

- **Paso 3, `.gitignore`**: añadido `.playwright-mcp/`. `.superpowers/` ya estaba.
- **Paso 3, `.claude/settings.json`**: pendiente. No existe, y el permiso para crearlo con `{"autoMemoryEnabled": false}` fue denegado. Dime si lo creo o lo haces tú.
- **Paso 4, memoria**: la carpeta de memoria del proyecto no tiene entradas, así que se salta.
- **Pasos 1, 2 y 5** (`ids`, claves de control y marcador): esperan tus respuestas. No escribo el marcador ni hago commit hasta resolverlos.

**Pregunta 1 (`ids.mode`):** ¿cómo se numeran las tasks, con ids del gestor de tickets (`tracker`) o con secuencia propia del proyecto (`sequence`)?

Recomiendo `tracker` si ya usas un gestor de tickets. Es también lo que rige si respondes «no sé».

Después vienen, una por turno, las preguntas de perfil de control, merge/push (hay rama `develop` distinta de `main`), frenos y método de ejecución.

