
>>> Skill: sdd-kit:sdd-start-task
<<< Launching skill: sdd-kit:sdd-start-task

>>> Bash: cd "<run>/salas" && ls -a .docs/sdd .docs/sdd/specs/*; cat .docs/sdd/sdd-kit.json .docs/sdd/sdd-kit.local.json 2>&1; cat .docs/sdd/specs/20260923-100000-task-0012-franja/spec.md
<<< .docs/sdd:
./
../
changelog.md
constitution.md
roadmap.md
sdd-kit.json
specs/

.docs/sdd/specs/20260923-100000-task-0012-franja:
./
../
spec.md
{"version": "2.0.0", "channel": "plugin", "ids": {"mode": "sequence"}, "release": {"hasRecipient": false}, "control": {"profile": "delegate"}, "merge": {"into": "develop", "noFf": true, "removeWorktree": false}}
cat: .docs/sdd/sdd-kit.local.json: No such file or directory
---
id: 20260923-100000-task-0012-franja
task: 0012
title: Validar el formato de la franja
mode: full
status: draft
created: 2026-09-23
approvers:
  - role: dev-lead
    name: Dev Lea

=== RESULTADO (4 turnos, 0.39669540000000003 $)
Ahora: presento la spec de la 0012 para que la apruebes. Queda: tu aprobación. Luego escribo el plan, unos 10 minutos, sin subagentes.

Perfil vigente: `delegate`, sacado de `.docs/sdd/sdd-kit.json`. No hay `sdd-kit.local.json`. Modo `full`. La rúbrica dejó la spec sin review de subagente.

## Decisiones que he tomado yo: valida estas

1. **El formato válido es solo el patrón `HH-HH`.** La spec no dice qué pasa con `12-10` (fin antes que inicio), `25-30` (horas imposibles) o `9-11` (un solo dígito). Leída al pie de la letra, `25-30` y `12-10` pasan la validación. Te recomiendo añadir un escenario que exija horas 00–23 e inicio < fin. Si no, el mensaje «Franja no válida» engaña al usuario en esos casos. Si prefieres dejarlo fuera, lo anoto en «No entra».
2. **Un único mensaje en los dos comandos.** `reservar` y `libres` devuelven el mismo literal: «Franja no válida: usa HH-HH, p. ej. 10-12». He comprobado el literal y coincide en los dos escenarios.
3. **La capacidad `booking` ya existe.** No se crea ninguna capacidad nueva, así que no hace falta slug.
4. **Faltan secciones de la plantilla.** La spec no tiene Scope/Approach ni el bloque de decisiones. Los añado al plasmar tu respuesta; no cambian el comportamiento.

## Qué pide la spec

`salas reservar Norte 1012` hoy acepta una franja sin sentido. Se añaden dos escenarios ADDED en `booking`:
- **Reservar** con una franja que no casa con `HH-HH` falla con «Franja no válida: usa HH-HH, p. ej. 10-12».
- **Consultar libres** con una franja así falla con el mismo mensaje.

## Sobre el modelo de la sesión

Esta sesión va con Opus 5.5, el modelo más capaz. Si el plan sale Native, la sesión implementa ella todas las tasks. Para eso basta la gama media (Sonnet con effort medium), y bajar solo el effort de Opus no es gama media. Yo no puedo cambiar mi propio modelo. En `delegate`, después de aprobar ya no hay otra parada donde puedas cambiarlo tú. Por eso tienes la opción C.

¿Apruebas la spec? Contesta con una de estas opciones:
- **A (recomendada):** «Apruebo con el escenario de rango (horas 00–23 e inicio < fin)». Lo añado y sigo con el plan.
- **B:** «Apruebo tal cual». El rango queda en «No entra».
- **C:** «Apruebo; escribe el plan y, si sale Native, para antes de la Task 1 para que baje la sesión a gama media». Dime también si quieres el escenario de rango.
- **D:** «Cambios: …»
