
>>> Skill: sdd-kit:sdd-end-task
<<< Launching skill: sdd-kit:sdd-end-task

>>> Bash: cd "<run>/salas" && git status --short && find .docs .claude -type f 2>/dev/null | grep -v node_modules | head -80 && cat .docs/sdd/sdd-kit.json 2>/dev/null
<<< .docs/sdd/changelog.md
.docs/sdd/constitution.md
.docs/sdd/roadmap.md
.docs/sdd/sdd-kit.json
.docs/sdd/specs/20260923-100000-task-0012-franja/plan.md
.docs/sdd/specs/20260923-100000-task-0012-franja/spec.md
.docs/sdd/specs/20260923-100000-task-0012-franja/tasks.md
{"version": "2.0.0", "channel": "plugin", "ids": {"mode": "sequence"}, "release": {"hasRecipient": false}, "control": {"profile": "delegate"}, "merge": {"into": "develop", "noFf": true, "removeWorktree": false}}

>>> Bash: cd "<run>/salas" && D=.docs/sdd/specs/20260923-100000-task-0012-franja; cat $D/spec.md $D/plan.md $D/tasks.md; echo ----; cat .docs/sdd/roadmap.md .docs/sdd/changelog.md; git log --format='%h %ad %s' --date=iso main..HEAD; git ls-files | head -30; ls -a

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0058-session-model-policy\1baf4cfe-fb0c-44d5-b5ff-116095bb7d46\scratchpad\kit-green\skills\sdd-end-task\references\aprendizajes-skills.md
<<< ---
id: 20260923-100000-task-0012-franja
task: 0012
title: Validar el formato de la franja
mode: full
status: approved
created: 2026-09-23
approvers:
  - role: dev-lead
    name: Dev Lead
    approved_at: 2026-09-23
---

# Spec — Validar el formato de la franja

## Intent

`salas reservar Norte 1012` reserva una franja sin sentido. Se quiere un error claro.

## Delta de comportamiento

### Capacidad: `booking`

**ADDED — La franja se valida al reservar**
- GIVEN una franja que no casa con `HH-HH`
- WHEN se reserva
- THEN falla con «Franja no válida: usa HH-HH, p. ej. 10-12»

**ADDED — La franj
<<< 1	# Aprendizajes a docs vivos y revisión de skills — detalle
2	
3	## Paso 4 — Aprendizajes
4	
5	4. **Aprendizajes → docs vivos** — cada aprendizaje del walkthrough se vuelca donde vive: convención nueva → `constitution.md`; cambio estructural → `architecture.md`; versión/herramienta → `tech-stack.md`; cambio de comportamiento observable → el delta de la spec se fusiona en `capabilities/<capability>.md` (ADDED añade, MODIFIED sustituye entero el requisito con ese título, REMOVED quita; las entradas de «Reglas de la capacidad» se sustituyen o añaden por su nombre, sin marcas; nunca una capacidad

>>> Bash: cd "<run>/salas" && cat src/slots.js; cat .docs/sdd/constitution.md | head -40; node --test 2>&1 | tail -12; ls ../../../kit-green/skills/sdd-templates ../../../kit-green/skills/sdd-templates/* | head -50; cat ../../../kit-green/skills/add-to-changelog/SKILL.md
<<< const SLOT = /^\d{2}-\d{2}$/;

export function reserve(room, slot) {
  if (!SLOT.test(slot)) throw new Error('Franja no válida: usa HH-HH, p. ej. 10-12');
  return { room, slot };
}

export function free(slot) {
  if (!SLOT.test(slot)) throw new Error('Franja no válida: usa HH-HH, p. ej. 10-12');
  return [];
}
# Constitution — salas

## Art. I — Commits

Tipo/scope en inglés, título y cuerpo en castellano, nunca title-only.

## Art. II — Tests

Suite: `node --test`. Todo verde antes de fusionar.
✔ libres rechaza una franja sin guion (1.3976ms)
✔ rechaza una franja sin guion (1.4215ms)
✔ reser

>>> Bash: cd "<run>/salas" && T=../../../kit-green/skills/sdd-templates/templates; cat $T/walkthrough-template.md $T/capability-template.md $T/roadmap-template.md; cat .githooks/pre-commit; cat README.md; git show --stat HEAD | head -20
<<< ---
id: <yyyyMMdd-HHmmss>-task-<id>-<slug>
task: <id>
title: Walkthrough — <título de la spec>
spec: ./spec.md
plan: ./plan.md
status: done
created: <YYYY-MM-DD>
---

# Walkthrough — <título>

> Documento post-implementación: resume lo que se hizo REALMENTE (no lo planeado), las desviaciones,
> la verificación con evidencia y los aprendizajes que vuelven a los docs vivos. El cuerpo no se
> reescribe tras el cierre: lo que cambia después (validación tardía, integración con otra task) se
> añade como entrada fechada en `## 6. Adendas`. Borra los bloques de ayuda (`>`) al redactar.

>>> Bash: cd "<run>/salas" && D=.docs/sdd/specs/20260923-100000-task-0012-franja
python - <<'EOF'
import re
D=".docs/sdd/specs/20260923-100000-task-0012-franja"
p=D+"/tasks.md"; s=open(p,encoding="utf-8").read()
s=s.replace("| 1 | Validar al reservar | pending | — | |","| 1 | Validar al reservar | done | 9b5f912 | |")
s=s.replace("| 2 | Validar al consultar libres | pending | — | |","| 2 | Validar al consultar libres | done | 2a52033 | |")
open(p,"w",encoding="utf-8",newline="\n").write(s)

p=".docs/sdd/changelog.md"; s=open(p,encoding="utf-8").read().rstrip("\n")
s+="\n\n### Added\n\n- **Task 0012** — `reserve` y `free` rechazan una franja que no casa con `HH-HH` con «Franja no válida: usa HH-HH, p. ej. 10-12». → [ref](specs/20260923-100000-task-0012-franja/)\n"
open(p,"w",encoding="utf-8",newline="\n").write(s)

p=".docs/sdd/roadmap.md"; s=open(p,encoding="utf-8").read()
s=s.replace("| 0012 | Validar el formato","| 0012 | ✅ Validar el formato")
s=s.rstrip("\n")+"""

## Deuda técnica

| Ítem | Impacto | Destino |
| --- | --- | --- |
| La validación de la franja y su mensaje se repiten en `reserve` y `free` (`src/slots.js`); revisión final de la task 0012, minor diferido | bajo: un mensaje que cambie hay que tocarlo en dos sitios | extraer al tocar `src/slots.js` (p. ej. patch 0013) |
| Ningún test cubre una franja válida como `10-12` en la validación nueva (revisión final de la task 0012, minor diferido) | bajo: una regex demasiado estricta no se detectaría | añadir el caso al tocar `tests/slot-format.test.js` |
| La capacidad `booking` de la spec 0012 no tiene `capabilities/booking.md`: los dos ADDED no se han fusionado | medio: el comportamiento observable no queda en un doc vivo | que una spec declare `booking` en «Decisiones que he tomado yo» y el dev-lead apruebe el slug |
"""
open(p,"w",encoding="utf-8",newline="\n").write(s)
EOF
cat > $D/walkthrough.md <<'EOF'
---
id: 20260923-100000-task-0012-franja
task: 0012
title: Walkthrough — Validar el formato de la franja
spec: ./spec.md
plan: ./plan.md
status: done
created: 2026-09-25
---

# Walkthrough — Validar el formato de la franja

## 1. Cambios realizados

- `src/slots.js`: `reserve(room, slot)` lanza «Franja no válida: usa HH-HH, p. ej. 10-12» si la franja no casa con `/^\d{2}-\d{2}$/` — Task 1, `9b5f912`, test `tests/slot-format.test.js`.
- `src/slots.js`: `free(slot)` aplica la misma validación y el mismo mensaje — Task 2, `2a52033`, test `tests/free-format.test.js`.
- `f6321f1`: revisión final de rama apuntada en `tasks.md`.

## 2. Tiempo y coste: estimado vs real

- No aplica: el proyecto no tiene `.docs/sdd/estimation.md`.
- Modelo del hilo: Opus 5.5, effort medium (spec y plan) → Sonnet 5, effort medium (ejecución, Native)
- Tokens del hilo: no medido
- Tokens de subagentes: no medido; 1 despacho — revisión final de rama, `sdd-kit:effort-high` con Opus (effort high)

## 3. Desviaciones del plan

- _Ninguna_

### Decisiones tomadas sin el dev-lead

- Deferred minor: la validación y el mensaje se repiten en `reserve` y `free` — se dejó duplicado por ser dos tasks encadenadas y pequeñas — coste si está mal: un cambio de mensaje hay que hacerlo en dos sitios.
- Deferred minor: ningún test cubre una franja válida como `10-12` — no se añadió, fuera del RED de cada task — coste si está mal: una regex demasiado estricta no se detectaría.

## 4. Verificación

### 4.1 Builds

- Sin build (JS sin compilar). `node --test` en la rama: 3 tests, 3 pass, 0 fail.

### 4.2 Smoke / tests

- Validado por el dev-lead: 2026-09-25 · «he probado `salas reservar Norte 1012` y `salas libres 1012` y los dos dan el error de la spec; funciona».
- Revisión final de rama (Opus, effort high): Ready, 0 Critical, 0 Important, 2 Minor (los dos diferidos de arriba).

| # | Caso | Resultado |
| --- | --- | --- |
| 1 | Suite `node --test` (verificado por el agente) | 3/3 pass |
| 2 | `salas reservar Norte 1012` (reportado por el dev-lead) | error «Franja no válida…» |
| 3 | `salas libres 1012` (reportado por el dev-lead) | error «Franja no válida…» |

### 4.3 Residuales / deuda generada

- Los dos minors diferidos y la capacidad `booking` sin fichero → filas en «Deuda técnica» del roadmap.

## 5. Aprendizajes

- La spec declara la capacidad `booking` pero no existe `capabilities/booking.md` ni la spec la declaró en «Decisiones que he tomado yo»; `sdd-end-task` no la crea por su cuenta → fila de deuda técnica en `roadmap.md`, delta sin fusionar.
- Sin convención, cambio estructural ni de versión que volcar → `constitution.md` sin cambios; no existen `architecture.md` ni `tech-stack.md` y no hay aprendizaje estructural que los requiera.
- Revisión de skills: el proyecto no tiene `.claude/skills/`; el trabajo no revela patrón reutilizable → no aplica.

## 6. Adendas

_Ninguna_
EOF
git status --short
<<<  M .docs/sdd/changelog.md
 M .docs/sdd/roadmap.md
 M .docs/sdd/specs/20260923-100000-task-0012-franja/tasks.md
?? .docs/sdd/specs/20260923-100000-task-0012-franja/walkthrough.md

=== RESULTADO (9 turnos, 0.35216600000000003 $)
Cierre de la task 0012 hecho hasta el paso 9. Dejé los cambios sin commitear y sin `env:clean`, sin merge ni push. Eso es del paso 10.

**Qué hice**
- **Paso 0:** la spec está `approved` con aprobación firmada. Tu frase dice qué probaste y que funciona, así que cuenta como validación.
- **Paso 1:** creé `walkthrough.md`. Marca como verificado por mí la suite (`node --test`, 3/3 pass) y como reportado por ti los dos comandos que probaste. Incluye el modelo del hilo (Opus 5.5 medium → Sonnet 5 medium) y los dos *Deferred minors* como decisiones sin el dev-lead.
- **Pasos 2 y 3:** no aplican, porque no existe `estimation.md`.
- **Paso 4:** no hay convención, cambio estructural ni de versión que volcar. Sobre la capacidad `booking`, ver «Pendiente».
- **Paso 5:** no existe `.claude/skills/`, así que no aplica.
- **Paso 6:** en `tasks.md` las dos tasks estaban en `pending` sin commit. Las puse en `done` con `9b5f912` y `2a52033`.
- **Paso 7:** entrada en `Added` de `[Unreleased]` en `changelog.md`.
- **Paso 8:** 0012 marcada ✅ en `roadmap.md`. Esa tabla no tiene columna Estado, así que puse el ✅ como prefijo en «Tarea» en vez de añadir columna. Añadí una sección «Deuda técnica» con tres filas: los dos minors diferidos y la capacidad `booking`.
- **Paso 9:** hubo revisión final de rama, con Opus y effort high. Salió Ready, 0 Critical, 0 Important y 2 Minor. No lancé otra.

**Pendiente**
- El delta de la spec no está fusionado en `capabilities/booking.md`. Ese fichero no existe y la spec no declaró la capacidad en «Decisiones que he tomado yo», así que `sdd-end-task` no puede crearlo. Está anotado como deuda: hace falta que una spec declare `booking` y que apruebes el slug.
- Los dos minors diferidos son la validación duplicada en `reserve` y `free`, y que ningún test cubre una franja válida como `10-12`. Ambos están en la deuda técnica del roadmap.

**No terminado.** Faltan el commit de cierre y el paso 10 (`env:clean` no aplica porque no hay `environments.md`, y falta el merge a `develop` con `--no-ff`). No borres todavía el worktree.

Cuando termine la task, puedo generar el ticket de mejora del kit con `sdd-feedback` si quieres.
