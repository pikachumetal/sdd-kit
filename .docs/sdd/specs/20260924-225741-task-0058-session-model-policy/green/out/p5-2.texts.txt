--- [9]
Leyendo la plantilla del plan y las referencias del kit para redactar `plan.md`.

--- [20]
Ahora: presentar el plan y un problema de la spec que cambia su forma. Queda: tu respuesta a la enmienda y después el gate del plan (aprobarlo y elegir el método); ~5 min.

**Perfil:** `pair`, fijado en `.docs/sdd/sdd-kit.json`. No hay `sdd-kit.local.json`. `execution` no está fijado, así que el método lo recomienda el plan.

He escrito el plan en `.docs/sdd/specs/20260923-100000-task-0012-franja/plan.md`. Todavía no he hecho commit: la apertura se junta cuando apruebes el plan.

## El problema: la spec pide validar una función que no existe

El segundo escenario de la spec es «La franja se valida al consultar libres», pero en el código no hay consulta de salas libres:
- `src/slots.js` solo exporta `reserve`.
- El README anuncia `salas libres <franja>`, pero no hay CLI ni ninguna función detrás.

Para cumplir ese escenario habría que crear una función pública nueva y decidir qué devuelve, y no hay datos de salas. Eso es otra funcionalidad, no validar un formato.

Te propongo la enmienda **E1**, ya escrita en `spec.md` bajo `## Enmiendas` y sin aprobar. El escenario pasaría a «No entra», y la consulta de libres usará el mismo validador cuando exista. El plan ya está escrito con esta opción: una sola task.

## Decisiones del plan (valida estas)

1. **Formato:** solo se acepta `^\d{2}-\d{2}$`, dos dígitos, guion y dos dígitos. `9-11` se rechaza, hay que escribir `09-11`. No compruebo el rango (`25-30`) ni el orden (`12-10`), porque la spec pide formato y no significado.
2. **Error:** `Error('Franja no válida: usa HH-HH, p. ej. 10-12')`, lanzado antes de crear la reserva. El validador `assertSlot` es privado y no se exporta hasta que tenga un segundo uso.
3. **Tests:** uno para `1012` y uno para los casos límite: `9-11`, espacios, cadena vacía y `undefined`.
4. **Ejecución:** recomiendo Native. Es una sola task de unas 10 líneas en un fichero, así que basta la revisión final de rama, con `sdd-kit:effort-medium` y `model: opus`.
5. **Coste:** unos 15 minutos de implementación y una revisión final de un diff muy pequeño.

Cuando respondas a la enmienda, te paso la pregunta del gate del plan. Entre sus opciones estará aprobar con Native y parar antes de la Task 1 para que bajes esta sesión a gama media (Sonnet, effort medium): la sesión va en Opus y no puede cambiar su propio modelo.

**¿Apruebas la enmienda E1: el escenario «consultar libres» sale de la task 0012?**

