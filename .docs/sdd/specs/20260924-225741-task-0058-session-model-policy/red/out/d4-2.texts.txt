--- [15]
Ahora: te presento la spec de la 0012 para que la apruebes (perfil `delegate`). Queda: tu aprobación. Después vienen el plan y la implementación sin más paradas hasta la validación, unos 15 minutos.

## Decisiones que he tomado yo — valida estas

La spec no trae su propio bloque de decisiones, así que lo he montado yo con lo que la spec decide sin decirlo:

1. **Formato `HH-HH` con dos dígitos por hora.** Tal como está escrito, `9-12` no pasa y `09-12` sí. `25-30` y `12-10` pasan, porque solo se mira la forma y no el rango ni el orden. Mi recomendación es no ampliar esta task: el roadmap pide el caso `1012` y nada más.
2. **Mensaje literal único:** «Franja no válida: usa HH-HH, p. ej. 10-12». Es el mismo para reservar y para consultar libres.
3. **Capacidad `booking`, con 2 escenarios ADDED.** Hoy no existe `capabilities/`, así que `booking` sería una capacidad nueva. La spec no la declara como tal.

## Lo que no me convence de la spec

- **El segundo escenario («consultar libres») se sale de la fila del roadmap.** La fila dice «al reservar» y solo toca `src/slots.js`. Además, `libres` no existe en el código: `src/slots.js` solo exporta `reserve`, y `libres` solo aparece en `README.md`. Validar la franja de algo que no está implementado obliga a crearlo o deja el escenario sin nada contra lo que probarlo. Mi recomendación es quitar ese escenario de esta task y apuntarlo para cuando exista `libres`.
- **Faltan secciones de la plantilla:** Scope, «No entra» y Approach. Con el alcance tan corto no es grave, pero el punto 1 (sin validar rango ni orden) debería quedar escrito en «No entra».

Si apruebas con cambios, ajusto la spec antes de seguir: quito el escenario de libres, añado «No entra» y declaro `booking` como capacidad nueva. La review de spec no aplica: el cambio es de tamaño S y tiene un único fichero.

¿Apruebas la spec (tal cual o con los cambios de arriba)?

