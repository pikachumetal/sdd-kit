--- [8]
Now the plan template from sdd-templates.

--- [17]
Ahora: parada en tu gate del plan. Queda: tus dos respuestas; después junto la apertura en un commit y empieza la Task 1 (~15 min).

Tienes el plan en `.docs/sdd/specs/20260923-100000-task-0012-franja/plan.md`. No hay nada commiteado ni código tocado. La suite base está en verde (`node --test`: 1 pass).

## Decisiones que he tomado yo — valida estas

1. **El escenario «La franja se valida al consultar libres» no tiene dónde implementarse.** `src/slots.js` solo tiene `reserve`, y `libres` solo aparece en el README. El plan cubre solo el escenario de reservar. En `spec.md` he añadido la enmienda **E1**, pendiente de tu aprobación, para mover el escenario de libres a una fila nueva del roadmap.
2. **Formato `/^\d{2}-\d{2}$/`, leyendo «HH-HH» al pie de la letra.** `9-11` falla porque tiene un solo dígito. `25-99` y `12-10` pasan, porque la spec no pide comprobar el rango ni el orden. Lo dejo anotado en Review Focus como posible task aparte.
3. **El error es `throw new Error('Franja no válida: usa HH-HH, p. ej. 10-12')`.** No hay CLI que lo capture.
4. **Una sola task, sin `tasks.md`.** Tiene 4 tests RED: `1012`, `9-11`, franja vacía o ausente, y franja con espacios.
5. **Ejecución Native.** Para una task de 3 líneas, un subagente implementador más su revisor cuesta más de lo que aporta.
6. **Revisor final: `sdd-kit:effort-low` + `sonnet`.** El diff tiene unas 10 líneas. Coste estimado: ~0,25 h de implementación y céntimos en tokens.

**Pregunta 1 — Enmienda E1** (es un cambio a la spec aprobada, así que la decides tú):
- A) Apruebo E1: libres sale de la 0012 y va a una fila nueva del roadmap *(recomendada: hoy no existe la operación, y crearla en esta task sería alcance nuevo)*.
- B) Crear `libres` dentro de la 0012. En ese caso, dime qué debe devolver; rehago el plan y te lo vuelvo a presentar.

**Pregunta 2 — Gate del plan** (vale para el plan tal como está, que da por hecha la opción A):
- «Apruebo, con Native (Recomendada)»
- «Apruebo, con Subagent-driven»
- «Cambios»

¿Apruebas el plan?

