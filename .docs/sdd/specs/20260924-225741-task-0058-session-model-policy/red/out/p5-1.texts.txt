--- [8]
Leyendo la plantilla del plan y las referencias del kit antes de escribirlo.

--- [22]
Plan y enmienda escritos. Compruebo que la regex se comporta como dice el plan.

--- [25]
Ahora: presentar el plan para el gate de aprobación. Queda: tu respuesta. Después, el commit de apertura y el arranque de la Task 1 (~15 min).

He escrito el plan en `.docs/sdd/specs/20260923-100000-task-0012-franja/plan.md`. También he propuesto una enmienda en `spec.md`, que sigue pendiente. No he tocado código ni he hecho commits.

**Decisiones que he tomado yo — valida estas**

1. **Enmienda a la spec: el escenario «consultar libres» sale de esta task.** `src/slots.js` solo tiene `reserve`, y no existe ninguna función ni CLI de libres donde validar. Crear ahora una `free(slot)` obligaría a inventar qué devuelve, y la spec no lo fija. La propuesta añade una fila nueva al roadmap para la consulta de libres, que reutilizará la misma validación. La he dejado en `## Enmiendas` como «pendiente de aprobación».
2. **Formato `/^\d{2}-\d{2}$/`, solo formato.** `9-12` falla porque tiene una sola cifra. `25-99` y `12-10` pasan: el rango y el orden de las horas no están en la spec.
3. **El error es un `Error` que lanza `reserve`**, con el mensaje literal de la spec. En el repo no hay CLI que lo imprima.
4. **Una sola task**, así que no hay `tasks.md`. Los tests RED cubren el THEN y el Review Focus: una cifra, espacios alrededor y la franja ausente. He comprobado la regex con esos casos en `node`.
5. **Ejecución recomendada: Native.** Es una task aislada y el plan ya trae el código. El revisor final va con `sdd-kit:effort-high` + `opus`.
6. **Coste:** unos 15 minutos y un solo subagente, el revisor final, por menos de 1 $.

Si apruebas, antes de la Task 1 haré tres cosas: reservar el id de la fila nueva con `Get-NextSddId.ps1 -Reserve`, añadir esa fila al roadmap y hacer el commit de apertura con la spec, la enmienda y el plan. Ahora mismo la base está limpia: `develop` coincide con el merge-base, así que no hay freno.

Opciones:
- **Apruebo el plan y la enmienda, con Native (recomendada)**
- **Apruebo el plan y la enmienda, con Subagent-driven**
- **Cambios** (por ejemplo, si prefieres mantener el escenario de libres e implementar aquí la consulta)

¿Apruebas el plan y la enmienda, y con qué método?

