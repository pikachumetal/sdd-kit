## HEAD antes: 1a68c42 · después: 1a68c42
## contador .git/sdd-ids
0014
## ramas
  develop
* feature/0014
  main
## git log
* 1a68c42 feat: base de reservas de salas
## status
## feature/0014
?? .docs/sdd/specs/20260924-113528-patch-0014-reserve-franja-vacia/patch.md
## specs/
20260924-113528-patch-0014-reserve-franja-vacia
## roadmap (develop)
| 0012 | Validar el formato de la franja al reservar (`10-12`, no `1012`) | dev-lead | `src/slots.js` | S |
| 0013 | `cancelar` sin hora debe pedir el uso en vez de fallar | dev-lead | `src/slots.js` | patch |
## llamadas a Get-NextSddId
Get-NextSddId.ps1
Get-NextSddId.ps1'
Get-NextSddId.ps1 -ProjectRoot <raíz del proyecto> -Reserve`. Sin `-Reserve` el script solo propone un id, y otro worktree que calcule a la vez se lleva el mismo. **Nunca el nombre de un módulo** (
Get-NextSddId.ps1 -ProjectRoot D:
Get-NextSddId.ps1 -Reserve` si no tiene fila (sin `-Reserve` solo propone, y otro worktree puede coger el mismo) — comparte secuencia con las tasks y nunca reutiliza un id entre carriles (detalle en `sdd-start-task/references/nombrado.md`)). Los artefactos viven SOLO ahí: no existe `.docs/sdd/patches/` ni ninguna otra ubicación, por ordenada que parezca.
Get-NextSddId.ps1 -Reserve`, con `parent: <id>` en el frontmatter **y su fila propia en el roadmap** — nunca un sufijo tipo `0006a`. Sin esa fila, la relación entre las dos mitades se pierde en cuanto se cierra la sesión.
Get-NextSddId.ps1 -Reserve`. No hay fila en el roadmap. El modo es `sequence`.
Get-NextSddId.ps1; cat 
