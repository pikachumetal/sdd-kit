## HEAD antes: 41fe7a2 · después: ec0a306
## contador .git/sdd-ids
0014
## ramas
  develop
* feature/0012
  main
## git log
* ec0a306 docs(0014): task nueva por la partición de la 0012
* 41fe7a2 docs(0012): spec aprobada de la task 0012
* 40d3bb9 feat: base de reservas de salas
## status
## feature/0012
## specs/
20260923-100000-task-0012-franja
20260924-113525-task-0014-franja-libres
## roadmap (develop)
| 0012 | Validar el formato de la franja al reservar (`10-12`, no `1012`) | dev-lead | `src/slots.js` | S |
| 0013 | `cancelar` sin hora debe pedir el uso en vez de fallar | dev-lead | `src/slots.js` | patch |
## llamadas a Get-NextSddId
Get-NextSddId*' && cat $(find . -name 'Get-NextSddId.ps1' | head -1)
Get-NextSddId.ps1
Get-NextSddId.ps1 -ProjectRoot <raíz del proyecto> -Reserve`. Sin `-Reserve` el script solo propone un id, y otro worktree que calcule a la vez se lleva el mismo. **Nunca el nombre de un módulo** (
Get-NextSddId.ps1 -ProjectRoot D:
Get-NextSddId.ps1 -Reserve`, con `parent: <id>` en el frontmatter **y su fila propia en el roadmap** — nunca un sufijo tipo `0006a`. Sin esa fila, la relación entre las dos mitades se pierde en cuanto se cierra la sesión.
Get-NextSddId.ps1 -Reserve`, porque el proyecto está en modo `sequence`.
Get-NextSddId.ps1'; pwsh -NoProfile -File $k -ProjectRoot (Get-Location).Path -Reserve; Get-Date -AsUTC -Format 'yyyyMMdd-HHmmss'
Get-NextSddId.ps1`, nunca de una convención inventada en el momento. |
Get-NextSddId.Tests.ps1
