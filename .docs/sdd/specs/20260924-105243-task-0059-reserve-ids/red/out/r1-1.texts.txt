--- [9]
Release 2 está escrita en el roadmap y publicada en `develop` (commit `e18500f`, solo toca `roadmap.md`). No he arrancado ninguna task.

Ids reservados con `Get-NextSddId.ps1 -Reserve`:

| id | Task | Ficheros que toca |
| --- | --- | --- |
| 0014 | Exportar las reservas a CSV | `src/export.js` (nuevo), `src/slots.js` |
| 0015 | Listado de reservas por sala | `src/slots.js` |
| 0016 | Avisar cuando una reserva solapa con otra | `src/slots.js` |

- **Estado:** la sección queda «en preparación». El proyecto tiene `release.hasRecipient: false`, así que nunca se marca «comprometida».
- **Solape de ficheros:** las tres tasks comparten `src/slots.js` con 0012 y 0013, que siguen en «Próximo». Conviene fijar el orden al arrancarlas. Lo dejé anotado en la sección.
- **Ramas:** no hay ramas `feature/*` ni otros worktrees, así que no había estado que reconciliar.
- **Changelog:** ya tiene un `[Unreleased]` abierto.

Cuando quieras arrancar la primera, dime cuál y lanzo `sdd-start-task`.

