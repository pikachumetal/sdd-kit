--- [3]
Ahora: leo el contexto SDD (roadmap, spec de la 0012, reglas de nombrado y plantilla) para crear la task nueva. Queda: fila, carpeta con `spec.md` y commit, ~5 min.

--- [22]
Creados la fila y el `spec.md`. Ahora reordeno la fila 0014 detrás de la 0013 y commiteo.

--- [24]
El commit falló porque `git commit -F -` no leyó el mensaje de la here-string. Uso un fichero temporal.

--- [26]
Commit `765fe37` en `feature/0012`: la task nueva es la **0014**, con fila en el roadmap y carpeta `.docs/sdd/specs/20260924-112810-task-0014-franja-libres/spec.md`. No hay plan ni código, como pedías.

- **Fila y id:** la fila 0014, tamaño S, va con `parent: 0012` en el frontmatter y toca `src/slots.js`. Reservé el id con `Get-NextSddId.ps1 -Reserve`, que da 0014 porque el roadmap ya tenía la 0012 y la 0013.
- **Spec sin aprobar:** el `spec.md` está en `draft`, con la aprobación del dev-lead pendiente, porque solo pediste abrir fila y carpeta. Lleva el requisito «La franja se valida al consultar libres» copiado tal cual de la 0012. La decisión de partir está registrada con tu instrucción como frase literal.
- **Falta tocar la spec de la 0012:** sigue teniendo el escenario de consultar libres, así que ahora está duplicado. No la toqué porque está aprobada y quitarle un requisito es un cambio a una spec aprobada. Hace falta una enmienda que lo quite y deje el Scope solo en reservar, y esa la tiene que aprobar el dev-lead.
- **Orden:** la 0014 reutiliza la validación de la 0012, así que se implementa después.

Sigo con la 0012, o me paro aquí hasta que vuelva el dev-lead.

