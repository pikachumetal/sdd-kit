--- [8]
Reproducido: devuelve `{ room: 'Norte', slot: '' }`. Leo nombrado, plantilla y script de ids.

--- [15]
Patch abierto. Paro antes de tocar `src/slots.js`.

- **Reproducido**: `reserve('Norte', '')` devuelve `{ room: 'Norte', slot: '' }`.
- **Causa raíz**: `reserve` es `return { room, slot };` (`src/slots.js:1-3`) y no valida ningún argumento. Es el único punto de entrada, sin otros llamadores.
- **Id**: `0014`. El proyecto usa `ids.mode: sequence` y no hay fila en el roadmap, así que lo reservé con `Get-NextSddId.ps1`.
- **Rama**: `feature/0014`, creada desde `develop`.
- **Carpeta**: `.docs/sdd/specs/20260924-113115-patch-0014-reserve-franja-vacia/`.
- **`patch.md`**: tiene síntoma y causa. El fix queda como «pendiente» y `status: in-progress`. Todavía no hay commit.

**Solape con la dev-lead**: las filas 0012 (validar el formato de la franja) y 0013 (`cancelar` sin hora) del roadmap también tocan `src/slots.js`. Dejé el patch acotado a rechazar la franja vacía y no interpreta ningún formato. Aun así, si la dev-lead cambia `reserve` para 0012, habrá conflicto al fusionar, y conviene avisarle. No he tocado el roadmap.

**Siguiente paso** (cuando me lo indiques): test rojo en `tests/slots.test.js`, guard mínimo en `reserve` que lance error con franja vacía, `node --test` en verde, un commit de fix y luego `sdd-end-patch`. Antes necesito que confirmes el mensaje o tipo de error que quieres lanzar. Si no lo tienes claro, uso `throw new Error('La franja es obligatoria')`.

