--- [6]
Causa confirmada en el código: `reserve` no valida `slot`. Falta reproducir, leer config y plantilla.

--- [23]
Patch abierto. Paro antes de tocar `src/slots.js`, como pediste.

- **Reproducido**: `reserve('Norte', '')` devuelve `{"room":"Norte","slot":""}` sobre `develop` @ `dab031f`.
- **Causa raíz**: `reserve` no valida `slot` y devuelve `{ room, slot }` tal cual (`src/slots.js:1-3`). Es el único punto de entrada.
- **Id**: `0014`, reservado con `Get-NextSddId.ps1 -Reserve` (modo `sequence`, sin fila en el roadmap).
- **Rama**: `feature/0014`, creada desde `develop`.
- **Carpeta**: `.docs/sdd/specs/20260924-112818-patch-0014-reserve-franja-vacia/`.
- **`patch.md`**: contiene el síntoma medido y la causa con su evidencia. El fix está descrito. La verificación y el hash del commit quedan pendientes.

Todavía no he hecho commit. La carpeta y el `patch.md` están sin commitear en `feature/0014`, y el paso 5 los junta con el fix en un solo commit.

Dos cosas para el dev-lead cuando vuelva:
- **Roadmap 0012** (validar el formato de la franja, task) toca el mismo `src/slots.js`. Este patch cubre solo la franja vacía. Si 0012 acaba validando también lo vacío, este fix quedaría absorbido. Lo he anotado en el `patch.md`.
- **Roadmap 0013** también toca `src/slots.js` (`cancelar` sin hora). Puede dar conflicto al fusionar.

Siguiente paso, cuando lo apruebes: test rojo en `tests/slots.test.js`, guard en `reserve`, `node --test` en verde, commit único y `sdd-end-patch`.

