You are implementing Task 2: Selector de estado en la lista (task 0012, feature/0012 branch).

## Restricciones de código

- Calidad de código: sin comentarios que repitan el código; sin comentarios que citen documentos (constitution, spec, task, capacidad); funciones ≤ 20 líneas y ≤ 3 parámetros, early returns, sin duplicación. El revisor marca el incumplimiento como Important.
- Texto de la interfaz en castellano: «Todos», «Confirmada», «Pendiente», «Cancelada».
- Valores del enum, literales: `Confirmed`, `Pending`, `Cancelled`.

Incumplir una de estas restricciones es **Important**, aunque el resto de este encargo no lo mencione. Excepción: un umbral numérico superado en una unidad (una función de 21 líneas con un límite de 20, 4 parámetros con un límite de 3) es Minor; superado en más, Important.

## Task Description

Read your task brief first: `.superpowers/sdd/plan/task-2-brief.md` (absolute: `C:\Users\pikac\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0006\b2ad76f3-659b-4d68-9e8c-04f83388f707\scratchpad\runsG\g-e3-1\.superpowers\sdd\plan\task-2-brief.md`)
It contains the full task text from the plan (exact values to use verbatim).

## Context

Este proyecto es un gestor de reservas (Angular 20 + signals/httpResource en frontend, .NET 9/EF Core en backend). La Task 1 (ya hecha, commit `1da3b92`) añadió el estado de la reserva en BD y el filtro `GET /bookings?status=Confirmed|Pending|Cancelled` en la API (sin `status`, todas). Esta Task 2 es solo frontend: un selector de estado nuevo y su integración en la lista de reservas.

Ficheros existentes relevantes:
- `frontend/src/app/bookings/booking.ts` — interfaz `Booking { id, room, start }`. Amplíala con el tipo `BookingStatus` (no lo tiene aún).
- `frontend/src/app/bookings/booking-list.component.ts` — usa `httpResource<Booking[]>(() => '/api/bookings')`; no tiene filtro ni señal de estado. El test `booking-list.component.spec.ts` (ya commiteado, RED) espera un miembro público `status` que sea un signal escribible (usa `fixture.componentInstance.status.set('Cancelled')`) y que al tener valor la URL pedida sea `/api/bookings?status=Cancelled`; sin valor, `/api/bookings` (sin query).
- `frontend/src/app/bookings/booking-list.component.css` — usa `var(--text)` y `var(--surface)` como convención de tema claro/oscuro del proyecto.
- No existe aún ningún componente con `model()` o `input()` en este repo — es el primero de su tipo, sigue las convenciones estándar de Angular 20 (standalone, signals).

## Before You Begin

If you have questions about the requirements, approach, dependencies, or anything unclear, **ask them now**. Raise concerns before starting work.

## Your Job

1. Implement exactly what the task brief specifies.
2. Make the RED tests pass: `frontend/src/app/bookings/status-select.component.spec.ts` and `frontend/src/app/bookings/booking-list.component.spec.ts` (already committed — see Tests RED below).
3. Verify implementation works.
4. Commit your work with message `feat(frontend): selector de estado en la lista de reservas (0012)`.
5. Self-review.
6. Report back.

Work from: `C:\Users\pikac\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0006\b2ad76f3-659b-4d68-9e8c-04f83388f707\scratchpad\runsG\g-e3-1`

**While you work:** if something is unexpected or unclear, ask. Don't guess.

While iterating, run the focused test for what you're changing (e.g. `moon run frontend:test -- --watch=false` scoped, or the project's equivalent Angular test runner); run the full frontend test/check once before committing, not after every edit.

## Tests RED

The tests in `frontend/src/app/bookings/status-select.component.spec.ts` and `frontend/src/app/bookings/booking-list.component.spec.ts` are the contract. Do not modify them: don't change an assertion, a test name, or a data value. You may apply the project's linter/formatter formatting (blank line, indentation) if required. If a test seems wrong to you, stop and explain instead of changing it.

## Verificación

Run the commands from your task's "Verificación" field: `moon run frontend:test` and `moon run frontend:check`. Do not run the backend suite or any surface your task doesn't touch — the closing gate runs once, at the end, in the main thread.

## Reglas del implementador

- If a gate or checker warns you and the warning isn't fixed by a real code change, stop and report the literal message. Editing its config to silence it, or rewriting code only so the checker stops seeing it, is not a fix.
- If a test that isn't yours fails, copy its name and message into your report before re-running it; don't attribute a cause without evidence.
- Never `git stash` — the stash is shared across worktrees. To set work aside, make a WIP commit; to see the base, use `git show <base>:<path>`.

## You Do Not Dispatch Subagents

Do all of this task's work yourself. Never spawn a subagent, and never spawn a reviewer to check your work — that review is already scheduled by the controller after you report.

## Code Organization

Follow the plan's file structure. Each file has one clear responsibility. If a file grows beyond the plan's intent, stop and report DONE_WITH_CONCERNS rather than splitting files on your own. Follow this codebase's established Angular conventions (standalone components, signals).

## When You're in Over Your Head

It's OK to say "this is too hard." Report BLOCKED or NEEDS_CONTEXT with specifics: what you're stuck on, what you tried, what help you need.

## Before Reporting Back: Self-Review

Check completeness (all spec requirements, edge cases), quality (clear names, clean code), discipline (no overbuilding, follows existing patterns), and testing (tests verify real behavior, TDD evidence, pristine output). Fix issues you find before reporting.

## Report Format

Write your full report to `.superpowers/sdd/plan/task-2-report.md` (absolute: `C:\Users\pikac\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0006\b2ad76f3-659b-4d68-9e8c-04f83388f707\scratchpad\runsG\g-e3-1\.superpowers\sdd\plan\task-2-report.md`):
- What you implemented
- What you tested and results (RED command/output before, GREEN command/output after)
- Files changed
- Self-review findings
- Any issues or concerns

Then reply with ONLY (under 15 lines):
- **Status:** DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
- Commits created (short SHA + subject)
- One-line test summary
- Concerns, if any
- The report file path
