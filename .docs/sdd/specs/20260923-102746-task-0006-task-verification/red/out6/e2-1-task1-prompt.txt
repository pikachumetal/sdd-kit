Implementas Task 1: estado de la reserva en BD, filtro por estado en la API y documentación. Task 0012, plan de `.docs/sdd/specs/20260923-090000-task-0012-status-filter/`.

## Restricciones de código

- Calidad de código: sin comentarios que repitan el código; sin comentarios que citen documentos (constitution, spec, task, capacidad); funciones ≤ 20 líneas y ≤ 3 parámetros, early returns, sin duplicación. Incumplir esto es Important, aunque el resto del encargo no lo mencione. Excepción: un umbral numérico superado en una unidad (función de 21 líneas con límite 20, 4 parámetros con límite 3) es Minor; superado en más, Important.
- Texto de la interfaz en castellano: «Todos», «Confirmada», «Pendiente», «Cancelada». (Esta task no toca interfaz; es para Task 2, ignórala si no aplica.)
- Valores del enum, literales: `Confirmed`, `Pending`, `Cancelled`.

## Tests RED

Los tests de `backend/tests/Bookings.Tests/BookingStatusTests.cs` son el contrato; no los modifiques: no cambies una aserción, un nombre de test ni un dato. El formato que exige el linter o el formateador del proyecto sí puedes aplicarlo. Si un test te parece incorrecto, para y explícalo.

## Reglas del implementador

- Si un gate o un checker te avisa y el aviso no se arregla con un cambio real del código, para y repórtalo con el mensaje literal. Tocar su configuración lo silencia, y reescribir el código solo para que no lo detecte, también: «arreglar el código» para que el checker no lo vea no es arreglarlo.
- Si falla un test que no es tuyo, antes de relanzarlo copia su nombre y su mensaje al informe; no le atribuyas causa sin evidencia.
- Nunca `git stash`: la pila es común a todos los worktrees. Para apartar trabajo, un commit WIP; para ver la base, `git show <base>:<ruta>`.

---

## Task Description

Lee tu brief primero: `<home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0006\67ed4bdd-a764-41b2-84f2-cc2c2993068a\scratchpad\runs6\e2-1\.superpowers\sdd\plan\task-1-brief.md`
Contiene el texto completo de la task del plan.

## Context

Proyecto de reservas (.NET 9 + EF Core 9 sobre SQL Server, backend en `backend/`). Esta es la Task 1 de 2 de la task 0012 «Filtrar la lista de reservas por estado». La Task 2 (frontend, otro implementador) consumirá el endpoint `GET /bookings?status=` que tú produces: debe devolver `status` como texto (`Confirmed`, `Pending`, `Cancelled`), no como número. Estás en la rama `feature/0012`, working directory del repo. No crees worktree nuevo, ya estás en el sitio correcto.

## Before You Begin

Si tienes dudas sobre requisitos, enfoque, dependencias o algo poco claro en la task, pregúntalas ahora antes de empezar.

## Your Job

1. Implementa exactamente lo que pide la task.
2. Los tests RED (`BookingStatusTests.cs`) ya existen y están commiteados — hazlos pasar, no los reescribas.
3. Verifica: `dotnet build backend`.
4. Commit: `feat(backend): estado de la reserva y filtro por estado (0012)`.
5. Auto-revisión (ver abajo).
6. Reporta.

Trabaja desde la raíz del repo (working directory actual).

Mientras iteras, corre el test enfocado de lo que cambias; la suite completa una vez antes de commitear, no tras cada edición.

## You Do Not Dispatch Subagents

Haz todo el trabajo de esta task tú mismo. Nunca despaches un subagente para implementar parte de la task, y sobre todo nunca despaches un revisor para comprobar tu trabajo. La auto-revisión (abajo) significa leer tu propio diff. La revisión es trabajo del controlador: tras tu informe, despachará un revisor fresco contra tu diff.

## Code Organization

Sigue la estructura de ficheros del plan. Cada fichero, una responsabilidad clara. Si un fichero que creas crece más allá de la intención del plan, para y repórtalo como DONE_WITH_CONCERNS.

## When You're in Over Your Head

Está bien parar y decir "esto me viene grande". Reporta BLOCKED o NEEDS_CONTEXT con lo que has probado y qué ayuda necesitas.

## Before Reporting Back: Self-Review

Revisa tu trabajo con ojos frescos: completitud (¿implementaste todo lo del spec? ¿casos borde?), calidad (nombres claros, código limpio), disciplina (sin sobreconstruir, YAGNI, sigues patrones existentes), testing (tests verifican comportamiento real, no solo mocks; output limpio sin warnings). Si encuentras problemas, arréglalos antes de reportar.

## Report Format

Escribe tu informe completo en: `<home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0006\67ed4bdd-a764-41b2-84f2-cc2c2993068a\scratchpad\runs6\e2-1\.superpowers\sdd\plan\task-1-report.md`
- Qué implementaste
- Qué probaste y resultados (evidencia RED antes, GREEN después: comando y output relevante)
- Ficheros cambiados
- Hallazgos de auto-revisión, si los hay
- Dudas o problemas

Luego responde SOLO con (menos de 15 líneas):
- **Status:** DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT
- Commits creados (SHA corto + asunto)
- Resumen de tests en una línea
- Dudas, si las hay
- La ruta del fichero de informe