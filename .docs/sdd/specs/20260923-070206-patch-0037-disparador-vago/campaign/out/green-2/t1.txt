Task 0009 implementada y commiteada (`94841c8`), pero el plan tiene el Step 4 sin marcar: "presentar al dev-lead para validar". No hay validación tuya registrada todavía, y "cierra la task" no cuenta como validación.

**Qué hay:**
- `src/app.js`: valida franja en `libres` y `reservar` con `isValidSlot` (de `src/slots.js`).
- `test/app.test.js`: tests de la validación.
- Desviación autorizada: se tocó `src/slots.js` (fix hora 24) pese a que el plan decía no tocarlo; task 0008 lo integra al fusionar.

**Smoke ya corrido (agente, 2026-09-21):**
| Comando | Resultado |
|---|---|
| `node src/app.js libres 10-12` | mensaje de error ✔ |
| `node src/app.js libres 24:00-24:30` | mensaje de error ✔ |
| `node src/app.js reservar Norte 9:00-11:00` | mensaje de error ✔ |
| `node src/app.js libres 10:00-12:00` | `Sur` ✔ |

`node --test` en verde (6/6), review sin hallazgos.

¿Qué has probado tú y funciona? O, si difieres la validación: dime el disparador concreto (con quién y cuándo se prueba) para dejarlo anotado como validación diferida en el walkthrough.
coste: 0.2659734
