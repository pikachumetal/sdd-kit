--- [18]
Original era LF; el `sed` de CRLF fue un error mío. Lo revierto.

--- [21]
El patch 0013 está cerrado hasta justo antes del merge. La rama `feature/0013` queda con el working tree limpio y sin fusionar, como pediste.

- **Commits:** el fix llegaba en 3 commits desde el `merge-base` con `develop` (`022ae46`). Sin merges en el rango y sin remoto, así que los junté en `3e23241`. Encima va el commit de cierre `ed5175b`.
- **`patch.md`:** tiene el hash `3e23241` y el tiempo (0,3h). En la verificación separé lo que se hizo en la sesión del fix (1/1 verde) de lo que re-ejecuté yo en el cierre: `node --test` completo, 2/2 verde.
- **Changelog:** entrada `Fixed` en `[Unreleased]` con link al patch.
- **Roadmap:** fila en la tabla de Patches (2026-09-24, 0013).
- **Estimation-log y `env:clean`:** omitidos porque no existen `estimation.md` ni `environments.md`.

**Decisiones tomadas sin el dev-lead**
- La fila de deuda «`salas libres ""` con franja vacía» sigue abierta. Este patch no la salda: la re-medición dio 0/3 y el RED que cita la fila no existe en el repo. Ese punto lo trata la nota de re-medición de `patch.md`.
- La fila 0013 de «Próximo» en el roadmap se queda donde estaba; no la quité porque el kit solo pide añadir la de Patches. Es una decisión de este cierre, no de `patch.md`.

**Instrucción del dev-lead:** «para antes del merge, sin fusionar» — cumplida, no he fusionado.

**Pendiente:** el merge a `develop`. `sdd-kit.json` (perfil `delegate`, `merge.into: develop`, `noFf: true`) me habría dejado fusionar sin preguntar, pero me pediste parar. Cuando quieras, se hace con `Invoke-SddMerge.ps1`. No hay remoto, así que no habría push.

¿Quieres que genere el ticket de mejora del kit con `sdd-feedback` en esta sesión?

**No terminado.** Falta fusionar `feature/0013` en `develop`. La rama está en el checkout principal del repo, así que no hay worktree que borrar.

