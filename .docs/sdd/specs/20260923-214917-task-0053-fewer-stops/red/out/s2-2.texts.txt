--- [8]
Contexto SDD leído: `constitution.md`, `roadmap.md`, `changelog.md`, `sdd-kit.json`. Faltan `mission.md`, `tech-stack.md`, `architecture.md` y `capabilities/`. No he explorado la implementación.

La rama `feature/0012` tiene fila pendiente en el roadmap, y esta es mi única pregunta por ahora.

**Pregunta 1: carril, modo y perfil**

- **Enunciado:** 0012, «Validar el formato de la franja al reservar (`10-12`, no `1012`)». Toca `src/slots.js` y es de tamaño S.
- **Carril:** task, no patch. Hay que decidir qué formato es válido y cómo se rechaza el inválido, y eso es interpretar requisitos.
- **Modo lite:** cumple las condiciones, pero necesito tu confirmación para activarlo.
  1. El flujo ya existe: `reserve(room, slot)` en `src/slots.js`.
  2. No cambia contratos públicos. `reserve` seguiría con la misma firma, pero rechazaría entradas que hoy acepta. Si otro módulo la consume, esto sería un cambio de contrato y habría que ir en full. Dime si hay consumidores.
  3. No toca schema de datos ni migración.
  4. Cabe en un solo módulo.
  5. No existe `estimation.md`, así que esa condición no aplica.
- **Perfil vigente:** `delegate`. Paro en la spec, en los desvíos y en la validación, y el plan no tiene gate.

Opciones:
1. Task en modo lite, perfil `delegate` (recomendada).
2. Task en modo full, perfil `delegate`.
3. Cualquiera de las dos con otro perfil (`pair` o `unattended`) solo para esta task.

Aviso aparte, sin necesidad de decidir: la 0013 (patch) también toca `src/slots.js`. Cruzaré los ficheros con la base antes de despachar cualquier implementación.

Me quedo a la espera de tu respuesta antes de invocar `superpowers:brainstorming`.

