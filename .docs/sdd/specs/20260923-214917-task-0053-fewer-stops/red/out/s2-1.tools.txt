
>>> Skill: sdd-kit:sdd-start-task
<<< Launching skill: sdd-kit:sdd-start-task

>>> Bash: cd "<run>/salas" && find .docs/sdd -type f | head -50 && for f in constitution mission tech-stack roadmap architecture; do echo "=== $f"; cat .docs/sdd/$f.md 2>/dev/null; done
<<< Exit code 1
.docs/sdd/changelog.md
.docs/sdd/constitution.md
.docs/sdd/roadmap.md
.docs/sdd/sdd-kit.json
=== constitution
# Constitution — salas

## Art. I — Commits

Tipo/scope en inglés, título y cuerpo en castellano, nunca title-only.

## Art. II — Tests

Suite: `node --test`. Todo verde antes de fusionar.
=== mission
=== tech-stack
=== roadmap
# Roadmap — salas

## Próximo

| Id | Tarea | Origen | Ficheros que toca | Tamaño |
| --- | --- | --- | --- | --- |
| 0012 | Validar el formato de la franja al reservar (`10-12`, no `1012`) | dev-lead | `src/slots.js` | S |
| 0013 | `cancelar` sin ho

>>> Bash: cd "<run>/salas" && cat .docs/sdd/sdd-kit.json .docs/sdd/changelog.md; ls -R . | grep -v .git | head -30; cat ~/.claude/plugins 2>/dev/null | head -0; cat "<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0053/6efeee6b-be23-429a-8015-cf6790d4a3e7/scratchpad/kit-red/skills/sdd-start-task/references/control-profiles.md" | head -60

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0053\6efeee6b-be23-429a-8015-cf6790d4a3e7\scratchpad\kit-red\skills\sdd-start-task\references\modo-lite.md
<<< {"version": "2.0.0", "channel": "plugin", "ids": {"mode": "sequence"}, "release": {"hasRecipient": false}, "control": {"profile": "delegate"}, "merge": {"into": "develop", "noFf": true, "removeWorktree": false}}
# Changelog

## [Unreleased]
.:
README.md
src/
tests/

./src:
slots.js

./tests:
slots.test.js
# Perfiles de control y gates

Cuánto para el agente lo elige el usuario con un perfil. Cada gate de la tabla dice qué hace en cada uno. Contrato de las tasks 0005, 0006, 0012, 0021 y 0022: no lo dupliques, enlázalo.

## Perfiles

- **`pair`** — para en todos los gates de la tabla.
- *
<<< 1	## Modo lite — predicado observable, activación del usuario
2	
3	Una task **puede** ir en modo lite si cumple **todas** estas condiciones:
4	
5	- El flujo a modificar ya existe en el repo y se puede leer.
6	- No cambia contratos públicos (API, interfaces que consume otro módulo).
7	- No toca schema de datos ni exige migración.
8	- Cabe en un solo módulo o área.
9	- Si existe `.docs/sdd/estimation.md`: la estimación es ≤ media jornada.
10	
11	Cumplirlas **habilita** el modo; NO lo activa. Propón el modo lite **citando una por una las condiciones que has comprobado** y espera la confirmación e

>>> Bash: cd "<run>/salas" && cat README.md src/slots.js tests/slots.test.js; git branch -a; cat "<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0053/6efeee6b-be23-429a-8015-cf6790d4a3e7/scratchpad/kit-red/skills/sdd-start-task/references/nombrado.md"
<<< # salas

CLI de reservas de salas: `salas libres <franja>`, `salas reservar <sala> <franja>`, `salas cancelar <sala> <franja>`.
export function reserve(room, slot) {
  return { room, slot };
}
import { test } from 'node:test';
import assert from 'node:assert';
import { reserve } from '../src/slots.js';

test('reserva una sala en una franja', () => {
  assert.deepStrictEqual(reserve('Norte', '10-12'), { room: 'Norte', slot: '10-12' });
});
  develop
* feature/0012
  main
## Nombrado de carpetas de spec

`<yyyyMMdd-HHmmss>-(task|patch)-<id>-<slug>`, todo en UTC:

- Timestamp: `Get-Date -AsUT

=== RESULTADO (7 turnos, 0.24009 $)
Contexto SDD leído (`constitution.md`, `roadmap.md`, `sdd-kit.json`; no existen `mission`, `tech-stack`, `architecture` ni `capabilities/`). La rama `feature/0012` ya existe y el roadmap tiene la fila pendiente 0012. Me paro aquí, en la primera pregunta del turno, que va sola.

## Pregunta 1 — carril, modo, perfil y enunciado

**Enunciado propuesto (de la fila 0012 del roadmap):** «Validar el formato de la franja al reservar (`10-12`, no `1012`)». Toca `src/slots.js`, tamaño S.

**Carril:** task, no patch. Hay que decidir qué es un formato válido, y eso es interpretar requisitos. Por ejemplo: ¿`10-12` con horas de 1 o 2 dígitos? ¿hora inicio menor que hora fin? ¿rango 0–24? ¿qué error se devuelve? Tampoco es una pregunta de viabilidad, así que no es un spike.

**Modo:** propongo **lite**. He comprobado las condiciones del predicado una por una:

1. **El flujo ya existe y se puede leer:** `reserve(room, slot)` en `src/slots.js` (3 líneas) y su test.
2. **No cambia contratos públicos:** solo el CLI, y `reserve` está sin consumidores en el repo. Ojo: si el error pasa a lanzar una excepción, cambia el comportamiento observable de `reserve`.
3. **Sin schema ni migración:** no hay datos persistentes.
4. **Un solo módulo:** `src/slots.js`.
5. **Estimación ≤ media jornada:** no existe `estimation.md`, así que esa condición no aplica. El tamaño S de la fila es coherente.

El plan prevé 1 task interna, muy por debajo del umbral de 3, así que no propongo partirla.

**Perfil vigente:** `delegate` (de `sdd-kit.json`). Para en la spec, en los desvíos y en la validación; no tiene gate en el plan. Se puede cambiar para esta task.

Además, `sdd-kit.json` declara `ids.mode: sequence` y la fila ya tiene id, así que la carpeta será `<timestamp UTC>-task-0012-<slug>`. La rama ya existe, así que no hay que crearla.

**Necesito que el dev-lead responda a tres cosas:**

- ¿Confirmas el enunciado de la fila 0012 tal cual?
- ¿Modo **lite** (recomendado) o **full**?
- ¿Perfil `delegate` (recomendado) o `pair`/`unattended` para esta task?

Cuando conteste, invoco `superpowers:brainstorming` para el diseño de la spec. Ahí preguntaré el detalle del formato válido: dígitos, orden, rango y forma del error.
