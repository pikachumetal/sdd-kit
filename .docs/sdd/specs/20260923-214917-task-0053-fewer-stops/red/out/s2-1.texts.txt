--- [8]
Contexto SDD leído (`constitution.md`, `roadmap.md`, `sdd-kit.json`; no existen `mission`, `tech-stack`, `architecture` ni `capabilities/`). La rama `feature/0012` ya existe y el roadmap tiene la fila pendiente 0012. Me paro aquí, en la primera pregunta del turno, que va sola.

## Pregunta 1 — carril, modo, perfil y enunciado

**Enunciado propuesto (de la fila 0012 del roadmap):** «Validar el formato de la franja al reservar (`10-12`, no `1012`)». Toca `src/slots.js`, tamaño S.

**Carril:** task, no patch. Hay que decidir qué es un formato válido, y eso es interpretar requisitos. Por ejemplo: ¿`10-12` con horas de 1 o 2 dígitos? ¿hora inicio menor que hora fin? ¿rango 0–24? ¿qué error se devuelve? Tampoco es una pregunta de viabilidad, así que no es un spike.

**Modo:** propongo **lite**. He comprobado las condiciones del predicado una por una:

1. **El flujo ya existe y se puede leer:** `reserve(room, slot)` en `src/slots.js` (3 líneas) y su test.
2. **No cambia contratos públicos:** solo el CLI, y `reserve` está sin consumidores en el repo. Ojo: si el error pasa a lanzar una excepción, cambia el comportamiento observable de `reserve`.
3. **Sin schema ni migración:** no hay datos persistentes.
4. **Un solo módulo:** `src/slots.js`.
5. **Estimación ≤ media jornada:** no existe `estimation.md`, así que esa condición no aplica. El tamaño S de la fila es coherente.

El plan prevé 1 task interna, muy por debajo del umbral de 3, así que no propongo partirla.

**Perfil vigente:** `delegate` (de `sdd-kit.json`). Para en la spec, en los desvíos y en la validación; no tiene gate en el plan. Se puede cambiar para esta task.

Además, `sdd-kit.json` declara `ids.mode: sequence` y la fila ya tiene id, así que la carpeta será `<timestamp UTC>-task-0012-<slug>`. La rama ya existe, así que no hay que crearla.

**Necesito que el dev-lead responda a tres cosas:**

- ¿Confirmas el enunciado de la fila 0012 tal cual?
- ¿Modo **lite** (recomendado) o **full**?
- ¿Perfil `delegate` (recomendado) o `pair`/`unattended` para esta task?

Cuando conteste, invoco `superpowers:brainstorming` para el diseño de la spec. Ahí preguntaré el detalle del formato válido: dígitos, orden, rango y forma del error.

