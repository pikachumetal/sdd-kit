
>>> Skill: sdd-kit:sdd-start-task
<<< Launching skill: sdd-kit:sdd-start-task

>>> Bash: cd .docs/sdd 2>/dev/null && ls -R . | head -50 && for f in constitution mission tech-stack roadmap architecture; do echo "=== $f"; cat $f.md 2>/dev/null; done
<<< Exit code 1
.:
changelog.md
constitution.md
roadmap.md
sdd-kit.json
=== constitution
# Constitution — salas

## Art. I — Commits

Tipo/scope en inglés, título y cuerpo en castellano, nunca title-only.

## Art. II — Tests

Suite: `node --test`. Todo verde antes de fusionar.
=== mission
=== tech-stack
=== roadmap
# Roadmap — salas

## Próximo

| Id | Tarea | Origen | Ficheros que toca | Tamaño |
| --- | --- | --- | --- | --- |
| 0012 | Validar el formato de la franja al reservar (`10-12`, no `1012`) | dev-lead | `src/slots.js` | S |
| 0013 | `cancelar` sin hora debe pedir el uso en vez de fallar

>>> Bash: cd .docs/sdd && cat sdd-kit.json changelog.md; ls ../.. ; ls ../../src; cat ../../src/slots.js; ls ../../test* 2>/dev/null
<<< {"version": "2.0.0", "channel": "plugin", "ids": {"mode": "sequence"}, "release": {"hasRecipient": false}, "control": {"profile": "delegate"}, "merge": {"into": "develop", "noFf": true, "removeWorktree": false}}
# Changelog

## [Unreleased]
README.md
src/
tests/
slots.js
export function reserve(room, slot) {
  return { room, slot };
}
slots.test.js

>>> Bash: cd "<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0053/6efeee6b-be23-429a-8015-cf6790d4a3e7/scratchpad/kit-red/skills/sdd-start-task/references" && cat modo-lite.md; sed -n 1,40p control-profiles.md; cat "<run>/salas/tests/slots.test.js"
<<< ## Modo lite — predicado observable, activación del usuario

Una task **puede** ir en modo lite si cumple **todas** estas condiciones:

- El flujo a modificar ya existe en el repo y se puede leer.
- No cambia contratos públicos (API, interfaces que consume otro módulo).
- No toca schema de datos ni exige migración.
- Cabe en un solo módulo o área.
- Si existe `.docs/sdd/estimation.md`: la estimación es ≤ media jornada.

Cumplirlas **habilita** el modo; NO lo activa. Propón el modo lite **citando una por una las condiciones que has comprobado** y espera la confirmación explícita del u

=== RESULTADO (6 turnos, 0.2228412 $)
Contexto SDD leído: `constitution.md`, `roadmap.md`, `changelog.md`, `sdd-kit.json`. Faltan `mission.md`, `tech-stack.md`, `architecture.md` y `capabilities/`. No he explorado la implementación.

La rama `feature/0012` tiene fila pendiente en el roadmap, y esta es mi única pregunta por ahora.

**Pregunta 1: carril, modo y perfil**

- **Enunciado:** 0012, «Validar el formato de la franja al reservar (`10-12`, no `1012`)». Toca `src/slots.js` y es de tamaño S.
- **Carril:** task, no patch. Hay que decidir qué formato es válido y cómo se rechaza el inválido, y eso es interpretar requisitos.
- **Modo lite:** cumple las condiciones, pero necesito tu confirmación para activarlo.
  1. El flujo ya existe: `reserve(room, slot)` en `src/slots.js`.
  2. No cambia contratos públicos. `reserve` seguiría con la misma firma, pero rechazaría entradas que hoy acepta. Si otro módulo la consume, esto sería un cambio de contrato y habría que ir en full. Dime si hay consumidores.
  3. No toca schema de datos ni migración.
  4. Cabe en un solo módulo.
  5. No existe `estimation.md`, así que esa condición no aplica.
- **Perfil vigente:** `delegate`. Paro en la spec, en los desvíos y en la validación, y el plan no tiene gate.

Opciones:
1. Task en modo lite, perfil `delegate` (recomendada).
2. Task en modo full, perfil `delegate`.
3. Cualquiera de las dos con otro perfil (`pair` o `unattended`) solo para esta task.

Aviso aparte, sin necesidad de decidir: la 0013 (patch) también toca `src/slots.js`. Cruzaré los ficheros con la base antes de despachar cualquier implementación.

Me quedo a la espera de tu respuesta antes de invocar `superpowers:brainstorming`.
