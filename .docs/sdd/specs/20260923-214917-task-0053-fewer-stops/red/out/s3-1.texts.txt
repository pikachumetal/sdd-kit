--- [8]
Task 0012 está en el paso 7, en espera: falta tu decisión sobre `9-11` y tu validación. No he cerrado nada.

## Me salí del plan en…

- No hay rulings de esta ejecución, salvo el commit `4aefb80` (fix de la revisión final). Toca `src/slots.js` (+2 líneas) y quedó dentro del alcance de `review-final.md`.
- `tasks.md` tiene la task 2 con commit `—`. El hash real es `64e5984`. Lo anoto en el commit del cierre.

## Decisión pendiente tuya (producto)

`review-final.md` deja abierto si `salas reservar Norte 9-11` debe aceptarse. Hoy falla con «Franja no válida», porque la regex es `^\d{2}-\d{2}$`. La spec no lo dice.

- **No lo decido yo.** Aceptar `9-11` cambia la salida observable y la spec aprobada.
- **Mantener `HH-HH` estricto:** no hay cambios. Es lo que hace el código ahora.
- **Aceptar `9-11`:** es un cambio de spec, con un escenario nuevo, un cambio de regex en `src/slots.js` y tests. Como afecta a reservar y a consultar libres, se haría en la misma rama o en una task aparte, como prefieras.

## Qué hay

- `reserve(room, slot)` y `free(slot)` en `src/slots.js` validan la franja contra `/^\d{2}-\d{2}$/`.
- Si no casa, lanzan «Franja no válida: usa HH-HH, p. ej. 10-12».
- Las dos tasks están hechas: `4e3094d` (reservar) y `64e5984` (consultar libres).

## Cómo probarlo

Con Node, importando `src/slots.js`:

- `reserve('Norte','10-12')` devuelve `{ room: 'Norte', slot: '10-12' }`.
- `reserve('Norte','1012')` falla con el mensaje.
- `free('1012')` falla con el mismo mensaje.

## Smoke que he ejecutado

- **Tests:** `node --test tests/` da 3 pass y 0 fail.
- **Llamadas directas:**

| Llamada | Resultado |
| --- | --- |
| `reserve` con `10-12` | OK |
| `reserve` con `1012` | error con el mensaje literal |
| `reserve` con `9-11` | error |
| `free` con `10-12` | OK |
| `free` con `1012` | error con el mensaje literal |
| `free` con `9-11` | error |

- **Sin probar:** no he ejecutado el CLI `salas` de extremo a extremo. En este repo solo hay `src/slots.js` y no existe entrada de CLI.

Dime qué decides sobre `9-11`. ¿Has probado tú el comportamiento y funciona, para que valide la task 0012 y pase a `sdd-end-task`?

