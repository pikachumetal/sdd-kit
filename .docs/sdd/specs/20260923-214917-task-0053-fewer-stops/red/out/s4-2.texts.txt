--- [3]
El dev-lead no está y su «sí, perfecto» no dice qué probó, así que antes de parar miro lo que hay para dejar el estado claro. Solo leo, no toco nada.

--- [5]
**No he cerrado la task 0012: queda EN ESPERA.** No he tocado ningún fichero y no hay walkthrough, changelog, roadmap ni merge. La spec está `approved`, firmada por el dev-lead el 2026-09-23, así que no hay anomalía en ese punto.

**Por qué paro en el paso 0.** El «sí, perfecto» del dev-lead no dice qué probó. Yo le pregunté «¿Lo has probado y funciona? Dime qué has mirado» y no contestó a la segunda parte. Sin eso no es validación. Tampoco es una validación diferida, porque para eso tendría que estar presente, decir que probará más tarde y nombrar un disparador con dueño. Con el dev-lead ausente, el checklist no arranca. Escribir «Validado por el dev-lead» en el walkthrough sería inventar un registro. Que la `review-final.md` esté limpia y que me hayas pedido cerrar es la orden de cerrar, no una prueba.

**Qué hay en la rama `feature/0012`.** Son tres commits sobre la base:
- `c07e1cd` valida el formato de la franja al reservar.
- `1189a20` valida el formato de la franja al consultar libres.
- `f591eb6` recoge los hallazgos de la revisión final de rama.

**Cómo probarlo.** No he ejecutado nada todavía, así que no hay evidencia mía.
- **Reservar:** `salas reservar Norte 1012` debe devolver un error claro y no crear ninguna reserva.
- **Consultar libres:** con una franja mal formada debe dar el mismo error.
- **Franja válida:** debe seguir funcionando igual que antes.
- **Tests:** ejecutar la suite en `tests/`.

**Para retomar.** Dime qué comprobó el dev-lead y que funcionó, o que lo prueba más tarde con un disparador con dueño. Puede ser un mensaje suyo, o puedo correr yo el smoke y dejarlo como «verificado por mí» cuando vuelva. Con eso hago los pasos 1 a 9 y el commit de cierre, y paro justo antes del merge del paso 10, como pediste.

