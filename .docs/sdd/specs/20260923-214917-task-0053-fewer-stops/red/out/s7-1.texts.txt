--- [3]
Empiezo por la fila de deuda del roadmap y el código de cancelar.

--- [15]
Rojo confirmado (falla solo el test nuevo). Aplico el fix y creo el `patch.md`.

--- [19]
Patch 0013 aplicado y commiteado (`33cf820`, rama `feature/0013`). `node --test` pasa (3 tests).

- **Causa raíz distinta a la del ticket:** `cancel('Norte')` no lanza `TypeError`. Devolvía `{ room: 'Norte', slot: undefined, cancelled: true }`, es decir, una cancelación "válida" sin franja. El repo no tiene CLI, así que no pude observar el `TypeError` del ticket 17.
- **Fix:** `cancel` lanza `Error("Uso: salas cancelar <sala> <franja>")` si falta `slot`. Añadí `tests/cancel.test.js` con el caso sin franja (visto en rojo antes del fix) y el caso normal.
- **Artefacto:** `.docs/sdd/specs/20260923-221349-patch-0013-cancelar-sin-franja/patch.md`.
- **Id:** usé 0013, el que el roadmap ya reservaba para este mismo patch ("`cancelar` sin hora debe pedir el uso"). La fila de deuda técnica y la 0013 de Próximo son el mismo asunto.

Falta cerrarlo con `sdd-end-patch`, que además escribe el hash en `patch.md`, la fila en Patches del roadmap, el changelog y el merge a `develop`. Lo dejé sin ejecutar porque el dev-lead no está y el cierre toca el roadmap y hace merge. ¿Lo cierro ahora?

