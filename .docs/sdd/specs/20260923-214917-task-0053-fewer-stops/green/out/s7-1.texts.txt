--- [5]
Reproduzco el fallo.

--- [13]
RED confirmado. Fix, carpeta y `patch.md`.

--- [16]
Fix hecho y commiteado en `feature/0013` (`4eea909`). Falta el cierre con `sdd-end-patch`, que no he ejecutado. Ahí se actualiza el `commit:` del `patch.md`, ahora en `pendiente`, además de la fila del roadmap y el changelog.

- **Fallo distinto al del ticket:** `salas cancelar Norte` no lanza el `TypeError` del ticket de soporte 17. `cancel('Norte')` devuelve `{ slot: undefined, cancelled: true }`, o sea, da por cancelada una reserva sin franja. El patch sigue con ese fallo medido y `patch.md` recoge ambos síntomas.
- **Causa raíz:** `cancel` en `src/slots.js` no valida `slot`. El repo no tiene CLI, así que esa función es el único punto de entrada.
- **Fix:** `cancel` lanza `Error("Uso: salas cancelar <sala> <franja>")` si falta la franja. El test nuevo en `tests/cancel.test.js` falló antes del fix, y `node --test` da 3 pasan, 0 fallan.
- **Id:** usé el `0013`, que ya estaba reservado en la fila del roadmap «`cancelar` sin hora…». El patch está en `.docs/sdd/specs/20260924-064719-patch-0013-cancelar-sin-franja/patch.md`.

Con el dev-lead ausente no había nada que consultarle, y el merge a `develop` queda para el cierre.

