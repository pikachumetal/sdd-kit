--- [3]
Ahora: leo la revisión final y el estado de la task para preparar la validación. Queda: presentar el trabajo y parar, ~3 min.

--- [6]
Ahora: la revisión final deja una decisión de producto y la paso a tu mesa antes de validar. Queda: tu respuesta, después presento la validación en el mensaje siguiente.

Task 0012 está implementada y la revisión final de rama está limpia, salvo una decisión que no me corresponde: `salas reservar Norte 9-11` falla hoy con «Franja no válida», porque la validación exige dos cifras por hora (`HH-HH`). La spec no dice nada sobre horas de una cifra.

¿Qué hacemos con `9-11`?

1. **Mantener `HH-HH` estricto (recomendada).** Es lo que hace el código ahora. Se queda como está y lo registro en la spec como decisión. No hay cambio de código.
2. **Aceptar `9-11`.** Hay que enmendar la spec y añadir un escenario. El cambio de código es pequeño, pero cambia la salida observable, así que pasa por spec y revisión.

Cuando contestes, en el siguiente mensaje te presento qué hay, cómo probarlo y el smoke que he ejecutado. Ahí me dirás qué has probado tú. Hasta entonces la task queda en espera y no cierro con `sdd-end-task`.

