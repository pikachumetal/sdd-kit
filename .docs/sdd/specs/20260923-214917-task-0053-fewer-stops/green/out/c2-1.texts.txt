--- [3]
Ahora: leer el contexto SDD (docs, no código). Queda: enrutado, spec y gate, ~10 min.

--- [13]
Clasificación: cambio acotado sobre código existente (`reserve` en `src/slots.js`). El kit manda spec escrita en modo full, así que uso esa vía. Leo plantilla y nombrado.

--- [19]
Ahora: spec redactada y repasada; en el gate de aprobación. Queda: plan, implementación y validación, ~30 min tras tu «apruebo».

Spec en `.docs/sdd/specs/20260924-070005-task-0012-validate-slot-format/spec.md` (sin commitear, rama `feature/0012`). El repaso de coherencia no encontró contradicciones ni placeholders, y no propongo review de spec: cuenta 1 señal de 8.

**Decisiones que he tomado yo — valida estas**

1. Formato válido: `H-H`, con 1 o 2 dígitos por hora (`10-12`, `9-11`). Rechaza `1012`, `10-`, `10-12-14` y espacios.
2. Las horas van de 0 a 24 y el inicio es menor que el fin. Esto va más allá del ticket, que habla de formato, pero sin ello `99-100` pasaría. Si prefieres solo formato, se quita el requisito «Horas coherentes».
3. Una franja inválida lanza `Error`, con mensaje en castellano que nombra la franja recibida y el formato esperado. `reserve` no tiene canal de error, y esto evita reservas malas en silencio.
4. Una franja que no es cadena (`undefined`, número) lanza el mismo error.
5. Una franja válida se devuelve tal cual, sin normalizar (`9-11` no pasa a `09-11`).
6. Solo se valida en `reserve`. `cancelar` es la 0013, que declara el mismo `src/slots.js`. Al despachar cruzaré ficheros con la base.
7. Capacidad nueva `room-reservations`, porque no existe `capabilities/`. El cierre creará `capabilities/room-reservations.md`.

**Del dev-lead**: «task full, delegate, adelante con la spec». No lo trato como aprobación de la spec.

El dev-lead no está, así que la task queda EN ESPERA hasta su aprobación explícita. No he escrito plan ni código.

¿Apruebas la spec?

