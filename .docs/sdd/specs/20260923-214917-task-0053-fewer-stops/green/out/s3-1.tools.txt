
>>> Skill: sdd-kit:sdd-start-task
<<< Launching skill: sdd-kit:sdd-start-task

>>> Bash: cd .docs/sdd/specs/20260923-100000-task-0012-franja && ls && cat review-final.md && cat tasks.md && sed -n 1,80p spec.md
<<< plan.md
review-final.md
spec.md
tasks.md
# Revisión final de rama — task 0012

Veredicto: limpia, salvo una decisión que no es del revisor.

- **Decisión del dev-lead**: `salas reservar Norte 9-11` falla con «Franja no válida», porque la validación exige dos cifras por hora. La spec no dice si una hora de una cifra (`9-11`) es válida. Es de producto: ¿se acepta `9-11` o se mantiene `HH-HH` estricto? Hoy el código mantiene el estricto.
# Tasks — Validar el formato de la franja (registro vivo)

| # | Task | Status | Commit | Notas |
| --- | --- | --- | --- | --- |
| 1 | Validar al reservar | don

=== RESULTADO (4 turnos, 0.19054039999999997 $)
Ahora: te paso la única decisión de producto que dejó la revisión final, sola, antes de presentar la validación. Queda: tu respuesta; en el turno siguiente presento qué hay, cómo probarlo y el smoke, y espero tu validación (~5 min, sin coste de subagentes).

La revisión final está limpia salvo un punto que es tuyo, no del revisor. Hoy `salas reservar Norte 9-11` falla con «Franja no válida», porque la validación exige dos cifras por hora (`HH-HH`). La spec no dice si `9-11` es válida.

¿Qué hacemos con las horas de una cifra?

1. **Mantener `HH-HH` estricto** (recomendada): es lo que dice la spec aprobada y lo que hace el código. No hay cambios.
2. **Aceptar `9-11`**: es un cambio de requisito. Enmendaría la spec y el código, y añadiría un test. Habría que decidir si `9-11` se normaliza a `09-11` o se guarda tal cual.

Contesta 1 o 2. Con tu respuesta, en el siguiente mensaje presento el trabajo para validar.
