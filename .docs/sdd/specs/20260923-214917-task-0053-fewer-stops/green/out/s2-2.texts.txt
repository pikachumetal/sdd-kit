--- [3]
Ahora: leo el contexto SDD (`.docs/sdd/`). Queda: confirmar carril y modo en una pregunta, ~2 min.

--- [13]
Ahora: contexto leído. Queda: tu respuesta a esta pregunta, y luego brainstorming y spec (~10 min hasta el gate de la spec).

La rama `feature/0012` coincide con la fila pendiente del roadmap. Propongo estas cuatro cosas juntas:

1. **Enunciado:** tarea 0012, «Validar el formato de la franja al reservar (`10-12`, no `1012`)». Toca `src/slots.js`, tamaño S.
2. **Carril:** task, no patch. Hay que decidir qué formatos son válidos y qué hace `reserve` cuando no lo son, y eso es interpretar requisitos.
3. **Modo:** lite. Cumple las condiciones del predicado:
   - El flujo ya existe: `reserve(room, slot)` está en `src/slots.js`.
   - No cambia contratos públicos: lo confirmo en la spec. Si `reserve` pasa a lanzar un error ante un formato inválido, eso sí lo cambia.
   - No toca schema de datos ni migración: el repo no tiene datos persistentes.
   - Cabe en un solo módulo: `src/slots.js`, con su test en `tests/slots.test.js`.
   - Estimación ≤ media jornada: no existe `estimation.md`, así que esa condición no aplica. El tamaño S de la fila es lo más cercano.
4. **Perfil:** `delegate`, el de `sdd-kit.json`.

Sin tu confirmación explícita del modo lite, la task va en modo full.

**Opciones:**
- **A (recomendada):** task 0012 en modo lite, perfil `delegate`.
- **B:** task 0012 en modo full, perfil `delegate`.
- **C:** otro perfil para esta task (`pair` o `unattended`).
- **D:** apruebo la spec por delegación, nos vemos en la validación. Va con A o B; dime cuál. Si te ausentas, esta opción evita que la task quede parada en el gate de la spec.

**Aparte, una pregunta de diseño para la spec:** ¿el único formato válido es `HH-HH` (`10-12`), o también `H-HH` y `H-H` (`9-11`, `9-9`)? ¿Debe cumplirse fin > inicio? ¿Y qué hace `reserve` con un formato inválido: lanza error o devuelve `null`?

Respóndeme en el siguiente mensaje.

