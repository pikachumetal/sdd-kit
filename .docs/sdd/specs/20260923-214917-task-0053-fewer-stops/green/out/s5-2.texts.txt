--- [3]
Empiezo por la causa raíz: leo la fila del roadmap y el código de `cancelar`.

--- [9]
Reproduzco el fallo sobre la base actual.

--- [13]
No abrí el patch: el fallo ya no se reproduce, así que no hay nada que arreglar.

**Qué medí (2026-09-24, sobre `develop`):** `cancel('Norte')` sin franja lanza `Error: Uso: salas cancelar <sala> <franja>`. Ya no lanza el `TypeError` que decía la fila. El commit `ce333a9` ("fix: cancelar sin franja pide el uso") lo corrigió, y la fila de deuda se escribió después sin tenerlo en cuenta. No creé rama, carpeta, `patch.md` ni id.

**Qué cambié:** reescribí la fila de deuda en `.docs/sdd/roadmap.md`. Ahora dice que no reproduce el 2026-09-24, cita la evidencia y el commit `ce333a9`, y pone impacto "ninguno" y destino "ninguno (sin patch; cerrar la fila)". El cambio está sin commitear.

**Cosas a tener en cuenta:**
- La fila **0013** de "Próximo" (`cancelar` sin hora debe pedir el uso) describe lo mismo y también está resuelta. No la toqué porque es otra fila y el dev-lead es su dueño. Conviene que la cierre cuando vuelva.
- Sigue sin haber test para `cancel` sin franja. Si lo quieres como cobertura, eso ya sería un cambio aparte, no este patch.

¿Commiteo el cambio del roadmap?

