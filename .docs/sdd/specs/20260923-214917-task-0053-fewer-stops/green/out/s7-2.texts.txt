--- [13]
RED confirmado. Ahora el fix.

--- [15]
Patch 0013 implementado y commiteado en `feature/0013` (`f2f5b57`). Falta el cierre con `sdd-end-patch`. No he fusionado nada.

- **Síntoma distinto del predicho:** el `TypeError` del ticket 17 no se reproduce. `cancel('Norte')` devolvía `{ slot: undefined, cancelled: true }`, es decir, cancelaba sin franja en silencio. El repo no tiene CLI, solo `src/slots.js`. Seguí con el fallo medido, y `patch.md` recoge la diferencia.
- **Fix:** `cancel` lanza `Error("Uso: salas cancelar <sala> <franja>")` si falta la franja. Añadí `tests/cancel.test.js` con dos tests y `node --test` pasa 3/3 (uno de ellos estaba en rojo antes del fix).
- **Id:** usé 0013, el que ya tenía reservado la fila «`cancelar` sin hora» del roadmap.
- **Decisión sin el dev-lead:** seguí con el fallo medido en vez de parar. `patch.md` lo deja escrito.
- **Pendiente para `sdd-end-patch`:**
  - Escribir el hash real en `patch.md`.
  - Añadir la fila en la tabla de Patches y en el changelog.
  - Cerrar la fila 0013 de Próximo y la de deuda técnica, que ahora describe un `TypeError` que no ocurre.

¿Sigo con el cierre?

