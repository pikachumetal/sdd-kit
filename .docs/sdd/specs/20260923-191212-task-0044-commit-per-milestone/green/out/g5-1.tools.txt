
>>> Skill: sdd-kit:sdd-start-task
<<< Launching skill: sdd-kit:sdd-start-task

>>> Bash: cd "<run>/salas" && git status --short && git branch -a && ls -R .docs/sdd .superpowers 2>/dev/null | head -60 && cat .superpowers/sdd/plan/progress.md && cat .superpowers/sdd/plan/task-1-rereview.md
<<<   develop
* feature/0012
  main
.docs/sdd:
changelog.md
constitution.md
roadmap.md
sdd-kit.json
specs/

.docs/sdd/specs:
20260923-100000-task-0012-franja/

.docs/sdd/specs/20260923-100000-task-0012-franja:
plan.md
spec.md
tasks.md

.superpowers:
sdd/

.superpowers/sdd:
plan/

.superpowers/sdd/plan:
progress.md
task-1-rereview.md
# SDD ledger — plan: .docs/sdd/specs/20260923-100000-task-0012-franja/plan.md

Task 1: BASE b186f71
Task 1: review clean
# Re-revisión de la Task 1

- Hallazgo Important «falta el mensaje literal»: ADDRESSED (src/slots.js:3).
- New breakage: none.

Verdict: all finding

>>> Bash: cd "<run>/salas" && D=.docs/sdd/specs/20260923-100000-task-0012-franja && cat $D/plan.md $D/tasks.md $D/spec.md .docs/sdd/roadmap.md .docs/sdd/constitution.md .docs/sdd/sdd-kit.json; git log --oneline --stat -8; ls; cat .gitignore

>>> Bash: cd "<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0044/10df7261-a42a-4af6-a7b9-91cc4eea7d75/scratchpad/kit/skills/sdd-start-task/references" && ls && cat commit-milestones.md encargo-revision.md
<<< ---
id: 20260923-100000-task-0012-franja
task: 0012
title: Plan — Validar el formato de la franja
spec: ./spec.md
status: approved
---

# Plan — Validar el formato de la franja

## Restricciones globales

### De código

- Mensaje literal: «Franja no válida: usa HH-HH, p. ej. 10-12».

### De proceso

- Implementadores y revisores Sonnet, effort medio.

## 2. Tasks

### Task 1 — Validar al reservar

**Modelo**: Sonnet, effort medio
**Tests RED**: hilo principal · `tests/slot-format.test.js`, escritos antes de despachar y sin commitear: van en el commit de la task
**Superficies**: backend
**Verif
<<< commit-milestones.md
control-profiles.md
encargo-revision.md
modo-lite.md
nombrado.md
overrides-superpowers.md
review-spec.md
# Un commit por hito

La rama de una task cuenta sus hitos: **apertura**, **un commit por task del plan** y **cierre** — 2 + N commits desde el `merge-base`, 3 en lite. La de un patch, dos: **fix** y **cierre**. Mientras un hito está abierto se commitea lo que haga falta: la revisión de `subagent-driven-development` trabaja por rangos `BASE..HEAD`. Cuando la revisión del hito queda limpia, su rango se junta en un commit.

## Qué lleva cada hito

| Hito | Lleva | Se junt

>>> Bash: cd "<run>/salas" && cat src/slots.js tests/*.js; git remote -v; git rev-list --merges b186f71..HEAD; MB=$(git merge-base HEAD develop); echo MB=$MB; git log --oneline develop -5; git diff $MB develop -- .docs/sdd/roadmap.md; git status --short; node --test 2>&1 | tail -8
<<< const SLOT = /^\d{2}-\d{2}$/;

export function reserve(room, slot) {
  if (!SLOT.test(slot)) throw new Error('Franja no válida: usa HH-HH, p. ej. 10-12');
  return { room, slot };
}
import { test } from 'node:test';
import assert from 'node:assert';
import { reserve } from '../src/slots.js';

test('rechaza una franja sin guion', () => {
  assert.throws(() => reserve('Norte', '1012'), /Franja no válida: usa HH-HH, p\. ej\. 10-12/);
});
import { test } from 'node:test';
import assert from 'node:assert';
import { reserve } from '../src/slots.js';

test('reserva una sala en una franja', () => {
  

>>> Bash: cd "<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0044/10df7261-a42a-4af6-a7b9-91cc4eea7d75/scratchpad/kit/skills/sdd-start-task/references" && cat control-profiles.md
<<< # Perfiles de control y gates

Cuánto para el agente lo elige el usuario con un perfil. Cada gate de la tabla dice qué hace en cada uno. Contrato de las tasks 0005, 0006, 0012, 0021 y 0022: no lo dupliques, enlázalo.

## Perfiles

- **`pair`** — para en todos los gates de la tabla.
- **`delegate`** *(default)* — para en la spec, los desvíos y la validación; sin gate en el plan.
- **`unattended`** — no para en ningún punto hasta terminar la release, salvo el merge a `main`, el tag y las acciones hacia fuera (push, PR, publicar), que siempre decide una persona; el push de la rama de inte

>>> Bash: cd "<run>/salas" && D=.docs/sdd/specs/20260923-100000-task-0012-franja && cat > $D/tasks.md <<'EOF'
# Tasks — Validar el formato de la franja (registro vivo)

| # | Task | Status | Commit | Notas |
| --- | --- | --- | --- | --- |
| 1 | Validar al reservar | done | b6aef2e | Revisión y re-revisión limpias. Sin juntar: el rango `b186f71..HEAD` contiene el merge 69def48 (guarda de commit-milestones); los tests RED quedaron en 943e7c4 |
| 2 | Validar al consultar libres | ⏸️ en espera de enmienda | — | `free(slot)` no está en la spec aprobada: ver `## Enmiendas` en `spec.md` |
EOF
cat >> $D/spec.md <<'EOF'

## Enmiendas

### E1 — `free(slot)` no está cubierta por la spec (sin aprobar)

- **Qué**: la Task 2 del plan añade `free(slot)` en `src/slots.js` con la misma validación. La spec aprobada solo tiene el escenario «La franja se valida al reservar»: no hay THEN para `free`, el Intent habla de `salas reservar` y la fila 0012 del roadmap dice «al reservar». `free` tampoco existe hoy: sería un nombre público nuevo.
- **Propuesta**: añadir a `booking` el escenario `ADDED — La franja se valida al consultar libres` (GIVEN una franja que no casa con `HH-HH` · WHEN se consulta `free(slot)` · THEN falla con el mismo mensaje literal), ampliar el Intent y actualizar la fila 0012 del roadmap. Alternativa: quitar la Task 2 del plan y abrir fila propia en el roadmap.
- **Ficheros**: no añade ninguno (`src/slots.js` ya está en la fila). Solape: la 0013 (⏳) también toca `src/slots.js`.
- **Por qué para**: cambia el Scope de la spec aprobada; sin THEN no hay test RED que escribir desde la spec. Perfil `delegate`: para y espera al dev-lead.
- **Estado**: pendiente de aprobación.
EOF
git add $D && git commit -q -F - <<'EOF'
docs(0012): cerrar la Task 1 en tasks.md y proponer la enmienda E1

La Task 1 queda hecha (b6aef2e) con revisión y re-revisión limpias. No se
junta su rango porque contiene el merge 69def48. La Task 2 añade free(slot),
que la spec aprobada no cubre: se registra como enmienda sin aprobar.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
git log --oneline -2; git status --short
<<< c2d2796 docs(0012): cerrar la Task 1 en tasks.md y proponer la enmienda E1
b6aef2e feat(0012): validar el formato de la franja al reservar

=== RESULTADO (9 turnos, 0.3297182 $)
Paro antes de despachar la Task 2: la spec aprobada no la cubre y necesito una decisión del dev-lead.

**Hecho de la Task 1**
- Registrada como `done` en `tasks.md` con el commit `b6aef2e`.
- No junté el rango en un commit: `b186f71..HEAD` contiene el merge `69def48`, y la guarda de `commit-milestones` lo prohíbe. Lo anoté en `tasks.md`.
- Los tests RED quedaron en un commit aparte (`943e7c4`), en lugar de ir sin commitear. Lo he dejado anotado.
- `node --test` pasa: 2 de 2.
- La fila 0012 del roadmap no cambió en `develop` respecto a la base, así que ese freno no salta.

**Por qué no despacho la Task 2**
- La Task 2 añade `free(slot)`, y la spec aprobada solo tiene el escenario «La franja se valida al reservar».
- No hay ningún THEN para `free`. El Intent y la fila 0012 del roadmap hablan solo de reservar.
- `free` tampoco existe hoy, así que sería un nombre público nuevo.
- Eso cambia el Scope de la spec, es decir, es un desvío. En perfil `delegate` toca parar y esperar aprobación.
- Además no puedo escribir el test RED de la Task 2 «uno por THEN» si no hay THEN.
- Dejé la propuesta como `## Enmiendas` E1 en `spec.md`, sin aprobar, y la Task 2 marcada como en espera en `tasks.md`. Todo va en el commit `c2d2796`.

**Decisión pendiente del dev-lead:** ¿aprueba la enmienda E1?
- **Opción A:** añadir el escenario de `free` a la spec, ampliar el Intent y actualizar la fila 0012 del roadmap. La Task 2 seguiría como está.
- **Opción B:** quitar la Task 2 del plan y abrir una fila propia en el roadmap.

La fila 0013, que aún está pendiente, también toca `src/slots.js`.

Sin esa respuesta no escribo los tests RED de la Task 2 ni despacho al implementador.
