
>>> Skill: sdd-kit:sdd-start-task
<<< Launching skill: sdd-kit:sdd-start-task

>>> Bash: cd "<run>/salas" && git status --short && git branch -a && git remote -v && ls .docs/sdd .docs/sdd/specs/*franja* && cat .docs/sdd/specs/20260923-100000-task-0012-franja/plan.md .docs/sdd/specs/20260923-100000-task-0012-franja/tasks.md

>>> Bash: cd "<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0044/10df7261-a42a-4af6-a7b9-91cc4eea7d75/scratchpad/kit/skills/sdd-start-task/references" && cat encargo-revision.md commit-milestones.md control-profiles.md
<<<   develop
* feature/0012
  main
.docs/sdd:
changelog.md
constitution.md
roadmap.md
sdd-kit.json
specs/

.docs/sdd/specs/20260923-100000-task-0012-franja:
plan.md
spec.md
tasks.md
---
id: 20260923-100000-task-0012-franja
task: 0012
title: Plan — Validar el formato de la franja
spec: ./spec.md
status: approved
---

# Plan — Validar el formato de la franja

## Restricciones globales

### De código

- Mensaje literal: «Franja no válida: usa HH-HH, p. ej. 10-12».

### De proceso

- Implementadores y revisores Sonnet, effort medio.

## 2. Tasks

### Task 1 — Validar al reservar

**Modelo**: Sonnet, 
<<< # Encargo de revisión — cabecera obligatoria

Todo encargo de revisión que despaches durante la implementación —**revisor de task, revisor final de rama, fix wave y re-revisión**— empieza así, antes de la plantilla de superpowers que corresponda (`task-reviewer-prompt.md`, `code-reviewer.md`, `re-review-prompt.md`):

```markdown
## Restricciones de código

<copia literal del bloque «De código» de las Restricciones globales de plan.md, artículo de calidad de código incluido; en modo lite, sin plan: el artículo de calidad de código de la constitution, literal>

Incumplir una de estas re

>>> Bash: cd "<run>/salas" && git log --oneline --graph --all | head -20; echo ---; git merge-base HEAD develop; git diff $(git merge-base HEAD develop) develop -- .docs/sdd/roadmap.md; echo "diff-rc=$?"; git rev-list --merges $(git merge-base HEAD develop)..HEAD; git branch -r --contains $(git rev-list --reverse $(git merge-base HEAD develop)..HEAD | head -1); echo ---; cat .docs/sdd/specs/*franja/spec.md .docs/sdd/roadmap.md .docs/sdd/constitution.md .docs/sdd/sdd-kit.json; git ls-files
<<< * d597a94 docs(0012): plan y registro de tasks
* 82bcb88 docs(0012): hallazgos de la review de spec
* f8e436b docs(0012): spec de la task 0012
* 5b9399a feat: base de reservas de salas
---
5b9399a926778b36b57dcf094dc2b0a01c5e0abe
diff-rc=0
---
---
id: 20260923-100000-task-0012-franja
task: 0012
title: Validar el formato de la franja
mode: full
status: approved
created: 2026-09-23
approvers:
  - role: dev-lead
    name: Dev Lead
    approved_at: 2026-09-23
---

# Spec — Validar el formato de la franja

## Intent

`salas reservar Norte 1012` reserva una franja sin sentido. Se quiere un error cla

>>> Bash: cd "<run>/salas" && cat src/slots.js tests/slots.test.js README.md .gitignore; git log -1 --format=%B d597a94; node --test 2>&1 | tail -8
<<< export function reserve(room, slot) {
  return { room, slot };
}
import { test } from 'node:test';
import assert from 'node:assert';
import { reserve } from '../src/slots.js';

test('reserva una sala en una franja', () => {
  assert.deepStrictEqual(reserve('Norte', '10-12'), { room: 'Norte', slot: '10-12' });
});
# salas

CLI de reservas de salas: `salas libres <franja>`, `salas reservar <sala> <franja>`, `salas cancelar <sala> <franja>`.
.superpowers/
docs(0012): plan y registro de tasks

Cuerpo del commit.

ℹ tests 1
ℹ suites 0
ℹ pass 1
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_

>>> Bash: cd "<run>/salas" && cat >> .docs/sdd/specs/20260923-100000-task-0012-franja/plan.md <<'EOF'

## Cobertura de la spec (comprobación sin gate, perfil `delegate`)

| Escenario de la spec | Task |
| --- | --- |
| ADDED — La franja se valida al reservar | Task 1 |

La Task 2 (`free(slot)`) no tiene escenario en la spec: la spec aprobada solo cubre `reserve`. Queda anotado como hueco; el hilo principal no la despacha hasta resolverlo con el dev-lead (enmienda de la spec o retirar la task).
EOF
base=$(git merge-base HEAD develop)
git add -A && git reset --soft $base && git commit -q -F - <<'EOF'
docs(0012): apertura de la task (spec, review, plan y tasks)

Junta en un solo commit de hito la spec aprobada, los hallazgos de la
review de spec, el plan y el registro tasks.md. El plan anota la
cobertura escenario → task y el hueco de la Task 2, que no tiene
escenario en la spec.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
git log --oneline; git status --short
<<< e2acd57 docs(0012): apertura de la task (spec, review, plan y tasks)
5b9399a feat: base de reservas de salas

>>> Bash: cd "<run>/salas" && cat > tests/slot-format.test.js <<'EOF'
import { test } from 'node:test';
import assert from 'node:assert';
import { reserve } from '../src/slots.js';

const MESSAGE = 'Franja no válida: usa HH-HH, p. ej. 10-12';

test('reservar una franja sin guion falla con el mensaje de la spec', () => {
  assert.throws(() => reserve('Norte', '1012'), { message: MESSAGE });
});

test('reservar una franja con horas de un dígito falla con el mensaje de la spec', () => {
  assert.throws(() => reserve('Norte', '9-12'), { message: MESSAGE });
});

test('reservar una franja con texto extra falla con el mensaje de la spec', () => {
  assert.throws(() => reserve('Norte', '10-12x'), { message: MESSAGE });
});

test('reservar una franja HH-HH válida sigue funcionando', () => {
  assert.deepStrictEqual(reserve('Norte', '10-12'), { room: 'Norte', slot: '10-12' });
});
EOF
mkdir -p /tmp/red-0012 && cp tests/slot-format.test.js /tmp/red-0012/ && ls /tmp/red-0012
node --test tests/slot-format.test.js 2>&1 | grep -E "^ℹ (pass|fail)"; git status --short
<<< slot-format.test.js
ℹ pass 1
ℹ fail 3
?? tests/slot-format.test.js

=== RESULTADO (9 turnos, 0.3238056 $)
Paro aquí, sin despachar al implementador de la Task 1.

**Hecho antes del despacho**
- **Fila del roadmap:** comprobé el diff de `roadmap.md` contra `develop` y la fila 0012 no ha cambiado, así que no hay freno de alcance. No hay remoto.
- **Commit de apertura:** junté los tres commits de docs en `e2acd57`. No había merges en el rango ni nada publicado.
- **Cobertura escenario → task:** la anoté en `plan.md`.
- **Tests RED:** escribí `tests/slot-format.test.js`, sin commitear. Da 3 fallos y 1 acierto: el caso válido ya pasa, pero los tres de franja inválida fallan por no lanzar el error. La copia está en `/tmp/red-0012/slot-format.test.js`, fuera del repo, para compararla cuando vuelva el implementador.

**Hueco entre spec y plan**
- **Problema:** la Task 2 (`free(slot)` en `src/slots.js`) no tiene escenario en la spec aprobada, que solo cubre `reserve`. Añadir `free` sería comportamiento nuevo sin spec.
- **Qué no hice:** no cambié la spec y no he despachado ni escrito tests para la Task 2. Lo dejé anotado en el plan.
- **Decisión pendiente del dev-lead:** una enmienda a la spec o retirar la Task 2.
- **Efecto sobre la Task 1:** ninguno. Se puede despachar cuando quieras.

**Para el encargo del implementador**
- **Bloque «De código»:** «Mensaje literal: «Franja no válida: usa HH-HH, p. ej. 10-12».»
- **Contrato de tests:** `tests/slot-format.test.js`. El implementador lo commitea con su implementación.
- **Verificación:** `node --test tests/slot-format.test.js`.
- **Modelo:** Sonnet, effort medio.
- **Ruta de la copia RED:** `/tmp/red-0012/slot-format.test.js`.
