# orders-api

API de pedidos del equipo. Un módulo por agregado, en tres capas: `domain`, `application` e `infrastructure`. Los casos de uso devuelven un `Result` y nunca lanzan excepciones de negocio.
