# Constitution — statusline

## Art. I — Sin dependencias

Node 22 y nada más; ninguna dependencia externa.

## Art. II — Ramas

git-flow: `main` estable, `develop` de integración, `feature/<id>` desde `develop`.

## Art. III — Commits e idioma

Los fija el `CLAUDE.md` global del dev-lead.

## Reglas de producto

- **Dónde viven los datos**: en memoria; nada persiste.
- **Idioma de los nombres**: código en inglés, mensajes en castellano.
- **Límites**: la línea no pasa de 120 caracteres.
- **Avisos**: ninguno.
- **Regla ante conflicto**: no aplica.
