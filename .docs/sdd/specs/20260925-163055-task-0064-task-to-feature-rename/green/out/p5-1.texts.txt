--- [3]
Primero investigo la causa raíz. Cargo semble y miro el repo.

--- [11]
No he abierto el patch: el bug no se reproduce sobre la base actual, así que no hay rama, carpeta, `patch.md` ni id reservado.

- **No hay lógica de reservas en el repo.** `src/app.js` solo tiene `console.log('salas')`. No existe código de cancelación, ni de reservas, ni tests. `git ls-files` lista solo `README.md`, `src/app.js` y documentación en `.docs/sdd/`. Solo hay una rama de trabajo (`develop`) y una `main`, con un único commit.
- **El bug ya figura como corregido, pero sin código.** El patch 0007 (`.docs/sdd/specs/20260918-090000-patch-0007-cancel/patch.md`) y el changelog dicen que la cancelación ya no borra reservas de otro día con la misma hora. La causa que anota el patch es «comparación solo por hora», y eso encaja con tu síntoma. Pero el patch cita un fix y un test de regresión que no existen en este repo. El commit `8e86de7` solo trae documentación.

Qué necesito de ti:
- ¿Puedes indicarme dónde está el código de reservas? Puede ser otra rama, otro repo o un commit sin traer aquí.
- Con el código delante, reproduzco el fallo. Si se reproduce, el patch sale con el fallo medido. Si el código ya incluye el fix del 0007, no hay nada que arreglar.
- No aparece ninguna fila del roadmap sobre este bug, así que no he reescrito nada allí.

