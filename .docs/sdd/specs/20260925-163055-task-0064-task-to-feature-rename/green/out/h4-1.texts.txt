--- [4]
Ahora: cargar el contexto SDD del proyecto. Queda: confirmar carril y modo contigo, ~2 min.

--- [20]
Ahora: confirmar carril, modo y perfil antes de crear rama y spec. Queda: tu respuesta, luego rama, spec y gate, ~5 min sin subagentes.

Contexto SDD leído. No hay `sdd-kit.local.json` ni claves que avisar. La rama es `develop` y el roadmap no tiene fila para esto. `ids.mode` es `sequence`, así que reservaré id con `Get-NextSddId.ps1 -Reserve` al confirmar. Esto es una **feature** (cambia comportamiento), no un patch ni un spike.

**Modo lite.** Lo propongo. Condiciones comprobadas una por una:

1. **Flujo existente y legible:** cumple, con matiz. Existe el modelo de reserva (`bookings` en `src/app.js`, 16 líneas). El comando `reservar` solo tiene la rama `--cada-semana`, que devuelve un texto fijo y no guarda nada.
2. **Sin cambio de contratos públicos:** cumple. Sería una opción nueva y opcional de la CLI, y nadie más consume la interfaz (misión: solo tú).
3. **Sin schema ni migración:** cumple. No hay persistencia, solo un array en memoria.
4. **Un solo módulo:** cumple. Tocaría `src/app.js` y `test/app.test.js`.
5. **Estimación ≤ media jornada:** no aplica, no existe `estimation.md`.

Lite significa `spec.md` corta, sin `plan.md` ni `tasks.md`. El gate de la spec, el smoke y el walkthrough se mantienen. Si al implementar cae alguna condición, sube a full.

**Perfil vigente:** `delegate`, que sale del default del proyecto porque `sdd-kit.json` no declara `control.profile`. Para en la spec, en los desvíos y en la validación.

Después, `brainstorming` te preguntará dónde se captura y dónde se ve la nota. Hoy `reservar` no crea reservas normales.

¿Cómo arrancamos?

1. **Feature en modo lite, perfil `delegate` (recomendada).**
2. Lite, con la spec aprobada por delegación: «apruebo la spec por delegación, nos vemos en la validación». La apruebo yo, registro tu frase y no paro hasta la validación.
3. Modo full, con plan.
4. Cambiar el perfil para esta feature (`pair` o `unattended`).

