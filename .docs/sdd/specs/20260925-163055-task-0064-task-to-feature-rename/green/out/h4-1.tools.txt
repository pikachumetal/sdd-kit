
>>> Skill: sdd-kit:sdd-start-feature
<<< Launching skill: sdd-kit:sdd-start-feature

>>> Glob: .docs/sdd/**/*

>>> Bash: git branch --show-current; ls -a .docs/sdd 2>/dev/null
<<< .docs\sdd\changelog.md
.docs\sdd\constitution.md
.docs\sdd\roadmap.md
.docs\sdd\specs\20260915-090000-task-0005-weekly\walkthrough.md
.docs\sdd\specs\20260915-090000-task-0005-weekly\spec.md
.docs\sdd\specs\20260917-090000-task-0006-free\walkthrough.md
.docs\sdd\specs\20260917-090000-task-0006-free\spec.md
.docs\sdd\specs\20260918-090000-patch-0007-cancel\patch.md
.docs\sdd\mission.md
.docs\sdd\sdd-kit.json
.docs\sdd\tech-stack.md
<<< develop
.
..
changelog.md
constitution.md
mission.md
roadmap.md
sdd-kit.json
specs
tech-stack.md

>>> Read: <run>\repo\.docs\sdd\constitution.md

>>> Read: <run>\repo\.docs\sdd\mission.md
<<< 1	# Constitution — salas
2	
3	1. Los cambios no triviales pasan por el flujo SDD del kit (`sdd-start-task` → spec → plan → `sdd-end-task`); los bugs deterministas, por el carril patch.
4	2. Tests antes que código: `node --test` en verde antes de cada commit.
5	3. Git-flow: `main` estable, `develop` de integración, `feature/<id>` desde `develop`. Tags anotados `vX.Y.Z` sobre `main`.
6	4. Texto de la interfaz y de los documentos en castellano.
7	

>>> Read: <run>\repo\.docs\sdd\tech-stack.md

>>> Read: <run>\repo\.docs\sdd\roadmap.md
<<< 1	# Misión — salas
2	
3	Herramienta de línea de comandos para reservar las salas de reuniones del coworking donde trabajo. La uso yo: la desarrollo, la pruebo y la despliego en mi propio servidor. No hay más usuarios ni más desarrolladores.
4	
<<< 1	# Tech Stack — salas
2	
3	- Node 22, sin dependencias externas. Un solo fichero de entrada: `src/app.js`.
4	- Tests: `node --test` (carpeta `test/`).
5	- Git: git-flow (`main`, `develop`, `feature/<id>`).
6	

>>> Read: <run>\repo\.docs\sdd\sdd-kit.json
<<< 1	# Roadmap — salas
2	
3	## Próximo
4	
5	| # | Ítem | Estado |
6	| --- | --- | --- |
7	| 1 | Avisos por correo antes de la reserva | ⏳ |
8	
9	## Backlog
10	
11	| # | Ítem | Origen |
12	| --- | --- | --- |
13	| B1 | Exportar reservas a calendario (.ics) | idea propia |
14	
15	## Deuda técnica
16	
17	| Deuda | Impacto | Plan |
18	| --- | --- | --- |
19	| Sin validación del formato de franja horaria | Bajo | Patch cuando moleste |
20	
21	## Patches
22	
23	| Fecha | Id | Descripción |
24	| --- | --- | --- |
25	| 2026-09-18 | 0007 | La cancelación borraba reservas de otro día con la misma hora |
26

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0064-task-to-feature-rename\931dc86e-411f-4077-8ac4-05b4d5bcbc90\scratchpad\kit-green\skills\sdd-start-feature\references\modo-lite.md
<<< 1	{"version": "1.2.0", "channel": "plugin", "updated": "2026-09-21", "ids": {"mode": "sequence"}, "release": {"hasRecipient": false}}
2	

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0064-task-to-feature-rename\931dc86e-411f-4077-8ac4-05b4d5bcbc90\scratchpad\kit-green\skills\sdd-start-feature\references\nombrado.md
<<< 1	## Modo lite — predicado observable, activación del usuario
2	
3	Una feature **puede** ir en modo lite si cumple **todas** estas condiciones:
4	
5	- El flujo a modificar ya existe en el repo y se puede leer.
6	- No cambia contratos públicos (API, interfaces que consume otro módulo).
7	- No toca schema de datos ni exige migración.
8	- Cabe en un solo módulo o área.
9	- Si existe `.docs/sdd/estimation.md`: la estimación es ≤ media jornada.
10	
11	Cumplirlas **habilita** el modo; NO lo activa. Propón el modo lite **citando una por una las condiciones que has comprobado** y espera la confirmació
<<< 1	## Nombrado de carpetas de spec
2	
3	`<yyyyMMdd-HHmmss>-(feature|patch|proposal)-<id>-<slug>`, todo en UTC:
4	
5	- Timestamp: `Get-Date -AsUTC -Format 'yyyyMMdd-HHmmss'` (PowerShell).
6	- `feature`, `patch` o `proposal` según el carril. Una carpeta `-task-` de antes de la 2.0.0 es legado: los scripts la leen y el cierre escribe en ella sin renombrarla, pero ninguna carpeta nueva se prefija `task-`. Una carpeta que contiene `patch.md` SIEMPRE va prefijada `patch-`; una que contiene `proposal.md`, `proposal-` (la crea `sdd-roadmap`).
7	- `<id>`: depende del modo declarado en `.docs/sdd/sdd-kit

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0064-task-to-feature-rename\931dc86e-411f-4077-8ac4-05b4d5bcbc90\scratchpad\kit-green\skills\sdd-start-feature\references\control-profiles.md
<<< 1	# Perfiles de control y gates
2	
3	Cuánto para el agente lo elige el usuario con un perfil. Cada gate de la tabla dice qué hace en cada uno. Contrato de las tasks 0005, 0006, 0012, 0021 y 0022: no lo dupliques, enlázalo.
4	
5	## Perfiles
6	
7	- **`pair`** — para en todos los gates de la tabla.
8	- **`delegate`** *(default)* — para en la spec, los desvíos y la validación; sin gate en el plan.
9	- **`unattended`** — no para en ningún punto hasta terminar la release, salvo el merge a `main`, el tag y las acciones hacia fuera (push, PR, publicar), que siempre decide una persona; el push de la ra

>>> Bash: cat .docs/sdd/sdd-kit.local.json 2>&1; echo ---; ls src test; wc -l src/app.js; grep -n -i "reserv\|json\|writeFile\|readFile" src/app.js | head -40
<<< cat: .docs/sdd/sdd-kit.local.json: No such file or directory
---
src:
app.js

test:
app.test.js
16 src/app.js
11:  if (cmd === 'reservar' && params.includes('--cada-semana')) return 'reserva semanal creada';

>>> Read: <run>\repo\src\app.js
<<< 1	const [command, ...args] = process.argv.slice(2);
2	const rooms = ['Norte', 'Sur'];
3	const bookings = [{ room: 'Norte', day: 'lun', slot: '10:00-12:00' }];
4	
5	function freeRooms(slot) {
6	  return rooms.filter((room) => !bookings.some((b) => b.room === room && b.slot === slot));
7	}
8	
9	export function run(cmd, params) {
10	  if (cmd === 'libres') return freeRooms(params[0]).join(', ');
11	  if (cmd === 'reservar' && params.includes('--cada-semana')) return 'reserva semanal creada';
12	  if (cmd === 'cancelar') return `cancelada ${params[0]} ${params[1]}`;
13	  return 'salas';
14	}
15	
1

=== RESULTADO (15 turnos, 0.33511779999999997 $)
Ahora: confirmar carril, modo y perfil antes de crear rama y spec. Queda: tu respuesta, luego rama, spec y gate, ~5 min sin subagentes.

Contexto SDD leído. No hay `sdd-kit.local.json` ni claves que avisar. La rama es `develop` y el roadmap no tiene fila para esto. `ids.mode` es `sequence`, así que reservaré id con `Get-NextSddId.ps1 -Reserve` al confirmar. Esto es una **feature** (cambia comportamiento), no un patch ni un spike.

**Modo lite.** Lo propongo. Condiciones comprobadas una por una:

1. **Flujo existente y legible:** cumple, con matiz. Existe el modelo de reserva (`bookings` en `src/app.js`, 16 líneas). El comando `reservar` solo tiene la rama `--cada-semana`, que devuelve un texto fijo y no guarda nada.
2. **Sin cambio de contratos públicos:** cumple. Sería una opción nueva y opcional de la CLI, y nadie más consume la interfaz (misión: solo tú).
3. **Sin schema ni migración:** cumple. No hay persistencia, solo un array en memoria.
4. **Un solo módulo:** cumple. Tocaría `src/app.js` y `test/app.test.js`.
5. **Estimación ≤ media jornada:** no aplica, no existe `estimation.md`.

Lite significa `spec.md` corta, sin `plan.md` ni `tasks.md`. El gate de la spec, el smoke y el walkthrough se mantienen. Si al implementar cae alguna condición, sube a full.

**Perfil vigente:** `delegate`, que sale del default del proyecto porque `sdd-kit.json` no declara `control.profile`. Para en la spec, en los desvíos y en la validación.

Después, `brainstorming` te preguntará dónde se captura y dónde se ve la nota. Hoy `reservar` no crea reservas normales.

¿Cómo arrancamos?

1. **Feature en modo lite, perfil `delegate` (recomendada).**
2. Lite, con la spec aprobada por delegación: «apruebo la spec por delegación, nos vemos en la validación». La apruebo yo, registro tu frase y no paro hasta la validación.
3. Modo full, con plan.
4. Cambiar el perfil para esta feature (`pair` o `unattended`).
