
>>> Skill: sdd-kit:sdd-start-feature
<<< Launching skill: sdd-kit:sdd-start-feature

>>> Bash: cd "<run>/repo" && git branch --show-current && find .docs -type f | head -50 && ls -a .docs/sdd
<<< develop
.docs/sdd/changelog.md
.docs/sdd/constitution.md
.docs/sdd/mission.md
.docs/sdd/roadmap.md
.docs/sdd/sdd-kit.json
.docs/sdd/specs/20260915-090000-task-0005-weekly/spec.md
.docs/sdd/specs/20260915-090000-task-0005-weekly/walkthrough.md
.docs/sdd/specs/20260917-090000-task-0006-free/spec.md
.docs/sdd/specs/20260917-090000-task-0006-free/walkthrough.md
.docs/sdd/specs/20260918-090000-patch-0007-cancel/patch.md
.docs/sdd/tech-stack.md
.
..
changelog.md
constitution.md
mission.md
roadmap.md
sdd-kit.json
specs
tech-stack.md

>>> Bash: cd "<run>/repo/.docs/sdd" && for f in constitution mission tech-stack roadmap; do echo "=== $f"; cat $f.md; done; echo "=== json"; cat sdd-kit.json; ls -a
<<< === constitution
# Constitution — salas

1. Los cambios no triviales pasan por el flujo SDD del kit (`sdd-start-task` → spec → plan → `sdd-end-task`); los bugs deterministas, por el carril patch.
2. Tests antes que código: `node --test` en verde antes de cada commit.
3. Git-flow: `main` estable, `develop` de integración, `feature/<id>` desde `develop`. Tags anotados `vX.Y.Z` sobre `main`.
4. Texto de la interfaz y de los documentos en castellano.
=== mission
# Misión — salas

Herramienta de línea de comandos para reservar las salas de reuniones del coworking donde trabajo. La uso yo: l

>>> Bash: cd "<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0064-task-to-feature-rename/931dc86e-411f-4077-8ac4-05b4d5bcbc90/scratchpad/kit-green/skills/sdd-start-feature/references" && cat modo-lite.md nombrado.md; sed -n 1,60p control-profiles.md

>>> Bash: cd "<run>/repo" && git log --oneline | head; git branch -a; ls; ls src test 2>/dev/null; wc -l src/app.js
<<< ## Modo lite — predicado observable, activación del usuario

Una feature **puede** ir en modo lite si cumple **todas** estas condiciones:

- El flujo a modificar ya existe en el repo y se puede leer.
- No cambia contratos públicos (API, interfaces que consume otro módulo).
- No toca schema de datos ni exige migración.
- Cabe en un solo módulo o área.
- Si existe `.docs/sdd/estimation.md`: la estimación es ≤ media jornada.

Cumplirlas **habilita** el modo; NO lo activa. Propón el modo lite **citando una por una las condiciones que has comprobado** y espera la confirmación explícita de
<<< 8c47f15 feat: base con cancelación de reservas
* develop
  main
README.md
src
src:
app.js
2 src/app.js

=== RESULTADO (7 turnos, 0.27134400000000003 $)
Contexto SDD leído (constitution, mission, tech-stack, roadmap, `sdd-kit.json`). No existe `sdd-kit.local.json`. Encaja con la fila B1 del roadmap («Exportar reservas a calendario (.ics)»).

Antes de nada, confirma el enfoque:

1. **Carril:** feature, no patch. Es comportamiento nuevo, no un bug.
2. **Modo: full** (spec → plan → implementación). No propongo lite. Un `.ics` es una salida nueva y el flujo a modificar no existe todavía, así que no se cumple la condición «el flujo ya existe en el repo». Además añade un comando a la interfaz de línea de comandos, que es un contrato público, así que tampoco se cumple «no cambia contratos públicos».
3. **Perfil: `delegate`.** Sale del proyecto: `sdd-kit.json` no fija `control.profile` y aplica el default. Paro en la spec, en los desvíos y en la validación. No paro en el plan.
4. **Enunciado:** fila B1 del roadmap. La rama actual es `develop`, así que crearé `feature/<id>-exportar-reservas-ics`. El id lo reservo con `Get-NextSddId.ps1 -Reserve`, porque `ids.mode` es `sequence`.
5. **Tamaño:** prevé 2 tasks (generar el `.ics` y exponerlo en la CLI), así que no propongo partirla.

¿Cómo seguimos?
- **A (recomendada):** confirmo full + `delegate` + B1 como enunciado, y presento la spec para que la apruebes.
- **B:** cambiar el perfil para esta feature (`pair` o `unattended`).
- **C:** apruebo la spec por delegación, nos vemos en la validación. La apruebo yo y solo paro en la validación final.
- **D:** otro enunciado o alcance (por ejemplo, exportar una sola reserva o todas).
