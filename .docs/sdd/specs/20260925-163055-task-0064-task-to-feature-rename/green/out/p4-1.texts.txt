--- [5]
Repo is tiny; read directly.

--- [10]
Ahora mismo **no hay código de cancelación en el repo**. Solo hay documentación que dice que existió.

**Código**
- `src/app.js` tiene solo `console.log('salas');`. No hay lógica de reservas ni de cancelación.

**Lo que dicen los docs**
- v0.3.0 (2026-09-01) añadió "Cancelar una reserva propia" (`changelog.md`, `roadmap.md`).
- El patch 0007 (`specs/20260918-090000-patch-0007-cancel/patch.md`) corrigió un bug. La causa raíz era que la comparación se hacía solo por hora. Por eso cancelar borraba reservas de otro día con la misma hora. Tras el fix, la cancelación ya no borra reservas de otro día con la misma hora. El patch indica que hay test de regresión y que quedó verificado.
- El changelog lo registra en `[Unreleased] / Fixed`, todavía sin release.

**Inferencia mía**
- Con esos datos, la cancelación identifica la reserva por día y hora. Los docs no dicen si hay más criterios, como el usuario propietario o la sala. Tampoco dicen qué pasa con las reservas recurrentes semanales (task 0005) al cancelar una ocurrencia.

**Discrepancia**
- El código, el test de regresión y la CLI (`salas reservar`, `salas libres`) que citan el changelog y los specs no están en `src/`. El último commit es `3b64e51 feat: base con cancelación de reservas`, pero `src/app.js` es un stub. Puede que el repo sea una base recortada o que el código viva en otra rama. No lo he comprobado.

Si quieres saber el comportamiento real, dime en qué rama o repo está el código. Si quieres implementar o definir la cancelación, eso pasa por `sdd-start-feature`.

