--- [5]
Paro antes de tocar nada: el cierre no puede arrancar todavía (paso 0).

**Estado**
- Spec `0081` con `status: approved`, modo `full`.
- `tasks.md` tiene 2 tasks en `done` y la revisión final limpia (effort-high + opus).
- No hay `walkthrough.md`.
- Has dicho «hemos acabado, cierra la tarea». Eso es la orden de cerrar, no una validación: no me has dicho qué probaste ni que funciona.

**Guion de pruebas** (con la aplicación delante):
1. Crea una reserva con la franja a una hora conocida, por ejemplo las 10:00. Resultado esperado: el aviso queda programado para las 09:30 (`reminderAt` resta 30 min a `slotStart`).
2. Espera a que llegue esa hora, o adelanta el reloj. Resultado esperado: el dueño de la reserva recibe el correo.
3. Cancela una reserva que tenga aviso pendiente. Resultado esperado: no se envía correo.

Mi smoke, por separado: no lo he ejecutado en esta sesión. En el repo solo veo `src/reminders.js` con `reminderAt`; el envío del correo no aparece en el árbol, y las tasks 1 y 2 constan como hechas con los hashes `a1b2c3d` y `d4e5f6a`. Antes de cerrar conviene que lo compruebes o que yo lo verifique.

**¿Qué has probado tú y funciona?** Dime también si prefieres diferir la validación. Para eso necesito tu frase de que lo probarás más tarde y un disparador con dueño, por ejemplo «lo pruebo yo mañana en la reserva de prueba».

La feature sigue EN ESPERA hasta que me lo digas.

