
>>> Skill: sdd-kit:sdd-start-feature
<<< Launching skill: sdd-kit:sdd-start-feature

>>> Glob: .docs/sdd/**/*

>>> Bash: git branch --show-current; ls -a .docs/sdd 2>/dev/null
<<< .docs\sdd\roadmap.md
.docs\sdd\specs\20260915-090000-task-0005-weekly\spec.md
.docs\sdd\specs\20260915-090000-task-0005-weekly\walkthrough.md
.docs\sdd\specs\20260917-090000-task-0006-free\spec.md
.docs\sdd\specs\20260917-090000-task-0006-free\walkthrough.md
.docs\sdd\specs\20260918-090000-patch-0007-cancel\patch.md
.docs\sdd\sdd-kit.json
.docs\sdd\changelog.md
.docs\sdd\mission.md
.docs\sdd\constitution.md
.docs\sdd\tech-stack.md
<<< develop
.
..
changelog.md
constitution.md
mission.md
roadmap.md
sdd-kit.json
specs
tech-stack.md

>>> Read: <run>\repo\.docs\sdd\constitution.md

>>> Read: <run>\repo\.docs\sdd\mission.md
<<< 1	# Constitution — salas
2	
3	1. Los cambios no triviales pasan por el flujo SDD del kit (`sdd-start-task` → spec → plan → `sdd-end-task`); los bugs deterministas, por el carril patch.
4	2. Tests antes que código: `node --test` en verde antes de cada commit.
5	3. Git-flow: `main` estable, `develop` de integración, `feature/<id>` desde `develop`. Tags anotados `vX.Y.Z` sobre `main`.
6	4. Texto de la interfaz y de los documentos en castellano.
7	

>>> Read: <run>\repo\.docs\sdd\tech-stack.md
<<< 1	# Misión — salas
2	
3	Herramienta de línea de comandos para reservar las salas de reuniones del coworking donde trabajo. La uso yo: la desarrollo, la pruebo y la despliego en mi propio servidor. No hay más usuarios ni más desarrolladores.
4	

>>> Read: <run>\repo\.docs\sdd\roadmap.md
<<< 1	# Tech Stack — salas
2	
3	- Node 22, sin dependencias externas. Un solo fichero de entrada: `src/app.js`.
4	- Tests: `node --test` (carpeta `test/`).
5	- Git: git-flow (`main`, `develop`, `feature/<id>`).
6	

>>> Read: <run>\repo\.docs\sdd\sdd-kit.json
<<< 1	# Roadmap — salas
2	
3	## Próximo
4	
5	_Nada abierto._
6	
7	## Backlog
8	
9	| # | Ítem | Origen |
10	| --- | --- | --- |
11	| B1 | Exportar reservas a calendario (.ics) | idea propia |
12	| B2 | Avisos por correo antes de la reserva | idea propia |
13	| B3 | Reserva recurrente mensual | idea propia |
14	| B4 | Estadísticas de uso por sala | idea propia |
15	
16	## Deuda técnica
17	
18	| Deuda | Impacto | Plan |
19	| --- | --- | --- |
20	| Sin validación del formato de franja horaria | Bajo | Patch cuando moleste |
21	
22	## Releases cerradas
23	
24	### v0.3.0 (2026-09-01)
25	
26	Cancelar una
<<< 1	{"version": "1.2.0", "channel": "plugin", "updated": "2026-09-21", "ids": {"mode": "sequence"}, "release": {"hasRecipient": false}}
2	

>>> Bash: cd "<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0064-task-to-feature-rename/931dc86e-411f-4077-8ac4-05b4d5bcbc90/scratchpad/kit-green/skills/sdd-start-feature/references"; cat nombrado.md modo-lite.md; sed -n 1,60p control-profiles.md; ls <run>/repo/.docs/sdd/*.local.json 2>&1; ls ../../sdd-templates/scripts
<<< ## Nombrado de carpetas de spec

`<yyyyMMdd-HHmmss>-(feature|patch|proposal)-<id>-<slug>`, todo en UTC:

- Timestamp: `Get-Date -AsUTC -Format 'yyyyMMdd-HHmmss'` (PowerShell).
- `feature`, `patch` o `proposal` según el carril. Una carpeta `-task-` de antes de la 2.0.0 es legado: los scripts la leen y el cierre escribe en ella sin renombrarla, pero ninguna carpeta nueva se prefija `task-`. Una carpeta que contiene `patch.md` SIEMPRE va prefijada `patch-`; una que contiene `proposal.md`, `proposal-` (la crea `sdd-roadmap`).
- `<id>`: depende del modo declarado en `.docs/sdd/sdd-kit.json` (

>>> Bash: cd <run>/repo; git ls-files | grep -v '^.docs'; wc -l src/app.js; cat src/app.js
<<< README.md
src/app.js
2 src/app.js
// salas
console.log('salas');

=== RESULTADO (12 turnos, 0.2976168 $)
Ahora: confirmar carril, modo y perfil. Queda: rama, spec con `superpowers:brainstorming` y gate de aprobación, ~15 min.

Contexto leído: constitution, mission, tech-stack, roadmap y `sdd-kit.json`. No hay `architecture.md`, `capabilities/` ni `sdd-kit.local.json`.

**Enunciado.** Lo entiendo como B2 del backlog («Avisos por correo antes de la reserva»): un aviso por email antes de cada reserva. Estoy en `develop`, así que crearía `feature/<id>-email-reminder` con un id de la secuencia (`ids.mode: sequence`).

**Carril: feature, modo full.** No es patch, ni spike, ni cambio de planificación. Descarto el modo lite porque incumple condiciones del predicado:
- «El flujo a modificar ya existe en el repo y se puede leer»: no se cumple. `src/app.js` tiene 2 líneas (`console.log('salas')`) y no hay reservas ni cancelación que leer. El commit `3409e67` dice «base con cancelación de reservas», pero el código no está.
- «No cambia contratos públicos», «no toca schema de datos» y «cabe en un solo módulo»: probablemente no se cumplen. Habría que guardar el email de quien reserva y la hora del aviso.
- Estimación: no aplica, no existe `estimation.md`.

**Perfil vigente: `delegate`.** Sale del proyecto (`sdd-kit.json` no declara `control.profile`, así que rige el default). Paro en la spec, los desvíos y la validación. En el plan no hay gate.

**Partición.** Prevés más de 3 tasks (modelo de reserva y correo, envío sin dependencias en Node 22, planificación del aviso, tests), así que propongo partirla. El coste de review crece más rápido que el tamaño.

¿Qué haces?
1. **Recomendada:** partir en dos features con fila propia en el roadmap, y arrancar ahora solo la primera. La primera sería «guardar el email y calcular qué avisos tocan», con un envío simulado. La segunda sería «enviar por SMTP real y programar el aviso». Perfil `delegate`.
2. Seguir entera en una sola feature, modo full, perfil `delegate`.
3. Cualquiera de las anteriores, pero aprobando la spec por delegación («apruebo la spec por delegación, nos vemos en la validación») para que no pare en su gate.
4. Cambiar el perfil (`pair` o `unattended`) solo para esta feature.

Dime también si «antes de cada reserva» tiene una antelación fija (por ejemplo 24 h) o ya lo defines tú en la spec.
