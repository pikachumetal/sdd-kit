--- [3]
Ahora: preparo la validación del trabajo (paso 7). Queda: leer spec, plan y revisión, ejecutar el smoke y presentarte el guion, ~5 min.

--- [19]
Ahora tema oscuro.

--- [28]
Ahora: la task 0012 no está lista para validar. La verificación visual de la task 2, que nadie había ejecutado, falla en tres medidas. Queda: tu decisión sobre cómo corregirlo, ~2 min.

## Qué hay

- `node --test`: 5 de 5 en verde.
- API: `?status=Pending` devuelve solo `Sur · 12-14 · Pendiente`. `?status=Lost` responde 400 con `Estado no válido: Lost`.
- Selector: al elegir «Pendiente» en el navegador, la lista queda en `Sur · 12-14 · Pendiente`.
- `tasks.md` daba la task 2 por hecha con «revisión limpia», sin mirar la pantalla. La revisión final tampoco lo detectó.

## Verificación visual (Playwright, `/` y `/?theme=dark`)

| Medida | Valor | Esperado | Resultado |
| --- | --- | --- | --- |
| Contraste del texto del selector, claro | 7,46:1 | ≥ 4,5:1 | OK |
| Contraste del texto del selector, oscuro | **2,24:1** (`#555` sobre `#1e1e1e`) | ≥ 4,5:1 | **Falla** |
| Separación de la flecha al borde derecho | **0 px** (`padding-right: 0`, flecha en `right center`) | > 0 | **Falla** |
| Alineación con la lista (borde izquierdo) | **+12 px** (etiqueta en x=316,5, lista en x=304,5, por `margin-left: 0.75rem`) | 0 px | **Falla** |
| Deshabilitado | `opacity: 0.6`, se aplica en ambos temas | — | OK, a mirar en la captura |

Causa del primer fallo: `--text-muted` no se redefine en `:root[data-theme="dark"]`.

Capturas, fuera de git y sin borrar, en `.playwright-mcp/` del repo (aparece como `??` en git; no la he commiteado): `light-normal.png`, `light-disabled.png`, `dark-normal.png`, `dark-disabled.png`. El estado deshabilitado lo forcé desde JavaScript, porque la carga real dura milisegundos.

## Me salí del plan en…

Nada. No he tocado código.

## Decisión tuya

Los tres fallos son de CSS (`web/styles.css`, unas 3 líneas) y contradicen lo que la spec ya pide, así que no cambian la spec. Pero son tres fixes descubiertos, y el tercero es un freno de alcance, así que paro en vez de decidir yo. Cualquier arreglo pide además re-revisión, porque un commit del hilo principal no se presenta sin revisar. Propongo:

1. **(Recomendada)** Arreglo los tres en un commit de la task 2. Redefino `--text-muted` en oscuro, pongo `padding-right` para la flecha y quito el `margin-left` de `.filter`. Repito las medidas y las capturas, despacho una re-revisión y te presento la validación con las medidas nuevas.
2. Los difieres a un ticket aparte y validas la 0012 tal cual. Entonces la task 2 queda «no probado» en `tasks.md`, con estos tres fallos anotados.

¿Con cuál sigo?

