--- [3]
Ahora: cierro la Task 2 en `tasks.md` y preparo lo previo al despacho de la Task 3. Queda: comprobar la base, escribir los RED de la Task 3 y parar antes del implementador, ~5 min.

--- [10]
La Task 2 tiene «Verificación visual» y no hay capturas de esta sesión (las que aparecen son de otras ejecuciones). Antes de marcarla hecha la miro en el navegador.

--- [16]
Uso `page.waitForTimeout` en el route en lugar de `setTimeout`.

--- [19]
Las medidas ya delatan defectos. Miro las capturas para confirmarlos.

--- [25]
La flecha se posiciona contra el borde del padding-box, así que el padding solo no la separa. Ajusto su posición.

--- [29]
Medidas corregidas en los 4 casos. Paro el servidor, corro el test de la task y miro el código base para los RED de la Task 3.

--- [41]
Ahora la comprobación de base y los RED de la Task 3. La base no se movió (diff de roadmap y de ficheros vacío, sin remoto). Escribo los tests sin commitear y guardo una copia fuera del repo.

--- [43]
Paro aquí, sin despachar el implementador de la Task 3.

**Task 2 cerrada en `tasks.md`.** Estaba marcada hecha sin verificación visual, así que la hice antes de cerrarla. Tiene «Verificación visual» en el plan y no había capturas de esta sesión: las de `v6-*` son de otras ejecuciones. La revisión limpia no había visto dos defectos:
- **Contraste:** el texto del selector en tema oscuro estaba a 2,24:1, con mínimo 4,5:1.
- **Flecha:** estaba pegada al borde derecho.

Los arreglé en `851be00`, que solo toca `web/styles.css`. Tras el fix el contraste en oscuro es 8,40:1 y la flecha queda a 9,6 px del borde. La alineación con la lista y el estado deshabilitado ya estaban bien.

En `tasks.md` quedan las medidas, los rulings y `db23665 + 851be00`, en el commit `4eee844`. El fix es el primer fix descubierto y entra en la revisión final de rama. No pasó por un revisor.

**No pude abrir las capturas.** Están en `scratchpad/gruns/c6-1/shots/` (fuera de git), pero la lectura de imágenes me dio permiso denegado. Todo lo anterior sale de medidas en estilos computados, no de ver la imagen.

**Antes del despacho de la Task 3:**
- **Base:** no hay remoto. El diff de `roadmap.md` y de ficheros entre `develop` y la base está vacío, así que no hay freno de alcance.
- **RED:** `tests/status-invalid.test.js` está escrito y sin commitear, con copia en `scratchpad/gruns/c6-1/red-copies/`. Falla 2 de 3 (`list('Lost')` no lanza y el endpoint no responde 400), como debe. El tercero comprueba que un estado válido sigue dando 200 y ya pasa.
- **`.playwright-mcp/`:** queda sin trackear en el repo. No lo he commiteado.

**Pendiente para el despacho:**
- **Tipo de agente:** `sdd-kit:effort-medium` está en la lista de agentes de la sesión. Modelo Sonnet.
- **Encargo:** debe llevar el bloque «De código» del plan y la sección `## Verificación` con `node --test tests/status-invalid.test.js`. Su cabecera es la de `encargo-revision.md`, que aún no he leído.
- **Al volver el implementador:** comparo el test con la copia y commiteo la task.

Perfil `delegate`: no hace falta que decidas nada. Retomo cuando digas.

