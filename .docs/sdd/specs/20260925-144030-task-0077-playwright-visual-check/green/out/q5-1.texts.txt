--- [11]
Ahora: contexto SDD leído (constitution, tech-stack, roadmap y `sdd-kit.json`; no hay `sdd-kit.local.json`). Queda: confirmar el carril, el modo y el perfil, y luego hacer el brainstorming y escribir la spec hasta su gate (~15 min).

La rama `feature/0012` tiene fila pendiente en el roadmap, así que propongo esta tarea como enunciado:

> **0012** — Filtrar la lista de reservas por estado. La API acepta `?status=`. La lista gana un selector de estado (Todos, Confirmada, Pendiente, Cancelada). Un estado desconocido devuelve 400. Ficheros: `src/bookings.js`, `server.mjs`, `web/`. Tamaño M.

**Carril:** task. No es un patch: es una feature con comportamiento nuevo. Tampoco es una pregunta ni un cambio de roadmap.

**Modo:** full. El modo lite no se cumple:
- ✅ El flujo ya existe y se puede leer (la lista de reservas y `/api/bookings`).
- ❌ Cambia un contrato público: el parámetro `?status=` es nuevo y responde 400 si el estado es desconocido.
- ✅ No toca el schema de datos ni exige migración.
- ❌ No cabe en una sola área: toca la API (`src/bookings.js`, `server.mjs`) y la web (`web/`).
- — No hay `estimation.md`.

**Perfil:** `delegate`, fijado en `.docs/sdd/sdd-kit.json` a nivel de proyecto. No hay perfil de task, local ni de release que lo pise. Puedes cambiarlo solo para esta task.

**Tamaño:** preveo 2 tasks internas: (1) el filtro y la validación en la API, y (2) el selector en la web, con verificación visual en los dos temas. No propongo partirla.

**¿Cómo seguimos?**
1. **(Recomendada)** Confirmo 0012 como task en modo full con perfil `delegate`. Presento la spec y paro en su gate.
2. «Apruebo la spec por delegación, nos vemos en la validación».
3. «Apruebo la spec por delegación, nos vemos en la validación, y paras antes de la Task 1 para que baje la sesión a gama media». Preveo 2 tasks. Con la spec delegada no queda otra parada donde cambiar el modelo, y la sesión va con Opus. En Native la sesión implementa todas las tasks, y basta con gama media (Sonnet, effort medium). Además, el hilo es la mayor parte del coste de una sesión.
4. Cambiar el perfil o el modo para esta task (indica cuál).

