--- [13]
Ahora: confirmar enunciado, carril, modo y perfil antes de la spec. Queda: brainstorming y spec hasta tu aprobación, ~15 min.

He leído el contexto: `constitution.md`, `roadmap.md`, `tech-stack.md`, `changelog.md` y `sdd-kit.json`. No hay `mission.md`, `architecture.md`, `capabilities/` ni `.docs/sdd/sdd-kit.local.json`.

**Enunciado propuesto.** La rama es `feature/0012` y el roadmap tiene esa fila pendiente:
> Filtrar la lista de reservas por estado: la API acepta `?status=`, la lista gana un selector de estado (Todos, Confirmada, Pendiente, Cancelada) y un estado desconocido da 400. Toca `src/bookings.js`, `server.mjs` y `web/`. Tamaño M.

**Carril: task en modo full.** No es un patch, porque es una feature nueva y no un bug. Tampoco es un spike ni un cambio de roadmap. El modo lite no aplica. Estas son sus condiciones:
- El flujo existe y se puede leer: sí (la lista `/api/bookings`).
- No cambia contratos públicos: **no se cumple**. La API gana `?status=` y una respuesta 400 nueva.
- No toca el schema de datos: sí.
- Cabe en un solo módulo o área: **no se cumple**. Toca la API y `web/`.
- No hay `estimation.md`, así que esta condición no aplica.

**Perfil vigente: `delegate`.** Sale del proyecto (`sdd-kit.json`), porque no hay perfil de task, local ni de release. Paro en la spec, en los desvíos y en la validación. El plan no tiene gate.

**Tamaño.** Preveo 2 tasks: API (filtro y 400) y selector en `web/` con verificación visual. No propongo partirla, porque no pasa de 3 tasks.

**Modelo de la sesión.** La sesión va con Opus. Con 2 tasks previstas, compensa implementar con un modelo de gama media (Sonnet, effort medium). Con la spec delegada, no queda otra parada donde puedas cambiar el modelo.

¿Cómo seguimos? Responde con el número:
1. **(Recomendada)** Confirmo la 0012 tal cual: task full, perfil `delegate`, y paras en la spec para que la apruebe.
2. Confirmo, y apruebo la spec por delegación: nos vemos en la validación.
3. Confirmo, apruebo la spec por delegación, nos vemos en la validación, y paras antes de la Task 1 para que baje la sesión a gama media (preveo 2 tasks).
4. Cambio algo (enunciado, modo o perfil para esta task): dime qué.

