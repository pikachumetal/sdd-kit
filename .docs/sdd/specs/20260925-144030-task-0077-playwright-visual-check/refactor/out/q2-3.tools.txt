
>>> Skill: sdd-kit:sdd-start-task
<<< Launching skill: sdd-kit:sdd-start-task

>>> PowerShell: Get-ChildItem -Force -Recurse .docs/sdd | Select-Object -ExpandProperty FullName; git log --oneline -5; git branch -a
<<< <run>\repo\.docs\sdd\changelog.md
<run>\repo\.docs\sdd\constitution.md
<run>\repo\.docs\sdd\roadmap.md
<run>\repo\.docs\sdd\sdd-kit.json
<run>\repo\.docs\sdd\tech-stack.md
0a62fae docs: fila 0012 del roadmap
8fa5491 feat: web de reservas de salas
  develop
* feature/0012
  main

>>> PowerShell: foreach ($f in 'constitution.md','roadmap.md','sdd-kit.json','tech-stack.md','changelog.md') { "=== $f"; Get-Content ".docs/sdd/$f" -Raw }; Test-Path .docs/sdd/sdd-kit.local.json; git status --short --ignored
<<< === constitution.md
# Constitution — salas

## Art. I — Commits

Tipo/scope en inglés, título y cuerpo en castellano, nunca title-only.

## Art. II — Tests

Todo verde antes de fusionar.

=== roadmap.md
# Roadmap — salas

## Próximo

| Id | Tarea | Origen | Ficheros que toca | Tamaño |
| --- | --- | --- | --- | --- |
| 0012 | `/api/bookings` responde 405 con la cabecera `Allow: GET` a cualquier método que no sea GET (hoy responde 200 con la lista) | dev-lead | `server.mjs` | S |

=== sdd-kit.json
{"version": "2.0.0", "channel": "plugin", "ids": {"mode": "sequence"}, "release": {"hasRecipi

>>> PowerShell: $b='<home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0077-playwright-visual-check\a63deee4-b15d-4fbf-ad0b-4222cb4f2925\scratchpad\kit\skills\sdd-start-task\references'; Get-Content "$b\modo-lite.md" -Raw; Get-Content "$b\control-profiles.md" -Raw
<<< ## Modo lite — predicado observable, activación del usuario

Una task **puede** ir en modo lite si cumple **todas** estas condiciones:

- El flujo a modificar ya existe en el repo y se puede leer.
- No cambia contratos públicos (API, interfaces que consume otro módulo).
- No toca schema de datos ni exige migración.
- Cabe en un solo módulo o área.
- Si existe `.docs/sdd/estimation.md`: la estimación es ≤ media jornada.

Cumplirlas **habilita** el modo; NO lo activa. Propón el modo lite **citando una por una las condiciones que has comprobado** y espera la confirmación explícita del u

=== RESULTADO (6 turnos, 0.5075923999999999 $)
Contexto SDD leído: constitution, roadmap, tech-stack, `sdd-kit.json` y changelog. No hay `sdd-kit.local.json`, `architecture.md` ni `capabilities/`. La rama es `feature/0012` y la fila 0012 del roadmap está pendiente, así que tomo esa fila como enunciado.

**Pregunta de arranque**

**Enunciado:** «`/api/bookings` responde 405 con la cabecera `Allow: GET` a cualquier método que no sea GET (hoy responde 200 con la lista)». Toca `server.mjs` y es de tamaño S.

**Carril y modo:**
- **Modo lite: no aplica.** Comprobé las condiciones una por una:
  - ✅ El flujo existe en el repo (`server.mjs`).
  - ❌ **Cambia un contrato público**: la respuesta HTTP de la API a los métodos que no son GET. Esta condición basta para descartar el modo lite.
  - ✅ No toca el schema de datos ni exige migración.
  - ✅ Cabe en un solo módulo.
  - ➖ No hay `estimation.md`.
- **Patch: tampoco lo recomiendo.** El fallo es determinista, pero quedan requisitos por interpretar. Hay que decidir qué pasa con `HEAD`: según la RFC 9110 va con GET, y la fila lo deja en 405. También con `OPTIONS` y con el cuerpo de la respuesta 405. Eso sale del carril patch.
- **Propuesta: task en modo full.** Prevé una sola task en el plan, así que no hace falta partirla.

**Perfil de control:** `delegate`. Sale del nivel proyecto (`sdd-kit.json`). No hay perfil en la task, ni `sdd-kit.local.json`, ni perfil de release en el roadmap. Si quieres otro perfil solo para esta task, puedes cambiarlo.

**Opciones:**
1. **Task 0012 en modo full, perfil `delegate` (recomendada).** Con el enunciado de la fila, paro en el gate de la spec.
2. **Task 0012 en modo full, perfil `delegate`, y «apruebo la spec por delegación, nos vemos en la validación».** Apruebo yo la spec, registro tu frase y sigo hasta la validación sin parar.
3. **Carril patch** en lugar de task.
4. **Otro perfil para esta task** (`pair` o `unattended`).

¿Qué opción eliges?
