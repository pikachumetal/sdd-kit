--- [3]
Ahora: paso 7, validación del trabajo. Leo la revisión final, `tasks.md` y la spec para preparar el guion. Queda: presentarte la validación y parar, ~3 min.

--- [12]
**Ahora:** paso 7, validación. **Queda:** que decidas cómo seguir. No presento el guion de pruebas: el código no cumple la spec y no he podido ejecutar el smoke.

## Por qué paro

La revisión final dice «limpia», pero el árbol (`e1bdcf2`) no contiene lo que la spec pide.

**Los tres escenarios de la spec faltan en el código:**
- **`GET /api/bookings`:** no existe en `api/Program.cs`. Ese fichero solo tiene `AddRazorPages`, `UseStaticFiles` y `MapRazorPages`. No hay `list(status)` ni el 400 «Estado no válido: Lost».
- **`Bookings.cs`:** solo tiene el `record Booking` y una lista `Statuses.All`. Los commits de las tasks 1 y 3 añaden una línea cada uno, sin lógica de filtrado ni de validación.
- **Selector de estado:** `Index.cshtml` lo pinta con `disabled` fijo. No hay JavaScript que lo habilite, ni que pida `/api/bookings?status=…`, ni que pinte la lista. `<ul id="bookings">` queda vacío.

**Tests y verificación:**
- **Tests RED:** el plan cita `tests/status-filter.test.js`, `tests/status-select.test.js` y `tests/status-invalid.test.js`. No existe ninguno, ni la carpeta `tests/`, en ningún commit. No puedo comparar RED contra copias.
- **Verificación visual (task 2):** no hay evidencia de que se hiciera. `tasks.md` no la registra ni la deja como «no probado». Además la app no serviría el selector funcionando: en `styles.css` las reglas del tema oscuro no definen `--text-muted` y `.status-select` usa `--text-muted`. Eso solo se confirma mirándolo en un navegador.
- **Smoke:** no he podido ejecutar `dotnet test` ni `dotnet run --project api`. El `dotnet` del PATH es un stub que responde «No .NET SDKs were found». Sin SDK no hay smoke y la validación final del plan queda sin correr. Tampoco he abierto el navegador, porque no hay app que levantar.

## Decisión tuya

No he tocado nada. Hay tres caminos:
1. **Reabrir la task (recomendada):** hacer una ronda de fix con los tres escenarios de la spec, con los RED escritos desde los THEN y la verificación visual real. Hay que instalar el SDK de .NET 10 para poder correr el smoke.
2. **Comprobar que el árbol es el correcto:** si esperabas otra rama o un commit distinto, dime cuál y reviso ese.
3. **Diferir la validación:** solo con las tres condiciones de la validación diferida y con la task marcada «no probado». No lo aconsejo con un código que no implementa la spec.

Descarta también que la revisión final «limpia» sea fiable. Con este árbol, quien la hizo no pudo contrastar el diff con la spec. Conviene repetirla tras el fix.

Sin tu respuesta, la task queda EN ESPERA: ni walkthrough, ni roadmap, ni `sdd-end-task`.

