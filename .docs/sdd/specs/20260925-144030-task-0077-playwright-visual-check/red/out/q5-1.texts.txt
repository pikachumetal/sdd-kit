--- [16]
Ahora: confirmo contigo carril, modo y perfil de la task. Queda: tu respuesta. Después, la spec hasta su gate de aprobación, ~10 min.

He leído el contexto SDD: `constitution.md`, `roadmap.md`, `tech-stack.md`, `changelog.md` y `sdd-kit.json`. No hay `mission.md`, `architecture.md`, `capabilities/` ni `sdd-kit.local.json`.

**Pregunta de arranque. Contesta a los cuatro puntos:**

1. **Enunciado.** Estás en la rama `feature/0012`, que tiene fila pendiente en el roadmap. Propongo como enunciado esa fila: «Filtrar la lista de reservas por estado: la API acepta `?status=`, la lista gana un selector de estado (Todos, Confirmada, Pendiente, Cancelada) y un estado desconocido da 400». Ficheros: `src/bookings.js`, `server.mjs` y `web/`.
2. **Carril y modo: task en modo full (recomendado).** No es patch, porque es una feature nueva. No es roadmap ni consulta. El modo lite no aplica. Esto dicen sus condiciones:
   - ✅ El flujo existe y se puede leer (lista de reservas, `/api/bookings`).
   - ❌ **Cambia un contrato público**: la API gana el parámetro `?status=` y una respuesta 400.
   - ✅ No toca el schema de datos ni exige migración, en principio.
   - ❌ **No cabe en un solo módulo**: toca la API (`src/` + `server.mjs`) y la web (`web/`).
   - No hay `estimation.md`, así que esa condición no aplica.
3. **Partir la task: no lo propongo.** El repo es pequeño (unas 50 líneas de código). Preveo 2 o 3 tasks internas: filtro con 400 en la API y selector en la web. Está por debajo del umbral de 3.
4. **Perfil: `delegate`.** Sale del proyecto (`sdd-kit.json`), porque no hay perfil de task, local ni de release. Con `delegate` paro en la spec, en los desvíos y en la validación. No paro en el plan. Opciones:
   - a) **`delegate` y paro en el gate de la spec (recomendada).**
   - b) `delegate`, pero **«apruebo la spec por delegación, nos vemos en la validación»**: la apruebo yo, registro tu frase literal y sigo hasta la validación.
   - c) Cambiar el perfil a `pair` o `unattended` solo para esta task.

¿Confirmas el enunciado y el modo full? ¿Eliges a, b o c?

