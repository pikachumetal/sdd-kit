--- [3]
Ahora: paso 7, validación del trabajo. Leo la revisión final, `tasks.md` y la spec para preparar el guion. Queda: presentar y parar, ~3 min.

--- [6]
Task 2 tiene «Verificación visual» y `tasks.md` no dice que se hiciera. La hago ahora. Antes, suite y servidor.

--- [22]
Estoy en la verificación visual de la task 2. Ya he visto dos defectos y voy a corregirlos. Miro las variables de tema.

--- [31]
La flecha sigue pegada al borde: el padding no mueve el fondo. Cambio la posición del fondo.

--- [38]
Ahora: paso 7, validación. Queda: tu respuesta a la revisión del fix (abajo) y a la validación, ~5 min, sin coste extra hasta que decidas.

## Me salí del plan en…

1. **Verificación visual de la task 2 hecha ahora, no antes.** `tasks.md` la daba por «done» sin que nadie mirara el navegador. La he hecho yo en `/` y `/?theme=dark`, y encontré dos defectos que la revisión final «limpia» no vio:
   - En oscuro, el texto del selector era casi ilegible: `--text-muted` (#555) no se redefinía en el tema oscuro, sobre fondo #1e1e1e.
   - La flecha del selector estaba pegada al borde derecho.
2. **Fix de hilo principal** en `web/styles.css`, commit `a0cf219`: `--text-muted: #b0b0b0` en el tema oscuro y la flecha a `right 0.5rem center` con `padding-right`. Es el primer fix descubierto, así que es un ruling y no un freno de alcance. Lo verifiqué otra vez en claro y en oscuro: el texto se lee y la flecha tiene margen.
3. **Falta la revisión de ese commit.** Debe pasar por una revisión, y no la he despachado. Propongo un revisor `sdd-kit:effort-low` con Sonnet sobre `a0cf219` (2 líneas de CSS, coste mínimo). Confírmame si lo lanzo. Mientras tanto, tenlo en cuenta al validar.
4. **Estado deshabilitado sin captura.** La carga es instantánea y no pude capturarlo. Solo lo cubre el test `el selector nace deshabilitado mientras carga`.
5. **Alineación con la lista.** El texto de «Estado» queda alineado con el texto de las filas (margen de 0.75rem), no con el borde de la lista. Lo dejo así salvo que prefieras lo contrario.

## Qué hay

- `GET /api/bookings?status=Pending` devuelve solo Sur, y con `Lost` responde 400 «Estado no válido: Lost». Ambos casos los cubren los tests de las tasks 1 y 3.
- Selector «Estado» con Todos, Confirmada, Pendiente y Cancelada.
- Smoke que ejecuté: `node --test` da 5/5 en verde, antes y después del fix. En el navegador elegí «Pendiente» en oscuro y la lista quedó en «Sur · 12-14 · Pendiente».

## Guion de pruebas

1. Arranca `npm start` y abre `http://localhost:4327/`. Verás Norte · 10-12 · Confirmada, Sur · 12-14 · Pendiente y Este · 16-18 · Cancelada, con el selector en «Todos».
2. Elige «Pendiente». La lista muestra solo `Sur · 12-14 · Pendiente`.
3. Abre `http://localhost:4327/?theme=dark`. El texto del selector se lee bien y la flecha no toca el borde derecho. Repite en tema claro.
4. Ejecuta `curl "localhost:4327/api/bookings?status=Pending"`. Devuelve solo Sur.
5. Ejecuta `curl -i "localhost:4327/api/bookings?status=Lost"`. Responde 400 con «Estado no válido: Lost».

¿Validas el trabajo? Dime qué has probado tú y si funciona, y si lanzo la revisión del fix.

