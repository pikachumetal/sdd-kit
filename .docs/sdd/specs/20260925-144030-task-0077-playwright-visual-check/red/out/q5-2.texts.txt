--- [15]
Leo el código que toca la fila 0012 para estimar cuántas tasks internas saldrían.

--- [18]
Ahora: confirmar carril, modo y perfil de la task. Queda: esta respuesta, luego brainstorming y spec hasta el gate de aprobación, ~15 min.

Contexto SDD leído (constitution, roadmap, tech-stack, changelog, `sdd-kit.json`). No hay `sdd-kit.local.json` ni `capabilities/`. Rama `feature/0012` y fila 0012 pendiente en el roadmap.

**Primera pregunta: confirma o cambia estos cuatro puntos.**

1. **Enunciado**, sacado de la fila 0012: «Filtrar la lista de reservas por estado. La API acepta `?status=`, la lista gana un selector de estado (Todos, Confirmada, Pendiente, Cancelada) y un estado desconocido da 400». Toca `src/bookings.js`, `server.mjs` y `web/`.
2. **Carril: task.** No es un patch, porque añade comportamiento nuevo. Tampoco es un cambio de roadmap ni un spike.
3. **Modo: full.** El modo lite no aplica. Condición por condición:
   - El flujo existe y se puede leer: ✅ (`bookings.list()` y `/api/bookings`).
   - No cambia contratos públicos: ❌. La API gana `?status=` y una respuesta 400.
   - No toca schema ni exige migración: ✅.
   - Cabe en un solo módulo o área: ❌. Toca el dominio, el servidor y la web.
   - Estimación ≤ media jornada: no aplica, porque no existe `estimation.md`.
4. **Perfil: `delegate`**, sacado del proyecto (`sdd-kit.json`). No hay perfil de task, local ni de release. Paro en la spec, en los desvíos y en la validación. El plan no tiene gate.

No propongo partir la task. Con el código leído, el plan tendría unas 2 o 3 tasks internas: filtro con 400 en la API y selector en la web. Eso no supera 3.

Opciones:
- **A (Recomendada):** «Confirmo: task 0012, modo full, perfil `delegate`». Paro en el gate de la spec.
- **B:** «Confirmo, y apruebo la spec por delegación, nos vemos en la validación». Apruebo yo la spec, registro tu frase y sigo hasta la validación.
- **C:** Cambiar algo: el enunciado, el modo o el perfil (`pair` / `unattended`) para esta task.

¿A, B o C?

