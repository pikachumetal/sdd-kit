--- [13]
Miro la task 2 en el navegador (verificación visual del plan) antes de marcarla hecha. Sigo con el screenshot.

--- [19]
Servidor caído (murió con la llamada Bash anterior). Lo relanzo en segundo plano.

--- [30]
Verifico el arreglo visual en oscuro y luego claro.

--- [41]
Commit del fix (propio, para que entre en la revisión de la task) y registro en `tasks.md` sin marcarla hecha.

--- [43]
Ahora: la task 2 **no queda cerrada**. Paro aquí porque necesito tu decisión sobre la re-revisión.

**Qué he hecho.** La task 2 tiene «Verificación visual» en el plan, y `tasks.md` no puede marcarla hecha sin mirarla en un navegador. La he mirado con Playwright, en `/` y `/?theme=dark`, con el selector normal y deshabilitado.

**Qué he encontrado.** La revisión limpia y los tests no habían visto dos defectos:
- **Tema oscuro:** el texto del selector se ve casi ilegible. `--text-muted` (#555) no se redefine en el tema oscuro y queda sobre el fondo #1e1e1e.
- **Flecha:** el selector tiene `padding-right: 0` y la flecha queda pegada al borde derecho.

La alineación con la lista está bien. El único error de consola es el 404 de `favicon.ico`, que no viene de esta task.

**Me salí del plan en…** Arreglé los dos defectos yo, en el hilo principal, en un commit propio: `8fec62a` (`web/styles.css`, 3 líneas). Lo he vuelto a mirar en claro y oscuro y se ve bien, y `node --test tests/status-select.test.js` da 2/2. Es un ruling. No es un freno de alcance: es el primer fix descubierto y no cambia la salida observable.

**Qué queda.**
- No he escrito el hash de la task en `tasks.md`. La fila 2 sigue `pending`, porque `8fec62a` todavía no ha pasado por revisión.
- Las reglas dicen que el commit del hilo principal entra en el alcance de la revisión de la task.
- No hay parada de guion todavía. En `pair` esa parada va al cerrar la task, y el guion es el de «Se prueba en la aplicación»: elegir «Pendiente» y ver solo «Sur · 12-14 · Pendiente».

**Decisión tuya.** ¿Despacho la re-revisión de `e3c23c8..8fec62a`? Propongo `sdd-kit:effort-medium` con Sonnet, el mismo tipo y modelo que fija el plan. Es un diff de 3 líneas de CSS, así que el coste es mínimo. Si la deja limpia, junto la task en un commit, apunto el hash en `tasks.md` como `done` y te presento el guion de pruebas.

