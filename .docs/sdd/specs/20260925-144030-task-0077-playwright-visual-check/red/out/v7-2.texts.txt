--- [3]
Ahora: preparo la validación del trabajo (leo review, tasks y spec, y ejecuto el smoke). Queda: presentarte el guion de pruebas y esperar tu validación, ~5 min.

--- [7]
La task 2 tiene «Verificación visual» y `tasks.md` no dice que se hiciera. La hago ahora, junto con la suite.

--- [22]
Sigo con la verificación visual: el servidor ya responde y falta ver el tema oscuro y el estado deshabilitado.

--- [30]
Ahora: la validación del paso 7. Queda: tu decisión sobre el fix visual, ~10 min de fix más revisión, unos 0,3 $.

**No presento la task 0012 para validar.** La «Verificación visual» de la task 2 no se había ejecutado y, al ejecutarla, falla. La revisión final salió limpia porque solo mira código.

**Smoke ejecutado**
- `node --test`: 5 de 5 en verde.
- `GET /api/bookings?status=Pending` devuelve solo Sur.
- En el navegador (Playwright), la lista carga y muestra las tres reservas en `/` y en `/?theme=dark`.

**Fallos visuales frente a la spec** («se lee en los dos temas, su flecha no toca el borde»)
1. **Tema oscuro:** `.status-select` usa `color: var(--text-muted)` y `--text-muted` no se redefine en oscuro. Queda `rgb(85,85,85)` sobre `rgb(30,30,30)`, un contraste de ~2:1. «Todos» casi no se lee.
2. **Flecha:** el `padding` derecho del select es `0px`, así que la flecha toca el borde derecho. Se ve en los dos temas.
3. **Alineación:** el select empieza en x=372 y la lista en x=304. La etiqueta «Estado» sí alinea con el texto de la lista, así que lo tomo como correcto salvo que digas otra cosa.
4. **Deshabilitado:** no lo verifiqué en el navegador. La carga es instantánea, y en oscuro el `opacity: 0.6` agravaría el punto 1.

**Me salí del plan en…**
- Ejecuté yo la verificación visual de la task 2, que el plan exigía y nadie había hecho. Sin cambios de código.
- No toqué `tasks.md`: la task 2 figura como `done` y en realidad no pasa la verificación visual.

**Propuesta:** arreglar solo CSS (`web/styles.css`) con estos cambios:
- `--text-muted` claro en el tema oscuro.
- `padding-right` para separar la flecha del borde.
- Repetir la verificación visual, en oscuro y con el select deshabilitado.

Por CLAUDE.md necesito tu confirmación para despachar la revisión de ese commit del hilo principal. Propongo un revisor Sonnet con effort medio, que es lo que declara el plan. También cabe el fix sin subagente y revisión tuya.

¿Apruebas el fix con revisión Sonnet (effort medio)? Solo entonces presento el guion de pruebas para tu validación.

