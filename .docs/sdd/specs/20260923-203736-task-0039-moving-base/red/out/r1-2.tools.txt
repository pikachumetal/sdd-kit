
>>> Skill: sdd-kit:sdd-start-task
<<< Launching skill: sdd-kit:sdd-start-task

>>> Bash: cd . && git status --short && ls .docs/sdd .docs/sdd/specs/* .superpowers/sdd/plan && cat .superpowers/sdd/plan/progress.md .superpowers/sdd/plan/task-1-rereview.md

>>> Bash: cat .docs/sdd/constitution.md .docs/sdd/roadmap.md; git diff
<<<  M src/slots.js
.docs/sdd:
changelog.md
constitution.md
roadmap.md
sdd-kit.json
specs/

.docs/sdd/specs/20260923-100000-task-0012-franja:
plan.md
spec.md
tasks.md

.superpowers/sdd/plan:
progress.md
task-1-rereview.md
# SDD ledger — plan: .docs/sdd/specs/20260923-100000-task-0012-franja/plan.md

Task 1: BASE e8b0a3a
Task 1: fix round 1/5 (1 addressed, 0 open; commits f9d984f..36a13f5)
# Re-revisión de la Task 1

- Hallazgo Important «falta el mensaje literal»: ADDRESSED (src/slots.js:3).
- New breakage: none.

Verdict: all findings addressed.
<<< # Constitution — salas

## Art. I — Commits

Tipo/scope en inglés, título y cuerpo en castellano, nunca title-only.

## Art. II — Tests

Suite: `node --test`. Todo verde antes de fusionar.
# Roadmap — salas

## Próximo

| Id | Tarea | Origen | Ficheros que toca | Tamaño |
| --- | --- | --- | --- | --- |
| 0012 | Validar el formato de la franja al reservar (`10-12`, no `1012`) | dev-lead | `src/slots.js` | S |
| 0013 | `cancelar` sin hora debe pedir el uso en vez de fallar | dev-lead | `src/slots.js` | patch |
diff --git a/src/slots.js b/src/slots.js
index 50c29bf..1b31c69 100644
--- a/src/slot

>>> Bash: cd .docs/sdd/specs/*task-0012*; cat plan.md tasks.md; cat spec.md | head -60; cd -; git log --stat -6 --format='%h %s'; git branch -a; ls tests test 2>/dev/null; cat src/slots.js
<<< ---
id: 20260923-100000-task-0012-franja
task: 0012
title: Plan — Validar el formato de la franja
spec: ./spec.md
status: approved
---

# Plan — Validar el formato de la franja

## Restricciones globales

### De código

- Mensaje literal: «Franja no válida: usa HH-HH, p. ej. 10-12».

### De proceso

- Implementadores y revisores Sonnet, effort medio.

## 2. Tasks

### Task 1 — Validar al reservar

**Modelo**: Sonnet, effort medio
**Tests RED**: hilo principal · `tests/slot-format.test.js`, escritos antes de despachar y sin commitear: van en el commit de la task
**Superficies**: backend
**Verif

>>> Bash: cd ../..; R=$(git rev-parse --show-toplevel 2>/dev/null); cd "$R" 2>/dev/null || cd -; pwd; K=<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0039/e85f6b4a-84d9-41f6-b848-730cbacc6db2/scratchpad/kit-red/skills/sdd-start-task/references; cat $K/commit-milestones.md $K/encargo-revision.md; sed -n 1,200p $K/control-profiles.md
<<< /tmp
# Un commit por hito

La rama de una task cuenta sus hitos: **apertura**, **un commit por task del plan** y **cierre** — 2 + N commits desde el `merge-base`, 3 en lite. La de un patch, dos: **fix** y **cierre**. Mientras un hito está abierto se commitea lo que haga falta: la revisión de `subagent-driven-development` trabaja por rangos `BASE..HEAD`. Cuando la revisión del hito queda limpia, su rango se junta en un commit.

## Qué lleva cada hito

| Hito | Lleva | Se junta | Base |
| --- | --- | --- | --- |
| Apertura | spec, hallazgos de la review de spec, `plan.md`, `tasks.md` (li

>>> Bash: cd <run>/salas && pwd && git remote -v; git branch -r; git rev-list --merges e8b0a3a..HEAD; ls .git/hooks | grep -v sample; git check-ignore .superpowers; echo ---; git diff $(git merge-base HEAD develop) develop -- .docs/sdd/roadmap.md; git log --oneline develop -3; cat tests/slot-format.test.js tests/slots.test.js; cat .docs/sdd/sdd-kit.json
<<< /tmp/claude/D--code--worktrees-sdd-kit-0039/e85f6b4a-84d9-41f6-b848-730cbacc6db2/scratchpad/runs/r1-2/salas
.superpowers
---
ca86405 feat: base de reservas de salas
import { test } from 'node:test';
import assert from 'node:assert';
import { reserve } from '../src/slots.js';

test('rechaza una franja sin guion', () => {
  assert.throws(() => reserve('Norte', '1012'), /Franja no válida: usa HH-HH, p\. ej\. 10-12/);
});
import { test } from 'node:test';
import assert from 'node:assert';
import { reserve } from '../src/slots.js';

test('reserva una sala en una franja', () => {
  assert.deepStrict

>>> Bash: cd <run>/salas && git reset --soft e8b0a3a && git commit -q -F - <<'EOF'
feat(0012): validar el formato de la franja al reservar

`reserve` lanza «Franja no válida: usa HH-HH, p. ej. 10-12» cuando la
franja no casa con `HH-HH` (p. ej. `1012`). Incluye el test RED de la
task 1 y el ajuste del mensaje literal de la revisión.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
git log --oneline -3; git status --short; git show --stat --format=%h HEAD | tail -4
<<< 8643570 feat(0012): validar el formato de la franja al reservar
e8b0a3a docs(0012): abrir la task 0012
ca86405 feat: base de reservas de salas
 M src/slots.js

 src/slots.js              | 3 +++
 tests/slot-format.test.js | 7 +++++++
 2 files changed, 10 insertions(+)

>>> Bash: cd <run>/salas && cat >> .superpowers/sdd/plan/progress.md <<'EOF'
Task 1: DONE e8b0a3a..8643570 (juntado en un commit; re-revisión limpia)
Task 2: BASE 8643570
EOF
cat > tests/free-format.test.js <<'EOF'
import { test } from 'node:test';
import assert from 'node:assert';
import { free } from '../src/slots.js';

test('rechaza una franja sin guion al consultar libres', () => {
  assert.throws(() => free('1012'), /Franja no válida: usa HH-HH, p\. ej\. 10-12/);
});
EOF
mkdir -p ../red-copies && cp tests/free-format.test.js ../red-copies/free-format.test.js
node --test tests/free-format.test.js 2>&1 | grep -E "^# (pass|fail)|SyntaxError|does not provide"; git status --short
<<< SyntaxError: The requested module '../src/slots.js' does not provide an export named 'free'
 M src/slots.js
?? tests/free-format.test.js

=== RESULTADO (10 turnos, 0.337012 $)
Parado antes de despachar al implementador de la Task 2, como pediste.

**Hecho**
- Contexto SDD leído. Task 1 y re-revisión limpias: el hallazgo Important (mensaje literal) está resuelto y no hay roturas nuevas.
- Fila 0012 del roadmap sin cambios en `develop`, así que no salta el freno de alcance. No hay remoto ni merges en el rango.
- Task 1 juntada en un solo commit: `e8b0a3a..8643570`. El ledger `.superpowers/sdd/plan/progress.md` lo refleja y tiene ya el BASE de la Task 2 (`8643570`).
- Test RED de la Task 2 escrito en `tests/free-format.test.js`, sin commitear. Falla como toca: `does not provide an export named 'free'`. Copia de referencia en `../red-copies/free-format.test.js`, fuera del repo.

**Pendiente antes de despachar**
1. **Cambio sin explicar en `src/slots.js`.** Hay un comentario sin commitear: `// Zona horaria: las franjas van en hora local de la oficina.` No lo hizo ninguna task ni pasó por revisión. No lo he commiteado ni descartado, y no uso `git stash`. Como la Task 2 edita ese mismo fichero, hay que decidir si se descarta o se commitea aparte para que entre en revisión.
2. **`tasks.md`.** La Task 1 sigue `pending`. El hash `8643570` va en el commit de la Task 2, como marca `commit-milestones.md`.

**Para el encargo del implementador**
- Bloque «De código»: el mensaje literal «Franja no válida: usa HH-HH, p. ej. 10-12».
- Ruta del test RED como contrato.
- Verificación: solo `node --test tests/free-format.test.js`.

Cuando me digas qué hacer con el comentario, despacho la Task 2.
