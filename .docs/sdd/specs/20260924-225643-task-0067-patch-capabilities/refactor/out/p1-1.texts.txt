--- [9]
Guardas limpias. Junto fix y patch.md en un commit.

--- [17]
Diff limpio. Commit de cierre y verificación.

--- [19]
Patch 0014 cerrado en `feature/0014`, pero el merge queda pendiente. `node --test` sigue en 2/2 verde y el working tree está limpio.

- **Fix:** junté fix y `patch.md` en `5c45c95`. Antes comprobé las guardas: no había merges en el rango ni commits publicados.
- **Cierre:** el commit `d506387` lleva el hash del fix en `patch.md`, la entrada `Fixed` del changelog, el roadmap (quité 0014 de «Próximo» y añadí su fila en «Patches») y la capacidad `bookings`.
- **Capacidad:** «Consultar salas libres» cambió de forma observable, porque ahora lista `Sur` y `Oeste (en mantenimiento)` al final. Rellené la sección de delta de `patch.md`, fusioné el cambio como MODIFIED en `capabilities/bookings.md` y añadí la línea de historial.
- **Estimation y entorno:** no existe `estimation.md`, así que no hay log que regenerar. Tampoco existe `environments.md`, así que no hay `env:clean`.

**Decisiones mías en este cierre:**
- Junté los dos commits del fix (código y `patch.md`) en uno. La receta lo pide y escribí el mensaje yo.
- Quité 0014 de «Próximo» en el roadmap porque el patch lo salda.

**Pendiente:** el merge. `sdd-kit.json` no tiene bloque `merge`, así que la decisión es tuya: dime a qué rama fusionar y si hago push. No he usado `Invoke-SddMerge.ps1`.

**Ticket para el kit:** ¿quieres que genere el ticket de mejora del kit con `sdd-feedback` en esta sesión?

**No terminado.** Falta el merge de `feature/0014`. No borres todavía el worktree `<home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0067-patch-capabilities\326411ec-37eb-4f35-ae3b-b5187fe68675\scratchpad\runs-refactor\p1-1\salas`.

