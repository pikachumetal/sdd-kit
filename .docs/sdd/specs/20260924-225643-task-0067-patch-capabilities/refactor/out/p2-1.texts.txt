--- [10]
Guardas limpias: sin merges y sin rama publicada. Junto el fix en un commit y preparo el cierre.

--- [12]
Ahora `patch.md` (hash, sección de delta fuera, pues la capacidad ya decía lo que hace el fix), changelog y roadmap.

--- [19]
Sin cambios a medias. Edito con las herramientas de edición.

--- [30]
Patch 0013 cerrado en `feature/0013` en dos commits, pero el merge queda pendiente de tu decisión.

- **Fix:** `c2580f8`, con el código, el test y `patch.md` juntados en un solo commit. Antes eran dos commits, sin merges en el rango y sin publicar.
- **Cierre:** `4a73d5b`, con `patch.md` (hash y tiempo de 0,3 h), la entrada `Fixed` en el changelog y la fila en la tabla de patches del roadmap.
- **Capacidad `bookings`:** sin delta. Ya decía el máximo de 2 h y el aviso `Máximo 2 h por reserva`, así que el fix solo devuelve el comportamiento a lo que ya documentaba. Quité de `patch.md` la sección de delta vacía.
- **Roadmap:** quité la fila 0013 de «Próximo», porque ya está en la tabla de patches.
- **estimation-log:** no existe `.docs/sdd/estimation.md`, así que no hay nada que regenerar.
- **`env:clean`:** tampoco existe `environments.md`, así que no aplica.

**Pendiente:** `sdd-kit.json` no trae el bloque `merge`, así que el merge lo decides tú. La rama está lista, con los commits hechos y el working tree limpio. No he fusionado nada.

**Ticket del kit:** esta sesión no ha generado ninguno. Si quieres, lo genero con `sdd-feedback`.

**Pregunta:** ¿fusiono `feature/0013` en `main` con `Invoke-SddMerge.ps1`, y con push o sin él?

**No terminado.** Falta el merge de `feature/0013` en `main`, que espera tu respuesta.

