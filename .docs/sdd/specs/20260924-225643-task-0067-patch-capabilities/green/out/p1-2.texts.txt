--- [10]
Cierre del patch 0014 hecho en `feature/0014`. El merge queda pendiente: `sdd-kit.json` no tiene bloque `merge`, así que lo decides tú.

**Cambios del cierre** (commit `5f10110`):
- **`patch.md`:** lleva el hash del fix (`73ce61a`) y la verificación (`node --test` 2/2 verde). Le he añadido la sección «Delta de capacidad».
- **Capacidad `bookings`:** `Consultar salas libres` pasa a decir que `libres` lista `Sur` y, aparte y al final, `Oeste (en mantenimiento)`. Antes decía que listaba `Sur` y `Oeste`. Queda anotado en el historial como MODIFIED.
- **Changelog:** entrada `Fixed` en `[Unreleased]`.
- **Roadmap:** fila añadida en la tabla de Patches.
- **Sin cambios:** no existen `estimation.md` ni `environments.md`, así que no hay log de estimación ni `env:clean`.

**Decisiones mías en este cierre:**
- Quité la fila 0014 de «Próximo» en el roadmap, porque ya está hecha.
- El patch queda en tres commits (fix, `patch.md` previo, cierre) en vez de los dos de la receta. No junté el de `patch.md` porque el hash del fix no cambia. Si prefieres dos, se junta antes del merge.

**Pendiente:**
- **Merge de `feature/0014`:** ¿a qué rama destino (`develop` o `main`) y con push o sin él? Con perfil `delegate` pero sin bloque `merge`, no lo he hecho por mi cuenta. Si quieres que lo haga, dime destino y push, y lo hago con `Invoke-SddMerge.ps1`.

Puedo generar el ticket de mejora del kit con `sdd-feedback` en esta sesión si lo quieres.

**No terminado.** Falta el merge de `feature/0014`. No borres todavía el worktree `<home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0067-patch-capabilities\326411ec-37eb-4f35-ae3b-b5187fe68675\scratchpad\runs-green\p1-2\salas`.

