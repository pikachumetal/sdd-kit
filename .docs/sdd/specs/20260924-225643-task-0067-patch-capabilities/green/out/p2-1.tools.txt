
>>> Skill: sdd-kit:sdd-end-patch
<<< Launching skill: sdd-kit:sdd-end-patch

>>> Bash: cd "<run>/salas" && git status --short && git log --oneline main..HEAD && git merge-base main HEAD && find .docs -type f | head -50 && cat .docs/sdd/sdd-kit.json 2>/dev/null
<<< 073c881 docs(0013): patch.md del fix de reservar
428a023 fix(0013): reservar rechaza más de 2 h
c57948ed5e329b07fc9362c99ffe47fda0f79300
.docs/sdd/capabilities/bookings.md
.docs/sdd/changelog.md
.docs/sdd/constitution.md
.docs/sdd/roadmap.md
.docs/sdd/sdd-kit.json
.docs/sdd/specs/20260925-080000-patch-0013-reserva-maxima/patch.md
{"version": "2.0.0", "channel": "plugin", "ids": {"mode": "sequence"}, "release": {"hasRecipient": false}, "control": {"profile": "delegate"}}

>>> Bash: cd "<run>/salas/.docs/sdd" && cat specs/*/patch.md; echo ----; cat capabilities/bookings.md; echo ----; cat changelog.md; echo ----; cat roadmap.md; cd ..; cd ..; git show 428a023 --stat; git show 428a023 | head -60
<<< ---
id: 20260925-080000-patch-0013-reserva-maxima
task: 0013
title: Patch — `reservar` acepta reservas de más de 2 h
type: patch
status: done
created: 2026-09-25
branch: feature/0013
commit: <hash>
---

# Patch 0013 — `reservar` acepta reservas de más de 2 h

## 1. Síntoma

Soporte: `salas reservar Norte 10-13` guardó una reserva de 3 h.

## 2. Causa raíz

`reserve` no comprobaba la duración de la franja.

## 3. Fix

- **Fichero(s)**: `src/slots.js`, `tests/slots.test.js`
- **Cambio**: `reserve` rechaza una franja de más de 2 h con `Máximo 2 h por reserva`.

## 4. Verificación

| # | Caso | Re

>>> Bash: cd "<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0067-patch-capabilities/326411ec-37eb-4f35-ae3b-b5187fe68675/scratchpad/kit-green/skills" && cat sdd-templates/references/roadmap-template.md 2>/dev/null || find . -name "roadmap-template*"; echo ----; cat sdd-end-task/references/merge-recipe.md; ls sdd-templates/scripts
<<< ./sdd-templates/templates/roadmap-template.md
----
# Receta del merge en el cierre

La usan el paso 10 de `sdd-end-task` y el paso 6 de `sdd-end-patch` cuando la política de merge ya dijo que se fusiona: la aplica el agente (bloque `merge` completo, perfil `delegate` o `unattended`) o el usuario la pidió. Qué se fusiona y cómo lo fija la política: la rama destino es `merge.into`, y el merge va con `--no-ff` si `merge.noFf` es `true`.

## El merge es un script

El merge lo hace `Invoke-SddMerge.ps1`, siempre, y no se rehace a mano con `git merge`, `git worktree` ni `git push`:

```text

>>> Bash: cd "<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0067-patch-capabilities/326411ec-37eb-4f35-ae3b-b5187fe68675/scratchpad/kit-green/skills" && cat sdd-templates/templates/roadmap-template.md; ls <run>/salas/.docs/sdd
<<< # Roadmap — <proyecto>

> Índice vivo del proyecto: qué viene, qué se debe y qué se cerró. Lo leen las skills del kit: `sdd-start-release` (Backlog y deuda técnica), `sdd-end-task` (marca la fila de la task), `sdd-end-patch` (tabla de Patches) y `sdd-end-release` (colapsa la sección de una release a resumen y enlaces). **Las secciones y las cabeceras de tabla van literales**: una columna renombrada rompe a la skill que la lee. Estados de fila: ⏳ pendiente · 🔄 en curso · ✅ hecho y validado · 🧪 validación diferida a <disparador> · ⏸️ aparcada: <motivo>. Con ids de secuencia (`ids.mode: seque

>>> Bash: cd "<run>/salas" && 
sed -i 's/^commit: <hash>/commit: 428a023/' .docs/sdd/specs/*/patch.md &&
printf '\n### Fixed\n\n- `reservar` rechaza una franja de más de 2 h con `Máximo 2 h por reserva` ([patch 0013](specs/20260925-080000-patch-0013-reserva-maxima/patch.md)).\n' >> .docs/sdd/changelog.md &&
printf '| 2026-09-25 | 0013 | `reservar` rechaza reservas de más de 2 h ([patch](specs/20260925-080000-patch-0013-reserva-maxima/patch.md)) |\n' >> .docs/sdd/roadmap.md &&
git diff --stat && tail -5 .docs/sdd/roadmap.md && git add -A && git commit -q -m "docs(0013): cierre del patch, changelog y roadmap

Sin delta de capacidad: bookings.md ya decía el máximo de 2 h.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>" && git log --oneline -3 && git status --short
<<<  .docs/sdd/changelog.md                                             | 4 ++++
 .docs/sdd/roadmap.md                                               | 1 +
 .docs/sdd/specs/20260925-080000-patch-0013-reserva-maxima/patch.md | 2 +-
 3 files changed, 6 insertions(+), 1 deletion(-)
## Patches

| Fecha | Id | Descripción |
| --- | --- | --- |
| 2026-09-25 | 0013 | `reservar` rechaza reservas de más de 2 h ([patch](specs/20260925-080000-patch-0013-reserva-maxima/patch.md)) |
04b1ccb docs(0013): cierre del patch, changelog y roadmap
073c881 docs(0013): patch.md del fix de reservar
428a023 fix(0013): rese

=== RESULTADO (8 turnos, 0.25373239999999997 $)
Patch 0013 cerrado en `feature/0013`. La rama no está fusionada.

- **`patch.md`:** ya tiene el hash `428a023` y 0,3 h. La verificación (`node --test` 2/2 verde) la corrió el agente.
- **Capacidad `bookings`:** sin delta. `capabilities/bookings.md` ya decía el máximo de 2 h y el aviso `Máximo 2 h por reserva`, así que el fix solo devuelve el comportamiento a lo documentado.
- **Commits:** el fix `428a023` es un solo commit. El cierre `04b1ccb` lleva `patch.md`, la entrada `Fixed` del changelog y la fila de la tabla de patches del roadmap.
- **`estimation.md` y `environments.md`:** no existen, así que no regeneré el estimation-log ni ejecuté `env:clean`.
- **Fila 0013 en «Próximo»:** la dejé como estaba, porque esa tabla no tiene columna de estado. Habría que quitarla a mano.

**Pendiente: el merge.** `sdd-kit.json` no tiene bloque `merge`, así que la decisión es tuya. Si lo autorizas, indícame destino (`main` o `develop`) y si hago push. Lanzaría `Invoke-SddMerge.ps1`, sin `git merge` a mano.

¿Quieres que genere con `sdd-feedback` el ticket de mejora del kit sobre esta sesión?

**No terminado.** Falta el merge de `feature/0013` (decisión tuya). No borres todavía el worktree `<run>\salas`.
