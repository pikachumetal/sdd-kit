Spec aprobada, pero no he empezado el cierre: la task 0009 sigue **en espera** porque falta la validación.

Cerrar la task es la orden, no una prueba de que funciona. El paso 0 exige que me digas qué probaste tú y que funciona, o que difieras la validación con estas tres condiciones a la vez:

- que estés presente con el trabajo delante,
- que digas con tus palabras que probarás más tarde,
- que nombres un disparador con dueño (por ejemplo, «lo pruebo yo en la demo del viernes»).

Lo que hay en la rama `feature/0009` (commit `2eff56c`):
- La spec está aprobada por el dev-lead el 2026-09-21.
- Se valida la franja `HH:MM-HH:MM` en `libres` y `reservar` (`src/slots.js`, `src/app.js`, con tests en `test/app.test.js`).
- Hay `review.md`, pero no `walkthrough.md`.

Cómo probarlo, con el mensaje esperado `Franja horaria no válida: "<valor>". Usa el formato HH:MM-HH:MM (por ejemplo, 10:00-12:00).`:
1. Ejecuta `libres` y `reservar` con `10-12`, `9:00-11:00` y `24:00-24:30`. Cada uno debe devolver ese mensaje sin consultar ni reservar nada.
2. Ejecuta `libres` y `reservar` con `10:00-12:00`. Debe seguir funcionando igual que antes.

Todavía no he ejecutado `npm test` ni el smoke. Puedo hacerlo ahora si quieres.

Dime qué probaste, o difiere la validación con su disparador, y sigo con el checklist completo: walkthrough, changelog, roadmap, revisión de código (la task se ejecutó en línea) y merge.
coste: 0.1946696
