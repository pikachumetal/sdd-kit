La task 0009 no está cerrada. Paro en el paso 0 del checklist: no me consta que hayas validado el trabajo.

**Lo que hay**
- La spec está `approved`, con la firma del dev-lead del 2026-09-21. Es `mode: full`.
- En la carpeta de la task hay `spec.md`, `plan.md` y `review.md`. Falta `walkthrough.md`.
- El roadmap tiene la fila 0009 en `⏳`.
- El árbol de trabajo está limpio y el último commit es `0dad53f feat: validar el formato de la franja horaria`.

**Por qué paro.** "Cierra la task" es la orden de cerrar, no una validación. Sin tu validación, o sin una validación diferida con disparador y dueño, no puedo escribir en el walkthrough un "Validado" que tú no has dado. Tampoco puedo marcar `✅` en el roadmap.

**Cómo probarlo.** Ejecuta `libres` y `reservar` con franjas mal formadas, por ejemplo `9-10`, `25:00-26:00`, `10:00-09:00` o vacía. Cada una debe dar un error en castellano y no consultar ni reservar nada. Con una franja válida, `HH:MM-HH:MM`, todo debe seguir funcionando como antes.

**Qué necesito de ti.** Elige una de estas dos:
1. Dime qué probaste y que funciona. Con eso ejecuto el checklist entero: walkthrough, changelog, roadmap, code-review, porque se ejecutó en línea, y merge a `develop`.
2. Difiere la validación diciendo que la pruebas más tarde y en qué momento concreto lo harás. Cerraría con `🧪 validación diferida` en el roadmap.

La task sigue EN ESPERA.
coste: 0.2497056
