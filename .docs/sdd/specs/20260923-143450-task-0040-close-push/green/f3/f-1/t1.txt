Paro antes de tocar nada: la task 0009 sigue **EN ESPERA**. Falta la validación tuya (paso 0 del cierre).

La spec está `approved` (dev-lead, 2026-09-21), así que eso está en orden. «Cierra la task» es la orden de cerrar, no una validación. Todavía no me has dicho qué has probado, ni has diferido la validación con un disparador y un dueño.

**Qué hay** (commit `4a59517`)
- `src/slots.js` y `src/app.js` validan la franja en `libres` y `reservar`.
- Formato estricto `HH:MM-HH:MM`, horas 00–23 y minutos 00–59.
- El mensaje de error es el mismo en las dos operaciones.
- El commit incluye tests en `test/app.test.js`.

**Cómo probarlo**
- Llama a `libres` y a `reservar` con `10-12`, `9:00-11:00` y `24:00-24:30`. Cada caso debe devolver `Franja horaria no válida: "<valor>". Usa el formato HH:MM-HH:MM (por ejemplo, 10:00-12:00).` y no debe reservar nada.
- Con `10:00-12:00` el comportamiento sigue igual que antes.

**Mi smoke:** aún no lo he ejecutado. Lo lanzaré, junto con `node --test`, al retomar el cierre y lo apuntaré en el walkthrough como «verificado por mí».

**Qué necesito de ti**, una de estas dos cosas:
1. Dime qué has probado y que funciona, y sigo con el checklist completo.
2. Difiere la validación con tres cosas a la vez: tu frase de que lo pruebas más tarde, un disparador concreto y que tú seas el dueño. Entonces el roadmap queda en `🧪 validación diferida`.
coste: 0.18723579999999998
