
>>> Skill: sdd-kit:sdd-init-brownfield
<<< Launching skill: sdd-kit:sdd-init-brownfield

>>> Bash: cd "<run>/salas" && ls -a . .docs/sdd 2>&1 | head -50; cat .docs/sdd/sdd-kit.json 2>&1

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0040\98d9bc4d-1ac4-498b-b33e-2e1c3272dd77\scratchpad\kit3\skills\sdd-init-brownfield\references\migrations\README.md
<<< 1	# Migraciones del kit — procedimiento
2	
3	Un proyecto ya inicializado (`.docs/sdd/` existe) no se re-inicializa: se migra. Cada fichero `vX.Y.Z.md` de esta carpeta recoge los cambios estructurales que esa versión del kit pide al proyecto, como pasos-predicado que se pueden verificar.
4	
5	1. **Desde dónde**: lee `.docs/sdd/sdd-kit.json`. Si no existe, el proyecto es anterior a v0.2.0: se aplican todas.
6	2. **Hasta dónde**: la mayor versión que tenga fichero en esta carpeta. **Nunca** `plugin.json`, el changelog ni el marcador del propio kit: el canal `npx skills add` no los instala, y dura
<<< .:
./
../
.claude/
.docs/
.git/
.gitignore
src/

.docs/sdd:
./
../
constitution.md
mission.md
roadmap.md
sdd-kit.json
tech-stack.md
{"version": "1.1.0", "channel": "plugin", "updated": "2026-09-10", "ids": {"mode": "sequence"}, "control": {"profile": "delegate", "maxParallelAgents": 3, "silence": {"betweenStepsMinutes": 8, "longCommandMinutes": 20}}, "merge": {"into": "develop", "noFf": true, "removeWorktree": false}}

>>> Bash: cd "<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0040/98d9bc4d-1ac4-498b-b33e-2e1c3272dd77/scratchpad/kit3/skills/sdd-init-brownfield/references/migrations" && ls; cd ../../../.. && ls; cat plugin.json .claude-plugin/plugin.json 2>/dev/null | head -20
<<< README.md
v0.2.0.md
v0.4.0.md
v1.0.0.md
v1.1.0.md
v1.2.0.md
skills/
{
  "name": "sdd-kit",
  "description": "Kit SDD del equipo: skills de proceso agnósticas (init, task, patch, release, consult, changelog) y plantillas para el flujo spec → plan → implementación → walkthrough con Claude Code.",
  "version": "1.2.0",
  "license": "MIT",
  "dependencies": [
    {
      "name": "superpowers",
      "marketplace": "claude-plugins-official"
    }
  ],
  "author": {
    "name": "Medysif"
  }
}

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0040\98d9bc4d-1ac4-498b-b33e-2e1c3272dd77\scratchpad\kit3\skills\sdd-init-brownfield\references\migrations\v1.2.0.md
<<< 1	# Migración a v1.2.0 — modo de ids, claves de control, configuración y memoria
2	
3	Aplica a cualquier proyecto cuyo `.docs/sdd/sdd-kit.json` le falte el campo `ids`, `control.profile`, un
4	bloque `merge` completo, `merge.push` con el bloque `merge` completo, o las claves de frenos (`control.maxParallelAgents`, `control.silence.*`), o cuyo
5	`.claude/settings.json` no desactive la memoria automática o cuyo `.gitignore` no ignore los temporales de las
6	herramientas. Un proyecto inicializado ya con el kit v1.2.0 o posterior recibe todo esto en su propia init
7	y aquí no encontrará nada que h

>>> Bash: cd "<run>/salas" && ls -a .claude; cat .claude/settings.json 2>&1; echo ---; cat .gitignore; echo ---; ls ~/.claude/projects/ | grep -i salas; git branch -a

>>> Grep: Pregunta 3|push
<<< ./
../
settings.json
{
  "autoMemoryEnabled": false
}
---
node_modules/
.playwright-mcp/
.superpowers/
---
<project-slug>/
<project-slug>/
<project-slug>/
<project-slug>
<<< 9:- **`unattended`** — no para en ningún punto hasta terminar la release, salvo el merge a `main`, el tag y las acciones hacia fuera (push, PR, publicar), que siempre decide una persona; el push de la rama de integración tras el merge del cierre sigue `merge.push`.
37:| Push de la rama de integración tras el merge del cierre | presenta el push con el merge y espera | con `merge.push: true`, lo hace; sin él, no | igual que `delegate` |
38:| Merge a main, tag, cualquier otro push, PR, publicar | persona | persona | persona |
137:| `merge.push` | booleano | `false` |
139:`merge` no tiene default:

>>> Bash: cd ~/.claude/projects/<project-slug>/ && ls -a; ls memory 2>&1
<<< Exit code 2
./
../
5f5e20ed-7cc9-4156-b52e-05075fef6fe1.jsonl
ls: cannot access 'memory': No such file or directory

=== RESULTADO (10 turnos, 0.2514672 $)
Migración de v1.1.0 a v1.2.0. Ya estaba hecho todo salvo una cosa:

- **Ya cumplido, se salta:** `ids.mode` (`sequence`), `control.profile`, los frenos, `merge.into/noFf/removeWorktree`, `autoMemoryEnabled: false` y las líneas de `.gitignore`.
- **Memoria automática:** la carpeta no existe, así que no hay nada que volcar.
- **Pendiente:** `merge.push`, que falta en el bloque `merge`.

Pregunta (una): tras fusionar en `develop`, ¿hago push de esa rama a su remoto sin preguntar?

Recomiendo **sí**. Tu convención es `main` estable y `develop` de integración. `develop` es compartida, y un merge sin push no lo ve nadie más. La rama estable, los tags y cualquier otro push siguen siendo tuyos.

Responde «sí», «no» o «no sé». Con «no sé» no escribo la clave y el cierre no hace push. Con tu respuesta escribo el marcador `sdd-kit.json` en 1.2.0 y hago el commit `chore(sdd): migrar al kit v1.2.0`.
