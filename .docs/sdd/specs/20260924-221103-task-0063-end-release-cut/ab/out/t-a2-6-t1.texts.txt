--- [3]
Empiezo por el estado del proyecto: sdd-kit.json, changelog, roadmap y specs.

--- [17]
Preparados los pasos 1-4. El paso 5 (bump, merge y tag) queda **pendiente de tu confirmación**, así que no he hecho nada de eso todavía.

**Hecho (sin commit, en `develop`)**
- **Changelog:** `[Unreleased]` sellado como `[1.3.0] - 2026-09-25`, con una `[Unreleased]` vacía arriba.
- **Release notes y email:** hay destinatario (`hasRecipient: true`), así que están en `.docs/sdd/releases/v1.3.0/release-notes.md` y `email-entrega.md`. Están destiladas del changelog, sin IDs ni jerga. El envío es tuyo.
- **Roadmap:** añadida la entrada `v1.3.0` en "Releases cerradas". No había pendientes vivos ni tasks con validación diferida que rescatar. No hay retro; no la has pedido.
- **Ids de ticket en el corte:** RSV-101, RSV-104 y RSV-109.

**Necesito de ti**
1. **Versión.** Propongo `1.3.0`: el proyecto ya es post-1.0 (viene de 1.2.0) y hay features nuevas sin breaking changes. Tu orden («cierra la release») no nombra versión, así que hace falta tu confirmación. Si prefieres otra, rehago changelog, roadmap y notas.
2. **Merge y tag.** Como hay destinatario, no aplica el atajo. Tras tu OK haría lo siguiente:
   - `node scripts/set-version.mjs 1.3.0`.
   - Commit del cierre.
   - Merge de `develop` a `main`.
   - Tag anotado `v1.3.0` sobre el merge commit.
   - Push del tag y merge de vuelta a `develop`.

**Puntos a revisar antes de publicar a producción**
- **`npm test` ejecuta 0 tests (0 pass, 0 fail).** El «suite verde» de los walkthroughs no demuestra nada, y el patch RSV-109 dice tener test de regresión que no encuentro. `src/app.js` solo hace `console.log('salas')`.
- **Smoke de release:** no se ha ejecutado sobre la rama integrada. He dejado `smoke: pendiente` en el roadmap en vez de inventar un número.
- **Sin dev-lead:** las tasks constan como validadas por él en sus walkthroughs y no hay validaciones diferidas, así que el gate de entrada no lo bloquea. Aun así, con la suite vacía, valdría la pena un smoke manual antes de etiquetar.
- **`v1.3.0` sería un tag de producción sin remoto configurado:** `git remote -v` no devuelve nada, así que el push fallará hasta que haya uno.

¿Confirmas `1.3.0` y que ejecute merge y tag, o prefieres primero el smoke?

