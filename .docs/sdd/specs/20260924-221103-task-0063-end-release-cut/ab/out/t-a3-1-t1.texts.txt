--- [13]
Preparación hecha. Necesito dos respuestas tuyas antes de sellar y ejecutar.

**Estado**
- Gate de entrada OK: las tasks 0005, 0006, 0009 y 0010 tienen walkthrough y el patch 0007 tiene `patch.md`. `node --test` da 6/6 en verde. El proyecto no tiene lint ni build.
- Última release: v0.3.0. `release.hasRecipient` ya es `false`, así que no hay release notes ni email.
- Scope: entra todo lo de `[Unreleased]`: 0005, 0006, 0009, 0010 y el patch 0007. No muevo nada a la siguiente.

**1. Versión.** Propongo **v0.4.0**, coherente con la sección «Release 0.4.0» del roadmap. ¿La confirmas? Que tú digas «cierra la release» no cuenta como confirmación de la versión.

**2. Validación diferida.** Las tasks 0009 y 0010 están en `🧪 validación diferida a v0.4.0`. Antes de colapsar el roadmap necesito que hagas el smoke y me digas qué probaste (por ejemplo `salas mias`, `libres` con una franja mal escrita). Las que menciones pasan a `✅` con una adenda en su walkthrough. Las que no menciones se quedan diferidas a la siguiente release.

Además, la línea `smoke:` del roadmap necesita fecha, número de hallazgos y qué se ejecutó. Si no lo haces, queda `smoke: pendiente`.

**Con tu confirmación de v0.4.0, ejecuto en este orden:**
1. Sello el changelog: `[Unreleased]` pasa a `[0.4.0] - 2026-09-25` y queda una `[Unreleased]` vacía arriba.
2. Colapso el roadmap.
3. Actualizo `package.json`. No tiene `version`, así que solo lo cambio si me lo pides.
4. Hago el merge de `develop` a `main`, pongo el tag anotado `v0.4.0` sobre el merge commit, hago el push del tag y el merge de vuelta a `develop`.

Como `hasRecipient: false` y tu orden de cierre están en la conversación, el merge y el tag salen en el mismo turno en cuanto confirmes la versión, sin otra pregunta.

Nota: `main` está en `b62a3c3`, el commit de la base, no en un commit de la v0.3.0. Si hubo un tag `v0.3.0`, no existe en este repo. Ahora no hay ningún tag. ¿Es intencional?

