--- [12]
Release `v0.4.0` cerrada, con el merge a `main` y el tag hechos. No hay remoto configurado, así que no hay push del tag.

Ejecuté el merge y el tag sin pedirte otra confirmación, con el atajo del paso 7. Cito tu orden «Cierra la release» y tu respuesta «Sí, v0.4.0». `release.hasRecipient` ya era `false` en `sdd-kit.json` y apliqué ese valor, aunque no sé quién lo escribió.

**Qué hice:**
- **Changelog:** sellado como `[0.4.0] - 2026-09-25`, con una `[Unreleased]` vacía arriba.
- **Roadmap:** la sección 0.4.0 está colapsada a resumen y enlace al changelog.
- **Release notes:** no hay, porque no hay destinatario.
- **Commit y merge:** el commit `c3b4961` está en `develop`. El merge `--no-ff` a `main` dio `3f21ca1`, y luego hice el merge de vuelta a `develop`.
- **Tag:** `v0.4.0` está anotado sobre `3f21ca1`.
- **Versión:** no hubo bump, porque `package.json` no lleva versión.
- **Ids de ticket:** no aplica, el modo es `sequence`.

**Estado de las tasks:**
- **0009:** validada solo en su parte de `libres`. La adenda de su `walkthrough.md` dice que `reservar` con franja mal escrita no se probó a mano y solo lo cubre el test automático.
- **0010:** no la mencionaste en el smoke, así que sigue en `🧪 validación diferida a v0.5.0` y está en `Próximo`. Ningún test cubre `salas mias`.
- **0008 restaurada:** el commit `0891412` había borrado «Avisos por correo antes de la reserva» de `Próximo`. Lo restauré como `⏳`, aunque no me dijiste si el borrado fue intencionado.

**Línea de smoke:** en el roadmap escribí `0 hallazgos`. Es una inferencia, porque no me dijiste si encontraste algo. Corrígela si hubo alguno.

Los dos cambios de detalle que quedan son la línea de smoke y el estado del 0008, si el borrado fue intencionado.

