--- [5]
v0.4.0 está preparada y commiteada en `develop` (`fdf9455`). El merge a `main` y el tag siguen pendientes.

**Hecho**
- **Changelog:** `[Unreleased]` sellado como `[0.4.0] - 2026-09-25`, con una `[Unreleased]` vacía arriba.
- **Task 0009:** pasa a `✅`, con una adenda fechada en su `walkthrough.md`.
- **Task 0010:** no la mencionaste en el smoke, así que queda como `🧪 validación diferida a v0.5.0` en `Próximo`. Sigue en el changelog de 0.4.0 porque el código sale en esta release. La v0.5.0 la elegí yo como "siguiente release"; dime si prefieres otro disparador.
- **Roadmap:** la sección 0.4.0 está colapsada a resumen con enlace al changelog. La línea de smoke dice `smoke: 2026-09-25 · 0 hallazgos (libres con franja, 0009; 0 corregidos)`. El "0 hallazgos" lo deduje de que validaste sin reportar nada; corrígelo si hubo alguno.
- **Omitido:** release notes y comunicación, porque `hasRecipient: false`.
- **Sin bump:** el proyecto no tiene campo de versión que actualizar. El `version` de `sdd-kit.json` es el del kit, así que no lo toqué.

**Pendiente: merge y tag.** No apliqué el atajo porque la 0010 pasó a la siguiente release y eso cuenta como mover scope. Con tu confirmación ejecuto:
1. Merge de `develop` a `main`.
2. Tag anotado `v0.4.0` sobre el merge commit, y push del tag.
3. Merge de `main` de vuelta a `develop`.

¿Confirmas?

