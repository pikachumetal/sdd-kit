--- [3]
Empiezo por el estado del proyecto.

--- [10]
Gate de entrada OK: las dos tasks y el patch tienen `walkthrough.md`/`patch.md`, el changelog está al día y `node --test` pasa (3/3). Aún no he tocado nada, porque falta confirmar la versión.

**Propuesta de cierre**
- **Versión: `v0.4.0`.** Añade dos features (reserva semanal y salas libres) y un fix, sobre `v0.3.0`.
- **Entra:** task 0005, task 0006 y patch 0007. No se mueve nada a la siguiente release. El ítem #1 del roadmap (avisos por correo) sigue en «Próximo».
- **Destinatario:** `release.hasRecipient` ya es `false` en `sdd-kit.json`, así que no hay release notes ni comunicación. No hay demo ni acta de feedback y no existe `estimation-log.md`, así que tampoco hay retro.

**Lo que haré cuando confirmes**
1. Sellar el changelog como `[0.4.0] - 2026-09-25`, con una `[Unreleased]` vacía arriba.
2. Añadir `v0.4.0` a «Releases cerradas» del roadmap, con la línea `smoke: pendiente` salvo que quieras que ejecute un smoke real.
3. Subir `package.json` a `0.4.0` y hacer el commit en `develop`.
4. Hacer merge `develop` → `main`, tag anotado `v0.4.0` sobre el merge commit y push del tag. Después haré merge de `main` de vuelta a `develop`, como marca el git-flow de la constitution.

Dices «publicar a producción». Si eso significa que esto es la puesta en producción, `v1.0.0` está reservado a ese hito y sería un salto desde `v0.3.0`. Si solo es otro corte, la versión es `v0.4.0`. No he elegido yo. ¿Cuál quieres? Con `v0.4.0` me basta un «sí, v0.4.0». Con cualquier otra versión, dime cuál.

