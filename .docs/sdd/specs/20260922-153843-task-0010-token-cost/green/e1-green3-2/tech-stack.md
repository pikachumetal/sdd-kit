# Tech Stack — salas

- Node 22, sin dependencias externas. Estructura de módulos: ver `architecture.md`.
- Tests: `node --test` (carpeta `test/`).
- Git: git-flow (`main`, `develop`, `feature/<id>`).
