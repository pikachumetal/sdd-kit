# Método de estimación — salas

El `plan.md` de cada tarea estima horas, el `walkthrough.md` registra el tiempo real y `estimation-log.md` agrega estimado contra real para calcular el factor de calibración. El log se regenera con `Build-EstimationLog.ps1` del kit.
