--- [3]
Ahora: leo el contexto SDD y la spec aprobada. Queda: escribir el plan y parar antes de la Task 1, ~10 min.

--- [20]
Plan escrito en `.docs/sdd/specs/20260923-100000-task-0012-franja/plan.md`, sin commitear todavía. Paro antes de la Task 1.

**Decisiones que necesito que valides:**

1. **`freeRooms` no existe en el código.** El README anuncia `salas libres`, pero `src/slots.js` solo exporta `reserve`. Sin esa función, el escenario «consultar libres» no tiene dónde aplicarse. El plan crea `freeRooms(slot)`, que valida la franja y devuelve `[]`, porque no hay almacén de reservas. La alternativa es quitar ese escenario de la spec. Eso cambia la spec aprobada y necesita tu aprobación.
2. **Regex `^\d{2}-\d{2}$`.** La spec no fija rango. `9-11` se rechaza y `25-30` o `12-10` se aceptan. Validar rango u orden queda fuera de alcance.
3. **Un validador compartido**, `assertValidSlot`, llamado desde `reserve` y `freeRooms`. Así el mensaje vive en un solo sitio.
4. **Una sola task, sin `tasks.md`.** Toca `src/slots.js` y `tests/slots.test.js`, y se verifica con `node --test`, como pide el Art. II.
5. **Coste:** ~0,3 h en Native, sin subagentes.

Recomiendo **Native**: es una task de un fichero, sin interfaces entre tasks que un revisor independiente proteja.

¿Apruebas el plan, y con qué método?
- Apruebo, con Native (Recomendada)
- Apruebo, con subagent-driven
- Cambios (por ejemplo, quitar el escenario de `freeRooms` de la spec)

