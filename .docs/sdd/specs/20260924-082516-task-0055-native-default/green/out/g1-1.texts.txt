--- [3]
Ahora: leer la spec aprobada y el contexto SDD para escribir el plan. Queda: plan.md y parada antes de la Task 1, ~10 min.

--- [25]
Ahora: junto spec y plan en el commit de apertura. Queda: parar antes de la Task 1.

--- [27]
Plan escrito en `.docs/sdd/specs/20260923-100000-task-0012-franja/plan.md`. Paro antes de la Task 1: no he tocado código ni tests.

- **Commit de apertura:** spec y plan van juntos en `20577d3`. La rama tiene un solo commit por encima de `develop`, sin remoto ni merges.
- **Sin gate de plan:** el perfil es `delegate`. Comprobé que cada escenario de la spec tiene su task.
- **Ejecución:** Native, con una sola task. Toca `src/slots.js` y `tests/slots.test.js`, unas 15 líneas, y la estimación es de 0,5 h.
- **Base sin cambios:** la fila de la 0012 en el roadmap no cambió en `develop`, que sigue en `d78ea1c`.

**Pregunta para el dev-lead (decisión 3 del plan).** El escenario «consultar libres» no tiene código al que engancharse: `src/slots.js` solo exporta `reserve`, y no hay CLI ni almacén de reservas. ¿Cómo lo cubrimos?

- **A (recomendada): añadir `freeRooms(slot)`.** Valida la franja y devuelve `[]`, con un marcador `ponytail:` que dice que hará falta un almacén para listar salas reales. Es una función pública nueva que la spec no nombra, pero queda dentro de `src/slots.js`, el único fichero que declara el roadmap.
- **B: dejar «libres» fuera de la 0012.** Solo se valida `reserve`. El segundo escenario pasa a otra task cuando exista la consulta, y eso cambia la spec aprobada: sería una entrada en `## Enmiendas`.

El plan está escrito con la A. Si eliges la B, lo reajusto antes de empezar.

Otras decisiones del plan:

- **Formato:** exactamente dos dígitos, guion, dos dígitos (`/^\d{2}-\d{2}$/`). `9-11` se rechaza, y `25-99` y `12-10` se aceptan porque la spec no pide rango ni orden. Si esto no es lo que quieres, dímelo.
- **No-strings:** cualquier valor que no sea un string, como `undefined` o `['10-12']`, se rechaza con el mismo mensaje.
- **Coordinación con la 0013:** también toca `src/slots.js`. Cuando se ejecute, debe reutilizar `assertValidSlot` y no duplicarlo.

Cuando respondas, empiezo la Task 1: primero escribo los tests en RED y luego la implementación.

