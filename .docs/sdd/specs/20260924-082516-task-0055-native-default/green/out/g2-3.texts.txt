--- [3]
Ahora: cargo el contexto SDD y leo la spec aprobada para escribir el plan. Queda: plan.md y parar antes de la Task 1, ~10 min.

--- [24]
Ahora: plan escrito y en espera de aprobación. Queda: tu respuesta. La Task 1 no está empezada.

Plan en `.docs/sdd/specs/20260923-100000-task-0012-franja/plan.md`. No he commiteado nada ni tocado código.

**Hueco entre la spec y el código.** El escenario 2 («valida al consultar libres») no tiene dónde aplicarse. `src/slots.js` solo exporta `reserve`. `salas libres` aparece en el README, pero no está implementado. Por eso el plan cubre solo el escenario 1: una task que añade `validateSlot` y la llama desde `reserve`, con 4 tests nuevos en RED. Cubrir el 2 exigiría crear la consulta de libres entera, y eso agranda el Scope (desvío, decides tú).

Otras decisiones del plan (el detalle está en su primer bloque):
- **Formato:** «casa con `HH-HH`» significa `/^\d{2}-\d{2}$/`. No se validan rangos ni inicio < fin. `9-11` falla.
- **Cómo falla:** `throw new Error('Franja no válida: usa HH-HH, p. ej. 10-12')`, con el texto literal de la spec.
- **Ejecución:** native. Es una task, un fichero, ~0,3 h y sin coste de subagentes.

**Pregunta 1 (hueco del escenario 2).** ¿Qué hacemos?
- **A (recomendada):** enmienda de la spec que mueve el escenario 2 a la task que cree `libres`, con su propia fila en el roadmap. `validateSlot` queda exportada para reutilizarla.
- **B:** enmienda que amplía el Scope: esta task crea también una `freeRooms(slot)` mínima, y hay que decir qué devuelve.
- **C:** otra cosa que indiques.

**Pregunta 2 (gate del plan).** ¿Apruebas el plan, con ejecución native (recomendada), con subagent, o quieres cambios?

