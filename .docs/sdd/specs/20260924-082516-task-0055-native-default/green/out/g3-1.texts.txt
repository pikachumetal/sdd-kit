--- [3]
Ahora: leo la spec aprobada y el contexto SDD para escribir el plan. Queda: plan.md y la apertura hasta justo antes de la Task 1, ~10 min.

--- [9]
Uso `writing-plans` para crear el plan. Leo constitution, roadmap, config, plantilla y el código que toca.

--- [31]
Plan escrito y commiteado (`1a0311c`). Paro antes de la Task 1, sin escribir tests ni despachar nada.

Necesito una decisión tuya sobre el escenario «consultar libres».

**Qué pasa:** el escenario presupone una función de salas libres, y no existe. `src/slots.js` solo exporta `reserve`, no hay CLI ni almacén de reservas, y `salas libres` solo aparece en el `README.md`. Arreglar el escenario cambia la spec o añade un nombre público, así que no lo decido yo.

**Opciones:**
- **A (recomendada):** la Task 2 crea `freeRooms(slot)`, que valida con el mismo `assertSlot` y devuelve `[]`. Sin almacén no hay salas que listar; queda un `ponytail:` con la vía de mejora. La spec no cambia, pero se añade el nombre público `freeRooms`.
- **B:** el escenario sale de la spec mediante `## Enmiendas` y la 0012 se queda solo con `reserve` (Task 1). El de libres iría a la task que cree esa función.

Dime A o B y sigo con el resto.

**Lo que hay en el plan:**
- **Task 1:** `assertSlot` con `/^\d{2}-\d{2}$/` (mensaje literal de la spec) y `reserve` que lo llama. Sus 5 tests de fallo salen de los THEN de la spec y de los casos de Review Focus: `1012`, `9-11`, espacios, sin franja y numérica.
- **Task 2:** `freeRooms`, solo si eliges A.
- **Formato, no rango:** `9-11` se rechaza, pero `25-99` y `12-10` pasan, porque la spec solo habla de formato. Corrígeme si querías validar también horas.
- **Ejecución:** `subagent`, fijado en `sdd-kit.json`, con las tasks en serie porque tocan el mismo fichero.

**Confirmación de modelos (tu CLAUDE.md la pide antes de despachar):**
- **Implementadores:** Haiku, con el código ya escrito en el plan.
- **Revisores de task:** Sonnet con effort low.
- **Nada de `fable` ni `opus xhigh`.**
- **Coste:** ~1 h y unas decenas de miles de tokens.

**Comprobado antes del despacho:** `develop` no ha avanzado desde la base, así que ni la fila del roadmap ni `src/slots.js` han cambiado allí. Lo repito justo antes de despachar.

**Pendiente:** juntar la apertura (spec + plan + `tasks.md`) en un commit al despachar, cuando responda A o B.

