--- [3]
Ahora: escribir el plan de la task 0012 a partir de la spec aprobada. Queda: plan escrito y parar justo antes de la Task 1, ~10 min.

--- [8]
Uso `superpowers:writing-plans` para el plan. Leo la plantilla del plan, el contexto SDD y la referencia de perfiles.

--- [18]
Plan escrito en `.docs/sdd/specs/20260923-100000-task-0012-franja/plan.md`. Paro aquí: el perfil es `pair`, así que el plan necesita tu aprobación, y no he empezado la Task 1. El plan no está commiteado; el commit de apertura lo hago tras tu aprobación.

**Hueco entre la spec y el código:** el escenario 2 de la spec, validar la franja al consultar libres, no tiene dónde aplicarse. `src/slots.js` solo exporta `reserve`. No hay `libres`, ni CLI, ni lista de salas, ni almacén de reservas. El roadmap declara solo ese fichero.

- **Task 1 (ejecutable):** `validateSlot(slot)` con `/^\d{2}-\d{2}$/` y el mensaje literal de la spec. `reserve` la llama antes de devolver la reserva. Los tests cubren el THEN del escenario 1 y cinco casos de borde: `undefined`, vacío, espacio o salto de línea final, `9-11` y `10-12-14`, y guion largo o dígitos no ASCII.
- **Task 2 (bloqueada):** el escenario 2. No he inventado una función `libres` con un comportamiento que nadie ha definido.
- **Formato:** `12-10` y `99-99` pasan. La spec valida el formato, no que sea una hora real ni que el inicio preceda al fin.
- **Ejecución:** native, porque es un fichero y unas 10 líneas. Coste estimado unos 30 min, sin subagentes.

Necesito dos respuestas:

1. **Escenario 2. ¿Qué hacemos con él?**
   - **A (recomendada):** sale de la 0012 y pasa a la task que cree `libres`, que reutilizará `validateSlot` con el mismo mensaje. Es una enmienda a la spec aprobada.
   - **B:** la 0012 crea una `listFree(slot)` mínima que solo valida. No hay salas ni reservas que listar, así que sería un esqueleto sin resultado útil y ampliaría el Scope.
2. **¿Apruebas el plan y la ejecución native?** Cuando respondas a la 1, aplico la enmienda si es la A y arranco la Task 1.

