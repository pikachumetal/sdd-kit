--- [3]
Ahora: leer spec aprobada y contexto SDD para escribir el plan. Queda: plan.md y commit de apertura, ~10 min.

--- [15]
Ahora: plan escrito y en espera de tu aprobación (perfil `pair`). Queda: tu respuesta y luego ejecutar la Task 1 (~30 min, sin subagentes, coste ≈ 0).

Plan en `.docs/sdd/specs/20260923-100000-task-0012-franja/plan.md`. No he empezado la Task 1, y el plan no está commiteado: lo junto con la apertura cuando lo apruebes.

**Decisiones mías, para que las valides**
- **Una sola task**, ejecución `native` (fijada en `sdd-kit.json`), sin subagentes.
- **Formato válido:** `^\d{2}-\d{2}$`. No valido el rango 00–23 ni inicio < fin, porque la spec no lo pide. `9-11` se rechaza.
- **«Falla» es lanzar un `Error`** con el mensaje literal de la spec, ya que no hay CLI en el código.
- **`assertValidSlot(slot)`** se exporta desde `src/slots.js` y `reserve` la llama primero.
- **Tests:** 5 franjas inválidas (`1012`, `9-11`, `10-12-14`, `ab-cd`, vacía) más el test existente. El gate final es `node --test`.

**Hueco en la spec.** `src/slots.js` solo tiene `reserve`. No existe ninguna función de «salas libres», así que el escenario 2 no tiene dónde aplicarse. La Task 1 cubre solo el escenario de reservar y el escenario 2 queda sin task.

¿Cómo resolvemos el escenario 2?
- **(a) Recomendada:** lo cumple la task que cree `libres`, reutilizando `assertValidSlot`, y añado una enmienda a la spec que lo diga.
- **(b)** Añado a esta task una `freeRooms(slot)` mínima que solo valide. Agranda el Scope y hace falta enmienda igualmente.

Cualquiera de las dos toca la spec aprobada, así que necesito tu decisión antes de ejecutar. Con eso, dime también si apruebas el plan.

