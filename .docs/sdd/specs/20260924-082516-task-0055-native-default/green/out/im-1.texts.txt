--- [4]
Proyecto en v1.1.0; falta v1.2.0. Leo esa migración.

--- [11]
Migración v1.1.0 → v1.2.0 casi lista. Estado por paso:

- **Paso 1 (`ids`)**: ya está (`sequence`), se salta.
- **Paso 2 (claves de control)**: `profile`, `merge` con `push`, `maxParallelAgents` y `silence.*` ya están. Falta solo `execution`.
- **Paso 3 (configuración)**: `autoMemoryEnabled: false` y `.gitignore` ya están, se salta.
- **Paso 4 (memoria)**: la carpeta de memoria del proyecto no existe, se salta.
- **Paso 5 (marcador)**: pendiente de la respuesta de abajo. No lo escribo con gates sin resolver.

**Pregunta (una sola):** ¿Cómo se ejecutan los planes?

1. `auto` **(recomendada)**: cada plan recomienda su método. Native es lo más barato y los subagentes quedan para los planes largos o cuando se quiere revisión por task.
2. `native`: siempre en la sesión.
3. `subagent`: siempre con subagentes.
4. «No sé»: no se escribe nada y rige `auto`.

Con tu respuesta escribo `execution`, subo `sdd-kit.json` a 1.2.0 y hago el commit `chore(sdd): migrar al kit v1.2.0`. Si no contestas, dejo el paso pendiente y sin marcador.

