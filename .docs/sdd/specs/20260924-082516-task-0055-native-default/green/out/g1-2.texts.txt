--- [3]
Ahora: cargo el contexto SDD y la spec aprobada para escribir el plan. Queda: plan, comprobación de escenarios y parar antes de la Task 1, ~10 min.

--- [9]
Using the writing-plans skill to create the implementation plan.

--- [25]
Plan escrito en `.docs/sdd/specs/20260923-100000-task-0012-franja/plan.md`. No he empezado la Task 1 ni he commiteado nada. Falta una decisión tuya, abajo.

**Pregunta para el dev-lead:** la spec exige validar la franja «al consultar libres», pero `libres` no existe en el código. `src/slots.js` solo exporta `reserve(room, slot)`. No hay CLI ni almacén de reservas, y `git grep libres` solo devuelve el README. Crearla añadiría un nombre público nuevo y comportamiento que la spec no describe. Eso cuenta como freno de alcance y no lo invento por mi cuenta. ¿Cuál de las dos?

- **A (recomendada):** enmienda a la spec para sacar el escenario «libres» de esta task. Cuando exista `libres`, reutiliza `assertSlot`, que queda exportado.
- **B:** amplías la task con una `libres` mínima, y necesito que me digas de dónde salen las reservas.

**Qué cubre el plan:**
- **Alcance:** solo el escenario «reservar», como una única task Native. Añade `assertSlot` en `src/slots.js`. `reserve` lo llama y lanza «Franja no válida: usa HH-HH, p. ej. 10-12».
- **Tests:** son de la Task 1 y se escriben primero (RED) en `tests/slots.test.js`.
- **Un solo fichero:** no hay `tasks.md`, porque hay una task.
- **Coste:** ~0,5 h, sin subagentes.

**Decisión mía que te toca validar:** el formato es `^\d{2}-\d{2}$`. `1012` y `9-11` fallan. `25-99` y `12-10` pasan, porque la spec no pide validar rango ni orden.

No he hecho el commit de apertura. Lo hago cuando respondas y el plan quede cerrado, antes de arrancar la Task 1. En `delegate` el plan no tiene gate, pero paro aquí por lo pedido y por la pregunta.

