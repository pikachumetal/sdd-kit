--- [28]
Sigo con el cierre: el walkthrough ya está escrito. Ahora actualizo la capacidad, el changelog y el roadmap.

--- [36]
Cierre hecho en la rama, pero **no he fusionado** en `develop`. Tengo una pregunta al final.

**Hecho** (commit de cierre `d9aae90`):
- **`walkthrough.md`:** el tiempo real es 0,6 h frente a 0,5 h estimadas (+20 %). Recoge tu validación con tu frase literal, y lo que verifiqué yo: `node --test` 3/3 y una prueba con `ROOMS` en otro orden.
- **`capabilities/bookings.md`:** fusionado el delta e historial añadido.
- **`changelog.md`:** entrada en `Changed`.
- **`roadmap.md`:** 0020 marcada ✅ y fila nueva en deuda técnica.
- **Omitido:** los pasos de estimation-log, tokens, `tasks.md`, `environments.md` y revisión de skills. El proyecto no tiene `estimation.md`, ni `environments.md`, ni `.claude/skills/`, y la task es `lite`.

**Decisiones que tomé sin ti:**
- **Delta de la spec desfasado.** Tras el patch 0014, la spec decía «lista `Oeste` y `Sur`» y `MODIFIED` habría borrado el comportamiento de mantenimiento. Fusioné el requisito conservando `Oeste (en mantenimiento)` al final y añadiendo el orden alfabético. La spec aprobada no la toqué; corrígelo si no es lo que querías.
- **Sin revisión final independiente.** Revisé yo el diff, que es de 2 líneas de código. Si quieres una revisión de verdad, `/code-review`.

**Deuda anotada:** el test `libres sale en orden alfabético` pasa también sin el `.sort()`, porque `ROOMS` ya trae `Norte` antes que `Sur`. Tu prueba manual tampoco lo distinguía; solo la mía con otro orden lo cubre. Está en la deuda técnica del roadmap.

**Pendiente: el merge.** `sdd-kit.json` no tiene bloque `merge`, así que no sé el destino, `--no-ff` ni el push. Con el bloque ausente el kit presenta el merge y espera.

**Pregunta:** ¿fusiono `feature/0020` en `develop` con `--no-ff` y sin push? Si quieres push, dímelo. Si prefieres que lo haga solo la próxima vez, añade el bloque `merge` a `sdd-kit.json`.

También puedo generar el ticket de mejora del kit con `sdd-feedback`. Sí tiene contenido: el delta `MODIFIED` que se queda viejo cuando un patch entra antes del cierre.

**No terminado.** Falta el merge de `feature/0020` en `develop`. No borres todavía el worktree `<home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0070-openspec-capabilities\4eeabe21-48b5-45f8-80d5-1535d483175d\scratchpad\runs\n-1\salas`.

