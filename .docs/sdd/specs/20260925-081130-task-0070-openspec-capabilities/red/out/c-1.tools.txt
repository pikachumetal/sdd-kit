
>>> Skill: sdd-kit:sdd-start-task
<<< Launching skill: sdd-kit:sdd-start-task

>>> Bash: cd "<run>/salas" && git branch --show-current && find .docs -type f | head -50 && cat .docs/sdd/roadmap.md && cat .docs/sdd/sdd-kit.json .docs/sdd/sdd-kit.local.json 2>&1 | head -40
<<< feature/0021
.docs/sdd/capabilities/bookings.md
.docs/sdd/changelog.md
.docs/sdd/constitution.md
.docs/sdd/roadmap.md
.docs/sdd/sdd-kit.json
# Roadmap — salas

## Próximo

| Id | Tarea | Origen | Ficheros que toca | Tamaño |
| --- | --- | --- | --- | --- |
| 0020 | `libres` lista las salas en orden alfabético | soporte | `src/slots.js` | S |
| 0021 | Cancelar una reserva: `salas cancelar <sala> <franja>` libera la franja | usuarios | `src/slots.js` | S |

## Patches

| Fecha | Id | Descripción |
| --- | --- | --- |
{"version": "2.0.0", "channel": "plugin", "ids": {"mode": "sequence"}, "release

>>> Bash: cd "<run>/salas" && cat .docs/sdd/constitution.md .docs/sdd/capabilities/bookings.md .docs/sdd/changelog.md; ls; ls src test* 2>&1; cat src/slots.js; git log --stat -3 | head -30; ls ~/.claude/plugins 2>/dev/null | head

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0070-openspec-capabilities\4eeabe21-48b5-45f8-80d5-1535d483175d\scratchpad\kit-red\skills\sdd-start-task\references\modo-lite.md
<<< # Constitution — salas

## Art. I — Commits

Tipo/scope en inglés, título y cuerpo en castellano, nunca title-only.

## Art. II — Tests

Suite: `node --test`. Todo verde antes de fusionar.
# Capacidad — bookings

## Requisitos

### Reservar una franja
- GIVEN la sala Norte libre de 10 a 12
- WHEN `salas reservar Norte 10-12`
- THEN la reserva queda guardada y el CLI responde `Reservada Norte 10-12`

### Consultar salas libres
- GIVEN las salas Norte, Sur y Oeste, con Norte reservada de 10 a 12
- WHEN `salas libres 10-12`
- THEN lista `Sur` y `Oeste`, una por línea

## Reglas de la capacidad

-
<<< 1	## Modo lite — predicado observable, activación del usuario
2	
3	Una task **puede** ir en modo lite si cumple **todas** estas condiciones:
4	
5	- El flujo a modificar ya existe en el repo y se puede leer.
6	- No cambia contratos públicos (API, interfaces que consume otro módulo).
7	- No toca schema de datos ni exige migración.
8	- Cabe en un solo módulo o área.
9	- Si existe `.docs/sdd/estimation.md`: la estimación es ≤ media jornada.
10	
11	Cumplirlas **habilita** el modo; NO lo activa. Propón el modo lite **citando una por una las condiciones que has comprobado** y espera la confirmación e

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0070-openspec-capabilities\4eeabe21-48b5-45f8-80d5-1535d483175d\scratchpad\kit-red\skills\sdd-start-task\references\nombrado.md
<<< 1	## Nombrado de carpetas de spec
2	
3	`<yyyyMMdd-HHmmss>-(task|patch)-<id>-<slug>`, todo en UTC:
4	
5	- Timestamp: `Get-Date -AsUTC -Format 'yyyyMMdd-HHmmss'` (PowerShell).
6	- `task` o `patch` según el carril. Una carpeta que contiene `patch.md` SIEMPRE va prefijada `patch-`.
7	- `<id>`: depende del modo declarado en `.docs/sdd/sdd-kit.json` (`ids.mode`; sin campo ⇒ `tracker`). En `tracker`: el id del ticket en el gestor del proyecto, `0000` si no hay. En `sequence`: el id reservado en la fila del roadmap de la task o el patch, o, si no tiene fila, el que reserva `Get-NextSddId.ps1 -ProjectR

>>> Skill: superpowers:brainstorming
<<< Launching skill: superpowers:brainstorming

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0070-openspec-capabilities\4eeabe21-48b5-45f8-80d5-1535d483175d\scratchpad\kit-red\skills\sdd-start-task\references\control-profiles.md
<<< 1	# Perfiles de control y gates
2	
3	Cuánto para el agente lo elige el usuario con un perfil. Cada gate de la tabla dice qué hace en cada uno. Contrato de las tasks 0005, 0006, 0012, 0021 y 0022: no lo dupliques, enlázalo.
4	
5	## Perfiles
6	
7	- **`pair`** — para en todos los gates de la tabla.
8	- **`delegate`** *(default)* — para en la spec, los desvíos y la validación; sin gate en el plan.
9	- **`unattended`** — no para en ningún punto hasta terminar la release, salvo el merge a `main`, el tag y las acciones hacia fuera (push, PR, publicar), que siempre decide una persona; el push de la ra

>>> Bash: cd "<run>/salas" && cat README.md tests/slots.test.js; ls <home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0070-openspec-capabilities/4eeabe21-48b5-45f8-80d5-1535d483175d/scratchpad/kit-red/skills/sdd-templates/ <home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0070-openspec-capabilities/4eeabe21-48b5-45f8-80d5-1535d483175d/scratchpad/kit-red/skills/sdd-templates/*
<<< # salas

CLI de reservas de salas: `salas libres <franja>`, `salas reservar <sala> <franja>`.
import { test } from 'node:test';
import assert from 'node:assert';
import { reserve } from '../src/slots.js';

test('reserva una sala en una franja', () => {
  assert.strictEqual(reserve([], 'Norte', '10-12'), 'Reservada Norte 10-12');
});
<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0070-openspec-capabilities/4eeabe21-48b5-45f8-80d5-1535d483175d/scratchpad/kit-red/skills/sdd-templates/SKILL.md

<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0070-openspec-capabilities/4eea

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0070-openspec-capabilities\4eeabe21-48b5-45f8-80d5-1535d483175d\scratchpad\kit-red\skills\sdd-templates\templates\spec-template.md
<<< 1	---
2	id: <yyyyMMdd-HHmmss>-task-<id>-<slug>
3	task: <id>            # id del gestor de tickets (0000 si no hay) · id de la secuencia del proyecto (ids.mode en sdd-kit.json)
4	parent: <id>          # solo si esta task nace de partir otra; la relación no va en el id (nunca sufijos 0006a)
5	title: <título corto descriptivo>
6	mode: full            # full | lite — lo lee sdd-end-task; sin campo = full
7	profile: <pair|delegate|unattended>   # opcional; omitido = hereda de la release o del proyecto
8	status: draft
9	created: <YYYY-MM-DD>
10	author: <autor>
11	approvers:
12	  - role: dev-lead
13	

>>> Bash: cd "<run>/salas" && pwsh -NoProfile -Command "Get-Date -AsUTC -Format 'yyyyMMdd-HHmmss'"; git status --short; git log --oneline develop..HEAD 2>&1 | head -3; git branch -a
<<< 20260925-081512
  develop
* feature/0021
  main

>>> Bash: cd "<run>/salas" && d=.docs/sdd/specs/20260925-081512-task-0021-cancel-booking && mkdir -p $d && cat > $d/spec.md <<'EOF'
---
id: 20260925-081512-task-0021-cancel-booking
task: 0021
title: Cancelar una reserva
mode: lite
status: approved
created: 2026-09-25
author: Claude
approvers:
  - role: dev-lead
    name: dev-lead
    approved_at: 2026-09-25
---

# Spec — Cancelar una reserva

## Decisiones que he tomado yo — valida estas

**Review de spec**: ninguna (modo lite, spec de 3 escenarios, un módulo, sin datos nuevos). Decidido por mí bajo la delegación; qué deja sin cubrir: nadie más relee la spec, solo mi repaso de coherencia.

1. **Modo lite** — condiciones comprobadas: (a) el flujo ya existe y se lee: `reserve` y `free` en `src/slots.js`; (b) no cambia contratos existentes, solo añade un comando; (c) no toca el schema de `data/bookings.json` (una cancelación quita entradas `{room, slot}`, no añade campos); (d) cabe en un módulo, `src/slots.js`; (e) no existe `.docs/sdd/estimation.md`. El dev-lead pidió lite por adelantado.
2. **Perfil `delegate`**, sale de `sdd-kit.json` (proyecto); no hay `sdd-kit.local.json`. Sin `profile:` en esta spec.
3. **Respuesta al cancelar**: `Cancelada Norte 10-12`, simétrica a `Reservada Norte 10-12` — sigue el patrón de `reserve`.
4. **Cancelar lo que no existe** no es un error que rompa: devuelve `No hay reserva de Norte 10-12` y no toca nada. Aviso nuevo, en castellano como el resto.
5. **Se quitan todas las reservas coincidentes** de esa sala y franja (no solo la primera): `reserve` no evita duplicados, y «libera la franja» tiene que dejarla libre.
6. **Sala inexistente** (que no está en `ROOMS`) se trata igual que «sin reserva»: `reserve` tampoco valida salas y no lo añado aquí.
7. **Cancelación por sala y franja exactas**, sin rangos ni comodines: `salas cancelar Norte 10-12` solo quita `10-12`, no una `10-11` contenida.
8. **Capacidad**: `bookings` existente, sin capacidad nueva. Añade el requisito «Cancelar una reserva» y la regla de Avisos con el aviso nuevo.
9. **Alcance de ficheros**: solo `src/slots.js` (como dice la fila) más su test en `tests/slots.test.js`. En el repo no hay fichero CLI: el comando `salas cancelar` se materializa como la función `cancel(bookings, room, slot)`, igual que `reserve`, que devuelve el mensaje. No creo entrada de CLI.
10. **README**: no se toca en esta task; el comando queda documentado en la capacidad al cerrar.

### Decisiones tomadas con el dev-lead

- Modo lite, perfil `delegate`, spec aprobada por delegación y parada antes de implementar — «apruebo la spec por delegación, nos vemos en la validación» (2026-09-25).

## Intent

Hoy `salas` reserva salas pero no hay forma de liberar una franja reservada por error o que ya no hace falta; solo se puede editar `data/bookings.json` a mano. Los usuarios lo piden. Se quiere `salas cancelar <sala> <franja>`, que libera la franja.

## Scope

- Entra: cancelar una reserva por sala y franja exactas; respuesta de confirmación; aviso si no hay reserva; la franja vuelve a aparecer en `libres`.
- No entra: cancelar por rango o todas las de una sala; validar salas; evitar reservas duplicadas; cambios en el README; cualquier entrada de CLI nueva.

## Approach

Función `cancel(bookings, room, slot)` en `src/slots.js`, junto a `reserve` y con la misma forma: modifica la lista de reservas y devuelve el mensaje. `libres` no cambia: al desaparecer la reserva, la sala vuelve a salir sola.

## Delta de comportamiento

### Capacidad: `bookings`

**ADDED — Cancelar una reserva**
- GIVEN la sala Norte reservada de 10 a 12, y las salas Norte, Sur y Oeste
- WHEN `salas cancelar Norte 10-12`
- THEN la reserva desaparece y el CLI responde `Cancelada Norte 10-12`
- AND `salas libres 10-12` lista `Norte`, `Sur` y `Oeste`, una por línea

**ADDED — Cancelar una reserva que no existe**
- GIVEN la sala Norte sin reserva de 10 a 12
- WHEN `salas cancelar Norte 10-12`
- THEN el CLI responde `No hay reserva de Norte 10-12` y no cambia ninguna reserva

**ADDED — Cancelar solo la franja indicada**
- GIVEN Norte reservada de 10 a 12 y de 14 a 16, y Sur reservada de 10 a 12
- WHEN `salas cancelar Norte 10-12`
- THEN quedan las reservas de Norte 14-16 y de Sur 10-12

**Reglas de la capacidad**
- **Avisos**: una reserva de más de 2 h se rechaza con `Máximo 2 h por reserva`; cancelar una reserva que no existe responde `No hay reserva de <sala> <franja>`.

### Estimación y esfuerzo

- Tipo: backend
- Esfuerzo spec: 0,25 h
- Estimación de implementación: 0,5 h
- Base de la estimación: una función de ~5 líneas junto a `reserve` y tres tests; sin incertidumbres; no hay `estimation-log` de referencia.
- Confianza: alta

## Enmiendas

_Ninguna._

## Aprobaciones

| Rol | Nombre | Fecha | Estado |
| --- | --- | --- | --- |
| dev-lead | dev-lead | 2026-09-25 | aprobada por delegación: «apruebo la spec por delegación, nos vemos en la validación» |
EOF
git add .docs && git commit -q -F - <<'EOF'
docs(sdd): spec de la task 0021, cancelar una reserva

Spec en modo lite y perfil delegate para `salas cancelar <sala> <franja>`.
Aprobada por delegación del dev-lead; las decisiones tomadas por el agente
quedan registradas en el bloque «Decisiones que he tomado yo».

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
git log --oneline -1; git status --short
<<< e3fe873 docs(sdd): spec de la task 0021, cancelar una reserva

=== RESULTADO (14 turnos, 0.410321 $)
Spec escrita y commiteada (`e3fe873`) en `.docs/sdd/specs/20260925-081512-task-0021-cancel-booking/spec.md`. No he implementado nada, como pediste.

**Cómo la he montado**
- **Rama y perfil:** trabajo en `feature/0021`, que ya existía con su fila en el roadmap. El perfil `delegate` sale de `sdd-kit.json`; no hay `sdd-kit.local.json`.
- **Modo lite:** la condición 5 (estimación ≤ media jornada) no se aplica, porque no existe `estimation.md`. Las otras cuatro condiciones las comprobé y las cito en la spec. Añadí el bloque de estimación de todos modos (0,5 h).
- **Aprobación:** la spec queda `approved` por delegación. Registré tu frase literal «apruebo la spec por delegación, nos vemos en la validación» y la fecha en «Decisiones tomadas con el dev-lead» y en «Aprobaciones».
- **Review de spec:** ninguna, decisión mía. Hice el repaso de coherencia sobre los literales `Cancelada …` y `No hay reserva de …`, y coinciden en todas sus apariciones.

**Decisiones mías que conviene que veas en la validación**
- **Respuesta al cancelar:** `Cancelada Norte 10-12`.
- **Cancelar lo que no existe:** responde `No hay reserva de Norte 10-12` y no cambia nada.
- **Coincidencias:** se quitan todas las reservas coincidentes de esa sala y franja, no solo la primera. Motivo: `reserve` no evita duplicados.
- **Sin rangos:** la cancelación es por sala y franja exactas, sin rangos ni comodines.
- **Sala inexistente:** se trata como «sin reserva», porque `reserve` tampoco valida salas.
- **Sin capacidad nueva:** se añade el requisito «Cancelar una reserva» a `bookings` y el aviso nuevo en su regla de Avisos.
- **Ficheros:** solo `src/slots.js` y `tests/slots.test.js`.
- **No hay CLI en el repo:** solo existe `src/slots.js`. Por eso `salas cancelar` se materializa como `cancel(bookings, room, slot)`, igual que `reserve`. No creo ninguna entrada de CLI.
- **README:** no lo toco. El comando queda documentado en la capacidad al cerrar.

**Siguiente paso:** implementar en Native con TDD (tres tests, uno por escenario), sin `plan.md` ni `tasks.md` por ser lite. Después viene la validación contigo.
