--- [20]
Cierre de la task 0020 preparado y commiteado (`4df83b8`), pero **no está fusionada**: falta tu confirmación del merge.

**Hecho**
- Spec `approved`, task `lite` (sin `plan.md` ni `tasks.md`) y validación tuya registrada en el walkthrough con tu frase literal.
- `node --test`: 3 de 3 pasan, ejecutado por mí.
- Creado `walkthrough.md`. Tiempo real: 0,6 h frente a 0,5 h estimadas (+20 %). El proyecto no tiene `estimation.md`, así que no hay estimation-log ni medición de tokens.
- Delta fusionado en `capabilities/bookings.md`, entrada en el changelog y `roadmap.md` con 0020 marcada `✅`.
- El proyecto no tiene `.claude/skills/`, `architecture.md` ni `environments.md`. No aplica nada de ahí.

**Decisiones tomadas sin el dev-lead**
- El delta de la spec era anterior al patch 0014 y su «antes» ya no coincide con la capacidad. Fusionarlo literal habría borrado el comportamiento de mantenimiento. Conservé `Oeste (en mantenimiento)` al final y añadí «salas libres en orden alfabético».
- La tabla «Próximo» del roadmap no tiene columna de estado, así que puse `✅` en la celda «Tarea» de la fila 0020.

**Pendiente**
- El test de orden no protege el requisito. `ROOMS` ya está en orden alfabético (Norte, Sur, Oeste), y con Oeste en mantenimiento solo salen una o dos salas libres, así que quitar `.sort()` no lo rompe. Lo dejé en «Deuda técnica» del roadmap; tu prueba manual también lo pasa igual.
- No hubo revisión final de rama. Al ser un cambio de una línea no la lancé.
- `sdd-kit.json` no tiene bloque `merge`, así que el cierre presenta el merge y espera. No he ejecutado `Invoke-SddMerge.ps1`.
- El changelog no tenía la entrada del patch 0014. No la añadí, es de otro cierre.
- Puedo generar el ticket de mejora del kit con `sdd-feedback` en esta sesión si quieres.

**Pregunta:** ¿fusiono `feature/0020` en `develop` con `Invoke-SddMerge.ps1` (`--no-ff` no está configurado)? ¿Con push o sin él? Añado el ticket del kit y la revisión de rama solo si me lo pides.

**No terminado.** Falta el merge de `feature/0020` en `develop`. No borres todavía el worktree `<home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0070-openspec-capabilities\4eeabe21-48b5-45f8-80d5-1535d483175d\scratchpad\runs\n-2\salas`.

