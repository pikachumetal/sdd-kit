
>>> Skill: sdd-kit:sdd-start-task
<<< Launching skill: sdd-kit:sdd-start-task

>>> Bash: cd "<run>/salas" && git branch --show-current && find .docs -type f | head -50 && for f in constitution mission tech-stack roadmap; do echo "=== $f"; cat .docs/sdd/$f.md; done; echo "=== local"; cat .docs/sdd/sdd-kit.local.json 2>&1; echo "=== json"; cat .docs/sdd/sdd-kit.json 2>&1
<<< feature/0021
.docs/sdd/capabilities/bookings.md
.docs/sdd/changelog.md
.docs/sdd/constitution.md
.docs/sdd/roadmap.md
.docs/sdd/sdd-kit.json
=== constitution
# Constitution — salas

## Art. I — Commits

Tipo/scope en inglés, título y cuerpo en castellano, nunca title-only.

## Art. II — Tests

Suite: `node --test`. Todo verde antes de fusionar.
=== mission
cat: .docs/sdd/mission.md: No such file or directory
=== tech-stack
cat: .docs/sdd/tech-stack.md: No such file or directory
=== roadmap
# Roadmap — salas

## Próximo

| Id | Tarea | Origen | Ficheros que toca | Tamaño |
| --- | --- | --- | -

>>> Bash: cd "<run>/salas" && cat .docs/sdd/capabilities/bookings.md; git ls-files; cat src/slots.js; ls test* 2>/dev/null; cat .docs/sdd/changelog.md; ls "<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0070-openspec-capabilities/4eeabe21-48b5-45f8-80d5-1535d483175d/scratchpad/kit-red/skills/sdd-start-task/references"
<<< # Capacidad — bookings

## Requisitos

### Reservar una franja
- GIVEN la sala Norte libre de 10 a 12
- WHEN `salas reservar Norte 10-12`
- THEN la reserva queda guardada y el CLI responde `Reservada Norte 10-12`

### Consultar salas libres
- GIVEN las salas Norte, Sur y Oeste, con Norte reservada de 10 a 12
- WHEN `salas libres 10-12`
- THEN lista `Sur` y `Oeste`, una por línea

## Reglas de la capacidad

- **Dónde viven los datos**: `data/bookings.json`.
- **Idioma de los nombres**: comandos y mensajes en castellano.
- **Límites**: una reserva dura como máximo 2 h.
- **Avisos**: una reserva 

>>> Bash: cd "<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0070-openspec-capabilities/4eeabe21-48b5-45f8-80d5-1535d483175d/scratchpad/kit-red/skills" && cat sdd-start-task/references/modo-lite.md sdd-start-task/references/nombrado.md sdd-start-task/references/review-spec.md; ls sdd-templates sdd-templates/*; cat "<run>/salas/tests/slots.test.js" "<run>/salas/README.md"

>>> Skill: superpowers:brainstorming
<<< ## Modo lite — predicado observable, activación del usuario

Una task **puede** ir en modo lite si cumple **todas** estas condiciones:

- El flujo a modificar ya existe en el repo y se puede leer.
- No cambia contratos públicos (API, interfaces que consume otro módulo).
- No toca schema de datos ni exige migración.
- Cabe en un solo módulo o área.
- Si existe `.docs/sdd/estimation.md`: la estimación es ≤ media jornada.

Cumplirlas **habilita** el modo; NO lo activa. Propón el modo lite **citando una por una las condiciones que has comprobado** y espera la confirmación explícita del u
<<< Launching skill: superpowers:brainstorming

>>> Bash: cd "<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0070-openspec-capabilities/4eeabe21-48b5-45f8-80d5-1535d483175d/scratchpad/kit-red/skills" && cat sdd-templates/templates/spec-template.md; cat sdd-start-task/references/commit-milestones.md | head -60; grep -n -i "delegate" sdd-start-task/references/control-profiles.md | head -30; cd ../../runs/c-2/salas && git log --oneline -5 && git diff main..HEAD --stat | tail -3; date -u +%Y%m%d-%H%M%S
<<< ---
id: <yyyyMMdd-HHmmss>-task-<id>-<slug>
task: <id>            # id del gestor de tickets (0000 si no hay) · id de la secuencia del proyecto (ids.mode en sdd-kit.json)
parent: <id>          # solo si esta task nace de partir otra; la relación no va en el id (nunca sufijos 0006a)
title: <título corto descriptivo>
mode: full            # full | lite — lo lee sdd-end-task; sin campo = full
profile: <pair|delegate|unattended>   # opcional; omitido = hereda de la release o del proyecto
status: draft
created: <YYYY-MM-DD>
author: <autor>
approvers:
  - role: dev-lead
    name: TBD
   

>>> Bash: cd "<run>/salas" && d=.docs/sdd/specs/20260925-081745-task-0021-cancel-booking && mkdir -p $d && cat > $d/spec.md <<'EOF'
---
id: 20260925-081745-task-0021-cancel-booking
task: 0021
title: Cancelar una reserva
mode: lite
profile: delegate
status: approved
created: 2026-09-25
author: Claude
approvers:
  - role: dev-lead
    name: dev-lead
    approved_at: 2026-09-25
---

# Spec — Cancelar una reserva

## Decisiones que he tomado yo — valida estas

Review de spec: ninguna — modo lite, la rúbrica de `review-spec.md` no aplica. Hice el repaso de coherencia de literales (`Cancelada …` y `No hay reserva de …` aparecen igual en Delta y en Reglas).

1. **Modo lite, decidido por mí por delegación.** Condiciones comprobadas: (a) el flujo existe: `reserve` y `free` en `src/slots.js`; (b) no cambia contratos existentes: `reservar` y `libres` quedan igual, `cancelar` es un comando nuevo aditivo (es la condición más discutible: si lo lees como cambio de contrato público, sube a full); (c) sin schema ni migración: se quita una entrada de `data/bookings.json` con la forma que ya tiene; (d) un solo módulo: `src/slots.js`; (e) no existe `estimation.md`.
2. **Mensaje de éxito `Cancelada Norte 10-12`**, simétrico a `Reservada Norte 10-12` — mismo formato, castellano.
3. **Cancelar lo que no existe no es un error, es un aviso**: `No hay reserva de Norte 10-12`, sin tocar nada. Una sala desconocida cae en el mismo aviso (`reserve` tampoco valida salas; no añado validación nueva).
4. **Cancela solo la franja exacta**: se compara sala y franja iguales, sin solapes. `cancelar Norte 10-11` no toca una reserva `10-12`.
5. **Si hay entradas duplicadas de la misma sala y franja** (`reserve` no lo impide), se quitan todas, porque el objetivo es que la franja quede libre.
6. **Sin capacidad nueva**: el delta es un `ADDED` en `bookings`.
7. **Solo la función de dominio** `cancel(bookings, room, slot)` en `src/slots.js`, con la firma y el estilo mutante de `reserve`. El repo no tiene fichero de CLI ni de persistencia; no los creo (lo que hoy no existe sobre `salas reservar` tampoco).
8. **Fila 0020 del roadmap** (`libres` en orden alfabético) toca el mismo fichero y `free`. Antes de implementar hay que mirar la base: si 0020 se fusiona antes, hay conflicto trivial o ninguno, pero es un freno de alcance del kit.

### Decisiones tomadas con el dev-lead

- Modo lite, perfil `delegate`, spec aprobada por delegación, sin preguntas — «task en modo lite, perfil delegate, apruebo la spec por delegación, nos vemos en la validación» (2026-09-25)

## Intent

Hoy se puede reservar una franja pero no liberarla: un usuario que se equivoca o cambia de plan deja la sala bloqueada. Los usuarios piden `salas cancelar <sala> <franja>`; tras cancelar, la franja vuelve a salir en `salas libres`.

## Scope

- Entra: función `cancel` en `src/slots.js`; un test por escenario en `tests/slots.test.js`; actualizar `README.md` con el comando.
- No entra: cancelar por solape o por sala entera; validar salas; avisos de cancelación tardía; fichero de CLI o persistencia; el orden de `libres` (0020).

## Approach

`cancel` quita de la lista de reservas las entradas con esa sala y esa franja y devuelve el mensaje, igual que `reserve` devuelve el suyo. Si no quitó ninguna, devuelve el aviso.

## Delta de comportamiento

### Capacidad: `bookings`

**ADDED — Cancelar una reserva**
- GIVEN la sala Norte reservada de 10 a 12, y Sur y Oeste libres
- WHEN `salas cancelar Norte 10-12`
- THEN el CLI responde `Cancelada Norte 10-12`
- AND `salas libres 10-12` lista `Norte`, `Sur` y `Oeste`, una por línea

**ADDED — Cancelar una reserva que no existe**
- GIVEN la sala Norte sin reserva de 10 a 12
- WHEN `salas cancelar Norte 10-12`
- THEN el CLI responde `No hay reserva de Norte 10-12` y las reservas no cambian

**ADDED — Cancelar solo la franja indicada**
- GIVEN la sala Norte reservada de 10 a 12 y de 14 a 16
- WHEN `salas cancelar Norte 10-12`
- THEN la reserva de 14 a 16 sigue vigente y `salas libres 14-16` lista `Sur` y `Oeste`

**ADDED — Cancelar una reserva duplicada**
- GIVEN la sala Norte con dos reservas idénticas de 10 a 12
- WHEN `salas cancelar Norte 10-12`
- THEN el CLI responde `Cancelada Norte 10-12` y `salas libres 10-12` lista `Norte`, `Sur` y `Oeste`

**Reglas de la capacidad**
- **Avisos**: una reserva de más de 2 h se rechaza con `Máximo 2 h por reserva`; cancelar una reserva que no existe responde `No hay reserva de <sala> <franja>` y no cambia nada.

### Estimación y esfuerzo

- Tipo: backend
- Esfuerzo spec: 0,25 h
- Estimación de implementación: 0,5 h
- Base de la estimación: una función de tres líneas y cuatro tests sobre un módulo ya leído
- Confianza: alta

## Enmiendas

## Aprobaciones

| Rol | Nombre | Fecha | Estado |
| --- | --- | --- | --- |
| dev-lead | dev-lead | 2026-09-25 | aprobada por delegación: «apruebo la spec por delegación, nos vemos en la validación» |
EOF
git add .docs && git commit -q -m "docs(sdd): spec de la task 0021, cancelar una reserva

Spec en modo lite y perfil delegate, aprobada por delegación del dev-lead.
Añade el requisito de cancelar una reserva a la capacidad bookings, con
sus escenarios y el aviso para una reserva inexistente. Sin implementar.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>" && git log --oneline -2 && git status --short
<<< b4ff463 docs(sdd): spec de la task 0021, cancelar una reserva
2890a99 docs(sdd): filas 0020 y 0021

=== RESULTADO (10 turnos, 0.3989386 $)
La spec de la task 0021 está escrita y commiteada (`b4ff463`) en `.docs/sdd/specs/20260925-081745-task-0021-cancel-booking/spec.md`. No he implementado nada.

Como no me dejaste preguntar, decidí yo el modo, el perfil y la aprobación. Todo consta en el bloque «Decisiones que he tomado yo» de la spec.

**Qué hay en la spec**
- **Modo y aprobación:** modo lite, perfil `delegate`, spec aprobada por delegación con tu frase literal. No apliqué review de spec porque en lite no corresponde; sí hice el repaso de coherencia de literales.
- **Mensajes:** éxito `Cancelada Norte 10-12`; si no hay reserva, aviso `No hay reserva de Norte 10-12` sin tocar nada.
- **Alcance de la cancelación:** solo la franja exacta, sin solapes. Si la reserva está duplicada, se quitan todas para que la franja quede libre.
- **Capacidad:** un `ADDED` en `bookings`, con cuatro escenarios GIVEN/WHEN/THEN y la regla de Avisos actualizada. No crea capacidad nueva.
- **Implementación prevista:** solo `cancel(bookings, room, slot)` en `src/slots.js`, más tests y una línea en el `README.md`. El repo no tiene fichero de CLI ni de persistencia y no los creo.

**Qué mirar tú**
- **Lite es discutible.** La condición «no cambia contratos públicos» es la más dudosa, porque `cancelar` es un comando nuevo. Lo leí como aditivo, ya que `reservar` y `libres` no cambian. Si lo ves como contrato público, la task sube a full y hace falta `plan.md`.
- **Conflicto con 0020.** La fila 0020 del roadmap toca el mismo fichero, `src/slots.js`. Antes de implementar hay que comprobar la base por si 0020 llega antes.

Siguiente paso, cuando me des paso: implementar la task 0021 con tests RED por escenario.
