
>>> Skill: sdd-kit:sdd-start-task
<<< Launching skill: sdd-kit:sdd-start-task

>>> Bash: cd "<run>/salas" && ls -a .docs/sdd .docs/sdd/specs .docs/sdd/capabilities 2>&1; cat .docs/sdd/roadmap.md; cat .docs/sdd/sdd-kit.local.json .docs/sdd/sdd-kit.json 2>&1

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0070-openspec-capabilities\4eeabe21-48b5-45f8-80d5-1535d483175d\scratchpad\kit-green\skills\sdd-start-task\references\modo-lite.md

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0070-openspec-capabilities\4eeabe21-48b5-45f8-80d5-1535d483175d\scratchpad\kit-green\skills\sdd-start-task\references\nombrado.md
<<< 1	## Modo lite — predicado observable, activación del usuario
2	
3	Una task **puede** ir en modo lite si cumple **todas** estas condiciones:
4	
5	- El flujo a modificar ya existe en el repo y se puede leer.
6	- No cambia contratos públicos (API, interfaces que consume otro módulo).
7	- No toca schema de datos ni exige migración.
8	- Cabe en un solo módulo o área.
9	- Si existe `.docs/sdd/estimation.md`: la estimación es ≤ media jornada.
10	
11	Cumplirlas **habilita** el modo; NO lo activa. Propón el modo lite **citando una por una las condiciones que has comprobado** y espera la confirmación e
<<< 1	## Nombrado de carpetas de spec
2	
3	`<yyyyMMdd-HHmmss>-(task|patch)-<id>-<slug>`, todo en UTC:
4	
5	- Timestamp: `Get-Date -AsUTC -Format 'yyyyMMdd-HHmmss'` (PowerShell).
6	- `task` o `patch` según el carril. Una carpeta que contiene `patch.md` SIEMPRE va prefijada `patch-`.
7	- `<id>`: depende del modo declarado en `.docs/sdd/sdd-kit.json` (`ids.mode`; sin campo ⇒ `tracker`). En `tracker`: el id del ticket en el gestor del proyecto, `0000` si no hay. En `sequence`: el id reservado en la fila del roadmap de la task o el patch, o, si no tiene fila, el que reserva `Get-NextSddId.ps1 -ProjectR
<<< Exit code 1
ls: cannot access '.docs/sdd/specs': No such file or directory
.docs/sdd:
./
../
capabilities/
changelog.md
constitution.md
roadmap.md
sdd-kit.json

.docs/sdd/capabilities:
./
../
bookings.md
# Roadmap — salas

## Próximo

| Id | Tarea | Origen | Ficheros que toca | Tamaño |
| --- | --- | --- | --- | --- |
| 0020 | `libres` lista las salas en orden alfabético | soporte | `src/slots.js` | S |
| 0021 | Cancelar una reserva: `salas cancelar <sala> <franja>` libera la franja | usuarios | `src/slots.js` | S |

## Patches

| Fecha | Id | Descripción |
| --- | --- | --- |
cat: .docs/sdd/s

>>> Bash: cd "<run>/salas" && cat .docs/sdd/constitution.md .docs/sdd/capabilities/bookings.md; git branch -a; ls -R src test* 2>&1 | head -30; cat src/slots.js

>>> Skill: superpowers:brainstorming

>>> Skill: sdd-kit:sdd-templates
<<< # Constitution — salas

## Art. I — Commits

Tipo/scope en inglés, título y cuerpo en castellano, nunca title-only.

## Art. II — Tests

Suite: `node --test`. Todo verde antes de fusionar.
# Capacidad — bookings

## Requisitos

### Reservar una franja
- GIVEN la sala Norte libre de 10 a 12
- WHEN `salas reservar Norte 10-12`
- THEN la reserva queda guardada y el CLI responde `Reservada Norte 10-12`

### Consultar salas libres
- GIVEN las salas Norte, Sur y Oeste, con Norte reservada de 10 a 12
- WHEN `salas libres 10-12`
- THEN lista `Sur` y `Oeste`, una por línea

## Reglas de la capacidad

-
<<< Launching skill: superpowers:brainstorming
<<< Launching skill: sdd-kit:sdd-templates

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0070-openspec-capabilities\4eeabe21-48b5-45f8-80d5-1535d483175d\scratchpad\kit-green\skills\sdd-templates\templates\spec-template.md
<<< 1	---
2	id: <yyyyMMdd-HHmmss>-task-<id>-<slug>
3	task: <id>            # id del gestor de tickets (0000 si no hay) · id de la secuencia del proyecto (ids.mode en sdd-kit.json)
4	parent: <id>          # solo si esta task nace de partir otra; la relación no va en el id (nunca sufijos 0006a)
5	title: <título corto descriptivo>
6	mode: full            # full | lite — lo lee sdd-end-task; sin campo = full
7	profile: <pair|delegate|unattended>   # opcional; omitido = hereda de la release o del proyecto
8	status: draft
9	created: <YYYY-MM-DD>
10	author: <autor>
11	approvers:
12	  - role: dev-lead
13	

>>> Bash: cd "<run>/salas" && cat tests/slots.test.js; ls; git log --oneline develop -3
<<< import { test } from 'node:test';
import assert from 'node:assert';
import { reserve } from '../src/slots.js';

test('reserva una sala en una franja', () => {
  assert.strictEqual(reserve([], 'Norte', '10-12'), 'Reservada Norte 10-12');
});
README.md
src/
tests/
6fe5050 docs(sdd): filas 0020 y 0021
7360e01 feat: base de reservas de salas

>>> Bash: cd "<run>/salas" && ts=$(date -u +%Y%m%d-%H%M%S) && d=".docs/sdd/specs/$ts-task-0021-cancel-booking" && mkdir -p "$d" && cat > "$d/spec.md" <<EOF
---
id: $ts-task-0021-cancel-booking
task: 0021
title: Cancelar una reserva
mode: lite
profile: delegate
status: approved
created: 2026-09-25
author: Claude
approvers:
  - role: dev-lead
    name: dev-lead
    approved_at: 2026-09-25
---

# Spec — Cancelar una reserva

## Capacidades

- Modificadas: \`bookings\` — se añade el requisito «Cancelar una reserva» y el aviso de reserva inexistente.

## Decisiones que he tomado yo — valida estas

Review de spec: ninguna (lite, un requisito, sin contratos nuevos entre módulos). Repaso de coherencia hecho: los literales \`Cancelada Norte 10-12\` y \`No hay reserva Norte 10-12\` coinciden entre escenarios y reglas.

1. **Modo lite** — cumple el predicado: el flujo (reservar/libres en \`src/slots.js\`) ya existe y se lee; no cambia contratos entre módulos (solo suma un comando al CLI); sin schema ni migración (mismo \`data/bookings.json\`, se quita una entrada); cabe en un módulo (\`src/slots.js\`); no hay \`estimation.md\`.
2. **Mensaje de éxito \`Cancelada <sala> <franja>\`**, simétrico a \`Reservada …\`.
3. **Cancelar una reserva que no existe no falla**: responde \`No hay reserva <sala> <franja>\` y no modifica nada. Sin código de error nuevo.
4. **Se eliminan todas las entradas que coinciden en sala y franja** (\`reserve\` hoy no impide duplicados y \`libres\` filtra por coincidencia: quitar solo una dejaría la sala ocupada).
5. **La capacidad sigue siendo \`bookings\`**, no se crea ninguna nueva.
6. **Riesgo de colisión con la 0020**: también toca \`src/slots.js\` (función \`free\`). Se resolverá en la base al fusionar; esta task solo añade una función nueva y no toca \`free\`.
7. **Sin tope ni validación de franja** en cancelar (la franja se compara como texto, igual que \`free\`).

### Decisiones tomadas con el dev-lead

- Spec aprobada por delegación, modo lite y perfil delegate — «apruebo la spec por delegación, nos vemos en la validación» (2026-09-25).

## Intent

Hoy se puede reservar una sala pero no deshacerlo: una reserva equivocada bloquea la franja para siempre. Los usuarios piden \`salas cancelar <sala> <franja>\` para liberarla.

## Scope

- Entra: comando \`cancelar\` que elimina la reserva de una sala en una franja; mensaje de éxito y de reserva inexistente.
- No entra: cancelar por rango, cancelar todas las de una sala, historial de cancelaciones, validar el formato de la franja.

## Approach

Una función \`cancel(bookings, room, slot)\` en \`src/slots.js\`, hermana de \`reserve\`, que quita las coincidencias y devuelve el mensaje. Tests en \`tests/slots.test.js\`, uno por THEN, escritos antes del código.

## Delta de comportamiento

### Capacidad: \`bookings\`

**ADDED — Cancelar una reserva**
- GIVEN Norte reservada de 10 a 12
- WHEN \`salas cancelar Norte 10-12\`
- THEN la reserva se elimina y el CLI responde \`Cancelada Norte 10-12\`
- AND \`salas libres 10-12\` vuelve a listar \`Norte\`

**ADDED — Cancelar una reserva inexistente**
- GIVEN Sur sin reserva de 10 a 12
- WHEN \`salas cancelar Sur 10-12\`
- THEN el CLI responde \`No hay reserva Sur 10-12\` y no cambia ninguna reserva

**Reglas de la capacidad**
- **Avisos**: una reserva de más de 2 h se rechaza con \`Máximo 2 h por reserva\`; cancelar una reserva que no existe responde \`No hay reserva <sala> <franja>\`.

## Aprobaciones

| Rol | Nombre | Fecha | Estado |
| --- | --- | --- | --- |
| dev-lead | dev-lead | 2026-09-25 | aprobada por delegación: «apruebo la spec por delegación, nos vemos en la validación» |
EOF
git add .docs && git commit -q -m "docs(sdd): spec lite de la task 0021, cancelar reserva

Spec aprobada por delegación del dev-lead (perfil delegate). Añade el
requisito de cancelar una reserva a la capacidad bookings. Sin
implementación todavía.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>" && git log --oneline -1 && git status --short
<<< 372caad docs(sdd): spec lite de la task 0021, cancelar reserva

=== RESULTADO (14 turnos, 0.3483582 $)
Spec de la 0021 escrita y commiteada (`372caad`) en la rama `feature/0021`. No he implementado nada. Antes de seguir, valida el bloque «Decisiones que he tomado yo». La carpeta es `.docs/sdd/specs/<timestamp>-task-0021-cancel-booking/spec.md`.

Decisiones que tomé por ti, todas anotadas en la spec:
- **Modo lite:** cumple las condiciones. El flujo ya existe en `src/slots.js`, no cambia contratos entre módulos, no toca el schema de datos, cabe en un solo módulo y no hay `estimation.md`.
- **Aprobación por delegación:** registrada con tu frase literal y la fecha 2026-09-25.
- **Review de spec:** ninguna, porque es un solo requisito. Sí hice el repaso de coherencia de los literales.
- **Mensajes:** `Cancelada Norte 10-12` si la reserva existe y `No hay reserva Sur 10-12` si no existe. En este segundo caso no se modifica nada y no hay código de error.
- **Duplicados:** cancelar elimina todas las entradas que coinciden en sala y franja. `reserve` hoy no impide duplicados, y quitar solo una dejaría la sala ocupada en `libres`.
- **Capacidad:** sigue siendo `bookings`, no se crea ninguna nueva. Se añaden dos requisitos y el aviso de reserva inexistente en las reglas.
- **Colisión con la 0020:** ambas tocan `src/slots.js`. Esta task solo añade una función nueva y no toca `free`, así que el conflicto se resolvería al fusionar.

Queda pendiente: tests RED de cada THEN, la función `cancel`, el smoke y tu validación. Cuando quieras, sigo con la implementación.
