--- [12]
Guardas limpias (sin merges, sin remoto). Junto los tres commits del fix en uno.

--- [25]
Patch 0013 cerrado en `feature/0013`, con dos commits: fix `4e629e5` y cierre `fdfeabf`. El merge queda pendiente y necesito tu decisión.

- **Fix:** junté los tres commits previos (fix, `patch.md` y `patch.md` calcado) en uno, `4e629e5`. No había merges en el rango ni remoto.
- **`patch.md`:** tiene el hash `4e629e5` y el tiempo de 0,3 h.
- **Capacidades:** el bloque dice `Ninguna`, porque el fix devuelve `salas reservar` a lo que ya dice `bookings` («Límites» y «Avisos»). Borré la sección de delta vacía.
- **Validador:** `Test-Capabilities.ps1` pasa.
- **Changelog y roadmap:** añadí la entrada `Fixed` en `[Unreleased]` y la fila en la tabla de Patches.
- **Sin estimation.md:** no hay `.docs/sdd/estimation.md`, así que no toqué el estimation-log.
- **Sin environments.md:** tampoco hay, así que no hice `env:clean`.
- **Decisiones sin ti:** `patch.md` no cuenta ninguna.
- **Instrucciones previas:** no diste ninguna, salvo cerrar el patch.

**Pendiente:** `sdd-kit.json` no tiene bloque `merge`, así que la política de merge no está declarada. Con perfil `delegate` eso significa que el merge lo decides tú, y no he fusionado nada. La fila 0013 sigue también en «Próximo» del roadmap.

**Pregunta:** ¿fusiono `feature/0013` en `main` con `Invoke-SddMerge.ps1`, y en ese caso `--no-ff`? No hay remoto, así que no habría push.

**Ticket del kit:** esta sesión no ha generado el suyo. Si quieres, lo genero con `sdd-feedback`.

**No terminado.** Falta el merge de `feature/0013`. No borres todavía el worktree `<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0070-openspec-capabilities/4eeabe21-48b5-45f8-80d5-1535d483175d/scratchpad/runs-g/p2-1/salas`.

