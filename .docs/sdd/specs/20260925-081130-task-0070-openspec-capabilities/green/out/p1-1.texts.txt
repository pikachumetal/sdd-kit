--- [10]
Fix juntado en `67f79a6`. Ahora finalizo `patch.md`, la capacidad y los registros.

--- [19]
Patch 0014 cerrado en dos commits, pero el merge de `feature/0014` queda PENDIENTE. `node --test` pasa 2/2 tras el cierre.

- **Fix `67f79a6`:** junté el fix, sus tests y `patch.md` en un solo commit. No había merges en el rango ni remoto donde estuviera publicado.
- **Cierre `7eeaf2e`:**
  - `patch.md` lleva el hash del fix, el bloque «Capacidades» y el delta de capacidad.
  - Fusioné el delta en `capabilities/bookings.md`: «Consultar salas libres» ahora lista `Oeste (en mantenimiento)` al final. Antes decía que listaba `Oeste` sin más. `Test-Capabilities.ps1` da «Capacidades válidas: 1».
  - Añadí la entrada `Fixed` en el changelog y la fila en la tabla de patches del roadmap. Quité 0014 de «Próximo», porque el patch la salda.
- **`estimation.md` y `environments.md`:** no existen, así que no regeneré el log ni hice `env:clean`.
- **Ticket del kit:** esta sesión no ha generado el suyo. ¿Quieres que lo genere ahora con `sdd-feedback`?

**Pendiente:** `sdd-kit.json` no tiene bloque `merge`, así que el merge lo decides tú. No fusioné nada. Si quieres fusionar, dime la rama destino, por ejemplo `main`. No hay remoto, así que tampoco hay push.

**No terminado.** Falta fusionar `feature/0014` en la rama destino. La rama está en el checkout principal del repo, sin worktree enlazado que borrar.

