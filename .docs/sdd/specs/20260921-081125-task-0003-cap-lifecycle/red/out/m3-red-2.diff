diff --git a/.docs/sdd/capabilities/bookings.md b/.docs/sdd/capabilities/bookings.md
index ec3ff86..30acd16 100644
--- a/.docs/sdd/capabilities/bookings.md
+++ b/.docs/sdd/capabilities/bookings.md
@@ -9,14 +9,14 @@
 
 ### Solo se reserva en días lectivos
 - GIVEN un profesor que pide un aula
-- WHEN la fecha de la reserva cae en sábado o domingo
-- THEN la petición se rechaza con «Solo se reserva de lunes a viernes»
+- WHEN la fecha de la reserva cae en domingo
+- THEN la petición se rechaza con «No se reserva en domingo»
 
 ## Reglas de la capacidad
 
 - **Dónde viven los datos**: tabla `bookings` de `data/aulario.db`.
 - **Idioma de los nombres**: estados en inglés; mensajes en castellano.
-- **Límites**: reservas de lunes a viernes, de 8:00 a 21:00.
+- **Límites**: reservas de lunes a sábado, de 8:00 a 21:00.
 - **Avisos**: no aplica.
 - **Regla ante conflicto**: la confirmación de secretaría manda sobre la reserva del profesor.
 
@@ -24,3 +24,4 @@
 
 - 2026-09-12 — task 0002 — ADDED Una reserva no puede solaparse con otra
 - 2026-09-12 — task 0002 — ADDED Solo se reserva en días lectivos
+- 2026-09-21 — patch 0007 — MODIFIED Solo se reserva en días lectivos (antes: sábado y domingo rechazados con «Solo se reserva de lunes a viernes»)
diff --git a/.docs/sdd/roadmap.md b/.docs/sdd/roadmap.md
index ec98a45..9dd0987 100644
--- a/.docs/sdd/roadmap.md
+++ b/.docs/sdd/roadmap.md
@@ -11,3 +11,4 @@
 
 | Fecha | id | Descripción |
 | --- | --- | --- |
+| 2026-09-21 | 0007 | Reservas en sábado para los cursos intensivos |
diff --git a/.docs/sdd/specs/20260920-110000-patch-0007-sat/patch.md b/.docs/sdd/specs/20260920-110000-patch-0007-sat/patch.md
index 3ac7d65..9d20172 100644
--- a/.docs/sdd/specs/20260920-110000-patch-0007-sat/patch.md
+++ b/.docs/sdd/specs/20260920-110000-patch-0007-sat/patch.md
@@ -6,7 +6,7 @@ type: patch
 status: done
 created: 2026-09-20
 branch: feature/0007
-commit: <hash>
+commit: f8c565b
 ---
 
 # Patch 0007 — Reservas en sábado para los cursos intensivos
@@ -28,5 +28,12 @@ commit: <hash>
 
 | # | Caso | Resultado |
 | --- | --- | --- |
-| 1 | reservar el sábado a las 10:00 | pendiente |
-| 2 | reservar el domingo a las 10:00 → rechazado | pendiente |
+| 1 | reservar el sábado a las 10:00 (aula 3) | ✅ reportado por el usuario (prueba manual) |
+| 2 | reservar el domingo a las 10:00 → rechazado | ✅ reportado por el usuario (prueba manual) |
+
+- **Verificado por el agente**: solo lectura de `src/bookings.js` (`isBookableDay` → `getDay() !== 0`; mensaje «No se reserva en domingo»). No se ha ejecutado nada: el repo no tiene `node_modules` (express/better-sqlite3 sin instalar) ni tests.
+- **Nota sobre el ticket**: el 2026-09-27 es domingo, no sábado. Con este fix esa fecha sigue rechazada; el caso probado usa un sábado (p. ej. 2026-09-26). Pendiente de confirmar con quien reportó qué día necesitaba realmente.
+
+## 5. Tiempo
+
+- Real: sin registrar (no hay `.docs/sdd/estimation.md` y no se ha indicado el tiempo invertido).
