
>>> Skill: sdd-kit:sdd-start-task
<<< Launching skill: sdd-kit:sdd-start-task

>>> Bash: cd .docs/sdd && ls -a . capabilities specs 2>&1; cat constitution.md mission.md tech-stack.md roadmap.md; cat architecture.md 2>/dev/null; cat sdd-kit.local.json sdd-kit.json 2>&1; git branch --show-current
<<< ls: cannot access 'capabilities': No such file or directory
ls: cannot access 'specs': No such file or directory
.:
.
..
changelog.md
constitution.md
mission.md
roadmap.md
sdd-kit.json
# Constitution — salas

## Art. I — Commits

Tipo/scope en inglés, título y cuerpo en castellano, nunca title-only.

## Art. II — Tests

Suite: `node --test`. Todo verde antes de fusionar.
# Mission — salas

CLI interna para reservar las salas de reuniones de la oficina. Usuarios: el equipo de oficina y, desde este año, clientes externos que alquilan salas.
cat: tech-stack.md: No such file or directory
# Roadmap

>>> Bash: git branch -a; git log --oneline --all | head; ls; grep -rn "franja\|slot" --include=*.js . 2>/dev/null | grep -v node_modules | head
<<<   develop
* feature/0013
  main
d56ef75 feat: base de reservas de salas
data
README.md
src
tests
./src/slots.js:1:export function reserve(room, slot) {
./src/slots.js:2:  return { room, slot };
./tests/slots.test.js:3:import { reserve } from '../src/slots.js';
./tests/slots.test.js:5:test('reserva una sala en una franja', () => {
./tests/slots.test.js:6:  assert.deepStrictEqual(reserve('Norte', '10-12'), { room: 'Norte', slot: '10-12' });

=== RESULTADO (5 turnos, 0.21505840000000004 $)
Ahora: he leído el contexto SDD y la rama `feature/0013` coincide con la fila pendiente 0013 del roadmap. Queda: esta pregunta, luego brainstorming y la spec hasta su gate, ~15 min.

Antes de nada, una cosa que cambia el enfoque: **0013 depende de 0012** («tras 0012: usa su validación de franja»), y 0012 sigue ⏳. En `develop` y en esta rama no hay validación de franja: `reserve` en `src/slots.js` acepta cualquier cadena. Sin 0012 no hay validación que reutilizar.

**Pregunta única (carril, modo y perfil):**

1. **Enunciado:** ¿confirmas como task la fila 0013, «Reservas recurrentes (cada lunes)»?
2. **Carril:** es una feature con comportamiento nuevo, así que va como task y no como patch ni spike.
3. **Modo:** no propongo lite. El predicado exige, entre otras condiciones, un cambio acotado sin decisiones de diseño abiertas. Aquí quedan abiertas la periodicidad (¿solo semanal o también otras?), el fin de la serie, los conflictos con reservas existentes y la dependencia de 0012. Propongo **modo full**.
4. **Perfil vigente:** `delegate`, que sale del proyecto (`.docs/sdd/sdd-kit.json`). No existe `sdd-kit.local.json`, así que no hay claves personales que ignorar. Puedes cambiarlo solo para esta task.
5. **Dependencia con 0012:** ¿cómo la tratamos?
   - (a) Recomendada: paro aquí y hacemos 0012 primero.
   - (b) Seguimos con 0013 e incluimos en su spec el uso de la validación de franja, asumiendo que 0012 llega antes.
   - (c) Absorbemos la validación en 0013. Esto solapa con la fila 0012 y ensancha el alcance.
6. **Aprobación por delegación:** si te vas a ausentar, puedes elegir «apruebo la spec por delegación, nos vemos en la validación». Con ella no paro en el gate de la spec. La validación del trabajo sigue haciéndose siempre contigo.

Respóndeme con: enunciado (sí/no), modo (full/lite), perfil (`delegate` u otro), opción a/b/c y si apruebas la spec por delegación.
