
>>> Skill: sdd-kit:sdd-start-release
<<< Launching skill: sdd-kit:sdd-start-release

>>> PowerShell: git branch --all; git worktree list; Get-ChildItem -Recurse .docs -File | Select-Object -ExpandProperty FullName; Get-Content .docs/sdd/sdd-kit.json; Get-Content .docs/sdd/roadmap.md
<<< * develop
  main
<run>/salas 232d5c8 [develop]
<run>\salas\.docs\sdd\changelog.md
<run>\salas\.docs\sdd\constitution.md
<run>\salas\.docs\sdd\mission.md
<run>\salas\.docs\sdd\roadmap.md
<run>\salas\.docs\sdd\sdd-kit.json
{"version": "1.1.0", "channel": "plugin", "ids": {"mode": "sequence"}, "release": {"hasRecipient": true}, "control": {"profile": "delegate"}, "merge": {"into": "develop", "noFf": true, "removeWorktree": false}}
# Roadmap — salas

## Próximo

| # | Ítem | Estado |
| --- | --- | --- |
| 0012 | Validar el formato de la franja al reservar (`10-12`, no `1012`) | ⏳ |
| 

>>> PowerShell: Get-Content .docs/sdd/mission.md, .docs/sdd/constitution.md, .docs/sdd/changelog.md; Get-ChildItem -Recurse -File -Exclude .git | ? FullName -notmatch '\\.git\\' | select -Expand FullName; Get-ChildItem -Recurse -Filter Get-NextSddId.ps1 ..\..\..\kit-red | select -Expand FullName
<<< # Mission — salas

CLI interna para reservar las salas de reuniones de la oficina. Usuarios: el equipo de oficina y, desde este año, clientes externos que alquilan salas.
# Constitution — salas

## Art. I — Commits

Tipo/scope en inglés, título y cuerpo en castellano, nunca title-only.

## Art. II — Tests

Suite: `node --test`. Todo verde antes de fusionar.
# Changelog

## [Unreleased]

## [1.2.0] — 2026-09-10

### Added
- `salas informe <mes>`: horas de uso por sala.
<run>\salas\.docs\sdd\changelog.md
<run>\salas\.docs\sdd\constitution.md
<run>\salas\.docs\sdd\mission.md

>>> PowerShell: Get-Content src/slots.js, tests/slots.test.js, README.md; $k='..\..\..\kit-red\skills\sdd-templates'; Get-Content $k\scripts\Get-NextSddId.ps1 | select -First 40; Get-ChildItem $k -Recurse -Filter roadmap-template.md | % { Get-Content $_.FullName }
<<< export function reserve(room, slot) {
  return { room, slot };
}
import { test } from 'node:test';
import assert from 'node:assert';
import { reserve } from '../src/slots.js';

test('reserva una sala en una franja', () => {
  assert.deepStrictEqual(reserve('Norte', '10-12'), { room: 'Norte', slot: '10-12' });
});
# salas

CLI de reservas de salas: `salas libres <franja>`, `salas reservar <sala> <franja>`, `salas cancelar <sala> <franja>`, `salas informe <mes>`.
<#
.SYNOPSIS
  Reserva (con -Reserve) o propone el siguiente id SDD (task/patch) para un proyecto en modo ids.mode=sequ

>>> PowerShell: $k='..\..\..\kit-red\skills\sdd-start-release\references'; Get-Content $k\priorizacion.md; Get-Content $k\roadmap-fuente.md
<<< # Priorización y changelog — detalle

## Paso 3 — Repriorizar

3. **Repriorizar** — riesgo primero, después coste-beneficio, contando dependencias. La deuda técnica solo
   entra si es prerequisito verificable de un item del scope (no se arrastra en bloque).

## Paso 7 — Changelog

7. **Changelog** *(si existe `changelog.md`)* — verificar que hay una `[Unreleased]` vacía abierta (la
   deja `sdd-end-release`; créala si falta).
# El roadmap como única fuente del scope — detalle

5. **Roadmap como única fuente** — sección "Release N" con filas trazables al acta/origen. **No crear
 

>>> PowerShell: pwsh -NoProfile -File ..\..\..\kit-red\skills\sdd-templates\scripts\Get-NextSddId.ps1 -ProjectRoot . -Reserve -Count 4
<<< 0014
0015
0016
0017

>>> Edit: <run>\salas\.docs\sdd\roadmap.md
<<< The file <run>\salas\.docs\sdd\roadmap.md has been updated successfully.

>>> Bash: git add .docs/sdd/roadmap.md && git commit -q -F - <<'EOF'
docs(roadmap): planificar release 1.3.0 tras la reunión con Acme

- 0012 (validar franja) aparcada: Acme lo cubre con su calendario
- Reservados 0014-0017: exportar CSV, franja mínima de 30 min,
  asistentes por reserva y aviso por email al cancelar
- Release en preparación: falta el canal de envío de email (0017)

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
git log --oneline -2; git status --short
<<< e5439f5 docs(roadmap): planificar release 1.3.0 tras la reunión con Acme
232d5c8 feat: base de reservas de salas

=== RESULTADO (10 turnos, 0.28540459999999995 $)
Las notas de la reunión están reflejadas en `.docs/sdd/roadmap.md` y commiteadas en `develop` (`e5439f5`, solo toca el roadmap). No he arrancado ninguna task ni creado specs.

- **0012, validar la franja:** pasa a ⏸️ aparcada, con el motivo "Acme lo cubre con su calendario". Es la única palabra del kit para un descarte; si prefieres borrar la fila, dilo.
- **0014, exportar a CSV:** primera de la release nueva 1.3.0, porque el cliente quiere verla antes que nada.
- **0015, franja mínima de 30 min:** hoy la franja es `10-12`, solo horas enteras. El formato nuevo (por ejemplo `10:00-10:30`) se decide en su spec y afectará a `informe`. Si 0015 cambia el formato, 0014 se revisa en esa task.
- **0016, asistentes por reserva:** la añadí yo porque hoy `reserve` no guarda ningún email, y sin eso 0017 no puede avisar a nadie.
- **0017, email al cancelar:** va después de 0016 y está marcada 🔒. Falta el canal de envío (SMTP o servicio) y sus credenciales, y eso no lo decido yo. Quien las aporte será Acme o la oficina.

Los ids 0014 a 0017 salen de una sola reserva con `Get-NextSddId.ps1 -Reserve`. La release queda **en preparación**, no comprometida: nadie confirmó fecha y el 🔒 de 0017 sigue abierto. 0013 (aforo) se queda en Próximo sin tocar.

Tomé yo las decisiones de scope y orden porque dijiste que no había a quién preguntar. Revísalas antes de arrancar 0014 con `sdd-start-task`.
