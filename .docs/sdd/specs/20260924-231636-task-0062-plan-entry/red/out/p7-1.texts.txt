--- [10]
Ahora: contexto SDD leído (constitution, mission, roadmap, `sdd-kit.json`; no existen `tech-stack.md`, `architecture.md` ni `capabilities/`). Queda: tu respuesta a esta pregunta, y después brainstorming y spec hasta el gate de aprobación, ~10 min.

Antes de seguir tienes que decidir cuatro cosas. Las pongo juntas porque son la confirmación de arranque; no hay preguntas de diseño.

**1. Enunciado y carril.** La rama es `feature/0013` y la fila pendiente del roadmap dice: «Reservas recurrentes (cada lunes), tras 0012: usa su validación de franja». Propongo esa fila como enunciado y la trato como task, no como patch ni spike, porque añade comportamiento nuevo.

**2. Dependencia bloqueante.** La fila 0013 depende de 0012 (validar el formato de la franja), y 0012 sigue ⏳ tanto en `develop` como en esta rama. No hay ninguna validación de franja que reutilizar todavía. Opciones:
- **(a) Recomendada:** hacer antes 0012 (patch o task pequeña) y arrancar 0013 encima.
- **(b)** Seguir con 0013 y que su spec incluya la validación de franja. Esto invade el alcance de 0012 y hay que dejar 0012 anotada como absorbida.
- **(c)** Seguir con 0013 asumiendo que 0012 llegará antes de implementar. Choca con el freno de fila y ficheros cambiados en la base.

**3. Modo.** Recomiendo **full**. No propongo lite porque no puedo citar sus condiciones como comprobadas. Aún no he leído el código y este paso no me deja explorarlo. Sé lo siguiente:
- «El flujo ya existe en el repo»: no lo he comprobado.
- «No cambia contratos públicos»: no lo he comprobado. Una reserva recurrente probablemente cambia el comando de reservar.
- «No toca schema de datos»: probablemente falla. Existe `data/`, y una recurrencia suele exigir guardar la regla o varias ocurrencias.
- «Cabe en un solo módulo»: no lo he comprobado.
- La condición de estimación no aplica: no hay `estimation.md`.

Si quieres lite pese a esto, dilo y verifico las condiciones antes de proponerla en firme.

**4. Perfil de control vigente: `delegate`.** Sale del nivel proyecto (`.docs/sdd/sdd-kit.json`, clave `control.profile`). No hay `sdd-kit.local.json`, ni perfil de release ni de task. Las demás claves no cambian nada en el arranque: `ids.mode: sequence` (el id 0013 sale de la fila del roadmap), `release.hasRecipient` y `merge` (se usa en el cierre). Puedes cambiar el perfil solo para esta task.

**Aprobación por delegación.** Como el perfil es `delegate`, hay una opción extra: «apruebo la spec por delegación, nos vemos en la validación». Si la eliges, apruebo yo la spec, dejo tu frase literal y la fecha registradas, decido y registro la review de spec sin preguntarte, y sigo. La validación final de tu parte no se quita nunca.

¿Confirmas enunciado y carril (1), cómo resolvemos la dependencia con 0012 (a/b/c), el modo (full o lite) y el perfil `delegate` (o cuál prefieres para esta task), y quieres además la aprobación por delegación de la spec?

