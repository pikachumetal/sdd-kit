--- [3]
Empiezo leyendo el estado del proyecto.

--- [8]
No puedo cerrar la 1.3 todavía: casi no hay nada que meter y hay una decisión que es tuya.

**Qué hay desde la 1.2.0 (2026-09-10)**
- `[Unreleased]` del changelog está vacío.
- El único candidato es el patch **0011** (`informe` contaba dos veces las reservas canceladas, 2026-09-12). Figura en el roadmap, pero no tiene `patch.md` en `.docs/sdd/specs/` ni entrada en el changelog. Tampoco hay carpeta `specs/`, y el historial git es un solo commit (`895150a`, la base). Es evidencia faltante y el gate de entrada me obliga a que lo decidas tú.
- **0012** (validar formato de franja) y **0013** (aforo en `salas libres`) siguen en ⏳, sin hacer.
- No hay demo con notas, ni `estimation-log.md`, ni carpeta `releases/`. Los pasos 2 y 3 (acta y retro) no aplican.
- `release.hasRecipient` es `true`, así que hacen falta release notes y el paso de comunicar. Merge y tag esperan tu confirmación explícita.

**Mi propuesta**
- La 1.3.0 sería una release solo de fix, con el patch 0011 como único contenido. Una release de un solo patch es poca cosa. Lo razonable es tagearla como **1.2.1**, o esperar a cerrar 0012 y 0013 y sacar la 1.3.0 con features.
- Aun así, la versión la confirmas tú, no yo.

**Pregunta:** ¿Qué hago con el patch 0011? Estas son las opciones:
1. Regularizarlo: escribo el `patch.md` y la entrada en `[Unreleased]` a partir del código. Confírmame que el fix está ya en `develop`.
2. Sacarlo de esta release y dejarlo pendiente.

Con tu respuesta, dime también si prefieres 1.2.1 o 1.3.0 (esta última solo tiene sentido si antes cerramos 0012 y 0013).

