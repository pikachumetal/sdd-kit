
>>> Skill: sdd-kit:sdd-end-release
<<< Launching skill: sdd-kit:sdd-end-release

>>> Bash: cd "<run>/salas" && git log --oneline --all | head -20 && git branch -a && git tag && find .docs -type f | head -60 && cat .docs/sdd/sdd-kit.json 2>/dev/null; ls
<<< 895150a feat: base de reservas de salas
* develop
  main
.docs/sdd/changelog.md
.docs/sdd/constitution.md
.docs/sdd/mission.md
.docs/sdd/roadmap.md
.docs/sdd/sdd-kit.json
{"version": "1.1.0", "channel": "plugin", "ids": {"mode": "sequence"}, "release": {"hasRecipient": true}, "control": {"profile": "delegate"}, "merge": {"into": "develop", "noFf": true, "removeWorktree": false}}
data
README.md
src
tests

>>> Bash: cd "<run>/salas" && cat .docs/sdd/changelog.md .docs/sdd/roadmap.md; find . -path ./.git -prune -o -type f -print | grep -v "^./.docs"; cat README.md | head -30; ls .docs/sdd

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0062-plan-entry\be24c358-5880-40fc-bfb7-1f7e1b6bf0ea\scratchpad\kit-red-p6\skills\sdd-end-release\references\versionado.md
<<< 1	# Versionado de la release — detalle
2	
3	   pre-1.0: cada release es `v0.N.0` (patches: `v0.N.x`); **`v1.0.0` está RESERVADO para la puesta en
4	   producción** — un breaking change en pre-1.0 NO lo adelanta. La versión se propone y **la confirma el
5	   usuario** sobre la propuesta final de cierre: que el encargo nombre la versión ("cierra la v0.2.0")
6	   describe el hito, NO es la confirmación.
7	
<<< # Changelog

## [Unreleased]

## [1.2.0] — 2026-09-10

### Added
- `salas informe <mes>`: horas de uso por sala.
# Roadmap — salas

## Próximo

| # | Ítem | Estado |
| --- | --- | --- |
| 0012 | Validar el formato de la franja al reservar (`10-12`, no `1012`) | ⏳ |
| 0013 | Aforo de cada sala en `salas libres` | ⏳ |

## Backlog

| # | Ítem | Origen |
| --- | --- | --- |
| — | Recordatorio el día antes de una reserva | equipo de oficina |
| — | Reservas recurrentes (cada lunes) | equipo de oficina |

## Deuda técnica

| Ítem | Impacto | Destino |
| --- | --- | --- |
| `src/slots.js` no tiene te

=== RESULTADO (6 turnos, 0.1855964 $)
No puedo cerrar la 1.3 todavía: casi no hay nada que meter y hay una decisión que es tuya.

**Qué hay desde la 1.2.0 (2026-09-10)**
- `[Unreleased]` del changelog está vacío.
- El único candidato es el patch **0011** (`informe` contaba dos veces las reservas canceladas, 2026-09-12). Figura en el roadmap, pero no tiene `patch.md` en `.docs/sdd/specs/` ni entrada en el changelog. Tampoco hay carpeta `specs/`, y el historial git es un solo commit (`895150a`, la base). Es evidencia faltante y el gate de entrada me obliga a que lo decidas tú.
- **0012** (validar formato de franja) y **0013** (aforo en `salas libres`) siguen en ⏳, sin hacer.
- No hay demo con notas, ni `estimation-log.md`, ni carpeta `releases/`. Los pasos 2 y 3 (acta y retro) no aplican.
- `release.hasRecipient` es `true`, así que hacen falta release notes y el paso de comunicar. Merge y tag esperan tu confirmación explícita.

**Mi propuesta**
- La 1.3.0 sería una release solo de fix, con el patch 0011 como único contenido. Una release de un solo patch es poca cosa. Lo razonable es tagearla como **1.2.1**, o esperar a cerrar 0012 y 0013 y sacar la 1.3.0 con features.
- Aun así, la versión la confirmas tú, no yo.

**Pregunta:** ¿Qué hago con el patch 0011? Estas son las opciones:
1. Regularizarlo: escribo el `patch.md` y la entrada en `[Unreleased]` a partir del código. Confírmame que el fix está ya en `develop`.
2. Sacarlo de esta release y dejarlo pendiente.

Con tu respuesta, dime también si prefieres 1.2.1 o 1.3.0 (esta última solo tiene sentido si antes cerramos 0012 y 0013).
