--- [3]
Primero investigo la causa raíz. Miro el repo.

--- [9]
Búsqueda en `/` demasiado ancha. Acoto.

--- [12]
Reproducido: `toHours('90 min')` → `90`. Reservo id y leo plantilla.

--- [20]
Rama creada, working tree limpio. Escribo fix, test y patch.md.

--- [26]
Fix commiteado en `9602116` (rama `feature/0014-duracion-minutos`, patch 0014). El patch sigue abierto, sin `sdd-end-patch`.

**Causa:** `toHours` en `src/report.js` hacía `parseFloat("90 min")`, que descarta la unidad y devuelve `90`. Lo reproduje antes de tocar nada. Toda fila del CSV pasa por esa función, así que la arreglé ahí.

**Fix:** `toHours` acepta `h` y `min` (los minutos se dividen entre 60). Con cualquier otro formato lanza un error, en vez de sumar un total equivocado sin avisar. Añadí `tests/report.test.js`. `node --test` pasa (3/3) y el informe con tu CSV corregido da `Norte · 3 h` y `Sur · 1.5 h`. El commit incluye el `patch.md`.

**Antes de mandar más informes:**
- **CSV sin tocar:** no he modificado `data/usage.csv`. Cualquier fila con otra unidad, o sin ella, ahora hace fallar el informe. Es intencionado.
- **Cierre pendiente:** en `sdd-end-patch` quedan la fila en el roadmap, el changelog y el hash en el `patch.md` (ahora pone `pendiente`).

**Duda para ti:** en el CSV solo hay una fila de Sur, «1.5 h». Un total de 91.5 h implica que había otra fila más aparte de la de 90 min. ¿Falta alguna fila de Sur, o el informe se generó con otro CSV?

