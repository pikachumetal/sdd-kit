
>>> Skill: sdd-kit:sdd-start-patch
<<< Launching skill: sdd-kit:sdd-start-patch

>>> Read: <run>\salas\src\slots.js
<<< 1	export function reserve(room, slot) {
2	  return { room, slot };
3	}
4	

>>> Bash: cd "<run>/salas" && git ls-files && cat .docs/sdd/sdd-kit.json; ls .docs/sdd .docs/sdd/specs; cat package.json; grep -rn "reserve" --include=*.js . | grep -v node_modules

>>> Glob: skills/sdd-templates/**/patch-template.md
<<< .docs/sdd/changelog.md
.docs/sdd/constitution.md
.docs/sdd/mission.md
.docs/sdd/roadmap.md
.docs/sdd/sdd-kit.json
.gitignore
README.md
data/usage.csv
src/report.js
src/slots.js
tests/slots.test.js
{"version": "1.1.0", "channel": "plugin", "ids": {"mode": "sequence"}, "release": {"hasRecipient": true}, "control": {"profile": "delegate"}, "merge": {"into": "develop", "noFf": true, "removeWorktree": false}}
ls: cannot access '.docs/sdd/specs': No such file or directory
.docs/sdd:
changelog.md
constitution.md
mission.md
roadmap.md
sdd-kit.json
cat: package.json: No such file or directory
./src/slo
<<< <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0062-plan-entry\be24c358-5880-40fc-bfb7-1f7e1b6bf0ea\scratchpad\kit-green\skills\sdd-templates\templates\patch-template.md

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0062-plan-entry\be24c358-5880-40fc-bfb7-1f7e1b6bf0ea\scratchpad\kit-green\skills\sdd-templates\templates\patch-template.md
<<< 1	---
2	id: <yyyyMMdd-HHmmss>-patch-<id>-<slug>
3	task: <id>            # id del gestor de tickets (0000 si no hay) · id de la secuencia del proyecto (ids.mode en sdd-kit.json)
4	parent: <id>          # solo si este patch nace de partir otro; la relación no va en el id (nunca sufijos 0006a)
5	title: Patch — <título corto>
6	type: patch
7	status: done
8	created: <YYYY-MM-DD>
9	branch: <feature|hotfix>/<id>   # el tipo de rama lo fija el git-flow del proyecto, no el carril
10	commit: <hash>        # hash del commit del fix; se escribe en el commit de cierre
11	---
12	
13	# Patch <id> — <título c

>>> Bash: cd "<run>/salas" && cat tests/slots.test.js .docs/sdd/roadmap.md; git log --oneline; git branch; ls <home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0062-plan-entry/be24c358-5880-40fc-bfb7-1f7e1b6bf0ea/scratchpad/kit-green/scripts 2>/dev/null; node --version
<<< import { test } from 'node:test';
import assert from 'node:assert';
import { reserve } from '../src/slots.js';

test('reserva una sala en una franja', () => {
  assert.deepStrictEqual(reserve('Norte', '10-12'), { room: 'Norte', slot: '10-12' });
});
# Roadmap — salas

## Próximo

| # | Ítem | Estado |
| --- | --- | --- |
| 0012 | Validar el formato de la franja al reservar (`10-12`, no `1012`) | ⏳ |
| 0013 | Aforo de cada sala en `salas libres` | ⏳ |

## Backlog

| # | Ítem | Origen |
| --- | --- | --- |
| — | Recordatorio el día antes de una reserva | equipo de oficina |
| — | Reservas recurr

>>> Bash: cd "<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0062-plan-entry/be24c358-5880-40fc-bfb7-1f7e1b6bf0ea/scratchpad/kit-green" && find . -name "Get-NextSddId.ps1" -o -name nombrado.md -o -name commit-milestones.md | head; 
<<< ./skills/sdd-start-task/references/commit-milestones.md
./skills/sdd-start-task/references/nombrado.md
./skills/sdd-templates/scripts/Get-NextSddId.ps1

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0062-plan-entry\be24c358-5880-40fc-bfb7-1f7e1b6bf0ea\scratchpad\kit-green\skills\sdd-start-task\references\nombrado.md
<<< 1	## Nombrado de carpetas de spec
2	
3	`<yyyyMMdd-HHmmss>-(task|patch|proposal)-<id>-<slug>`, todo en UTC:
4	
5	- Timestamp: `Get-Date -AsUTC -Format 'yyyyMMdd-HHmmss'` (PowerShell).
6	- `task`, `patch` o `proposal` según el carril. Una carpeta que contiene `patch.md` SIEMPRE va prefijada `patch-`; una que contiene `proposal.md`, `proposal-` (la crea `sdd-plan`).
7	- `<id>`: depende del modo declarado en `.docs/sdd/sdd-kit.json` (`ids.mode`; sin campo ⇒ `tracker`). En `tracker`: el id del ticket en el gestor del proyecto, `0000` si no hay. En `sequence`: el id reservado en la fila del roadmap 

>>> Read: <home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0062-plan-entry\be24c358-5880-40fc-bfb7-1f7e1b6bf0ea\scratchpad\kit-green\skills\sdd-start-task\references\commit-milestones.md
<<< 1	# Un commit por hito
2	
3	La rama de una task cuenta sus hitos: **apertura**, **un commit por task del plan** y **cierre** — 2 + N commits desde el `merge-base`, 3 en lite. La de un patch, dos: **fix** y **cierre**. Mientras un hito está abierto se commitea lo que haga falta: la revisión de `subagent-driven-development` trabaja por rangos `BASE..HEAD`. Cuando la revisión del hito queda limpia, su rango se junta en un commit.
4	
5	## Qué lleva cada hito
6	
7	| Hito | Lleva | Se junta | Base |
8	| --- | --- | --- | --- |
9	| Apertura | spec, hallazgos de la review de spec, `plan.md`, `tasks.md

>>> Bash: cd "<run>/salas" && node -e "import('./src/slots.js').then(m=>console.log(m.reserve('', '10-12')))"
<<< { room: '', slot: '10-12' }

>>> PowerShell: $s = "<home>\AppData\Local\Temp\claude\D--code--worktrees-sdd-kit-0062-plan-entry\be24c358-5880-40fc-bfb7-1f7e1b6bf0ea\scratchpad\kit-green\skills\sdd-templates\scripts\Get-NextSddId.ps1"; & $s -ProjectRoot "<run>\salas" -Reserve; Get-Date -AsUTC -Format 'yyyyMMdd-HHmmss'
<<< 0014
20260925-081228

>>> Bash: cd "<run>/salas" && git branch -m feature/0014-sala-obligatoria && git status --short && mkdir -p .docs/sdd/specs/20260925-081228-patch-0014-sala-obligatoria
cat > src/slots.js <<'EOF'
export function reserve(room, slot) {
  if (!room?.trim()) throw new Error('Sala obligatoria');
  return { room, slot };
}
EOF
cat >> tests/slots.test.js <<'EOF'

test('rechaza una reserva sin sala', () => {
  assert.throws(() => reserve('', '10-12'), { message: 'Sala obligatoria' });
  assert.throws(() => reserve('  ', '10-12'), { message: 'Sala obligatoria' });
  assert.throws(() => reserve(undefined, '10-12'), { message: 'Sala obligatoria' });
});
EOF
node --test tests/
<<< ✔ reserva una sala en una franja (1.2425ms)
✔ rechaza una reserva sin sala (6.7709ms)
ℹ tests 2
ℹ suites 0
ℹ pass 2
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 151.909

>>> Bash: cd "<run>/salas" && cat > .docs/sdd/specs/20260925-081228-patch-0014-sala-obligatoria/patch.md <<'EOF'
---
id: 20260925-081228-patch-0014-sala-obligatoria
task: 0014
title: Patch — reserve() exige sala
type: patch
status: done
created: 2026-09-25
branch: feature/0014-sala-obligatoria
commit: <hash>
---

# Patch 0014 — reserve() exige sala

## 1. Síntoma

`reserve('', '10-12')` reserva una sala sin nombre; debería lanzar el error «Sala obligatoria».

## 2. Causa raíz

`reserve` en `src/slots.js` no validaba `room`: devolvía `{ room, slot }` tal cual. Reproducido con
`reserve('', '10-12')` → `{ room: '', slot: '10-12' }`. Único punto de entrada: no hay más
llamadores en el repo (solo el test), así que el guard va en la propia función.

## 3. Fix

- **Fichero(s)**: `src/slots.js`, `tests/slots.test.js`
- **Cambio**: `reserve` lanza `Error('Sala obligatoria')` si `room` es vacío, solo espacios, `null` o `undefined`. Test nuevo con los tres casos.

## 4. Verificación

| # | Caso | Resultado |
| --- | --- | --- |
| 1 | `reserve('', '10-12')` lanza «Sala obligatoria» | ✅ agente, `node --test tests/` |
| 2 | `reserve('  ', …)` y `reserve(undefined, …)` también | ✅ agente |
| 3 | reserva válida sin cambios | ✅ agente (test existente) |
EOF
git add -A && git commit -q -F - <<'EOF'
fix(slots): exigir sala en reserve (patch 0014)

`reserve('', '10-12')` reservaba una sala sin nombre. Ahora lanza
«Sala obligatoria» si la sala está vacía o no se indica. Incluye test
y patch.md.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
git log --oneline -2; git status --short
<<< ccec894 fix(slots): exigir sala en reserve (patch 0014)
a34334a feat: base de reservas de salas

=== RESULTADO (15 turnos, 0.2910834 $)
`reserve('', '10-12')` ya lanza «Sala obligatoria», y el fix está commiteado en `ccec894`. El patch sigue abierto.

- **Causa raíz:** `reserve` en `src/slots.js` no validaba `room` y devolvía `{ room, slot }` tal cual. Lo reproduje: `reserve('', '10-12')` devolvía `{ room: '', slot: '10-12' }`. No hay más llamadores en el repo.
- **Fix:** `reserve` lanza `Error('Sala obligatoria')` si `room` es vacío, solo espacios o `undefined`. Añadí un test con esos tres casos en `tests/slots.test.js`. Con `node --test tests/` pasan los 2 tests.
- **Id y rama:** reservé el id `0014`. Como no había commits propios, renombré la rama `feature/fix-sala` a `feature/0014-sala-obligatoria`.
- **Registro del patch:** `.docs/sdd/specs/20260925-081228-patch-0014-sala-obligatoria/patch.md`. Tiene `commit: <hash>` sin rellenar a propósito, porque el hash se escribe en el commit de cierre.
- **Pendiente para `sdd-end-patch`:** el `commit` de `patch.md`, la fila en el roadmap y la entrada en el changelog.
