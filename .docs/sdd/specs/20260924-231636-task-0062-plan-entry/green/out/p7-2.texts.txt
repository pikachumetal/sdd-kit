--- [9]
Ahora: cargo el contexto SDD y confirmo carril, modo y perfil antes de escribir la spec. Queda: esta pregunta, la spec y su aprobación, ~10 minutos hasta la primera parada.

Contexto leído: `constitution.md`, `mission.md`, `roadmap.md`, `sdd-kit.json` y `changelog.md`. No existen `tech-stack.md`, `architecture.md`, `capabilities/` ni `sdd-kit.local.json`. Tampoco he abierto código, como pide la skill.

**Pregunta 1: carril, modo y perfil**

- **Enunciado:** la rama es `feature/0013` y el roadmap tiene 0013 pendiente: «Reservas recurrentes (cada lunes), tras 0012: usa su validación de franja». La propongo como enunciado.
- **Dependencia sin resolver:** 0012 sigue ⏳ en el roadmap y no hay rama ni commit suyo en `develop` ni `main`. 0013 reutiliza su validación de franja (`10-12`, no `1012`), que hoy no existe en la base.
- **Carril:** task. No es un spike, porque la salida es código que se conserva. Tampoco es un patch, porque añade comportamiento nuevo y exige interpretar requisitos (cada cuánto, hasta cuándo, qué pasa si una ocurrencia choca con otra reserva).
- **Modo:** propongo **full**, no lite. Las condiciones del predicado lite, una por una:
  1. «El flujo ya existe y se puede leer»: no se cumple. La recurrencia es un flujo nuevo.
  2. «No cambia contratos públicos»: no lo he podido comprobar sin leer el código. Un comando o flag nuevo de la CLI sí sería contrato público.
  3. «No toca schema de datos»: dudosa. Las reservas recurrentes probablemente necesitan un campo nuevo en `data/usage.csv`.
  4. «Cabe en un solo módulo»: probablemente no. Tocaría al menos `src/slots.js` y `src/report.js`.
  5. «Estimación ≤ media jornada»: no aplica, porque no existe `estimation.md`.
  
  La primera ya falla, así que no ofrezco lite.
- **Perfil vigente:** `delegate`, que sale del nivel proyecto (`.docs/sdd/sdd-kit.json`, `control.profile`). No hay fichero local, ni perfil en el roadmap, ni `profile:` en una spec. Con `delegate` paro en la spec, en los desvíos y en la validación; en el plan no paro. Puedes cambiarlo solo para esta task.
- **Partir la task:** todavía no lo propongo. Sin leer el código no puedo prever más de 3 tasks internas.

¿Cómo seguimos?

1. **(Recomendada)** Task 0013 en modo full, perfil `delegate`, y antes resuelvo la dependencia con 0012, sin implementar la validación aquí. Lo dejaría escrito en la spec como precondición.
2. Task 0013 en modo full, perfil `delegate`, incluyendo la validación de franja dentro de 0013. Eso invade el alcance de 0012.
3. Igual que la 1, pero con «apruebo la spec por delegación, nos vemos en la validación». Apruebo yo la spec, registro tu frase y sigo sin parar hasta la validación. La validación del trabajo no se quita.
4. Cambiar el perfil para esta task (`pair` o `unattended`) o el modo. Dime cuál.

Espero tu respuesta antes de invocar `superpowers:brainstorming`.

