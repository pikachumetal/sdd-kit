--- [7]
Apuntado en el roadmap como **0014 — Exportar las reservas a CSV**, en «Próximo» y con estado ⏳, justo después de 0013. El id salió de `Get-NextSddId.ps1 -Reserve`. Está commiteado en `develop` (581585f) y solo toca `roadmap.md`. No he creado rama, spec ni código.

Como hay una sola feature, no hay propuesta. No existen ramas `feature/*`, así que `develop` era el único estado que leer.

Pregunta: ¿va en «Próximo» o en el «Backlog» sin id? Yo la puse en «Próximo», pero no dijiste si se va a hacer ya. Si prefieres el Backlog, lo muevo.

Cuando quieras arrancarla, va primero 0012 y se hace con `sdd-start-task`. La 0014 también iría con `sdd-start-task`.

