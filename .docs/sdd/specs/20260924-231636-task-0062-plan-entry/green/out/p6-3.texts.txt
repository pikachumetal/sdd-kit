--- [1]
Skill de roadmap aplica: "prepara la release N".

--- [7]
Aún no he escrito nada: el perfil es `delegate`, así que primero necesito que decidas el scope. La release 1.3 sería la primera tras la 1.2.0, con estos candidatos:

| Candidato | Ficheros previstos | Mi recomendación |
| --- | --- | --- |
| 0012 Validar formato de franja (`10-12`, no `1012`) | `src/slots.js`, `tests/slots.test.js` | **Entra.** Es el único con cambio de comportamiento ya definido y toca la función `reserve`. |
| 0013 Aforo en `salas libres` | módulo nuevo en `src/`. El comando `salas libres` aún no existe en el código, y el CSV solo tiene `sala,duración`. | **Entra, con riesgo.** No sé de dónde sale el aforo. Hay que decidirlo al arrancarla, y puede pedir un fichero de datos nuevo. |
| Deuda: tests de franjas límite (`00-24`) en `src/slots.js` | `tests/slots.test.js` | **Entra como prerrequisito de 0012.** Validar el formato de la franja toca esas mismas franjas límite. Recibiría un id nuevo, reservado con `Get-NextSddId.ps1 -Reserve` y publicado en `develop`, y iría **antes** de 0012. |
| Recordatorio el día antes de una reserva | Sin definir | **Fuera.** Necesita canal de envío y no hay nada definido. |
| Reservas recurrentes (cada lunes) | Sin definir | **Fuera.** Es grande (varias features) y pediría su propia `proposal.md`. |

- **Orden:** deuda de `00-24`, luego 0012, luego 0013. La 0013 va al final por su riesgo. Si prefieres el riesgo primero, se invierte.
- **Estado:** en preparación. Solo la marco comprometida si me lo dices y no hay bloqueos.
- **Destinatario:** `release.hasRecipient` ya es `true`, así que no pregunto por ello.
- **Contexto revisado:** no hay ramas `feature/*` abiertas ni retro de una release anterior.

Cuando confirmes, creo la sección `## Release 1.3` en `.docs/sdd/roadmap.md`. La publicaré en un commit que solo toca ese fichero.

**Pregunta:** ¿entran 0012, 0013 y la deuda de `00-24`, con recordatorio y recurrentes fuera, o cambias algo?

