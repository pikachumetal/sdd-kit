--- [6]
Ahora: primera pregunta del paso 2 (carril, modo, perfil y enunciado). Queda: la pregunta, spec, aprobación, plan y ejecución, ~1 min hasta tu respuesta.

**Contexto leído:** constitution (commits en castellano, `node --test` verde antes de fusionar), mission, roadmap y `sdd-kit.json`. No existen `tech-stack.md`, `architecture.md`, `capabilities/` ni `sdd-kit.local.json`. La rama es `feature/0013` y la fila 0013 está pendiente.

**Aviso: 0012 sigue pendiente.** La fila 0013 dice «tras 0012: usa su validación de franja». En `feature/0013` (que solo contiene el commit base) y en `develop` no hay ninguna rama ni commit de 0012, y el roadmap la marca ⏳. Si arrancamos 0013 ahora, no existe la validación de franja que debe reutilizar.

**Pregunta (una sola decisión):** ¿Cómo procedemos?

1. **Recomendada: hacer primero 0012** y dejar 0013 parada. La tarea 0013 depende de su validación y construir sobre ella todavía no es posible.
2. Arrancar 0013 sin 0012, con la validación de franja incluida o simulada dentro de esta task. Amplía el alcance y se solapa con 0012.
3. Arrancar 0013 y dejar su implementación bloqueada hasta que 0012 llegue a `develop`. Solo avanzaría la spec.

Con la opción que elijas, confirma también estos tres puntos:

- **Enunciado:** «Reservas recurrentes (cada lunes)», tomado de la fila 0013.
- **Carril y modo: task en modo full.** No cumple las condiciones de lite: añade comportamiento nuevo (recurrencia) y depende de otra task, y el predicado exige un cambio acotado. No es un bug ni un spike.
- **Perfil: `delegate`**, sale de `.docs/sdd/sdd-kit.json` (nivel proyecto). No hay `sdd-kit.local.json`, ni perfil de task ni de release. ¿Lo cambias para esta task?

Como el perfil es `delegate`, hay una opción más: «apruebo la spec por delegación, nos vemos en la validación». Si te vas a ausentar, di esa frase literal y apruebo la spec yo, sin parar en el gate. La validación final no se quita nunca.

