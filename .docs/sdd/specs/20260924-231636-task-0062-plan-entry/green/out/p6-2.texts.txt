--- [5]
Estado real leído: solo existen `develop` y `main`, sin ramas `feature/*`. El roadmap tiene 0012 y 0013 en «Próximo», dos ideas en Backlog y una deuda técnica. No hay retro previa: `releases/` no existe. Con `delegate` no escribo nada hasta tu decisión.

**Propuesta de scope para la 1.3, en orden (riesgo primero, luego coste-beneficio):**

| Orden | Ítem | Origen | Ficheros previstos | Recomendación |
| --- | --- | --- | --- | --- |
| 1 | 0012 Validar formato de franja (`10-12`, no `1012`) | Próximo | `src/slots.js`, `tests/slots.test.js` | Entra. Es el único que evita reservas corruptas. |
| 2 | Tests de franjas límite (`00-24`) | Deuda técnica | `tests/slots.test.js` | Entra. Es prerrequisito verificable de 0012, porque toca el mismo parser. |
| 3 | 0013 Aforo en `salas libres` | Próximo | `src/` (aún sin módulo de `salas libres`, ver pregunta 2), `README.md` | Entra. Es independiente y de bajo riesgo. |
| — | Recordatorio el día antes | Backlog | — | Fuera. Necesita notificaciones y no tiene definición. |
| — | Reservas recurrentes | Backlog | — | Fuera. Es grande, con reglas propias. Iría a `proposal.md` en otra sesión de `sdd-plan`. |

No hay bloqueos ni dependencias entre filas. Sugiero que 0012 vaya antes que la deuda de tests, así los tests cubren la validación nueva.

Los ids de las filas nuevas saldrían de una sola reserva con `Get-NextSddId.ps1 -Reserve`. Los dos ítems de Backlog (0012, 0013) conservan su id. Solo hay que reservar uno nuevo, para la deuda de tests. Haría un commit en `develop` que toque solo `roadmap.md`.

Ya consta `release.hasRecipient: true`, así que no pregunto por el destinatario.

**Preguntas:**
1. ¿Confirmas este scope y este orden, o quieres añadir o quitar ítems?
2. ¿La release 1.3 queda **comprometida** (scope prometido al destinatario, normalmente con fecha) o **en preparación**? Solo la marco comprometida si me lo dices. Si es comprometida, dime la fecha.
3. `src/` solo contiene `slots.js` y `report.js`, y no encuentro `salas libres` ni la reserva en el código. ¿Dónde vive ese comando? Necesito el fichero para la columna «Ficheros que toca» de 0013.

