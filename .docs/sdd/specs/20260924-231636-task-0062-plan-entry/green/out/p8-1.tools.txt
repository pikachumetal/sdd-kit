
>>> Skill: sdd-kit:sdd-start-patch
<<< Launching skill: sdd-kit:sdd-start-patch

>>> Skill: superpowers:systematic-debugging

>>> Read: <run>\salas\src\slots.js
<<< Launching skill: superpowers:systematic-debugging

>>> Bash: cd "<run>/salas" && git ls-files && cat package.json .docs/sdd/sdd-kit.json 2>/dev/null; ls .docs/sdd .docs/sdd/specs
<<< 1	export function reserve(room, slot) {
2	  return { room, slot };
3	}
4	
<<< Exit code 2
.docs/sdd/changelog.md
.docs/sdd/constitution.md
.docs/sdd/mission.md
.docs/sdd/roadmap.md
.docs/sdd/sdd-kit.json
.gitignore
README.md
data/usage.csv
src/report.js
src/slots.js
tests/slots.test.js
{"version": "1.1.0", "channel": "plugin", "ids": {"mode": "sequence"}, "release": {"hasRecipient": true}, "control": {"profile": "delegate"}, "merge": {"into": "develop", "noFf": true, "removeWorktree": false}}
ls: cannot access '.docs/sdd/specs': No such file or directory
.docs/sdd:
changelog.md
constitution.md
mission.md
roadmap.md
sdd-kit.json

>>> Bash: cd "<run>/salas" && cat tests/slots.test.js src/report.js .docs/sdd/roadmap.md .docs/sdd/changelog.md README.md; git branch; grep -rn "reserve" --include=*.js . ; node -e "import('./src/slots.js').then(m=>console.log(m.reserve('','10-12')))"; ls <home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0062-plan-entry/be24c358-5880-40fc-bfb7-1f7e1b6bf0ea/scratchpad/kit-green/skills/sdd-start-task/references <home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0062-plan-entry/be24c358-5880-40fc-bfb7-1f7e1b6bf0ea/scratchpad/kit-green/skills/sdd-templates
<<< import { test } from 'node:test';
import assert from 'node:assert';
import { reserve } from '../src/slots.js';

test('reserva una sala en una franja', () => {
  assert.deepStrictEqual(reserve('Norte', '10-12'), { room: 'Norte', slot: '10-12' });
});
import { readFileSync } from 'node:fs';

// Convierte la duración de una fila de data/usage.csv a horas.
export function toHours(duration) {
  return parseFloat(duration);
}

export function usageReport(csvPath) {
  const rows = readFileSync(csvPath, 'utf8').trim().split('\n').slice(1);
  const totals = {};
  for (const row of rows) {
    const [ro

>>> Bash: cd "<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0062-plan-entry/be24c358-5880-40fc-bfb7-1f7e1b6bf0ea/scratchpad/kit-green/skills" && cat sdd-start-task/references/nombrado.md sdd-templates/templates/patch-template.md; ls sdd-templates/scripts sdd-templates/templates; cat sdd-start-task/references/commit-milestones.md | head -40
<<< ## Nombrado de carpetas de spec

`<yyyyMMdd-HHmmss>-(task|patch|proposal)-<id>-<slug>`, todo en UTC:

- Timestamp: `Get-Date -AsUTC -Format 'yyyyMMdd-HHmmss'` (PowerShell).
- `task`, `patch` o `proposal` según el carril. Una carpeta que contiene `patch.md` SIEMPRE va prefijada `patch-`; una que contiene `proposal.md`, `proposal-` (la crea `sdd-plan`).
- `<id>`: depende del modo declarado en `.docs/sdd/sdd-kit.json` (`ids.mode`; sin campo ⇒ `tracker`). En `tracker`: el id del ticket en el gestor del proyecto, `0000` si no hay. En `sequence`: el id reservado en la fila del roadmap de la ta

>>> Bash: cd "<run>/salas" && git log --oneline --all --graph | head; git status --short; sed -n 1,60p ../../../kit-green/skills/sdd-templates/scripts/Get-NextSddId.ps1
<<< * 5370242 feat: base de reservas de salas
<#
.SYNOPSIS
  Reserva (con -Reserve) o propone el siguiente id SDD (task/patch/proposal) para un proyecto en modo ids.mode=sequence.
.DESCRIPTION
  Forma parte del kit SDD (skill sdd-templates). No se copia al proyecto: se ejecuta desde el kit con -ProjectRoot.
  Con -Reserve toma un cerrojo en el directorio común de git y consume los ids en el contador sdd-ids, que comparten
  todos los worktrees de la máquina: un id reservado no vuelve a salir aunque el trabajo se abandone. Sin -Reserve
  solo propone el id y no escribe nada.
  El escaneo inicializa

>>> Bash: cd "<run>/salas" && pwsh -NoProfile -File ../../../kit-green/skills/sdd-templates/scripts/Get-NextSddId.ps1 -ProjectRoot . -Reserve; pwsh -NoProfile -Command "Get-Date -AsUTC -Format 'yyyyMMdd-HHmmss'"
<<< 0014
20260925-081234

>>> Bash: cd "<run>/salas" && git switch -c feature/0014-sala-obligatoria && mkdir -p .docs/sdd/specs/20260925-081234-patch-0014-sala-obligatoria
cat >> tests/slots.test.js <<'EOF'

test('rechaza una sala vacía', () => {
  assert.throws(() => reserve('', '10-12'), { message: 'Sala obligatoria' });
});
EOF
node --test tests/ 2>&1 | grep -E "^# (pass|fail)"
<<< Switched to a new branch 'feature/0014-sala-obligatoria'

>>> Bash: cd "<run>/salas" && node --test tests/slots.test.js 2>&1 | grep -E "^# (pass|fail)|not ok"
<<< (Bash completed with no output)

>>> Bash: cd "<run>/salas" && node --test tests/slots.test.js 2>&1 | tail -30; node --version
<<< ✔ reserva una sala en una franja (1.8779ms)
✖ rechaza una sala vacía (1.6712ms)
ℹ tests 2
ℹ suites 0
ℹ pass 1
ℹ fail 1
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 136.0041

✖ failing tests:

test at tests\slots.test.js:9:1
✖ rechaza una sala vacía (1.6712ms)
  AssertionError [ERR_ASSERTION]: Missing expected exception.
      at TestContext.<anonymous> (file:///<run>/salas/tests/slots.test.js:10:10)
      at Test.runInAsyncScope (node:async_hooks:226:14)
      at Test.run (node:internal/test_runner/test:1402:25)
      at Test.processPendingSubtests (node:internal/test_runner/test:974:18)
 

>>> Bash: cd "<run>/salas" && cat > src/slots.js <<'EOF'
export function reserve(room, slot) {
  if (!room?.trim()) throw new Error('Sala obligatoria');
  return { room, slot };
}
EOF
cat > .docs/sdd/specs/20260925-081234-patch-0014-sala-obligatoria/patch.md <<'EOF'
---
id: 20260925-081234-patch-0014-sala-obligatoria
task: 0014
title: Patch — reserve() exige sala
type: patch
status: done
created: 2026-09-25
branch: feature/0014-sala-obligatoria
commit: <hash>
---

# Patch 0014 — reserve() exige sala

## 1. Síntoma

`reserve('', '10-12')` reserva una sala sin nombre; debería lanzar el error «Sala obligatoria».
Medido: devuelve `{ room: '', slot: '10-12' }`, igual que lo reportado.

## 2. Causa raíz

`src/slots.js:1-3`: `reserve` devuelve `{ room, slot }` sin validar ningún argumento. Evidencia:
reproducido con `node -e` (devuelve el objeto con `room: ''`) y con un test que fallaba con
«Missing expected exception». `reserve` no tiene más callers en el repo (solo `tests/slots.test.js`),
así que la guarda va en la propia función.

## 3. Fix

- **Fichero(s)**: `src/slots.js`, `tests/slots.test.js`
- **Cambio**: `reserve` lanza `Error('Sala obligatoria')` si `room` es vacío, solo espacios o nulo.

## 4. Verificación

| # | Caso | Resultado |
| --- | --- | --- |
| 1 | `reserve('', '10-12')` lanza «Sala obligatoria» (test nuevo, RED antes del fix) | ✅ verificado por el agente |
| 2 | `reserve('Norte', '10-12')` sigue devolviendo la reserva | ✅ verificado por el agente |
EOF
node --test tests/ 2>&1 | grep -E "^ℹ (pass|fail)"
<<< ℹ pass 2
ℹ fail 0

>>> Bash: cd "<run>/salas" && git add -A && git commit -q -F - <<'EOF'
fix(slots): lanzar «Sala obligatoria» al reservar sin sala

`reserve('', '10-12')` devolvía una reserva con `room` vacío porque la
función no validaba sus argumentos. Ahora lanza `Error('Sala obligatoria')`
si la sala es vacía o solo espacios. Incluye test y patch 0014.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
git log --oneline -2; git status --short
<<< ac36bf5 fix(slots): lanzar «Sala obligatoria» al reservar sin sala
5370242 feat: base de reservas de salas

=== RESULTADO (16 turnos, 0.3656784000000001 $)
Fix commiteado (`ac36bf5`) en la rama `feature/0014-sala-obligatoria`. El patch queda abierto, sin `sdd-end-patch`.

- **Causa raíz:** `reserve` en `src/slots.js` devolvía `{ room, slot }` sin validar nada. Lo reproduje: `reserve('', '10-12')` devolvía `{ room: '', slot: '10-12' }`. No tiene más callers que el test.
- **Fix:** `reserve` lanza `Error('Sala obligatoria')` si la sala es vacía, solo espacios o nula.
- **Tests:** añadí un test que falló antes del fix, y con el fix pasan los 2 tests.
- **Patch:** `.docs/sdd/specs/20260925-081234-patch-0014-sala-obligatoria/patch.md`. Le falta el hash del commit, que se escribe al cerrar.
- **Id y rama:** reservé el id `0014` con `Get-NextSddId.ps1 -Reserve`. Estabas en `feature/0013`, que es el ítem «aforo» del roadmap, así que no la renombré. Creé `feature/0014-sala-obligatoria` desde el mismo commit y `feature/0013` queda intacta.

Pregunta: ¿la sala con solo espacios (`'  '`) también debe rechazarse? Lo hice así porque sigue siendo una sala sin nombre. Si solo quieres rechazar la cadena vacía, cambio la comprobación.
