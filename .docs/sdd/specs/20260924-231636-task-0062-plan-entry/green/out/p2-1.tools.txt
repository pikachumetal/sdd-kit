
>>> Skill: sdd-kit:sdd-plan
<<< Launching skill: sdd-kit:sdd-plan

>>> Bash: cd "<run>/salas" && git branch --all && git worktree list && cat .docs/sdd/sdd-kit.json && echo ---- && git show develop:.docs/sdd/roadmap.md && ls .docs/sdd .docs/sdd/specs 2>/dev/null
<<< Exit code 2
* develop
  main
<run>/salas a27ac91 [develop]
{"version": "1.1.0", "channel": "plugin", "ids": {"mode": "sequence"}, "release": {"hasRecipient": true}, "control": {"profile": "delegate"}, "merge": {"into": "develop", "noFf": true, "removeWorktree": false}}
----
# Roadmap — salas

## Próximo

| # | Ítem | Estado |
| --- | --- | --- |
| 0012 | Validar el formato de la franja al reservar (`10-12`, no `1012`) | ⏳ |
| 0013 | Aforo de cada sala en `salas libres` | ⏳ |

## Backlog

| # | Ítem | Origen |
| --- | --- | --- |
| — | Recordatorio el día antes de una reserva | equipo de oficin

=== RESULTADO (4 turnos, 0.1701548 $)
Aún no he escrito nada. En `delegate` hay que proponer la fila y esperar tu decisión, y tengo una pregunta.

Es un item concreto: una sola fila y sin propuesta. La lectura del estado ya está hecha. `develop` es la única rama, no hay ramas `feature/*`, los ids van en `sequence` y el último es el 0013.

**¿Dónde apunto "Exportar las reservas a CSV"?**

1. **Próximo, con id nuevo (0014), estado ⏳.** Es lo que recomiendo, porque pediste apuntarlo como trabajo a hacer. Reservaría el id con `Get-NextSddId.ps1 -Reserve`, lo pondría tras el 0013 y haría un commit solo de `roadmap.md` en `develop`.
2. **Backlog, sin id (`—`), con origen.** Sería una idea sin compromiso, como los recordatorios y las reservas recurrentes que ya hay. No consume id.

Cuando me respondas escribo la fila, hago el commit y te digo con qué se arranca (`sdd-start-task`). No arranco nada.
