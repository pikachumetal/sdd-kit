
>>> Skill: sdd-kit:sdd-plan
<<< Launching skill: sdd-kit:sdd-plan

>>> PowerShell: git branch --all; git worktree list; Get-Content .docs/sdd/sdd-kit.json; git show develop:.docs/sdd/roadmap.md
<<< * develop
  main
<run>/salas ae60282 [develop]
{"version": "1.1.0", "channel": "plugin", "ids": {"mode": "sequence"}, "release": {"hasRecipient": true}, "control": {"profile": "delegate"}, "merge": {"into": "develop", "noFf": true, "removeWorktree": false}}
# Roadmap — salas

## Próximo

| # | Ítem | Estado |
| --- | --- | --- |
| 0012 | Validar el formato de la franja al reservar (`10-12`, no `1012`) | ⏳ |
| 0013 | Aforo de cada sala en `salas libres` | ⏳ |

## Backlog

| # | Ítem | Origen |
| --- | --- | --- |
| — | Recordatorio el día antes de una reserva | equipo de oficina |
| — | Reserv

>>> PowerShell: git ls-files; Test-Path .docs/sdd/releases; Get-Content .docs/sdd/constitution.md -ErrorAction SilentlyContinue | Select-Object -First 30; Select-String -Path src/*.js -Pattern 'slot|libres|aforo|capacity' | Select-Object -First 30
<<< .docs/sdd/changelog.md
.docs/sdd/constitution.md
.docs/sdd/mission.md
.docs/sdd/roadmap.md
.docs/sdd/sdd-kit.json
.gitignore
README.md
data/usage.csv
src/report.js
src/slots.js
tests/slots.test.js
False
# Constitution — salas

## Art. I — Commits

Tipo/scope en inglés, título y cuerpo en castellano, nunca title-only.

## Art. II — Tests

Suite: `node --test`. Todo verde antes de fusionar.

src\slots.js:1:export function reserve(room, slot) {
src\slots.js:2:  return { room, slot };

=== RESULTADO (5 turnos, 0.1857816 $)
Estado leído: `develop` es la única rama (no hay `feature/*`) y no hay `releases/` previa, así que no hay retro que sumar. La última release cerrada es v1.2.0. Con `control.profile: delegate` te propongo el scope y espero tu decisión antes de escribir nada o reservar ids.

## Propuesta para Release 1.3

**Entra (recomendado)**

| Ítem | Origen | Ficheros que toca | Razón |
| --- | --- | --- | --- |
| 0012 Validar formato de franja (`10-12`, no `1012`) | roadmap, Próximo | `src/slots.js` (`reserve`) | Riesgo primero: hoy una franja mal escrita se acepta sin error. |
| 0013 Aforo por sala en `salas libres` | roadmap, Próximo | `src/slots.js` y el módulo del comando `salas libres` (aún no existe en el repo) | Ya está en Próximo. Es una feature visible para quien recibe la release. |

**Se queda fuera**

- **Recordatorio el día antes** y **Reservas recurrentes** (Backlog, equipo de oficina). Son grandes, y la recurrencia toca las reglas de solape. Cada una necesitaría su propuesta con reglas y ejemplos. Los dos ítems son de la misma petición, así que te sugiero planificarlos aparte para 1.4.
- **Tests de franjas límite (`00-24`)** (deuda técnica). Solo lo meto si quieres, o como prerrequisito de 0012. Yo lo meto: 0012 cambia la validación de franjas en `src/slots.js`, que hoy no tiene tests de `00-24`, y el freno de regresión sale barato. Sería una fila nueva con id reservado.

**Bloqueos:** ninguno detectado. 0013 no dice de dónde sale el aforo (¿dato fijo por sala, fichero de configuración?). Es una decisión de la task, no bloquea el scope.

**Estado:** en preparación. Ya me constaba que la release se entrega a alguien (`hasRecipient: true`), pero «comprometida» solo si tú lo dices.

**Pregunta (bloquea la escritura):** ¿la release queda en preparación, o la marco comprometida con una fecha? Si me dices «vale» sin más, la dejo en preparación.

Cuando confirmes el scope, y en concreto si entra o no la deuda de `00-24`:
1. Reservo el id de la deuda, si entra, con `Get-NextSddId.ps1 -Reserve`.
2. Escribo `## Release 1.3` en el roadmap, con 0012 y 0013 tal cual (ya tienen id).
3. Commiteo en `develop` un commit que solo toca `roadmap.md`.

No arranco nada. Empezaría por 0012, con `sdd-start-patch` si lo ves determinista o con `sdd-start-task` si prefieres spec.
