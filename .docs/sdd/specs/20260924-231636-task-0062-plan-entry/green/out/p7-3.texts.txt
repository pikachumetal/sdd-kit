--- [3]
Ahora: leer el contexto SDD del proyecto. Queda: enrutado y la primera pregunta, ~2 min.

--- [10]
Ahora: contexto SDD leído. Queda: tu respuesta a esta primera pregunta; después la spec (brainstorming), ~10 min hasta el gate.

**Contexto**
- Proyecto: `salas`, CLI interna de reservas de salas. Usuarios: equipo de oficina y clientes externos.
- Constitution: Art. I, commits con tipo/scope en inglés y título y cuerpo en castellano. Art. II, suite `node --test` en verde antes de fusionar.
- No existen `tech-stack.md`, `architecture.md` ni `capabilities/`.
- Config del proyecto (`sdd-kit.json`): ids en modo `sequence`, perfil `delegate`, merge a `develop` con `--no-ff`. No hay `sdd-kit.local.json`, así que no ignoro ninguna clave local.
- Rama actual: `feature/0013`, y el roadmap tiene la fila pendiente **0013 · Reservas recurrentes (cada lunes), tras 0012: usa su validación de franja**.

**Problema de dependencia**
La 0013 depende de la 0012 (validar el formato de la franja, `10-12` y no `1012`). La 0012 sigue ⏳ y no está en el historial: solo existe el commit `da830f0`. Sin ella, la 0013 no tiene la validación de franja que debe reutilizar.

**Pregunta: ¿cómo arrancamos?**

Propongo este enunciado, tomado de la fila del roadmap: *reservas recurrentes (cada lunes) en `salas`, reutilizando la validación de franja de la 0012*.

- Carril: task, no patch ni spike. Añade comportamiento nuevo y no es un bug determinista.
- Modo: **full**. No propongo lite porque no he podido comprobar sus condiciones (sin leer el código de la feature, que este paso me prohíbe). Una recurrencia probablemente añade un concepto nuevo a la reserva y quizá cambia el modelo de datos, y eso rompe la condición de no tocar schema. Si prefieres lite, tendría que verificarlas una por una antes de que lo confirmes.
- Perfil: **`delegate`**, que sale del nivel de proyecto (`sdd-kit.json`). No hay perfil de release ni de task que lo pise. Puedes cambiarlo para esta task.
- Rama: `feature/0013` ya existe, así que no creo otra.

Opciones:
1. **Full, `delegate`, y esperar a la 0012 (recomendada).** Escribo la spec ya, pero no implemento hasta que la 0012 esté fusionada en `develop`, para reutilizar su validación en vez de duplicarla.
2. Full, `delegate`, y arrancar sin esperar la 0012. Implementaría una validación de franja propia y provisional, lo que arriesga duplicarla o divergir de la 0012.
3. Lite, si confirmas que se cumplen las condiciones del predicado (flujo existente, sin contratos públicos, sin schema, un solo módulo).
4. Cualquiera de las anteriores con otro perfil (`pair` o `unattended`) para esta task.
5. Cualquiera de las anteriores más «apruebo la spec por delegación, nos vemos en la validación», si vas a ausentarte. Quita la parada de la spec, pero la validación final del trabajo se mantiene siempre.

¿Qué opción eliges?

