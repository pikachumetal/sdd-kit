--- [12]
Commit falló por la sintaxis de PowerShell; uso un fichero temporal para el mensaje.

--- [14]
Reunión con Acme reflejada en el roadmap y en una propuesta nueva, ya commiteada en `develop` (`d76d767`). No arranqué ninguna task ni toqué código.

- **0012 (validar franja):** pasa a una sección «Aparcadas» como `⏸️ aparcada: descartada por Acme, 2026-09-25`. No la borré.
- **0015:** exportar reservas a CSV. Va primera, porque es lo que Acme quiere ver antes.
- **0016:** franja mínima de 30 minutos.
- **0017:** email a los asistentes al cancelar una reserva.
- **0013 (aforo):** la dejé donde estaba, detrás de las nuevas.
- **Propuesta:** `.docs/sdd/specs/20260925-110657-proposal-0014-reunion-acme/proposal.md`. Lleva tus notas literales como acta, las reglas con ejemplos con datos y el reparto de las tres features.

**Decisiones que tomé por ti** (también quedaron en el cuerpo del commit):
- **Formato del CSV:** el comando será `salas exportar <mes>`, con columnas `mes,sala,franja,estado`. Las reservas canceladas salen con `estado=cancelada`.
- **Asistentes:** hoy no existen en el modelo (solo `reserve(room, slot)`). Se captan con `--asistentes a@x,b@y` al reservar, y eso va dentro de 0017. Sin asistentes, no se envía nada al cancelar.
- **Franja mínima:** la aplica `salas` aunque el calendario de Acme valide el formato. `10-12` sigue siendo válido y `10:00-10:30` es el formato nuevo.
- **Dependencias:** no hay ninguna entre 0015, 0016 y 0017. Tampoco abrí una sección de release.

Arranca con `sdd-start-task` sobre 0015. Conviene confirmar con Acme dos cosas: si quieren las canceladas en el CSV y si `--asistentes` les sirve como forma de captar los emails.

