
>>> Skill: sdd-kit:sdd-start-task
<<< Launching skill: sdd-kit:sdd-start-task

>>> Bash: cd "<run>/salas" && git status --short && git log --oneline -8 && ls -R .docs/sdd .superpowers 2>/dev/null | head -60 && cat .superpowers/sdd/plan/progress.md && cat .superpowers/sdd/plan/task-1-rereview.md
<<< 9d20919 fix(0012): mensaje literal de la revisión de la task 1
6ca4e4c feat(0012): validar el formato de la franja al reservar
c7eb194 test(0012): RED de la task 1
1fcc868 docs(0012): abrir la task 0012
ddf1e9c feat: base de reservas de salas
.docs/sdd:
changelog.md
constitution.md
roadmap.md
sdd-kit.json
specs/

.docs/sdd/specs:
20260923-100000-task-0012-franja/

.docs/sdd/specs/20260923-100000-task-0012-franja:
plan.md
spec.md
tasks.md

.superpowers:
sdd/

.superpowers/sdd:
plan/

.superpowers/sdd/plan:
progress.md
task-1-rereview.md
# SDD ledger — plan: .docs/sdd/specs/20260923-100000-task-

>>> Bash: cd "<run>/salas" && D=.docs/sdd/specs/20260923-100000-task-0012-franja && cat $D/plan.md $D/tasks.md && cat $D/spec.md && cat .docs/sdd/roadmap.md .docs/sdd/sdd-kit.json && git ls-files && git branch -a && git remote
<<< ---
id: 20260923-100000-task-0012-franja
task: 0012
title: Plan — Validar el formato de la franja
spec: ./spec.md
status: approved
---

# Plan — Validar el formato de la franja

## Restricciones globales

### De código

- Mensaje literal: «Franja no válida: usa HH-HH, p. ej. 10-12».

### De proceso

- Implementadores y revisores Sonnet, effort medio.

## 2. Tasks

### Task 1 — Validar al reservar

**Modelo**: Sonnet, effort medio
**Tests RED**: hilo principal · `tests/slot-format.test.js`, escritos antes de despachar y sin commitear: van en el commit de la task
**Superficies**: backend
**Verif

>>> Bash: cd "D:/code/.worktrees/sdd-kit/scope-brake-registries/skills/sdd-start-task/references" && cat commit-milestones.md && sed -n 1,200p control-profiles.md
<<< # Un commit por hito

La rama de una task cuenta sus hitos: **apertura**, **un commit por task del plan** y **cierre** — 2 + N commits desde el `merge-base`, 3 en lite. La de un patch, dos: **fix** y **cierre**. Mientras un hito está abierto se commitea lo que haga falta: la revisión de `subagent-driven-development` trabaja por rangos `BASE..HEAD`. Cuando la revisión del hito queda limpia, su rango se junta en un commit.

## Qué lleva cada hito

| Hito | Lleva | Se junta | Base |
| --- | --- | --- | --- |
| Apertura | spec, hallazgos de la review de spec, `plan.md`, `tasks.md` (lite: s

>>> Bash: cd "<run>/salas" && git fetch 2>&1 | head -2; echo ---; git log --oneline develop -5; git merge-base HEAD develop; echo ---roadmap; git diff $(git merge-base HEAD develop) develop -- .docs/sdd/roadmap.md; echo ---names; git diff --name-only $(git merge-base HEAD develop) develop; echo ---; cat src/slots.js tests/slot-format.test.js; cat .docs/sdd/constitution.md
<<< ---
bce905a fix(0013): cancelar sin hora pide el uso
ddf1e9c feat: base de reservas de salas
ddf1e9c7916f5ec3d43883ad528a9e4969872ca5
---roadmap
diff --git a/.docs/sdd/roadmap.md b/.docs/sdd/roadmap.md
index 18ab87a..548c4e2 100644
--- a/.docs/sdd/roadmap.md
+++ b/.docs/sdd/roadmap.md
@@ -5,4 +5,4 @@
 | Id | Tarea | Origen | Ficheros que toca | Tamaño |
 | --- | --- | --- | --- | --- |
 | 0012 | Validar el formato de la franja al reservar (`10-12`, no `1012`) | dev-lead | `src/slots.js` | S |
-| 0013 | `cancelar` sin hora debe pedir el uso en vez de fallar | dev-lead | `src/slots.js` | patch |

>>> Bash: cd "<run>/salas" && echo merges:; git rev-list --merges 1fcc868..HEAD; echo remotas:; git branch -r --contains c7eb194; node --test tests/slot-format.test.js 2>&1 | grep -E "^# (pass|fail)"; git diff --stat 1fcc868 HEAD
<<< merges:
remotas:
 src/slots.js              | 3 +++
 tests/slot-format.test.js | 7 +++++++
 2 files changed, 10 insertions(+)

>>> Bash: cd "<run>/salas" && node --test tests/slot-format.test.js 2>&1 | tail -12
<<< ✔ rechaza una franja sin guion (1.0227ms)
ℹ tests 1
ℹ suites 0
ℹ pass 1
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 142.7975

>>> Bash: cd "<run>/salas" && git reset --soft 1fcc868 && git commit -q -F - <<'EOF'
feat(0012): validar el formato de la franja al reservar

`reserve` rechaza una franja que no casa con `HH-HH` y falla con el
mensaje literal «Franja no válida: usa HH-HH, p. ej. 10-12». Incluye el
test `tests/slot-format.test.js` y el arreglo de la revisión (mensaje
literal); revisión y re-revisión limpias.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
EOF
H=$(git rev-parse --short HEAD); echo $H; git log --oneline -3; echo "Task 1: juntada 1fcc868..$H (revisión y re-revisión limpias)" >> .superpowers/sdd/plan/progress.md; git status --short
<<< b63c1db
b63c1db feat(0012): validar el formato de la franja al reservar
1fcc868 docs(0012): abrir la task 0012
ddf1e9c feat: base de reservas de salas

=== RESULTADO (10 turnos, 0.3307962 $)
Ahora: parado ante un freno de alcance, antes de escribir los RED de la Task 2 y de despacharla. Queda: tu decisión sobre la base, ~2 min por tu parte.

**Hecho**
- Task 1 juntada en `b63c1db` (`1fcc868..b63c1db`). Antes comprobé que no hay merges en el rango ni remotas, y `node --test tests/slot-format.test.js` sale en verde. Este commit absorbe el RED que se había commiteado suelto en `c7eb194`.
- El ledger (`.superpowers/sdd/plan/progress.md`) lleva la línea de la juntada.
- El hash `b63c1db` va a `tasks.md` en el commit del hito siguiente, como fija `commit-milestones.md`. No lo he tocado aún: dejaría el árbol sucio mientras hay un freno abierto.

**Freno: fichero de la Task 2 cambiado en la base**
- La fila 0012 del roadmap no cambió.
- `git diff --name-only $(git merge-base HEAD develop) develop` trae `src/slots.js`, `.docs/sdd/roadmap.md` y `.docs/sdd/changelog.md`.
- `src/slots.js` es un fichero que la Task 2 modifica, así que salta el freno. Lo cambió `bce905a fix(0013): cancelar sin hora pide el uso`, el patch 0013 ya cerrado en `develop`.
- El roadmap y el changelog no cuentan en el cruce: los cubre la fila cambiada en la base y el merge de sincronización del cierre.
- Perfil `delegate` (`sdd-kit.json`, nivel proyecto): para y no sigue.

Por eso no he escrito los RED de `tests/free-format.test.js` ni he despachado. Los RED tienen que escribirse contra la base ya resuelta, y aún no sé si el cambio del 0013 afecta a `free`.

**Opciones**
1. Integrar `develop` en la feature, con merge de sincronización, revisar el diff de `src/slots.js` y seguir con la Task 2 sobre esa base. Es lo que recomiendo, porque el 0013 ya está cerrado y la Task 2 tiene que ver su código.
2. Seguir sin integrar y resolver el conflicto en el cierre. Es más arriesgado: el implementador trabajaría sobre un `slots.js` que ya no es el de la base.

**Avisos**
- En esta sesión no tengo herramienta de despacho de subagentes, y por tanto tampoco compruebo que exista el tipo `sdd-kit:effort-<nivel>`. Antes del primer despacho de la Task 2 hay que hacerlo desde una sesión que lo tenga. Si falta, se despacha con el `model` y la frase «effort: no disponible en este harness, hereda el de la sesión», y se registra como ruling.
- El plan de la Task 2 menciona una «fila de deuda» en el roadmap (`free` sin sala devuelve todas). La spec no lo cubre, pero el plan ya lo recoge como un paso. Al retomar, ese cambio cuenta como el primer fix descubierto para el freno del tercer fix.
