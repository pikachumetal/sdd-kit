--- [4]
Proyecto en v1.2.0; falta v2.0.0.

--- [13]
Verificación OK. Commit.

--- [15]
Migré el proyecto de kit v1.2.0 a v2.0.0 y lo dejé commiteado (`4f16a9d`). Como ya existía `.docs/sdd/`, apliqué solo la migración, sin onboarding.

- **Migración aplicada:** `v2.0.0` era la única que faltaba.
- **Historial:** borré la sección `## Historial` de `bookings.md`. Sigue en git.
- **Propósito:** añadí `## Propósito` a las 9 capacidades. En 8 lo saqué del párrafo de procedencia que había bajo el título, y ese párrafo desapareció.
- **`quotas.md`:** no tenía párrafo. El propósito («Tope semanal de horas reservadas por departamento.») lo redacté a partir del título de su único requisito.
- **`access.md`:** conservé la mención a `src/auth.js` dentro del propósito.
- **`house-rules.md`:** su bloque `## Reglas de la capacidad` es legítimo en v2.0.0, así que no lo toqué.
- **Marcador:** `sdd-kit.json` queda en `"version": "2.0.0"` con fecha 2026-09-25 y las demás claves intactas.

`Test-Capabilities.ps1` da «Capacidades válidas: 9» y no queda ningún `## Historial`.

No hay gates pendientes ni preguntas abiertas. Conviene revisar en el diff los 9 propósitos, que son texto mío (los de `quotas` y `access` son los más interpretados).

Los conectores de claude.ai (Gmail, Calendar, Drive, Slack) piden autorización desde sus ajustes; no los necesité.

