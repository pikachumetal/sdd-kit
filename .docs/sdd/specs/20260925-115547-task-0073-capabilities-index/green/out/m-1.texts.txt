--- [5]
Marcador en 1.2.0; falta v2.0.0.

--- [13]
Ahora el marcador y el commit.

--- [15]
Migré el proyecto de la v1.2.0 a la v2.0.0 del kit y lo dejé commiteado (`6e79bc3`, `chore(sdd): migrar al kit v2.0.0`). Como ya existía `.docs/sdd/`, no hice onboarding, sino solo la migración pendiente. De las migraciones del kit, la única que faltaba era `v2.0.0.md`.

- **Historial:** borré la sección `## Historial` de `bookings.md`, la única que la tenía. Las otras ocho capacidades no la tenían.
- **Propósito:** añadí `## Propósito` (una frase por capacidad) a las nueve capacidades: access, billing, bookings, calendar-sync, house-rules, notifications, quotas, rooms y usage-reports. En las siete que tenían un párrafo de procedencia bajo el título, lo quité, como pide la migración. Ese párrafo era del tipo «La declaró la spec de la task…».
- **`access.md`:** su párrafo decía además que el login con tarjeta vive en `src/auth.js`. Lo quité con el resto, así que esa referencia ya no está.
- **`house-rules.md`:** su párrafo decía que las normas antes vivían en `bookings`. Ese dato tampoco se conserva.
- **Marcador:** `sdd-kit.json` queda en `"version": "2.0.0"` con fecha 2026-09-25; el resto de claves no cambió.
- **Verificación:** `Test-Capabilities.ps1` da «Capacidades válidas: 9». No queda ningún `## Historial`.

No hay gates pendientes. No hice inventario, cosecha, reglas de oro ni regeneración de documentos, porque no forman parte de una migración. No existe `CLAUDE.md` en el repo, y no lo creé.

Los propósitos están redactados por mí; conviene revisarlos en el diff (`git show 6e79bc3`).

