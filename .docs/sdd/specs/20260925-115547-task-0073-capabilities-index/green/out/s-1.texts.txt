--- [3]
Ahora: cargar el contexto SDD (docs, no código). Queda: elegir carril y rama, redactar y commitear la spec, ~10 min.

--- [20]
Ahora: redactar `spec.md` de la task 0021 (rama `feature/0021-invitado-1h` ya creada). Queda: repaso de coherencia y commit, ~3 min.

--- [24]
Spec de la task 0021 escrita y commiteada (`733a069`) en la rama `feature/0021-invitado-1h`. Paro antes del plan, como pediste.

La spec está en `.docs/sdd/specs/20260925-122815-task-0021-invitado-1h/spec.md`. Es modo full, perfil `delegate`, y la aprobación por delegación va registrada con tu frase literal. No he lanzado review de spec: solo se cumple una señal, un `MODIFIED` en `house-rules`. No he ejecutado ningún test ni `Test-Capabilities.ps1`.

**Decisiones que tomé yo, para que las valides:**
1. **Tope por reserva:** «1 h seguida» significa 1 h por reserva, como el tope de 2 h actual. Dos reservas de 1 h consecutivas siguen permitidas.
2. **Mensajes:** el invitado recibe `Máximo 1 h por reserva` y el empleado sigue con `Máximo 2 h por reserva`.
3. **Reserva de 1 h justa:** `10-11` se acepta para el invitado y `10-12` se rechaza.
4. **Capacidad tocada:** solo `house-rules`. Un `MODIFIED` de «Duración máxima por rol» y la regla **Límites** reescrita entera.
5. **Fuera de alcance:** franjas con minutos, total por día del invitado y tope del `administrador`.
6. **Ficheros:** el roadmap dice `src/cli.js`, pero `house-rules` dice que los límites viven en `data/rules.json`, que no existe en el repo. Lo dejé para el plan.

Solo leí `house-rules`, `access`, `bookings` y `quotas`. Las demás capacidades (`billing`, `notifications`, `calendar-sync`, `rooms`, `usage-reports`) no las abrí, y eso queda anotado en la spec como lo que la ausencia de review deja sin mirar.

Siguiente paso, cuando quieras: `superpowers:writing-plans` para el `plan.md`. La validación final sigue siendo tuya.

