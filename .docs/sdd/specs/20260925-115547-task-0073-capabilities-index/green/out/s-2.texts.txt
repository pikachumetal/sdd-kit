--- [3]
Ahora: leer el contexto SDD (docs, no código). Queda: enrutado, rama, spec y commit, ~5 min.

--- [9]
Ahora: leer capacidades que toca la task (`house-rules`, `access`, `bookings`) y la plantilla de spec. Queda: brainstorming, spec, commit, ~4 min.

--- [12]
Ahora: invocar `superpowers:brainstorming` (paso 4) y crear la rama. Queda: spec, coherencia, commit, ~3 min.

--- [16]
Ahora: escribir `spec.md` en la carpeta de la task. Queda: repaso de coherencia y commit, ~2 min.

--- [21]
Spec de la task 0021 escrita y commiteada (`c8585b5`) en la rama `feature/0021-invitados-1h`. Paro antes del plan, como pediste.

Está en `.docs/sdd/specs/20260925-122813-task-0021-invitados-1h/spec.md`. Es modo full, perfil `delegate` (sale de `sdd-kit.json`, no había fichero local). La aprobación está registrada con tu frase literal y la fecha. No he leído código, solo los docs SDD. No he lanzado `Test-Capabilities.ps1`, que corre al cerrar.

Decisiones mías, que puedes validar cuando vuelvas:
1. **«1 h seguida» = tope por reserva individual.** No se suman reservas contiguas: dos de 1 h pegadas dan 2 h seguidas. Lo dejo en «No entra». Es el hueco más obvio si recepción pretendía un tope real de continuidad.
2. **Mensaje del invitado:** `Los invitados reservan como máximo 1 h seguida`. Sigue el patrón de `Los invitados reservan de lunes a viernes`. El `Máximo 2 h por reserva` queda solo para el empleado.
3. **Solo cambia `house-rules`** (un `MODIFIED` de «Duración máxima por rol» y el valor de «Límites»). No toco `access`, `bookings` ni `quotas`.
4. **1 h justa es válida;** 1 h 30 min no.
5. **Sin review de spec:** la rúbrica da 1 señal de 8, y el repaso de coherencia no encontró contradicciones.

Riesgo para el plan: la capacidad dice que el tope vive en `data/rules.json`, pero la fila del roadmap solo declara `src/cli.js`. Si hay que tocar `data/rules.json`, sale del alcance de la fila y el plan debe registrarlo.

La validación del final no se quita: la haces tú cuando vuelvas.

