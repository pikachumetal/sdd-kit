--- [3]
Ahora: leer el contexto SDD del proyecto. Queda: enrutar, crear rama, escribir y commitear la spec (~10 min).

--- [17]
Ahora: escribo la spec de la 0021 (rama y carpeta creadas). Queda: commitear y parar antes del plan, ~3 min.

--- [21]
Spec de la 0021 escrita y commiteada en `feature/0021-guest-booking-cap` (`30a2509`). Paro antes del plan. Todavía no hay `plan.md` ni código.

**Carpeta:** `.docs/sdd/specs/20260925-120121-task-0021-guest-booking-cap/spec.md`

**Qué cambia:** el invitado pasa de 2 h a 1 h por reserva. Es un `MODIFIED` de «Duración máxima por rol» en `house-rules`, con «Límites» actualizado. El empleado sigue con 2 h.

**Decisiones a validar en la validación:**
1. **«1 h seguida» = tope por reserva.** No detecto reservas pegadas (`10-11` más `11-12` suman 2 h). Es el atajo evidente. Cerrarlo exige mirar las reservas ya guardadas del invitado y la fila del roadmap no lo pide. Si lo quieres, va como task aparte.
2. **El aviso cambia para el invitado.** Ahora ve `Máximo 1 h por reserva`; antes veía `Máximo 2 h por reserva`. Lo he fijado en la spec porque cambia la salida observable.
3. **Exactamente 1 h se permite.** `10-11` se reserva; `10-12` no.
4. **Solo cambia `invitado`.** El `administrador` no tiene tope propio en ninguna capacidad y no lo toco. Las reservas ya guardadas no se cancelan ni se recortan.
5. **Review de spec: ninguna.** Conté solo dos señales, `MODIFIED` y regla de permiso por rol.

**Registrado como delegación:** modo full, perfil `delegate` (nivel proyecto, en `sdd-kit.json`) y tu frase «apruebo la spec por delegación, nos vemos en la validación». No ofrecí lite porque fijaste full, y no propuse partir la task.

**Nota sobre el código:** `src/cli.js` es solo un stub (`run` devuelve `salas <args>`) y no hay `data/rules.json`. El plan tendrá que crear esa parte, no modificarla. La spec dice que los topes viven en `data/rules.json`, que es lo que declara `house-rules`.

Para seguir: dime «sigue» y escribo el plan.

