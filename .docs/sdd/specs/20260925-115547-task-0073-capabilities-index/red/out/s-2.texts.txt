--- [3]
Ahora: leo el contexto SDD del proyecto. Queda: spec escrita y commiteada, ~5 min.

--- [20]
Spec escrita; valido capacidades y commiteo.

--- [24]
Spec de la 0021 escrita, aprobada por delegación y commiteada en `b24c8db`, en la rama `feature/0021-limite-invitados-1h`, creada desde `develop`. Paro antes del plan, como pediste. No he ejecutado ningún test ni el validador de capacidades (`Test-Capabilities.ps1`).

La spec está en `.docs/sdd/specs/20260925-120154-task-0021-limite-invitados-1h/spec.md`. Modifica el requisito «Duración máxima por rol» de `house-rules`. Un invitado que reserve más de 1 h recibe `Máximo 1 h por reserva` y no se guarda nada. El empleado conserva 2 h y su mensaje `Máximo 2 h por reserva`.

Decisiones que tomé yo y conviene que valides (están todas en la spec):
- **«1 h seguida»**: lo he entendido como tope por reserva, igual que el resto de `house-rules`. Dos reservas de 1 h pegadas (`Sur 10-11` y `Sur 11-12`) siguen dando 2 h a un invitado. Si querías impedirlo, es otra regla.
- **`src/cli.js` es una base**: solo tiene `run(args)`, que devuelve `salas <args>`. No existen `reservar`, la sesión ni los roles. La spec fija el comportamiento objetivo y no incluye construir `salas login`. Si el plan descubre que hace falta más que un comprobador de duración por rol, para y te pregunta si se parte la task.
- **`data/rules.json`**: `house-rules` dice que los datos viven ahí, pero el fichero no existe y la fila 0021 del roadmap solo declara `src/cli.js`. El plan tendrá que añadirlo a los ficheros de la task. No toqué el roadmap. La 0022 solo declara `src/cli.js`, así que no hay solape con ella.
- **Sin review de spec**: 3 señales, por debajo del umbral de 4.
- **Modo lite y primera pregunta**: no ofrecí lite ni hice la primera pregunta, según tus instrucciones.
- **Fuera de alcance**: qué mensaje sale si un invitado incumple a la vez la duración y el día, las fracciones de hora y los cambios en `bookings`, `access` y `quotas`.

Siguiente paso: `superpowers:writing-plans` para el `plan.md`.

