--- [3]
Ahora: cierro el hito de la Task 1 (leo la regla de hitos y el ledger). Queda: commit del hito y parar antes de la Task 2, ~5 min.

--- [9]
Guardas limpias: sin merges y sin remoto. El rango es un solo commit, pero su cuerpo no lleva tildes. Reescribo el mensaje del hito.

--- [11]
Hito de la Task 1 cerrado en `ee730a3`, sobre la base `5544d5d`. No he despachado la Task 2.

- **Guardas:** el rango no tenía merges y no hay remoto, así que no había nada publicado. Se podía juntar.
- **Juntado:** el rango era un solo commit, `bf709dd`. Aun así reescribí el mensaje yo, porque el del implementador no llevaba tildes ("Anade", "validacion"). El mensaje nuevo sigue la convención del proyecto y lleva el `Co-Authored-By`.
- **Test:** `node --test tests/slot-format.test.js` sale en verde (2 pass, 0 fail) sobre el commit del hito.
- **Ledger:** `.superpowers/sdd/plan/progress.md` lleva ahora `Task 1: milestone squashed 5544d5d..ee730a3`.
- **`tasks.md`:** no lo he tocado. Sigue con la Task 1 en `pending`. Según la regla de hitos, el hash `ee730a3` y el estado ✅ entran en el commit del hito de la Task 2, no en este. Ese mismo commit lleva el estado final de la Task 1, así que hasta entonces `tasks.md` no refleja el ledger.
- **Ruling:** reescribir el mensaje del implementador no es un desvío del plan, así que no lo he registrado como ruling.

**Pendiente para la Task 2**, que queda parada:
1. Comprobar si la fila de la Task 2 cambió en la base, con `git diff` de `.docs/sdd/roadmap.md` contra `develop`. Con el dev-lead ausente, si aparece la fila es un freno de alcance y no despacho.
2. Cruzar `git diff --name-only` de la base con los ficheros de «Crear» y «Modificar» de la Task 2. Si coincide alguno, es el mismo freno.
3. Escribir los RED de la Task 2 sin commitear y guardar una copia fuera del repo.
4. Comprobar que el tipo `sdd-kit:effort-medium` está entre los agentes de la sesión.
5. Despachar con el bloque «De código» de las Restricciones globales al principio del encargo.

