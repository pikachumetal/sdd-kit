--- [3]
Ahora: retomo la ejecución Native del plan de la 0012 en la Task 3. Queda: Task 3 hasta registrarla en el ledger, ~10 min.

--- [11]
Base sin cambios. Escribo el RED y guardo copia fuera del repo.

--- [15]
Task 3 queda completa en el ledger: `Task 3: complete (commits 436d675..b0113ea)`. Paro aquí, sin empezar la Task 4.

- **RED:** `tests/cancel-format.test.js` falló primero porque `slots.js` no exportaba `cancel`. Guardé una copia fuera del repo y `git diff --no-index` la da igual que el test commiteado.
- **Implementación:** `cancel(room, slot)` valida el formato de la franja con el mismo mensaje que `reserve` y `free`.
- **Verificación:** `node --test tests/cancel-format.test.js` pasa. La suite entera (4 tests) también pasa, gracias al hook pre-commit.
- **Base:** `develop` y `main` no cambiaron desde la rama, así que no hay freno de alcance.

**Ruling registrado en el ledger:** extraje un `assertSlot` común en `src/slots.js` para no duplicar por tercera vez el literal del mensaje. Con eso toqué el código de las Tasks 1 y 2, aunque la salida observable no cambia. Si prefieres verlo revertido, es un refactor de 3 líneas.

**Pendiente en `tasks.md`:** su tabla sigue en `pending` para todas las tasks. El plan y el ledger no piden actualizarla aquí, así que la dejé como está. Iría en el commit del hito siguiente, según `commit-milestones.md`.

**Aviso para la Task 4:** el plan dice que, si la sesión se retoma tras una compactación y quedan dos o más tasks sin cerrar, se despachan con subagent-driven-development. Aquí no hubo compactación, así que seguí en Native.

