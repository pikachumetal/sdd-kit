--- [3]
Ahora: cargo el contexto SDD y el plan de la 0012. Queda: Task 1 y Task 2 con su ledger, hasta registrar la Task 2 completa, ~20 min.

--- [19]
Base sin cambios en la fila ni en ficheros. Escribo el test RED de la Task 1 y lo ejecuto.

--- [21]
RED correcto (falla por falta de la validación). Implemento.

--- [25]
Task 1 completa (`a62f929`). Paso a la Task 2.

--- [28]
RED por compilación (`free` no existe). `free` no tiene fuente de salas en el código, así que tras validar devuelve `[]`; lo registro como ruling.

--- [30]
Registro en `tasks.md` (hito) y paro.

--- [32]
Las Tasks 1 y 2 están completas y registradas en el ledger y en `tasks.md`. Paro aquí, antes de la revisión final de rama. Suite completa (`node --test`): 3/3 en verde.

| Task | Commit | Qué hace |
|---|---|---|
| 1 | `a62f929` | `reserve` lanza «Franja no válida: usa HH-HH, p. ej. 10-12» si la franja no casa con `^\d{2}-\d{2}$`. |
| 2 | `271bb5a` | `free(slot)` con la misma validación. |
| Registro | `a1c452d` | Hashes de las dos tasks en `tasks.md`. |

Cada task siguió TDD: escribí el test, lo vi fallar y luego lo puse en verde. La copia de cada RED guardada fuera del repo es idéntica a la commiteada. La base (`develop`) no cambió ni la fila del roadmap ni los ficheros de las tasks. La ejecución fue en línea, sin subagentes, así que no hizo falta comprobar el tipo `effort-*`.

**Rulings que tomé** (el dev-lead no está):
1. **Helper compartido:** la validación va en un helper privado `assertValidSlot`, creado en la Task 1, para no duplicar regex ni mensaje en la Task 2. El plan no lo pedía. Si sobra, hay que renombrar o inlinear tres líneas.
2. **Orden del test RED de la Task 2:** no lo escribí hasta abrirla, porque el pre-commit corre `node --test` sobre todo el árbol, ficheros sin seguimiento incluidos, y habría rechazado el commit de la Task 1.
3. **`free(slot)` devuelve `[]`:** la función no existía y el repo no tiene almacén de reservas. Tras validar devuelve una lista vacía, con un marcador `ponytail:`. La spec solo pide el fallo con franja inválida. El listado real de salas libres queda para otra task.

**Pendiente:** la revisión final de rama, la validación del dev-lead (paso 7) y el cierre con `sdd-end-task`.

