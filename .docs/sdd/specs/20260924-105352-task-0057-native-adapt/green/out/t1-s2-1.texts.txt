--- [3]
Ahora: leo el plan de la task 0012 y compruebo la base antes de despachar la Task 1. Queda: preparar los RED y el encargo del implementador, y parar sin lanzarlo, ~5 min.

--- [20]
Estoy juntando la apertura (spec y plan) en un solo commit, como pide el kit, antes de escribir los RED de la Task 1.

--- [31]
Despacho de la Task 1 preparado y parado justo antes de lanzar el implementador, como pediste. No he lanzado ningún subagente.

**Hecho**
- **Base:** `develop` coincide con el merge-base (`a7381bd`). No hay cambios en la fila 0012 del roadmap ni en ficheros, y no hay remoto.
- **Apertura:** junté spec y plan en un solo commit local sin publicar, `e09dd13`, como pide el kit. Este es `BASE` de la Task 1.
- **RED:** escribí `tests/franja.test.js`, con un test por el THEN de reservar. Falla con «Missing expected exception» y sigue sin commitear. La copia está fuera del repo, en `%TEMP%\tmp.p8xp9boHLn\franja.test.js`, para compararla cuando vuelva el implementador.
- **Workspace de superpowers** (`.superpowers/sdd/plan/`):
  - `progress.md`: el ledger, con la tabla del pre-flight scan, los rulings y la base.
  - `task-1-brief.md`: el texto de la Task 1.
  - `task-1-dispatch.md`: el encargo completo, con las restricciones de código, el contrato de los RED, la sección «Verificación» y las reglas del implementador.

**Rulings registrados, para tu validación**
1. El tipo `sdd-kit:effort-medium` no consta entre los agentes de esta sesión, y no tengo herramienta de despacho. Al lanzar, irá con `model: sonnet` y la frase «effort: no disponible en este harness, hereda el de la sesión». Si el tipo existiera y fallara, cuesta un re-despacho.
2. Juntar la apertura reescribe dos commits locales sin publicar. Es reversible con el reflog.
3. Los RED de la Task 2 se escriben justo antes de despacharla, no ahora. Con ellos en el árbol, el pre-commit (que corre `node --test` entero) rechazaría el commit de la Task 1. Además, `free(slot)` no existe hoy: la Task 2 lo crea.

**Para tu atención**
- La fila 0012 del roadmap dice solo «al reservar». La spec aprobada también cubre «consultar libres», y la spec manda.
- Si la Task 2 copia la regex y el mensaje de la Task 1, quedarán duplicados. Lo decidiré al despacharla, con lo que deje la Task 1.

Siguiente paso, cuando quieras: lanzar el implementador con `task-1-dispatch.md`.

