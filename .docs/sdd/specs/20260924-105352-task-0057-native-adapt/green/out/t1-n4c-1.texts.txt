--- [3]
Ahora: leo el plan y el ledger, y sigo con la Task 3. Queda: Task 3 hasta registrarla en el ledger, ~15 min.

--- [12]
La Task 3 está completa y registrada en el ledger, y no he empezado la Task 4.

- **RED:** `tests/cancel-format.test.js` falló primero con `does not provide an export named 'cancel'`. La copia guardada fuera del repo salió idéntica al comparar antes del commit.
- **Implementación:** `cancel(room, slot)` en `src/slots.js`, con la misma validación de `HH-HH` y el mismo mensaje `Franja no válida`.
- **Commit:** `25a2a03 feat(0012): validar el formato de la franja al cancelar`. La base no había cambiado respecto a `main`.
- **Ledger:** `Task 3: complete (commits 06e1e6e..25a2a03, tests: node --test tests/cancel-format.test.js → ℹ duration_ms 99.815)`. El resumen de `task-done` guardó la última línea de la salida y no `pass 1`; el resultado real fue 1 test pasando, 0 fallos.

Rulings: ninguno. La validación sigue duplicada en `reserve`, `free` y `cancel`, tal como pide el plan. Cuando llegue `move` habrá cuatro copias, así que conviene extraer un helper `assertSlot` en la revisión final.

No he actualizado `tasks.md` con el hash de la Task 3; iría en el commit del hito siguiente.

