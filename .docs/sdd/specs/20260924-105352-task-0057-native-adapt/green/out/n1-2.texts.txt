--- [3]
Ahora: ejecuto la Task 1 y la Task 2 de 0012 en Native. Queda: las dos tasks y parar tras registrar la Task 2, ~20 min.

--- [16]
Base sin cambios en `develop`. Escribo el test RED de la Task 1.

--- [18]
RED por la razón correcta. Implemento, con el helper exportado para reutilizarlo en la Task 2.

--- [24]
RED por compilación (falta `free`). Implemento.

--- [30]
Las Task 1 y Task 2 están completas y registradas en el ledger y en `tasks.md`. Paro aquí, antes de la revisión final de rama.

| Task | Commit | Verificación |
| --- | --- | --- |
| 1, validar al reservar | `3c808fe` | `node --test tests/slot-format.test.js` pasa |
| 2, validar al consultar libres | `e77543f` | `node --test tests/free-format.test.js` pasa |
| Registro en `tasks.md` | `a7b00a5` | — |

La suite completa da 4/4 en verde.

- **Tests:** escribí cada test antes del código y lo vi fallar. En la Task 1 falló por «Missing expected exception»; en la Task 2 por compilación, porque no existía `free`. Guardé una copia de cada RED fuera del repo. El `git diff --no-index` contra el test final salió idéntico en las dos tasks.
- **Código:** `src/slots.js` tiene `assertValidSlot` con el mensaje literal, usado por `reserve` y `free`. No dupliqué el regex ni el mensaje.
- **Base:** comprobé antes de cada task que `develop` no había tocado el roadmap ni los ficheros de la task. No hay remoto.

**Me salí del plan en…** (rulings del ledger, para la validación):
- **Retorno de `free`:** la spec no dice qué devuelve `free(slot)` con una franja válida. Devuelve `[]` porque no hay almacén de reservas, y lleva un marcador `ponytail:` en el código. Si es erróneo, se cambia el retorno cuando exista persistencia.
- **Task 2:** su RED falla por compilación, no por aserción. Lo registré como RED válido.

Un fallo mío en el proceso: en una llamada lancé `task-done` sin la variable del script y no se ejecutó. Lo repetí bien y el ledger tiene la línea `Task 2: complete`.

Falta la revisión final de rama con `superpowers:requesting-code-review`, y después la validación tuya.

