--- [3]
Ahora: leer el plan y las referencias del kit para ejecutar la Task 1 y la Task 2 en línea. Queda: implementar las dos tasks y registrarlas, ~15 min.

--- [14]
Base sin cambios (develop == merge-base). Task 1: test RED primero.

--- [16]
RED por la razón correcta. GREEN mínimo, con el helper ya extraíble para la Task 2.

--- [22]
Task 2: test RED.

--- [24]
RED por compilación (falta el export). GREEN mínimo.

--- [30]
Task 1 y Task 2 están hechas y registradas como completas en `tasks.md`. Paro aquí, antes de la revisión final de rama.

- **Task 1** (`8abff0f`): `reserve` valida `HH-HH` con un helper `assertValidSlot`. Vi el test fallar (`Missing expected exception`) y luego pasar.
- **Task 2** (`0ad133c`): `free(slot)` reutiliza el mismo helper. El test falló porque `slots.js` no exportaba `free` y después pasó.
- **Verificación**: `node --test` da 3/3 en la suite entera. Corrí la suite completa aunque las tasks declaran solo su test, porque el hook `pre-commit` de `.githooks/` la lanza en cada commit.
- **Comprobaciones previas**: la fila de la task y los ficheros no cambiaron en `develop` respecto a la base.

**Me salí del plan en:**
- **`free` solo valida y no devuelve nada.** No existe listado de salas y la spec solo pide el error. Si hace falta que devuelva algo, se añade cuando exista ese listado. Está como ruling en `tasks.md`.
- **Extraje `assertValidSlot`.** El plan no lo nombraba, pero evita duplicar el literal del mensaje en `reserve` y en `free`.

**Pendiente, para que lo sepas:**
- **Cambios sin commitear.** `tasks.md` (hash de la Task 2) y `plan.md` (checkboxes de la Task 2) tienen cambios sin commitear. Un commit no puede contener su propio hash, así que van en el commit de cierre, como marca `commit-milestones.md`.
- **Historia sin juntar.** No he juntado los commits de las tasks: no hubo revisión por task (Native). Lo decidirá el cierre.
- **Sin ledger de superpowers.** No he creado el ledger de `.superpowers/`. El registro durable es `tasks.md`.
- **`.githooks/` sigue sin seguimiento**, como al empezar.

Siguiente paso: la revisión final de rama.

