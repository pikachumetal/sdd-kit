--- [9]
RED (falla porque `cancel` no existe). Implemento.

--- [13]
Task 3 completa y registrada en el ledger (commit `5c780a2`). Su test falló primero (RED) y pasa ahora (GREEN). La suite entera pasa con 4 tests de 4, y el árbol queda limpio.

Paro aquí, sin empezar la Task 4 (mover), como pediste.

`cancel` repite la misma línea de validación y el mismo mensaje que `reserve` y `free`, y `move` la repetirá una cuarta vez. Extraer un helper `assertSlot` sería un refactor pequeño. No lo hice porque el plan no lo pide.

