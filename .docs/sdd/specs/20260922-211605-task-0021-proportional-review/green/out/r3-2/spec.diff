diff --git a/.docs/sdd/specs/20260922-090000-task-0008-booking-code-search/spec.md b/.docs/sdd/specs/20260922-090000-task-0008-booking-code-search/spec.md
index cdf7266..e120374 100644
--- a/.docs/sdd/specs/20260922-090000-task-0008-booking-code-search/spec.md
+++ b/.docs/sdd/specs/20260922-090000-task-0008-booking-code-search/spec.md
@@ -16,10 +16,11 @@ approvers:
 
 ## Decisiones que he tomado yo — valida estas
 
-1. El buscador reconoce un código de reserva con la expresión `^R-\d{4}$` y, si la consulta encaja, busca por código en vez de por cliente — es la forma del código que fija la capacidad `bookings`.
+1. El buscador reconoce un código de reserva con la expresión `^([A-Za-z]+-)?R-\d{4}$` y, si la consulta encaja, busca por código en vez de por cliente. Cubre tanto el código propio (`R-0042`) como el importado con prefijo de sede (`MAD-R-0042`) — la capacidad `bookings` no acota la longitud del prefijo de sede, así que el patrón lo deja abierto en vez de fijarlo a un valor concreto.
 2. Los códigos de las reservas importadas de la otra sede (`MAD-R-0042`) también se reconocen como código — la recepción las atiende igual que las propias.
 3. Búsqueda por código exacta, sin distinguir mayúsculas — un código parcial no identifica una reserva.
 4. Sin capacidad nueva: el delta va a `bookings`.
+5. Repaso de coherencia: el regex de la decisión 1 en el borrador (`^R-\d{4}$`) solo cubría el código propio y contradecía la decisión 2, que exige reconocer también el importado (`MAD-R-0042`). Lo he corregido a `^([A-Za-z]+-)?R-\d{4}$` en la decisión 1, el Approach y el escenario «Consulta que no es un código».
 
 ## Intent
 
@@ -32,7 +33,7 @@ Hoy la recepción solo busca por nombre de cliente, y con dos clientes del mismo
 
 ## Approach
 
-El buscador decide el modo por la forma de la consulta: si encaja con `^R-\d{4}$` busca por código; si no, por cliente como hoy.
+El buscador decide el modo por la forma de la consulta: si encaja con `^([A-Za-z]+-)?R-\d{4}$` busca por código; si no, por cliente como hoy.
 
 ## Delta de comportamiento
 
@@ -46,7 +47,7 @@ El buscador decide el modo por la forma de la consulta: si encaja con `^R-\d{4}$
 
 **ADDED — Consulta que no es un código**
 - GIVEN reservas de varios clientes
-- WHEN el recepcionista escribe un texto que no encaja con `^R-\d{4}$`
+- WHEN el recepcionista escribe un texto que no encaja con `^([A-Za-z]+-)?R-\d{4}$`
 - THEN se busca por cliente, como en «Búsqueda por cliente»
 
 ## Enmiendas
