---
id: 20260923-100000-task-0012-franja
task: 0012
title: Walkthrough — Validar el formato de la franja
spec: ./spec.md
plan: ./plan.md
status: done
created: 2026-09-25
---

# Walkthrough — Validar el formato de la franja

## 1. Cambios realizados

- Reservar: `reserve` en `src/slots.js` lanza «Franja no válida: usa HH-HH, p. ej. 10-12» si la franja no casa con `/^\d{2}-\d{2}$/`. Test: `tests/slot-format.test.js`. Commit `7e7abe9`.
- Consultar libres: `free(slot)` en `src/slots.js` aplica la misma validación y el mismo mensaje. Test: `tests/free-format.test.js`. Commit `136114a`.
- Revisión final de rama: limpia (`review-final.md`), commit `3e759b4`.

## 2. Tiempo y coste: estimado vs real

- Tipo: backend
- Estimación de implementación (del plan): no consta — `plan.md` no trae estimación en horas
- Esfuerzo real: no medido — no hay reloj del hilo; las marcas de los commits (09:07:37 a 09:07:42 del 2026-09-25) abarcan segundos y no reflejan trabajo real. Aproximado, no comparable con la estimación
- Desviación: no calculable (falta la estimación)
- Causa de la desviación (obligatoria si |desviación| > 30%): no aplica, sin desviación calculable
- Modelo del hilo: claude-sonnet-5
- Tokens del hilo: 135.002 - claude-sonnet-5 135.002
- Tokens de subagentes: no aplica
- Coste de la sesión: 0,16 $ (hilo 0,16 $)
- Coste de sujetos: no aplica
- Review de spec: no · hallazgos 0, aceptados 0

> Las líneas de tokens y coste salen de `Measure-SessionTokens.ps1` sin retocar. Miden la sesión de cierre; lo anterior a esta sesión (planificación y ejecución de las tasks) no consta en ese transcript.

## 3. Desviaciones del plan

- _Ninguna_

### Decisiones tomadas sin el dev-lead

- _Ninguna_

## 4. Verificación

### 4.1 Builds

- `node --test` en el worktree (2026-09-25): 3 tests, 3 pasan, 0 fallan.

### 4.2 Smoke / tests

- Validado por el dev-lead: 2026-09-25 · «he probado `node bin/salas.js reservar Norte 1012` y `node bin/salas.js libres 1012`, los dos dan el error de franja; funciona».
- Verificado por el agente (2026-09-25): los mismos dos comandos, con la salida y el código de salida reales.

| # | Caso | Resultado |
| --- | --- | --- |
| 1 | `node bin/salas.js reservar Norte 1012` | «Franja no válida: usa HH-HH, p. ej. 10-12», exit 1 (agente y dev-lead) |
| 2 | `node bin/salas.js libres 1012` | «Franja no válida: usa HH-HH, p. ej. 10-12», exit 1 (agente y dev-lead) |
| 3 | `node --test` | 3/3 pasan (agente) |

### 4.3 Residuales / deuda generada

- Ninguno. La validación solo comprueba el formato `HH-HH`, no que las horas existan (p. ej. `99-99`): la spec no lo pide.

## 5. Aprendizajes

- _Ninguno_ pendiente de volcar: el proyecto no tiene `architecture.md` ni `tech-stack.md`, y esta task no descubre nada estructural (pasos 4 y 5 del cierre, fuera del alcance pedido).

## 6. Adendas

- _Ninguna_
