---
id: 20260923-100000-task-0012-franja
task: 0012
title: Walkthrough — Validar el formato de la franja
spec: ./spec.md
plan: ./plan.md
status: done
created: 2026-09-25
---

# Walkthrough — Validar el formato de la franja

## 1. Cambios realizados

- **Reservar** (`20f663d`): `src/slots.js` valida la franja contra `/^\d{2}-\d{2}$/` y lanza «Franja no válida: usa HH-HH, p. ej. 10-12». Test en `tests/slot-format.test.js`.
- **Consultar libres** (`72b06b1`): `free(slot)` en `src/slots.js` aplica la misma validación y el mismo mensaje. Test en `tests/free-format.test.js`.
- **Revisión final de rama** (`c3505c1`): `review-final.md`, limpia, sin hallazgos (general-purpose + sonnet, según `tasks.md`).

## 2. Tiempo y coste: estimado vs real

- Tipo: backend
- Estimación de implementación (del plan): no consta — `plan.md` no lleva estimación en horas
- Esfuerzo real: no medido; aproximado. Las marcas de los commits (2026-09-25 09:05:05–09:05:06) están a segundos entre sí y no reflejan el trabajo real. Sin reloj del hilo, no hay cifra fiable.
- Desviación: no calculable (sin estimación previa)
- Causa de la desviación: no aplica (no calculable)
- Modelo del hilo: no consta
- Tokens del hilo: no medido
- Tokens de subagentes: no medido — el plan prevé implementadores y revisores Sonnet, effort medio, en 2 tasks
- Coste de sujetos: no aplica
- Review de spec: no

> Pendiente del dev-lead: aportar el esfuerzo real y, si quiere calibración, la estimación que faltó en el plan.

## 3. Desviaciones del plan

- _Ninguna_ detectada: las dos tasks del plan están hechas, cada una con su commit.

### Decisiones tomadas sin el dev-lead

- _Ninguna_ registrada: no hay informe de rulings de la ejecución en el repo ni en la sesión de cierre.

## 4. Verificación

### 4.1 Builds

- Proyecto Node sin paso de build. `node --test` (2026-09-25, en el cierre): 3 tests, 3 pass, 0 fail.

### 4.2 Smoke / tests

- Validado por el dev-lead: 2026-09-25 · «he probado `node bin/salas.js reservar Norte 1012` y `node bin/salas.js libres 1012`, los dos dan el error de franja; funciona».

| # | Caso | Resultado | Fuente |
| --- | --- | --- | --- |
| 1 | `node bin/salas.js reservar Norte 1012` | «Franja no válida: usa HH-HH, p. ej. 10-12», exit 1 | verificado por el agente y por el dev-lead |
| 2 | `node bin/salas.js libres 1012` | mismo mensaje, exit 1 | verificado por el agente y por el dev-lead |
| 3 | `node --test` | 3/3 pass (`rechaza una franja sin guion`, `libres rechaza una franja sin guion`, `reserva una sala en una franja`) | verificado por el agente |

- No probado por nadie: una franja con formato válido pero fuera de rango (p. ej. `99-99`); la spec solo exige el formato `HH-HH`.

### 4.3 Residuales / deuda generada

- La validación no comprueba rango horario ni orden (`99-99`, `12-10` pasan). Fuera de la spec; candidata a backlog si el dev-lead lo quiere.

## 5. Aprendizajes

- Ninguno registrado en esta redacción; el volcado a docs vivos (paso 4 del cierre) queda pendiente.

## 6. Adendas

- _Ninguna_
