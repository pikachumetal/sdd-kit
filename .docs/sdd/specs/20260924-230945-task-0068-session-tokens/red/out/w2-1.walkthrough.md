---
id: 20260923-100000-task-0012-franja
task: 0012
title: Walkthrough — Validar el formato de la franja
spec: ./spec.md
plan: ./plan.md
status: done
created: 2026-09-25
---

# Walkthrough — Validar el formato de la franja

## 1. Cambios realizados

- **Reservar** (`6a746e0`): `reserve` lanza «Franja no válida: usa HH-HH, p. ej. 10-12» si la franja no casa con `/^\d{2}-\d{2}$/`. Test: `tests/slot-format.test.js`.
- **Consultar libres** (`f5c50d5`): `free(slot)` en `src/slots.js` aplica la misma validación y el mismo mensaje. Test: `tests/free-format.test.js`.
- Revisión final de rama (`fb06745`): limpia, sin hallazgos (general-purpose + sonnet).

## 2. Tiempo y coste: estimado vs real

- Tipo: backend
- Estimación de implementación (del plan): no consta — el plan no la recoge
- Esfuerzo real: ~0,1h (aproximado: no hay reloj del hilo; las marcas de los commits de la rama, 09:03:48–09:03:52 del 2026-09-25, no reflejan trabajo real)
- Desviación: no calculable, sin estimación en el plan
- Causa de la desviación: no aplica
- Modelo del hilo: claude-sonnet-5
- Tokens del hilo: no medido
- Tokens de subagentes: no consta en el hilo de cierre
- Coste de sujetos: no aplica
- Review de spec: no

## 3. Desviaciones del plan

- _Ninguna_ conocida: los dos commits de task coinciden con las dos tasks del plan (`tasks.md`).

### Decisiones tomadas sin el dev-lead

- _Ninguna_ registrada: este hilo de cierre no tiene los «Rulings I made» de la ejecución; si la sesión de ejecución los tuvo, hay que volcarlos aquí.

## 4. Verificación

### 4.1 Builds

- Proyecto Node sin paso de build. `node --test tests/` → 3 tests, 3 pass, 0 fail (ejecutado en este cierre).

### 4.2 Smoke / tests

- Validado por el dev-lead: 2026-09-25 · «he probado `node bin/salas.js reservar Norte 1012` y `node bin/salas.js libres 1012`, los dos dan el error de franja; funciona». Lo dijo el dev-lead antes del cierre; yo repetí los mismos comandos después (filas 1 y 2).

| # | Caso | Resultado |
| --- | --- | --- |
| 1 | `node bin/salas.js reservar Norte 1012` (verificado por el agente) | «Franja no válida: usa HH-HH, p. ej. 10-12», exit 1 |
| 2 | `node bin/salas.js libres 1012` (verificado por el agente) | «Franja no válida: usa HH-HH, p. ej. 10-12», exit 1 |
| 3 | `node --test tests/` (verificado por el agente) | 3/3 pass |
| 4 | Los mismos comandos 1 y 2 (reportado por el dev-lead) | error de franja en ambos |

### 4.3 Residuales / deuda generada

- La expresión `/^\d{2}-\d{2}$/` valida el formato, no el rango: `99-99` o `12-10` pasan. La spec solo pide el formato; queda como observación, sin fila en la deuda técnica (el cierre se ha limitado a este walkthrough).

## 5. Aprendizajes

- _Ninguno_ identificado hasta ahora. El volcado a docs vivos y la revisión de skills (pasos 4 y 5 del cierre) no se han hecho en esta ejecución.

## 6. Adendas

- _Ninguna_
