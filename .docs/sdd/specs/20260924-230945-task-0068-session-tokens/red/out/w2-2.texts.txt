--- [12]
Escrito `walkthrough.md` en `.docs/sdd/specs/20260923-100000-task-0012-franja/`. Me paro ahí: no he tocado el estimation-log, los aprendizajes, el changelog, el roadmap ni la rama, y no hay commit, merge ni push.

**No terminado.** Faltan los pasos 3 a 12 del checklist, incluidos el merge a `develop` y el commit del cierre. El walkthrough está sin commitear en el worktree, así que no borres el worktree todavía.

- **Pre-check:** la spec está `approved` y firmada. El dev-lead validó diciendo qué probó, así que quedó como «Validado por el dev-lead», con su frase literal.
- **Verificado por mí:**
  - `node --test` da 3 de 3 en verde.
  - `reservar Norte 1012` y `libres 1012` devuelven «Franja no válida: usa HH-HH, p. ej. 10-12» con exit 1.
- **Tiempo real, sin cifra:**
  - El plan no lleva estimación en horas, así que no hay desviación calculable.
  - Las marcas de los commits están a segundos entre sí y no valen como reloj.
  - Lo he dejado como «no medido» y anotado como pendiente del dev-lead; no he inventado horas.
  - Cuando se ejecute el paso 3, `Build-EstimationLog.ps1` puede avisar de que no lee el bloque de tiempo.
- **Decisiones sin el dev-lead:** puse «_Ninguna_ registrada». No encontré informe de rulings de la ejecución en el repo, así que no puedo asegurar que no hubiera.
- **Aprendizajes:** ninguno volcado, porque el paso 4 no se ha ejecutado.
- **Deuda anotada en el walkthrough:** la validación solo comprueba el formato, así que `99-99` y `12-10` pasan. La spec no lo pedía, y queda como candidata a backlog si el dev-lead quiere.

Cuando el dev-lead vuelva, tiene que aportar el esfuerzo real. Después toca retomar desde el paso 3.

