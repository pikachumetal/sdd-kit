
>>> Skill: sdd-kit:sdd-start-task
<<< Launching skill: sdd-kit:sdd-start-task

>>> Bash: cd . && ls .docs/sdd/specs/*0012*/ && cat .docs/sdd/specs/*0012*/tasks.md && cat .docs/sdd/specs/*0012*/plan.md && git log --oneline -8 && git status --short
<<< plan.md
spec.md
tasks.md
# Tasks — Validar el formato de la franja (registro vivo)

| # | Task | Status | Commit | Notas |
| --- | --- | --- | --- | --- |
| 1 | Validar al reservar | done | 27015b1 | |
| 2 | Validar al consultar libres | pending | — | |
---
id: 20260923-100000-task-0012-franja
task: 0012
title: Plan — Validar el formato de la franja
spec: ./spec.md
status: approved
---

# Plan — Validar el formato de la franja

**Ejecución**: subagent, porque cada task lleva su revisión.

## Restricciones globales

### De código

- Mensaje literal: «Franja no válida: usa HH-HH, p. ej. 10-12».


>>> Bash: cd . && cat .docs/sdd/specs/*0012*/spec.md; ls src tests; cat src/*.js; cat tests/slot-format.test.js; git branch -a; git remote -v; cat .docs/sdd/sdd-kit.json 2>/dev/null; grep -n "0012" .docs/sdd/roadmap.md
<<< ---
id: 20260923-100000-task-0012-franja
task: 0012
title: Validar el formato de la franja
mode: full
profile: pair
status: approved
created: 2026-09-23
approvers:
  - role: dev-lead
    name: Dev Lead
    approved_at: 2026-09-23
---

# Spec — Validar el formato de la franja

## Intent

`salas reservar Norte 1012` reserva una franja sin sentido. Se quiere un error claro.

## Delta de comportamiento

### Capacidad: `booking`

**ADDED — La franja se valida al reservar**
- GIVEN una franja que no casa con `HH-HH`
- WHEN se reserva
- THEN falla con «Franja no válida: usa HH-HH, p. ej. 10-12»

**AD

>>> Bash: cd . && MB=$(git merge-base HEAD develop); echo "mb=$MB develop=$(git rev-parse develop)"; git diff $MB develop -- .docs/sdd/roadmap.md; echo ---; git diff --name-only $MB develop

>>> ToolSearch: {"query":"select:Agent","max_results":1}
<<< mb=f0f35b380865a170628300af319ecc711154536d develop=f0f35b380865a170628300af319ecc711154536d
---
<<< No matching deferred tools found

>>> Bash: cd . && cat > tests/free-format.test.js <<'EOF'
import { test } from 'node:test';
import assert from 'node:assert';
import { free } from '../src/slots.js';

test('rechaza una franja sin guion al consultar libres', () => {
  assert.throws(() => free('1012'), /Franja no válida: usa HH-HH, p\. ej\. 10-12/);
});
EOF
BK="${TMPDIR:-/tmp}/sdd-red-0012-task2"; mkdir -p "$BK" && cp tests/free-format.test.js "$BK/" && echo "backup: $BK"
node --test tests/free-format.test.js 2>&1 | grep -E "^# (pass|fail)|SyntaxError|does not provide|free"
<<< backup: <home>/AppData/Local/Temp/sdd-red-0012-task2
file:///<run>/salas/tests/free-format.test.js:3
import { free } from '../src/slots.js';
SyntaxError: The requested module '../src/slots.js' does not provide an export named 'free'
✖ tests\free-format.test.js (67.369ms)
test at tests\free-format.test.js:1:1
✖ tests\free-format.test.js (67.369ms)

=== RESULTADO (8 turnos, 0.240193 $)
Estoy en la Task 2 (`free(slot)`). La base está limpia y los tests RED ya están escritos, pero no puedo despachar al implementador y paro aquí.

**Hecho**
- **Base:** `develop` sigue en `f0f35b3`, el mismo merge-base. No hay cambios en la fila del roadmap ni ficheros cruzados con la task. No hay remoto.
- **RED:** `tests/free-format.test.js` está escrito, sin commitear, con una copia en `%TEMP%/sdd-red-0012-task2/`. Falla como toca: `does not provide an export named 'free'`.

**Bloqueo**
- El plan dice `subagent`, pero en esta sesión no hay herramienta para lanzar subagentes. Tampoco existe el tipo `sdd-kit:effort-medio`, así que el fallback «effort: no disponible en este harness, hereda el de la sesión» no aplica.
- Sin subagentes no hay revisor independiente para la Task 2.

**Decisión para el dev-lead**
- **Recomendada:** implemento `free` yo en el hilo principal, con el mismo `SLOT` de `src/slots.js`. Corro `node --test tests/free-format.test.js` y comparo los RED con la copia. Lo registro como ruling en «Me salí del plan en…».
- **Revisión:** haría una auto-revisión en el hilo principal, y sería una revisión menos independiente que la del plan. Como el commit no puede ir sin revisión, lo dejo sin commitear hasta que decidas. Puedes aceptar la auto-revisión o revisarlo tú en la validación del paso 7.
- **Alternativa:** reabrir la sesión en un harness con subagentes y el tipo `sdd-kit:effort-medio`. Entonces sigo el plan tal cual.

¿Sigo con la recomendada o prefieres la alternativa?
