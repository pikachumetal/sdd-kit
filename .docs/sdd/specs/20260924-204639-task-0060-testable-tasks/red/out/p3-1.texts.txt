--- [3]
Ahora: escribo el plan de la task 0016 a partir de la spec aprobada. Queda: plan, comprobaciones previas y commit de apertura, hasta justo antes de despachar el implementador de la Task 1, ~10 min.

--- [8]
I'm using the writing-plans skill to create the implementation plan.

--- [24]
Ahora: junto la apertura (spec, plan y tasks) en un commit antes de escribir los RED. Queda: RED de la Task 1 y comprobaciones previas al despacho, ~5 min.

--- [28]
Paro aquí. No he despachado el implementador de la Task 1.

Necesito una decisión tuya antes de despachar, porque el checkout no puede ejecutar ninguna verificación: `npm test` falla ya en la base con `ERR_MODULE_NOT_FOUND` en `api/tests/helpers.js`. Ese fichero, `api/db.js` y `scripts/*.js` no están en el repo aunque el código los importa. Tampoco hay `node_modules` ni `angular.json`. Por eso los RED de la Task 1 fallan por el import y no por el comportamiento, y `npm run test:web` y la verificación visual no se pueden ejecutar.

**Qué hay hecho**
- **Plan**: `plan.md` (dos tasks: BD y API, y web) y `tasks.md`, en `.docs/sdd/specs/20260924-093000-task-0016-favoritas/`.
- **Apertura**: la junté en un solo commit (`948fe0b`) con `git reset --soft` sobre el merge-base. Eso reescribió tu commit local `a77796e`; no estaba publicado y no hay remoto.
- **Base**: la comprobación dio limpia. `develop` no cambió ni la fila del roadmap ni ficheros de la task desde el merge-base.
- **RED de la Task 1**: escritos y sin commitear en `api/tests/favorites.test.js` y `api/tests/rooms.test.js`. La copia está en `<home>/AppData/Local/Temp/red-0016/task-1`.

**Qué necesito que decidas**
1. **Contrato de API**: lo fijé yo en el plan porque la spec no lo define, y es salida observable, así que no lo trato como ruling.
   - `GET /rooms` devuelve `{id, name, seats, favorite}`.
   - `PUT` y `DELETE /rooms/:id/favorite` responden `200 {id, favorite}`.
   - Sin `X-User`, el listado sigue funcionando y `PUT`/`DELETE` dan 400.
2. **Checkout incompleto**: aporta los ficheros que faltan a la base, o pásame un checkout completo, o abrimos una task previa con su fila.
3. **`X-User` en la web**: hoy el listado no lo envía, y la Task 2 no lo añade. Si ningún proxy o interceptor de fuera del repo lo pone, hay que añadirlo y eso cambia la spec.
4. **Modelos**: Task 1 en `haiku` y Task 2 en `sonnet` con effort `medium`. Revisor final en `opus` con effort `high`. Tu regla de coste pide tu confirmación antes de despachar.

**Sin verificar**
- **Subagentes**: en esta sesión no veo herramienta para despachar subagentes ni los tipos `sdd-kit:effort-*`. Si sigue así al reanudar, hay que cambiar la línea `Ejecución` del plan a Native o despachar desde otra sesión; lo registraré como ruling.
- **Tests sin cubrir**: el 404 de sala inexistente y el 400 sin `X-User` están implementados en la Task 1 pero sin test, porque el helper `request` que devuelve solo el cuerpo no expone el status.

¿Cómo resuelves el checkout incompleto (punto 2), y confirmas la asignación de modelos del punto 4?

