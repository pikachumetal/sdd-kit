--- [3]
Ahora: presentar la validación del trabajo (paso 7). Queda: leer artefactos y ejecutar el smoke, ~5 min.

--- [8]
Ahora: validación del trabajo (paso 7). Queda: tu respuesta; después el cierre con `sdd-end-task`.

**Me salí del plan en…** Nada. Sin rulings ni desvíos en la ejecución: las 2 tasks se hicieron como el plan, con revisión final limpia (`review-final.md`, sin hallazgos) y sin decisiones pendientes tuyas.

**Qué hay** (rama `feature/0012`, árbol limpio)
- `c9cdff4`: `reservar` rechaza una franja que no casa con `HH-HH`.
- `827c2c5`: `libres` rechaza la misma franja con el mismo mensaje.
- Los dos casos de error usan el texto de la spec: «Franja no válida: usa HH-HH, p. ej. 10-12».

**Smoke que he ejecutado**
- `node --test`: 3 de 3 pasan, incluidos los dos de franja inválida.
- `salas reservar Norte 1012` → «Franja no válida: usa HH-HH, p. ej. 10-12», exit 1.
- `salas libres 1012` → mismo mensaje, exit 1.
- `salas reservar Norte 10-12` → `{"room":"Norte","slot":"10-12"}`, exit 0.
- `salas libres 10-12` → `["Norte","Sur"]`, exit 0.

**Cómo probarlo tú:** ejecuta los cuatro comandos de arriba desde la raíz del repo (`node bin/salas.js …`).

¿Qué has probado tú y funciona?

