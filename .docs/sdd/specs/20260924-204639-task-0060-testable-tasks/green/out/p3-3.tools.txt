
>>> Skill: sdd-kit:sdd-start-task
<<< Launching skill: sdd-kit:sdd-start-task

>>> Bash: cd "<run>/salas" && git branch --show-current && ls -R .docs/sdd | head -60 && cat .docs/sdd/specs/20260924-093000-task-0016-favoritas/spec.md && cat .docs/sdd/roadmap.md .docs/sdd/constitution.md .docs/sdd/tech-stack.md; ls; cat sdd-kit.json 2>/dev/null
<<< Exit code 1
feature/0016
.docs/sdd:
capabilities/
changelog.md
constitution.md
roadmap.md
sdd-kit.json
specs/
tech-stack.md

.docs/sdd/capabilities:
rooms.md

.docs/sdd/specs:
20260924-093000-task-0016-favoritas/

.docs/sdd/specs/20260924-093000-task-0016-favoritas:
spec.md
---
id: 20260924-093000-task-0016-favoritas
task: 0016
title: Salas favoritas
mode: full
status: approved
created: 2026-09-24
approvers:
  - role: dev-lead
    name: Dev Lead
    approved_at: 2026-09-24
---

# Spec — Salas favoritas

## Decisiones que he tomado yo — valida estas

1. La favorita es por persona (cabecera `X-U

>>> Bash: cd "<run>/salas" && cat .docs/sdd/sdd-kit.json .docs/sdd/capabilities/rooms.md package.json; git ls-files | grep -v '^.docs'; git branch -a; git remote -v; ls ../../../kit-refactor/skills/sdd-templates ../../../kit-refactor/skills/sdd-start-task/references
<<< {"version": "2.0.0", "channel": "plugin", "ids": {"mode": "sequence"}, "release": {"hasRecipient": false}, "control": {"profile": "delegate"}, "merge": {"into": "develop", "noFf": true, "removeWorktree": false}}
# Capacidad — rooms

## Requisitos

### Se listan las salas
- GIVEN las salas Norte (4 plazas) y Sur (12 plazas)
- WHEN Ana abre la pantalla de salas
- THEN ve Norte y Sur, en ese orden, con sus plazas
{ "name": "salas-web", "scripts": { "dev": "node scripts/dev.js", "test": "node --test api/tests", "test:web": "ng test --watch=false", "migrate": "node scripts/migrate.js" } }
.gitignor

>>> Bash: cd "<run>/salas" && cat api/routes/rooms.js api/tests/rooms.test.js db/migrations/001_rooms.sql web/src/app/rooms/rooms.component.*; K=../../../kit-refactor/skills; cat $K/sdd-templates/templates/plan-template.md; cat $K/sdd-start-task/references/control-profiles.md $K/sdd-start-task/references/commit-milestones.md $K/sdd-start-task/references/encargo-revision.md
<<< <persisted-output>
Output too large (40.1KB). Full output saved to: <home>\.claude\projects\C--Users-<usuario>-AppData-Local-Temp-claude-D--code--worktrees-sdd-kit-0060-4a3ac886-d053-4b87-a215-063196a2d726-scratchpad-runs-p3-3-salas\55260f98-be9b-41d1-8cc1-5e59ecd5ffda\tool-results\b398427vh.txt

Preview (first 2KB):
import { Router } from 'express';
import { db } from '../db.js';

export const rooms = Router();

rooms.get('/rooms', async (req, res) => {
  const { rows } = await db.query('SELECT id, name, seats FROM rooms ORDER BY name');
  res.json(rows);
});
import { test } from 'node:test';
imp

>>> Bash: cd "<run>/salas" && K=../../../kit-refactor/skills; cat $K/sdd-templates/templates/plan-template.md; echo =====; cat $K/sdd-start-task/references/control-profiles.md
<<< ---
id: <yyyyMMdd-HHmmss>-task-<id>-<slug>
task: <id>
title: Plan de implementación — <título de la spec>
spec: ./spec.md
status: draft
created: <YYYY-MM-DD>
---

# Plan de implementación — <título>

> Compatible con `superpowers:writing-plans`. El método lo recomienda su handoff y va en la línea
> `Ejecución` de abajo, para el plan entero ([tabla de gates](../../sdd-start-task/references/control-profiles.md)).
> Borra los bloques de ayuda (`>`) al redactar.

## Decisiones que he tomado yo — valida estas

> Es lo único que el dev-lead necesita leer para aprobar el plan; el res

>>> Skill: superpowers:writing-plans
<<< Launching skill: superpowers:writing-plans

>>> Bash: cd "<run>/salas" && K=../../../kit-refactor/skills/sdd-start-task/references; cat $K/commit-milestones.md $K/encargo-revision.md; cat ../../../kit-refactor/skills/sdd-templates/templates/tasks-template.md 2>/dev/null | head -50; ls ../../../kit-refactor/skills/sdd-templates/templates
<<< # Un commit por hito

La rama de una task cuenta sus hitos: **apertura**, **un commit por task del plan** y **cierre** — 2 + N commits desde el `merge-base`, 3 en lite. La de un patch, dos: **fix** y **cierre**. Mientras un hito está abierto se commitea lo que haga falta: la revisión de `subagent-driven-development` trabaja por rangos `BASE..HEAD`. Cuando la revisión del hito queda limpia, su rango se junta en un commit.

## Qué lleva cada hito

| Hito | Lleva | Se junta | Base |
| --- | --- | --- | --- |
| Apertura | spec, hallazgos de la review de spec, `plan.md`, `tasks.md` (lite: s

>>> Bash: cd "<run>/salas" && cat .gitignore README.md; find . -path ./.git -prune -o -path ./node_modules -prune -o -type f -print | grep -v '^./.docs' ; git log --stat --oneline -3 | head -30; ls ../../../kit-refactor/skills/sdd-templates/scripts; ls ~/.claude/plugins/cache/claude-plugins-official/superpowers/6.4.1/skills/ | head -20
<<< .superpowers/
# salas-web

Reservas de salas. `db/` migraciones SQL numeradas (Postgres), `api/` servidor Express, `web/` frontend Angular.
Arranque: `npm run dev` (API en :3000, web en :4200).
./.gitignore
./api/routes/rooms.js
./api/tests/rooms.test.js
./db/migrations/001_rooms.sql
./package.json
./README.md
./web/src/app/rooms/rooms.component.html
./web/src/app/rooms/rooms.component.ts
9f1208d docs(0016): spec aprobada de la task 0016
 .../20260924-093000-task-0016-favoritas/spec.md    | 61 ++++++++++++++++++++++
 1 file changed, 61 insertions(+)
a298991 feat: salas-web con listado de salas

>>> Bash: cd "<run>/salas" && ls node_modules 2>&1 | head -3; npm test 2>&1 | head -15; git fetch 2>&1 | head -2; MB=$(git merge-base HEAD develop); echo MB=$MB; git rev-parse develop HEAD; git diff $MB develop -- .docs/sdd/roadmap.md; git diff --name-only $MB develop
<<< ls: cannot access 'node_modules': No such file or directory
npm notice run test
npm notice run node --test api/tests
(node:24300) [MODULE_TYPELESS_PACKAGE_JSON] Warning: Module type of file:///<run>/salas/api/tests/rooms.test.js is not specified and it doesn't parse as CommonJS.
Reparsing as ES module because module syntax was detected. This incurs a performance overhead.
To eliminate this warning, add "type": "module" to \\?\<run>\salas\package.json.
(Use `node --trace-warnings ...` to show where the warning was created)
node:internal/modules/esm/resolve:241
    throw new ERR_MODULE_NOT_FOUND

>>> Bash: cd "<run>/salas/.docs/sdd/specs/20260924-093000-task-0016-favoritas" && cat > plan.md <<'EOF'
---
id: 20260924-093000-task-0016-favoritas
task: 0016
title: Plan de implementación — Salas favoritas
spec: ./spec.md
status: draft
created: 2026-09-25
---

# Plan de implementación — Salas favoritas

## Decisiones que he tomado yo — valida estas

1. **Ejecución: subagent** — 2 tasks con contrato compartido (forma de `GET /rooms`) y un repo cuya suite nadie ha visto en verde (ver riesgo 1): una revisión independiente por task pesa más que el ahorro de Native. Alternativa: Native, más barato (~la mitad de contexto), sin revisión hasta el final.
2. **Modelo por task** — Task 1: `sdd-kit:effort-medium` + `sonnet` (atraviesa BD, API y web, hay que interpretar). Task 2: `sdd-kit:effort-low` + `sonnet` (el cambio de SQL va escrito en el plan y los RED ya lo cubren). Revisor final: `sdd-kit:effort-high` + `opus`. Sin `fable` ni `opus xhigh`.
3. **Corte en 2 tasks verticales**: T1 marcar/desmarcar/persistir con su estrella; T2 orden con favoritas primero. Cada una acaba en algo que se prueba en la app.
4. **Contrato de API que la spec no fija**: `GET /rooms` devuelve `{id, name, seats, favorite}`; `PUT /rooms/:id/favorite` y `DELETE /rooms/:id/favorite` responden 204 y son idempotentes. Sin `X-User` → 400; sala inexistente en el `PUT` → 404. Cambia la salida observable de la API: si no lo quieres así, dilo antes de la Task 1.
5. **Identidad en la web (hueco de la spec)**: la web no tiene login y hoy no envía `X-User`. Asumo que lo lee de `localStorage['user']` (sin valor → la API responde 400 y la lista queda vacía). Es la decisión más frágil del plan: se valida antes de la Task 1, porque la Task 1 la lleva.
6. **Precondición (riesgo alto)**: faltan `api/db.js`, `api/tests/helpers.js`, `scripts/migrate.js` y el arranque de Express, y `package.json` no declara `"type": "module"` ni hay `node_modules`. `npm test` ya falla hoy con `ERR_MODULE_NOT_FOUND`. No lo invento aquí: amplía el Scope de la spec. Sin resolverlo, ningún RED se puede ver fallar por la razón correcta ni ningún verde comprobar.
7. **Coste estimado**: ~3 h de implementación con la precondición resuelta; ~1,5-2 M tokens en subagentes (2 implementadores, 2 revisores, revisor final).

**Goal**: cada persona marca y desmarca salas favoritas con una estrella y las ve primero en la lista.

**Architecture**: tabla `favorite_rooms (user_name, room_id)`; `GET /rooms` hace `LEFT JOIN` con la persona de `X-User` y devuelve `favorite`; dos endpoints marcan y desmarcan; el componente pinta la estrella y recarga la lista tras cada cambio. El orden lo decide la API, la web pinta lo que recibe.

**Tech Stack**: Postgres (migraciones SQL numeradas), Express en `api/`, Angular en `web/`.

**Spec**: `./spec.md`

**Ejecución**: subagent, porque las 2 tasks comparten contrato y la revisión por task cubre un repo sin suite verificada.

## Restricciones globales

### De código

- Sin comentarios que repitan el código ni que citen documentos (constitution, spec, task, capacidad); la constitution del proyecto no tiene artículo de calidad de código, valen estas dos reglas.
- Clean code: nombres que dicen la intención, funciones cortas de un solo cometido.
- Las favoritas viven en la tabla `favorite_rooms (user_name, room_id)`, una fila por persona y sala.
- La favorita es por persona (cabecera `X-User`), no global.
- Migraciones SQL numeradas en `db/migrations/`.

### De proceso

- Commits: tipo/scope en inglés, título y cuerpo en castellano, nunca solo título (Art. I). Atribución con `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`.
- Política de modelos: gama media como suelo cuando hay que interpretar; `opus effort-high` solo el revisor final.
- Todo verde antes de fusionar: `npm test` y `npm run test:web` (Art. II), una vez, en §3.

---

## Phase -1 — Pre-Implementation Gates

- [x] **Simplicity gate**: una tabla, dos endpoints, un `LEFT JOIN`; sin capa de servicio ni estado global en la web.
- [x] **YAGNI gate**: nada se abstrae; el «No entra» de la spec (compartir, límite) queda fuera.
- [x] **Brownfield gate**: `GET /rooms` añade un campo y no quita ninguno; el test existente de nombres sigue valiendo.
- [x] **Constitution check**: Art. I en los commits y Art. II en §3.

---

## 1. Decisiones técnicas

### 1.1 Estructura de ficheros

**Crear**:

- `db/migrations/002_favorite_rooms.sql` — tabla `favorite_rooms`.
- `web/src/app/rooms/rooms.component.spec.ts` — tests de la estrella.

**Modificar**:

- `api/routes/rooms.js` — `X-User` obligatorio, `favorite` en el listado, `PUT` y `DELETE` de favorita, orden.
- `api/tests/rooms.test.js` — tests de los escenarios.
- `web/src/app/rooms/rooms.component.ts` y `.html` — estrella y cabecera `X-User`.

**NO se tocan**:

- `db/migrations/001_rooms.sql` — una migración aplicada no se edita.
- `.docs/sdd/capabilities/rooms.md` — se fusiona en el cierre, no durante la implementación.

### 1.2 Modelo de datos

```sql
CREATE TABLE favorite_rooms (
  user_name text NOT NULL,
  room_id int NOT NULL REFERENCES rooms(id),
  PRIMARY KEY (user_name, room_id)
);
```

### 1.3 Migraciones

`db/migrations/002_favorite_rooms.sql`, se aplica con `npm run migrate`.

### 1.4 Contratos API

- `GET /rooms` → `200 [{ id, name, seats, favorite }]` para la persona de `X-User`.
- `PUT /rooms/:id/favorite` → `204`; `404` si la sala no existe.
- `DELETE /rooms/:id/favorite` → `204`.
- Sin `X-User` → `400 { error }` en las tres rutas.

### 1.5 UX

Una estrella por sala: `☆` vacía, `★` llena. Botón con `aria-pressed` y `aria-label` «Marcar <sala> como favorita» / «Quitar <sala> de favoritas».

### 1.6 Dependencias

Ninguna nueva. Precondición: infraestructura de tests y arranque de la API (decisión 6).

### 1.7 Riesgos

| Riesgo | Probabilidad | Impacto | Mitigación |
| --- | --- | --- | --- |
| Faltan `db.js`, `helpers.js`, `migrate.js` y el arranque de Express; ninguna suite corre | Alta (confirmado: `npm test` falla) | Alto: sin RED válidos ni verdes | El dev-lead decide antes de despachar: ampliar el Scope con una task de base o aportar los ficheros |
| La web no sabe quién es el usuario | Alta | Medio | `localStorage['user']`, validado por el dev-lead (decisión 5) |
| Los tests de API comparten BD y se contaminan | Media | Medio | Cada test limpia `favorite_rooms` en su arranque |

### 1.8 Rollout

Directo. Aplicar `npm run migrate` antes de desplegar la API.

### 1.9 Excepciones a la constitution

Ninguna.

---

## 2. Tasks

### Task 1 — Marcar y desmarcar una favorita, con su estrella

**Modelo**: `subagent_type: sdd-kit:effort-medium` + `model: sonnet`
**Tests RED**: hilo principal · `api/tests/rooms.test.js`, `web/src/app/rooms/rooms.component.spec.ts`, escritos antes de despachar y sin commitear: van en el commit de la task.

**Superficies**: BD · backend · frontend
**Verificación**: `npm run migrate` · `npm test` · `npm run test:web`
**Verificación visual**: pantalla de salas; estrella vacía y llena, alineación con el nombre y contraste, tema claro.
**Se prueba en la aplicación**: con `localStorage.user = 'ana'`, Ana abre salas y ve «Norte (4 plazas) ☆» y «Sur (12 plazas) ☆»; pulsa la estrella de Sur y queda `★`; recarga y sigue `★`; la pulsa otra vez y vuelve a `☆`. El orden no cambia todavía.

**Interfaces**:
- Consume: `rooms` de `db/migrations/001_rooms.sql` (`id serial`, `name`, `seats`); `db` de `api/db.js` (`db.query(sql, params)` → `{ rows }`); `request(method, path, { user })` de `api/tests/helpers.js`, que devuelve el cuerpo JSON (o `undefined` con 204).
- Produce: `favorite_rooms (user_name text, room_id int)`; `GET /rooms` → `[{ id, name, seats, favorite: boolean }]`; `PUT|DELETE /rooms/:id/favorite` → `204`; en la web, `RoomsComponent.toggle(room)`.

**Ficheros**: crear `db/migrations/002_favorite_rooms.sql`, `web/src/app/rooms/rooms.component.spec.ts`; modificar `api/routes/rooms.js`, `api/tests/rooms.test.js`, `web/src/app/rooms/rooms.component.ts`, `web/src/app/rooms/rooms.component.html`.

- [ ] **Step 1: Implementación**

`db/migrations/002_favorite_rooms.sql`: el SQL de §1.2.

`api/routes/rooms.js`:

```js
import { Router } from 'express';
import { db } from '../db.js';

export const rooms = Router();

rooms.use((req, res, next) =>
  req.get('X-User') ? next() : res.status(400).json({ error: 'Falta la cabecera X-User' }));

rooms.get('/rooms', async (req, res) => {
  const { rows } = await db.query(
    `SELECT r.id, r.name, r.seats, (f.room_id IS NOT NULL) AS favorite
       FROM rooms r
       LEFT JOIN favorite_rooms f ON f.room_id = r.id AND f.user_name = $1
      ORDER BY r.name`,
    [req.get('X-User')]);
  res.json(rows);
});

rooms.put('/rooms/:id/favorite', async (req, res) => {
  try {
    await db.query(
      'INSERT INTO favorite_rooms (user_name, room_id) VALUES ($1, $2) ON CONFLICT DO NOTHING',
      [req.get('X-User'), req.params.id]);
    res.sendStatus(204);
  } catch (error) {
    if (error.code === '23503') return res.sendStatus(404);
    throw error;
  }
});

rooms.delete('/rooms/:id/favorite', async (req, res) => {
  await db.query('DELETE FROM favorite_rooms WHERE user_name = $1 AND room_id = $2',
    [req.get('X-User'), req.params.id]);
  res.sendStatus(204);
});
```

`web/src/app/rooms/rooms.component.ts`:

```ts
import { Component, inject } from '@angular/core';
import { HttpClient, httpResource } from '@angular/common/http';

interface Room { id: number; name: string; seats: number; favorite: boolean }

@Component({ selector: 'app-rooms', templateUrl: './rooms.component.html' })
export class RoomsComponent {
  private readonly http = inject(HttpClient);
  private readonly headers = { 'X-User': localStorage.getItem('user') ?? '' };

  readonly rooms = httpResource<Room[]>(() => ({ url: '/api/rooms', headers: this.headers }));

  toggle(room: Room) {
    const url = `/api/rooms/${room.id}/favorite`;
    const request = room.favorite
      ? this.http.delete(url, { headers: this.headers })
      : this.http.put(url, null, { headers: this.headers });
    request.subscribe(() => this.rooms.reload());
  }
}
```

`web/src/app/rooms/rooms.component.html`:

```html
<ul>
  @for (room of rooms.value(); track room.id) {
    <li>
      {{ room.name }} ({{ room.seats }} plazas)
      <button type="button" [attr.aria-pressed]="room.favorite"
        [attr.aria-label]="(room.favorite ? 'Quitar ' + room.name + ' de favoritas' : 'Marcar ' + room.name + ' como favorita')"
        (click)="toggle(room)">{{ room.favorite ? '★' : '☆' }}</button>
    </li>
  }
</ul>
```

- [ ] **Step 2: Build** — `npm run migrate` y `ng build` sin errores. Esperado: verde.
- [ ] **Step 3: Verificación** — `npm test` y `npm run test:web`. Esperado: todos los RED de la task en verde, el test previo de nombres incluido.
- [ ] **Step 4: Commit de la task** — uno solo, al quedar limpia su revisión: `feat(rooms): marcar y desmarcar salas favoritas (0016)`, cuerpo en castellano.

### Task 2 — Las favoritas salen primero

**Modelo**: `subagent_type: sdd-kit:effort-low` + `model: sonnet` — el cambio de SQL va escrito abajo y los RED ya lo cubren.
**Tests RED**: hilo principal · `api/tests/rooms.test.js`, escritos antes de despachar y sin commitear: van en el commit de la task.

**Superficies**: backend
**Verificación**: `npm test`
**Se prueba en la aplicación**: Ana marca Sur y la lista pasa a «Sur, Norte»; con `localStorage.user = 'luis'` (sin favoritas) sigue «Norte, Sur»; Ana desmarca Sur y vuelve a «Norte, Sur».

**Interfaces**:
- Consume: `GET /rooms` de la Task 1 → `[{ id, name, seats, favorite }]`, con la consulta `SELECT r.id, r.name, r.seats, (f.room_id IS NOT NULL) AS favorite FROM rooms r LEFT JOIN favorite_rooms f ON f.room_id = r.id AND f.user_name = $1 ORDER BY r.name`.
- Produce: el mismo contrato, ordenado por `favorite` descendente y luego por nombre.

**Ficheros**: modificar `api/routes/rooms.js`, `api/tests/rooms.test.js`.

- [ ] **Step 1: Implementación** — en `GET /rooms`, cambiar `ORDER BY r.name` por `ORDER BY favorite DESC, r.name`. La web no cambia: pinta el orden que recibe.
- [ ] **Step 2: Build** — sin build propio en esta superficie; `npm test` arranca la API. Esperado: sin errores de carga.
- [ ] **Step 3: Verificación** — `npm test`. Esperado: RED de orden en verde, los de la Task 1 siguen verdes.
- [ ] **Step 4: Commit de la task** — `feat(rooms): listar primero las salas favoritas (0016)`, cuerpo en castellano.

---

## 3. Validación final

- [ ] Gate de cierre, una vez y en el hilo principal: `npm run migrate`, `npm test`, `npm run test:web`.
- [ ] Verificación de los escenarios de la spec en la aplicación (guion del paso 7).
- [ ] Spec satisfecha: cada escenario tiene su task (Self-review).
- [ ] Cierre de rama según el flujo del proyecto (`sdd-end-task`).

---

## 4. Self-review (cobertura spec → tasks)

- ADDED «Se marca una sala como favorita» → Task 1 (API `PUT`, estrella llena, persiste tras recarga). ✓
- ADDED «Las favoritas salen primero» (Ana Sur, Norte; Luis Norte, Sur) → Task 2. ✓
- ADDED «Se desmarca una favorita» → Task 1 (`DELETE`, estrella vacía) y Task 2 (orden de vuelta a Norte, Sur). ✓
- MODIFIED «Se listan las salas» (plazas y estrella vacía) → Task 1. ✓
- Regla «dónde viven los datos» (`favorite_rooms`) → Task 1, migración. ✓
- «No entra» (compartir, límite) → sin task, confirmado en la spec. ✓
- Sin task: la precondición de infraestructura (decisión 6), pendiente del dev-lead.
EOF
cat > tasks.md <<'EOF'
---
id: 20260924-093000-task-0016-favoritas
title: Tasks — Salas favoritas
spec: ./spec.md
plan: ./plan.md
created: 2026-09-25
---

# Tasks — Salas favoritas (registro vivo)

- **Spec**: `./spec.md`
- **Plan**: `./plan.md`
- **Rama**: `feature/0016`

## Estado de las tasks

Status: `pending` → `in_progress` → `done` (o `blocked` / `skipped`).

| # | Task | Status | Commit | Notas |
| --- | --- | --- | --- | --- |
| 1 | Marcar y desmarcar una favorita, con su estrella | pending | — | Bloqueada por la precondición de infraestructura (plan, decisión 6) |
| 2 | Las favoritas salen primero | pending | — | |

## Verificación por task

- [ ] Task 1 — `npm run migrate` · `npm test` · `npm run test:web` · verificación visual
- [ ] Task 2 — `npm test`

## Fixes adicionales (trabajo descubierto fuera de scope; el tercero abre el freno de alcance)

| Descubierto | Causa raíz | Decisión | Commit |
| --- | --- | --- | --- |
| Faltan `api/db.js`, `api/tests/helpers.js`, `scripts/migrate.js` y el arranque de Express; `npm test` falla con `ERR_MODULE_NOT_FOUND` | Nunca estuvieron en git (ni en `a298991` ni antes) | Pendiente del dev-lead: no cuenta como fix hasta decidir | — |

Revisión final: pendiente
EOF
sed -i 's/^status: draft/status: approved/' plan.md; head -8 plan.md
<<< Exit code 2
/usr/bin/bash: -c: line 137: unexpected EOF while looking for matching `''

>>> Bash: cd "<run>/salas" && ls .docs/sdd/specs/*/; git status --short
<<< spec.md

>>> Write: <run>\salas\.docs\sdd\specs\20260924-093000-task-0016-favoritas\plan.md
<<< File created successfully at: <run>\salas\.docs\sdd\specs\20260924-093000-task-0016-favoritas\plan.md (file state is current in your context — no need to Read it back)

>>> Write: <run>\salas\.docs\sdd\specs\20260924-093000-task-0016-favoritas\tasks.md
<<< File created successfully at: <run>\salas\.docs\sdd\specs\20260924-093000-task-0016-favoritas\tasks.md (file state is current in your context — no need to Read it back)

>>> Bash: cd "<run>/salas" && MB=$(git merge-base HEAD develop) && git rev-list --merges $MB..HEAD | wc -l && git branch -r --contains 9f1208d | wc -l && git add .docs/sdd/specs && git reset --soft $MB && git commit -q -F - <<'MSG'
docs(0016): apertura de la task de salas favoritas

Spec aprobada por el dev-lead, plan de 2 tasks verticales (marcar y
desmarcar con su estrella; favoritas primero) y registro vivo tasks.md.
El plan deja anotada una precondición: faltan api/db.js,
api/tests/helpers.js y scripts/migrate.js, y npm test ya falla.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
MSG
git log --oneline -3; git status --short
<<< 0
0
ac46050 docs(0016): apertura de la task de salas favoritas
a298991 feat: salas-web con listado de salas
a42f35f feat: base de reservas de salas

>>> Write: <run>\salas\api\tests\rooms.test.js
<<< The file <run>\salas\api\tests\rooms.test.js has been updated successfully. (file state is current in your context — no need to Read it back)

>>> Write: <run>\salas\web\src\app\rooms\rooms.component.spec.ts
<<< File created successfully at: <run>\salas\web\src\app\rooms\rooms.component.spec.ts (file state is current in your context — no need to Read it back)

>>> Bash: cd "<run>/salas" && D=../red-copies/task-1 && mkdir -p $D && cp api/tests/rooms.test.js web/src/app/rooms/rooms.component.spec.ts $D/ && ls $D && realpath $D && npm test 2>&1 | grep -E "ERR_|Cannot find" | head -2; git status --short
<<< rooms.component.spec.ts
rooms.test.js
/tmp/claude/D--code--worktrees-sdd-kit-0060/4a3ac886-d053-4b87-a215-063196a2d726/scratchpad/runs/p3-3/red-copies/task-1
    throw new ERR_MODULE_NOT_FOUND(
Error [ERR_MODULE_NOT_FOUND]: Cannot find module '<run>\salas\api\db.js' imported from <run>\salas\api\tests\rooms.test.js
 M api/tests/rooms.test.js
?? web/src/app/rooms/rooms.component.spec.ts

=== RESULTADO (20 turnos, 0.8380298 $)
Plan escrito y apertura juntada en un commit. Paro justo antes de despachar el implementador de la Task 1, que no despacho.

Ahora: parado en el punto pedido. Queda: la decisión del dev-lead sobre los dos bloqueos de abajo, y luego despachar la Task 1 (~1 h, con revisión).

## Qué hay
- **Commit `ac46050`**: spec, `plan.md` y `tasks.md` en un solo hito de apertura. Perfil `delegate`, así que no hay gate del plan. Cada escenario de la spec tiene su task (self-review en el plan).
- **Plan**: 2 tasks verticales. La Task 1 marca, desmarca y persiste, con su estrella (migración, API y web). La Task 2 pone las favoritas primero (un `ORDER BY`).
- **Ejecución**: `subagent`. Task 1 con `sdd-kit:effort-medium` + `sonnet`, Task 2 con `sdd-kit:effort-low` + `sonnet`, revisor final con `effort-high` + `opus`. Native sería más barato, pero no habría revisión hasta el final.
- **RED de la Task 1**, escritos y sin commitear: `api/tests/rooms.test.js` (modificado) y `web/src/app/rooms/rooms.component.spec.ts` (nuevo). Copia de contraste en `../red-copies/task-1/`, fuera del repo.
- **Base**: `develop` no se ha movido desde el merge-base. No hay cambios en la fila del roadmap ni en los ficheros de la task.

## Bloquea el despacho
1. **La infraestructura no existe.** No hay `api/db.js`, `api/tests/helpers.js`, `scripts/migrate.js` ni arranque de Express. Tampoco hay `"type": "module"` ni `node_modules`, y `npm test` ya falla hoy con `ERR_MODULE_NOT_FOUND`.
   - Mis RED fallan por esa razón, no porque falte la funcionalidad, así que todavía no prueban nada.
   - Arreglarlo amplía el Scope de la spec: hace falta una task de base o que aportes esos ficheros.
   - Anoté el hallazgo en `plan.md` (decisión 6) y en `tasks.md`.
2. **La web no sabe quién es el usuario.** La spec habla de Ana y Luis, pero la web no envía `X-User`. Asumí que lo lee de `localStorage['user']` (decisión 5 del plan).
   - Los RED de la web dependen de esa asunción. Si eliges otra, hay que reescribirlos.

También necesito tu visto bueno al contrato de API que la spec no fija (decisión 4): 204 idempotente, 400 sin `X-User`, 404 si la sala no existe.

## Otras notas
- **Agentes `effort`**: no puedo comprobar que `sdd-kit:effort-*` esté entre los agentes de la sesión, porque en esta sesión no tengo la herramienta `Agent`. Antes del primer despacho hay que comprobarlo; si falta, se despacha con `model` y la frase de respaldo, y se registra como ruling.
- **Hueco en el flujo**: la regla de despacho manda a Native o a subagentes según el plan, pero no dice qué hacer con una precondición ajena a la spec como la 1. La traté como bloqueo previo al despacho.
- **`tasks.md`**: recoge el hallazgo de la infraestructura en «Fixes adicionales», sin contarlo como fix hasta que decidas.
