
>>> Skill: sdd-kit:sdd-start-task
<<< Launching skill: sdd-kit:sdd-start-task

>>> Bash: cd "<run>/salas" && git branch --show-current && find .docs -type f | head -80 && cat .docs/sdd/roadmap.md

>>> Bash: cd "<run>/salas" && cat .docs/sdd/constitution.md .docs/sdd/mission.md .docs/sdd/tech-stack.md; ls .docs/sdd/architecture.md .docs/sdd/capabilities 2>&1
<<< feature/0015
.docs/sdd/capabilities/booking.md
.docs/sdd/changelog.md
.docs/sdd/constitution.md
.docs/sdd/roadmap.md
.docs/sdd/sdd-kit.json
# Roadmap — salas

## Próximo

| Id | Tarea | Origen | Ficheros que toca | Tamaño |
| --- | --- | --- | --- | --- |
| 0014 | **Reservas que se guardan y se cancelan**: hoy las reservas se pierden al acabar cada ejecución y no hay forma de cancelar. Guardarlas en un fichero, listarlas con `salas reservas` y cancelar con `salas cancelar <sala> <franja>` | dev-lead | `src/slots.js`, `bin/salas.js`, fichero de datos nuevo | M |
| 0015 | **Tope de reservas y sa
<<< Exit code 2
# Constitution — salas

## Art. I — Commits

Tipo/scope en inglés, título y cuerpo en castellano, nunca title-only.

## Art. II — Tests

Suite: `node --test`. Todo verde antes de fusionar.
cat: .docs/sdd/mission.md: No such file or directory
cat: .docs/sdd/tech-stack.md: No such file or directory
ls: cannot access '.docs/sdd/architecture.md': No such file or directory
.docs/sdd/capabilities:
booking.md

>>> Bash: cd "<run>/salas" && cat .docs/sdd/capabilities/booking.md .docs/sdd/sdd-kit.json .docs/sdd/changelog.md; ls -R . -I .git | head -40
<<< # Capacidad — booking

Reservar salas por franjas desde la CLI.

## Requisitos

### Se reserva una sala libre
- GIVEN la sala Norte libre en 10-12
- WHEN `salas reservar Norte 10-12`
- THEN imprime la reserva y Norte deja de salir en `salas libres 10-12`

### La franja se valida al reservar
- GIVEN una franja que no casa con `HH-HH`
- WHEN se reserva
- THEN falla con «Franja no válida: usa HH-HH, p. ej. 10-12»

### La franja se valida al consultar libres
- GIVEN una franja que no casa con `HH-HH`
- WHEN se consultan las salas libres
- THEN falla con el mismo mensaje

## Reglas de la capacidad


>>> Skill: superpowers:brainstorming
<<< Launching skill: superpowers:brainstorming

>>> Bash: cd "<run>/salas" && cat src/slots.js bin/salas.js tests/slots.test.js package.json README.md; git log --stat -3 | head -30; ls ../../../kit-green/skills/sdd-templates ../../../kit-green/skills/sdd-templates/* | head -40
<<< const SLOT = /^\d{2}-\d{2}$/;
const ROOMS = ['Norte', 'Sur'];
const bookings = [];

function checkSlot(slot) {
  if (!SLOT.test(slot)) throw new Error('Franja no válida: usa HH-HH, p. ej. 10-12');
}

export function reserve(room, slot) {
  checkSlot(slot);
  if (bookings.some((b) => b.room === room && b.slot === slot)) throw new Error('Sala ocupada en esa franja');
  bookings.push({ room, slot });
  return { room, slot };
}

export function free(slot) {
  checkSlot(slot);
  return ROOMS.filter((room) => !bookings.some((b) => b.room === room && b.slot === slot));
}
#!/usr/bin/env node
import * 

>>> Bash: cd "<home>/AppData/Local/Temp/claude/D--code--worktrees-sdd-kit-0060/4a3ac886-d053-4b87-a215-063196a2d726/scratchpad/kit-green/skills" && cat sdd-templates/templates/spec-template.md; cat sdd-start-task/references/nombrado.md | head -60; cat sdd-start-task/references/review-spec.md | head -40
<<< ---
id: <yyyyMMdd-HHmmss>-task-<id>-<slug>
task: <id>            # id del gestor de tickets (0000 si no hay) · id de la secuencia del proyecto (ids.mode en sdd-kit.json)
parent: <id>          # solo si esta task nace de partir otra; la relación no va en el id (nunca sufijos 0006a)
title: <título corto descriptivo>
mode: full            # full | lite — lo lee sdd-end-task; sin campo = full
profile: <pair|delegate|unattended>   # opcional; omitido = hereda de la release o del proyecto
status: draft
created: <YYYY-MM-DD>
author: <autor>
approvers:
  - role: dev-lead
    name: TBD
   

>>> Bash: cd "<run>/salas" && TS=$(date -u +%Y%m%d-%H%M%S) && D=".docs/sdd/specs/$TS-task-0015-tope-salas-grandes" && mkdir -p "$D" && echo "$D" && echo "id: $TS-task-0015-tope-salas-grandes"
<<< .docs/sdd/specs/20260924-220608-task-0015-tope-salas-grandes
id: 20260924-220608-task-0015-tope-salas-grandes

>>> Write: <run>/salas/.docs/sdd/specs/20260924-220608-task-0015-tope-salas-grandes/spec.md
<<< File created successfully at: <run>/salas/.docs/sdd/specs/20260924-220608-task-0015-tope-salas-grandes/spec.md (file state is current in your context — no need to Read it back)

>>> Edit: <run>/salas/.docs/sdd/specs/20260924-220608-task-0015-tope-salas-grandes/spec.md
<<< The file <run>/salas/.docs/sdd/specs/20260924-220608-task-0015-tope-salas-grandes/spec.md has been updated successfully. (file state is current in your context — no need to Read it back)

=== RESULTADO (13 turnos, 0.43265380000000003 $)
Ahora: gate de aprobación de la spec. Queda: nada hasta que el dev-lead responda (plan e implementación no se tocan).

Spec escrita en `.docs/sdd/specs/20260924-220608-task-0015-tope-salas-grandes/spec.md` (modo full, perfil `delegate`, sin commitear). No está aprobada: «adelante con la spec, sin review de spec» me permitió redactarla, pero no la aprueba.

## Decisiones que he tomado yo — valida estas

1. **Quién reserva** es un tercer argumento posicional obligatorio: `salas reservar <sala> <franja> <persona>`. Rompe la forma actual, así que «Se reserva una sala libre» pasa a MODIFIED.
2. **«Día» no existe en el modelo.** No añado fecha: el tope cuenta las reservas de la persona que haya en memoria. Hasta que la 0014 persista las reservas, la CLI no puede llegar al tope, porque cada ejecución es un proceso nuevo. Se comprueba a nivel de módulo en los tests.
3. **Identidad de la persona**: se compara sin mayúsculas y sin espacios en los extremos («ana» = «Ana»). Si no, escribir «ana» esquivaría el tope.
4. **Sala grande** es más de 10 plazas (hoy solo Sur, con 12). **«2 horas o más»** es fin − inicio ≥ 2. Una franja con fin ≤ inicio (12-10) no la valida nadie hoy; en Sur se rechaza por la regla de 2 horas y en Norte pasa. No lo arreglo aquí.
5. **Orden de avisos**, con uno solo por intento: franja no válida, falta persona, sala ocupada, sala grande, tope.
6. **Textos de los avisos**, inventados por mí:
   - «Sala grande: Sur (12 plazas) solo se reserva en franjas de 2 horas o más»
   - «Tope alcanzado: Ana ya tiene 3 reservas hoy»
   - «Falta quién reserva: usa salas reservar <sala> <franja> <persona>»
7. **Una reserva rechazada no cuenta** para el tope.
8. **Sin capacidad nueva**: todo va en `booking`, con un MODIFIED y tres ADDED.
9. **Solape entre franjas** (10-12 frente a 11-13) sigue sin controlarse, porque no está en el enunciado.
10. **Riesgo con la 0014**: está pendiente y toca `src/slots.js` y `bin/salas.js`, como esta. Si se fusiona antes, el paso 6 parará por freno de alcance. Recomiendo cerrar la 0014 primero o aceptar el conflicto; no lo decido yo.

**Review de spec**: ninguna. Cuento 2 señales (contrato público y un MODIFIED), por debajo del umbral de 4, y además dijiste «sin review». Queda sin cubrir que se me haya escapado otra lectura de «día» o de «2 horas», por eso van las decisiones 2 y 4 arriba.

**Repaso de coherencia**: la decisión 5 omitía «falta persona» y contradecía la regla ante conflicto. Lo unifiqué.

¿Apruebas la spec?
