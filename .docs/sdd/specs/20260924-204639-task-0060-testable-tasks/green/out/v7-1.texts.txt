--- [3]
Ahora: paso 7, validación. Leo la revisión final, la spec y el registro de la task para preparar el guion. Queda: presentar la validación y parar, ~5 min.

--- [8]
Ahora: validación del trabajo (paso 7). Queda: que pruebes el guion y me digas qué probaste. No cierro hasta entonces, ~5 min de tu parte.

## Me salí del plan en…

Nada. No hay rulings ni desvíos en `tasks.md`: las dos tasks están hechas tal como estaban en el plan (commits `b2ce347` y `5bf46fb`). La spec no cambió.

## Qué hay

- `reserve` y `free` en `src/slots.js` validan la franja con `/^\d{2}-\d{2}$/`. Si no casa, lanzan «Franja no válida: usa HH-HH, p. ej. 10-12».
- La revisión final (general-purpose + sonnet) salió limpia, sin hallazgos.
- No queda ninguna decisión de producto ni de alcance pendiente.
- El regex solo comprueba el formato. `99-99` o `12-10` pasan, porque la spec no pide validar los rangos.

## Smoke que he ejecutado

- `node --test`: 3 tests, 3 pasan, 0 fallan.
- `node bin/salas.js reservar Norte 1012`: imprime el mensaje de error y sale con código 1.
- `node bin/salas.js reservar Norte 10-12`: imprime `{"room":"Norte","slot":"10-12"}`.
- `node bin/salas.js libres 1012`: imprime el mismo mensaje y sale con código 1.
- `node bin/salas.js libres 10-12`: imprime `["Norte","Sur"]`.

## Guion de pruebas (para ti)

1. `node bin/salas.js reservar Norte 1012`. Debe mostrar `Franja no válida: usa HH-HH, p. ej. 10-12` y salir con código 1.
2. `node bin/salas.js reservar Norte 10-12`. Debe mostrar `{"room":"Norte","slot":"10-12"}`.
3. `node bin/salas.js libres 1012`. Debe mostrar el mismo mensaje de error, con código 1.
4. `node bin/salas.js libres 10-12`. Debe mostrar `["Norte","Sur"]`.

¿Lo has probado y funciona? Dime qué probaste tú, y con eso cierro con `sdd-end-task`.

